---
phase: quick
plan: 260526-o28
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/room-card.ts
  - custom_components/climate_manager/www/panel.js
autonomous: true
requirements:
  - D-32
  - D-14c
  - D-14d
must_haves:
  truths:
    - "Row 1 of the room card header shows: room name, then period badge, then
      room mode badge (in that order)."
    - "Period badge displays `${PERIOD_DISPLAY_NAMES[active_period]} ·
      ${temp}°C` using `PERIOD_COLORS[active_period]` as background and white
      text."
    - "When room mode is `frost_protection`, no period badge is rendered (mode
      badge already conveys state)."
    - "When global mode is `off`, the period badge shows `Off` using
      `--secondary-background-color` / `--secondary-text-color`."
    - "When `active_period` is null/undefined, no period badge is rendered."
    - "Row 2 status line shows exactly 3 items in order: 🌡 temperature · 💧
      humidity · 👥 persons (the clock-outline period item is gone)."
    - "D-14d person count logic remains unchanged
      (`_getAssignedPersonIds().length`, with `present/total` in presence mode)."
    - "`npm run build` in `frontend/` completes with 0 errors and regenerates
      `custom_components/climate_manager/www/panel.js`."
  artifacts:
    - path: "frontend/src/components/room-card.ts"
      provides: "Period badge in row 1, 3-item status line in row 2"
      contains: "PERIOD_COLORS[period]"
    - path: "custom_components/climate_manager/www/panel.js"
      provides: "Rebuilt panel bundle with period badge changes"
  key_links:
    - from: "frontend/src/components/room-card.ts (render())"
      to: "PERIOD_COLORS / PERIOD_DISPLAY_NAMES in ../types.js"
      via: "import (already present at line 17)"
      pattern: "PERIOD_COLORS\\[.*\\]"
    - from: "frontend/src/components/room-card.ts (period badge)"
      to: "panelConfig.period_temperatures[active_period]"
      via: "lookup in render() or helper"
      pattern: "period_temperatures"
---

<objective>
Implement D-32: add a period badge to the room card header row 1 (before the existing mode badge) and remove the clock-outline period item from the row 2 status line, so the global period state leads the card and the status line is reduced to 3 items (temperature · humidity · persons).

Purpose: Make the active global period (e.g., "Normal · 19°C") immediately
visible at the top of each room card, while keeping per-room mode information
adjacent. Eliminates duplicated period info between row 1 and row 2.

Output: Updated `room-card.ts` plus a rebuilt `panel.js` bundle. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md
@frontend/src/components/room-card.ts
@frontend/src/types.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add period badge to row 1 and trim status line to 3 items in room-card.ts</name>
  <files>frontend/src/components/room-card.ts</files>
  <behavior>
    - Row 1 (`card-header-top`) order: `[room name]` → `[period badge]` (NEW) → `[room mode badge]` (existing, unchanged position-wise — appears after the period badge).
    - Period badge content: `${PERIOD_DISPLAY_NAMES[active_period]} · ${temp}°C` where `temp = panelConfig.period_temperatures[active_period]`. Falls back to just the period name if `period_temperatures[active_period]` is missing.
    - Period badge style: pill shape matching `.program-badge` (same padding, radius, font-size); background = `PERIOD_COLORS[active_period]`, color = white. Implemented via the existing `.program-badge` class with inline `style="background: ...; color: white;"`.
    - Skip cases (no period badge rendered at all):
        * `resolvedMode === "frost_protection"` — room mode badge already conveys state in its deep-blue color.
        * `active_period` (from `roomStatus?.active_period`) is `null` or `undefined`.
    - Special case: when `globalMode === "off"`, render a gray badge with text `Off`, using CSS vars `--secondary-background-color` and `--secondary-text-color` (NOT `PERIOD_COLORS`). Room mode badge still renders after it.
    - Row 2 (`card-header-status`) becomes exactly 3 `.status-item` spans in this order:
        1. 🌡 `mdi:thermometer` + `temp` (existing)
        2. 💧 `mdi:water-percent` + `humidity` (existing)
        3. 👥 `mdi:account-group` + `personDisplay` (existing — keep D-14d logic intact)
      The 3rd existing item (`mdi:clock-outline` + `periodDisplay`) is removed.
  </behavior>
  <action>
    Edit `frontend/src/components/room-card.ts` (per D-32):

    1. **Add a helper `_renderPeriodBadge()`** (place near `_renderHeaderStatus`, around line 435). Logic:
       - Read `globalMode = this.status?.global_mode ?? this.panelConfig?.global_mode ?? ""`.
       - Read `resolvedMode = this.config?.room_mode ?? "global"` (or accept it as a parameter — see step 3).
       - If `resolvedMode === "frost_protection"`, return `""` (empty TemplateResult).
       - If `globalMode === "off"`, return a badge: `<span class="program-badge" style="background: var(--secondary-background-color); color: var(--secondary-text-color);">Off</span>`.
       - Otherwise: read `period = this.roomStatus?.active_period ?? null`. If `period == null`, return `""`. Look up `label = PERIOD_DISPLAY_NAMES[period] ?? period` and `temp = this.panelConfig?.period_temperatures?.[period]`. Build content as `temp != null ? \`${label} · ${temp}°C\` : label`. Render: `<span class="program-badge" style="background: ${PERIOD_COLORS[period]}; color: white;">${content}</span>`.

    2. **Modify the `card-header-top` block in `render()`** (around lines 587–593): insert `${this._renderPeriodBadge()}` between the `room-name` span and the existing mode `program-badge` span so the order becomes `[room name] [period badge] [mode badge]`. Do not change the existing mode badge JSX or its `style=` line.

    3. **Modify `_renderHeaderStatus()`** (lines 435–492):
       - Remove the entire `periodDisplay` computation block (lines 447–462) — the `globalMode === "off"` branch and the `period` lookup + `periodTempVal` formatting are no longer needed in row 2.
       - Remove the third `<span class="status-item">` that renders `mdi:clock-outline` + `${periodDisplay}` (lines 482–485) from the returned template.
       - Leave the temperature (`mdi:thermometer`), humidity (`mdi:water-percent`), and persons (`mdi:account-group` — D-14d, with `personDisplay` / `presentCount` logic intact) items unchanged.
       - The `modeLabel` / `modeLabels` / `isPresenceMode` locals stay if still referenced by `title` attributes; otherwise drop unused ones to keep the file clean.

    4. **No new CSS class required** — reuse the existing `.program-badge` rule (lines 121–128) for both badges. Period badge uses inline `style` for color (consistent with how the `frost` mode badge already does it at line 591). Do NOT introduce a new `.program-badge.period` class — inline style keeps this minimal and matches the existing pattern.

    5. **Do not change** `_getAssignedPersonIds`, the room-mode `<select>`, the time-bar section, or any other render branch. Scope is strictly row 1 + row 2 of the header.

    Coding constraints (per CLAUDE.md and project conventions):
    - Import surface is unchanged: `PERIOD_COLORS` and `PERIOD_DISPLAY_NAMES` are already imported (line 17).
    - No inline hex colors anywhere — use `PERIOD_COLORS[period]` or HA CSS vars only (D-26, D-27).
    - No `ha-textfield` / `ha-select` / `ha-tabs` introduced (D-29 — those are confirmed broken).

  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | tail -20 &amp;&amp; grep -c "_renderPeriodBadge" src/components/room-card.ts &amp;&amp; ! grep -q "mdi:clock-outline" src/components/room-card.ts</automated>
  </verify>
  <done>
    - `frontend/src/components/room-card.ts` contains `_renderPeriodBadge` (or equivalent inline logic in `render()`) that returns: empty for `frost_protection`, gray "Off" badge for `globalMode === "off"`, `PERIOD_COLORS[period]`-backed badge with `${name} · ${temp}°C` otherwise, and empty when `active_period` is null.
    - Row 1 renders `[room name] [period badge] [mode badge]` in that order.
    - Row 2 renders exactly 3 status items (no `mdi:clock-outline`).
    - `grep "mdi:clock-outline" frontend/src/components/room-card.ts` returns no matches.
    - D-14d person count logic untouched (`_getAssignedPersonIds`, `present/total` formatting in presence mode).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rebuild frontend bundle and verify panel.js regenerated</name>
  <files>custom_components/climate_manager/www/panel.js</files>
  <action>
    Run `npm run build` in `frontend/` (per CLAUDE.md Vite 5.x build pipeline) to regenerate `custom_components/climate_manager/www/panel.js` from the updated TypeScript source.

    1. From the project root, run: `cd frontend && npm run build`.
    2. Confirm the Vite build completes with 0 errors. TypeScript type errors (if any from Task 1 changes) MUST be fixed before considering this task done — do not commit a broken bundle.
    3. Confirm `custom_components/climate_manager/www/panel.js` mtime is newer than the source file.
    4. Sanity-check the bundle by grepping for the badge order: the literal `_renderPeriodBadge` is mangled by Vite, but a substring like `· ` or `PERIOD_COLORS` lookups should appear (or the equivalent minified code). At minimum, the build should not error and the file should exist.

    Do NOT manually edit `panel.js` — it is a generated artifact.

  </action>
  <verify>
    <automated>cd frontend && npm run build 2>&1 | grep -E "(error|✓ built)" | tail -5 &amp;&amp; test -f ../custom_components/climate_manager/www/panel.js &amp;&amp; test ../custom_components/climate_manager/www/panel.js -nt src/components/room-card.ts</automated>
  </verify>
  <done>
    - `npm run build` exits 0.
    - `custom_components/climate_manager/www/panel.js` exists and is newer than `frontend/src/components/room-card.ts`.
    - No TypeScript compilation errors reported in build output.
  </done>
</task>

</tasks>

<verification>
- Build: `cd frontend && npm run build` → exit 0, zero errors.
- Source check: `grep -c "PERIOD_COLORS\[" frontend/src/components/room-card.ts` → at least 2 references (one already existed for the frost mode badge; one new for the period badge).
- Source check: `grep "mdi:clock-outline" frontend/src/components/room-card.ts` → no matches (the clock-outline status item is gone).
- Source check: `grep -c "card-header-status" frontend/src/components/room-card.ts` → still appears (CSS rule + JSX), but the rendered template inside `_renderHeaderStatus` has exactly 3 `<span class="status-item">` blocks.
- Manual smoke (optional, post-build, in HA dev instance): open the Climate Manager panel, Rooms tab. Confirm visually for at least one room:
  1. Row 1 shows `[room name] [colored period badge "<period> · <temp>°C"] [mode badge]`.
  2. Row 2 shows exactly 🌡 temp · 💧 humidity · 👥 persons.
  3. A room with mode = Frost protection shows no period badge.
  4. Switching global mode to Off makes every room's period badge gray and say "Off".
</verification>

<success_criteria>

- `room-card.ts` renders period badge per D-32 spec (color from `PERIOD_COLORS`,
  white text, `${name} · ${temp}°C` format, off-mode gray fallback,
  frost_protection skip, null active_period skip).
- Row 2 status line is reduced to 3 items; D-14d person count unchanged.
- `npm run build` produces a fresh `panel.js` with zero errors.
- No new hardcoded colors introduced (D-26 / D-27 compliance).
- No `ha-select` / `ha-textfield` / `ha-tabs` introduced (D-29 compliance).
  </success_criteria>

<output>
Create `.planning/quick/260526-o28-implement-period-badge-in-room-card-d-32/260526-o28-SUMMARY.md` when done, summarizing the diff in `room-card.ts` (lines added/removed, new helper) and confirming the Vite build output line.
</output>
