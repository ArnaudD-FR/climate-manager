---
phase: quick-260526-k0q
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/room-card.ts
autonomous: false
requirements:
  - QUICK-260526-k0q-01
must_haves:
  truths:
    - "Clicking 'Reset to global configuration' on a room in Custom mode keeps the room in Custom mode"
    - "After the click, the room's time_program equals the current global_time_program from panelConfig"
    - "The inline time-bar re-renders to show the global schedule (deep-copied — not a shared reference)"
    - "The room mode selector still reads 'Custom program' after the reset (no mode switch)"
  artifacts:
    - path: "frontend/src/components/room-card.ts"
      provides: "Reset button now copies global time program into the room's custom schedule instead of switching mode"
      contains: "_onResetToGlobal"
  key_links:
    - from: "frontend/src/components/room-card.ts::_onResetToGlobal"
      to: "panelConfig.global_time_program"
      via: "JSON.parse(JSON.stringify(...)) deep copy passed to ws.setRoomConfig"
      pattern: "global_time_program"
---

<objective>
Fix `_onResetToGlobal()` in `frontend/src/components/room-card.ts` so the
"Reset to global configuration" button on a room card (visible only in Custom
mode) overwrites the room's `time_program` with a deep copy of the current
`panelConfig.global_time_program`, while keeping `room_mode` set to `"custom"`.

Purpose: The previous quick task (260526-jvg) implemented the button as a
mode-switch back to `"global"`, which is wrong. The intended UX is: stay in
Custom mode, but throw away local schedule edits and re-seed from the global
program — so the user can iterate on their custom schedule starting from the
global baseline without losing the Custom mode opt-in.

Output: Updated `_onResetToGlobal()` method. Button label, placement, styling,
and visibility condition (Custom mode only) remain unchanged.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@frontend/src/components/room-card.ts
@frontend/src/types.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rewrite _onResetToGlobal() to copy global time program into custom schedule</name>
  <files>frontend/src/components/room-card.ts</files>
  <behavior>
    - Before: clicking the reset button calls `ws.setRoomConfig(roomId, { room_mode: "global" })` — wrong, switches mode away from Custom.
    - After: clicking the reset button calls `ws.setRoomConfig(roomId, { room_mode: "custom", time_program: <deep copy of panelConfig.global_time_program> })`.
    - The deep copy must use the same `JSON.parse(JSON.stringify(...))` pattern already used at line 381 of the same file (`_onRoomModeChange` when first switching to Custom) — this avoids sharing object references between the panel config and the room's stored schedule.
    - Toast and panel.reloadConfig() flow stays identical (success toast on success, retry toast on error).
    - If `panelConfig.global_time_program` is falsy (defensive), still send `room_mode: "custom"` plus an empty `DailyProgram` (`{ mon: [], tue: [], wed: [], thu: [], fri: [], sat: [], sun: [] }`) so the room never ends up with `undefined` time_program in Custom mode.
  </behavior>
  <action>
    Edit `frontend/src/components/room-card.ts`, method `_onResetToGlobal()` (currently lines 421-429). Replace the body so the payload sent to `this.ws.setRoomConfig` includes both `room_mode: "custom"` and a freshly deep-copied `time_program` derived from `this.panelConfig.global_time_program`.

    Implementation guidance:
    - Build the new `time_program` by deep-cloning `this.panelConfig.global_time_program` via `JSON.parse(JSON.stringify(...))`, cast to `DailyProgram` — mirror the exact pattern at line 381 so reviewers see one consistent idiom in this file.
    - Fall back to an empty seven-key `DailyProgram` when the global program is missing/undefined, so we never write `undefined` into a Custom-mode room.
    - Keep the `try`/`catch` shape, the `await this.panel.reloadConfig()` call, and the two toast messages identical to the current implementation — only the payload changes.
    - Do NOT touch the button's template (line 632), its CSS class (`.reset-btn`), its label text ("Reset to global configuration"), or its visibility guard (`resolvedMode === "custom"`). Per the bug description, only the handler behaviour changes.
    - Do NOT add new imports — `DailyProgram` is already imported on line 16.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <done>
    - `_onResetToGlobal()` sends `{ room_mode: "custom", time_program: <deep copy of global_time_program> }` to `ws.setRoomConfig`.
    - The deep copy uses `JSON.parse(JSON.stringify(...))` — confirmed by grep on the method body.
    - Defensive empty-program fallback present for the missing-global-program edge case.
    - TypeScript compiles with no new errors (`npx tsc --noEmit` from `frontend/`).
    - No other handler, render method, CSS rule, or import is modified.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — reset button keeps Custom mode and reloads global schedule</name>
  <what-built>
    `_onResetToGlobal()` now overwrites the room's custom `time_program` with a
    deep copy of the global program while keeping `room_mode: "custom"`.
  </what-built>
  <how-to-verify>
    1. Rebuild the panel: `cd frontend && npm run build` (or the project's standard build command), then reload the Climate Manager panel in Home Assistant (hard refresh: Ctrl+Shift+R).
    2. Open the Rooms tab. Pick any room that is currently in Custom mode (or switch one to Custom mode first via the Mode dropdown). The inline time-bar should be visible.
    3. Edit the schedule visibly — e.g. drag to add a Comfort block on Monday at 09:00–10:00 that is NOT present in the current global program. Confirm the change persists (toast says "Saved", the bar still shows the edit after the next status push).
    4. Click "Reset to global configuration".
    5. Verify:
       - Toast says "Saved".
       - The room mode selector still reads "Custom program" (NOT "Global program").
       - The program badge in the room header still reads "Custom program".
       - The inline time-bar is still visible.
       - The schedule shown in the bar now matches the Global program from the "Global settings" tab — your Monday 09:00–10:00 Comfort block from step 3 is gone, and the bar shows the global baseline.
    6. Switch to the Global settings tab and edit the global program (e.g. add a distinct block). Switch back to the Rooms tab, expand the same Custom-mode room, click "Reset to global configuration" again, and confirm the room's bar now reflects the updated global program.
    7. (Optional regression check) Switch a room to "Global program" via the Mode dropdown — confirm the reset button is hidden (it should only appear in Custom mode).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Visual: Button click in Custom mode keeps Custom mode active and reloads the global schedule into the time-bar.
- Code: `_onResetToGlobal()` payload contains both `room_mode: "custom"` and a deep-cloned `time_program`.
- Type-check: `npx tsc --noEmit` from `frontend/` passes with no new errors.
</verification>

<success_criteria>
- Clicking "Reset to global configuration" on a Custom-mode room overwrites that room's schedule with the current global program AND keeps the room in Custom mode.
- No regressions to other room-card behaviours (mode selector, persons section, TRV section, time-bar drag editing).
- TypeScript compiles cleanly.
</success_criteria>

<output>
Create `.planning/quick/260526-k0q-for-the-room-custom-program-in-rooms-tab/260526-k0q-SUMMARY.md` when done
</output>
