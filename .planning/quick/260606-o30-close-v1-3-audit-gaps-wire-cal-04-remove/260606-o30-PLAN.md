---
quick_id: 260606-o30
title: Close v1.3 audit gaps (CAL-04 + UI drift)
status: in-progress
---

# Quick Task 260606-o30: Close v1.3 audit gaps

Closes the two substantive findings from `.planning/v1.3-MILESTONE-AUDIT.md`
before the v1.3 milestone is completed. User-directed: fix, don't defer.

## CAL-04 — wire `wakeup_advance_minutes` (was a runtime no-op)

`resolve_calendar_presence` ignored `preheat_lead_minutes`. Wire it so a
calendar-absent person is treated as present once the active calendar window
ends within the wake-up lead, enabling pre-heat before they return.

- `schedule.py`: add `_active_window_end()` (computes the return-home moment
  per gap_handling — exact/day_span/threshold via contiguous run); use it in
  `resolve_calendar_presence` to flip absent→present inside the lead; `0`
  disables. Covers both top-level Calendar mode and per-period calendar (both
  route through this function).
- `tests/test_calendar.py`: repurpose the two tests that asserted the no-op
  (`test_absent_for_full_event_duration` → `test_absent_deep_in_event_beyond_lead`;
  isolate `test_gap_threshold_in_event_always_absent` with lead=0); add 5
  CAL-04 tests (within-lead, boundary, lead=0 disables, present-means
  unaffected, day_span window-end).

## UI-01 / UI-02 — spec drift

Shipped design (quick task 260601-d04 + memory ha-mode-rename) shows the HA
option always with a ⚠ + labels it "HA home tracking", not the originally
specced "hide option" / "Live tracking".

- `frontend/src/components/presence-mode.ts`: remove dead `shouldShowHaOption`
  (always-true, unused by any component — confirmed tree-shaken from panel.js).
- `presence-mode.test.ts`: drop its two tests + import.
- `.planning/REQUIREMENTS.md`: rewrite UI-01/UI-02 to the shipped design,
  check them off.

## Verify

- `pytest` full suite green; `node --test` presence-mode green.
- `make build` clean; `panel.js` unchanged (dead helper already shaken out).
