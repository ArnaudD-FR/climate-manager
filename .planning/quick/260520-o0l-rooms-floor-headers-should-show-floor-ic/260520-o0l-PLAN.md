---
quick_id: 260520-o0l
slug: rooms-floor-headers-should-show-floor-ic
description: rooms floor headers should show floor icon
date: 2026-05-20
status: ready
must_haves:
  truths:
    - Floor section headers show a ha-icon next to the floor name
    - Icon uses hass.floors[fid].icon if set, else falls back to level-based MDI icon
    - Level < 0 uses mdi:home-floor-b, 0 uses mdi:home-floor-0, 1/2/3 use matching icons
    - floors type in Hass interface includes optional icon field
---

# Quick Task 260520-o0l: Rooms Floor Headers Should Show Floor Icon

## Goal

Add an icon to each floor section header in the Rooms tab. Use the floor's configured icon if available, otherwise derive from floor.level (mdi:home-floor-0, mdi:home-floor-1, etc.).

## Tasks

### Task 1: Add icon field to Hass floors type
`frontend/src/types.ts` — add `icon?: string | null` to floors record type.

### Task 2: Add icon to floor headers
`frontend/src/components/rooms-tab.ts` — add getFloorIcon helper, update .floor-header CSS to flex, render ha-icon in header.

### Task 3: Build and deploy
`make build` + deploy.
