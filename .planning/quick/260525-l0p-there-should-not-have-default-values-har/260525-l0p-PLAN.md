---
phase: 260525-l0p
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/climate_manager/websocket.py
  - tests/test_websocket.py
  - frontend/src/ws-client.ts
  - frontend/src/components/global-settings-tab.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "Frontend has no hardcoded period temperature defaults — backend const.py
      is the only source of truth"
    - "Frontend has no hardcoded global time program defaults — backend const.py
      is the only source of truth"
    - "Reset Temperatures button writes backend DEFAULT_PERIOD_TEMPERATURES into
      runtime_config and triggers re-evaluation"
    - "Reset Configuration button writes backend DEFAULT_GLOBAL_MODE + default
      daily program into runtime_config and triggers re-evaluation"
    - "After reset, UI refreshes via the existing get_config/reload path showing
      the new (backend default) values"
  artifacts:
    - path: "custom_components/climate_manager/websocket.py"
      provides:
        "Two new WS handlers: reset_period_temperatures, reset_time_program"
      contains: "reset_period_temperatures"
    - path: "tests/test_websocket.py"
      provides:
        "Tests that the two reset handlers persist DEFAULT_* values from
        const.py"
      contains: "test_ws_reset_period_temperatures"
    - path: "frontend/src/ws-client.ts"
      provides: "resetPeriodTemperatures() and resetTimeProgram() typed methods"
      contains: "resetPeriodTemperatures"
    - path: "frontend/src/components/global-settings-tab.ts"
      provides:
        "Reset buttons that call backend reset commands; no hardcoded defaults"
      contains: "ws.resetPeriodTemperatures"
  key_links:
    - from:
        "frontend/src/components/global-settings-tab.ts (_onResetTemperatures)"
      to: "WsClient.resetPeriodTemperatures"
      via: "method call"
      pattern: "ws\\.resetPeriodTemperatures"
    - from:
        "frontend/src/components/global-settings-tab.ts (_onResetConfiguration)"
      to: "WsClient.resetTimeProgram + setGlobalMode"
      via: "method calls"
      pattern: "ws\\.resetTimeProgram"
    - from: "WsClient.resetPeriodTemperatures"
      to: "climate_manager/reset_period_temperatures WS handler"
      via: "sendMessagePromise"
      pattern: "climate_manager/reset_period_temperatures"
    - from: "websocket.py reset handlers"
      to: "DEFAULT_PERIOD_TEMPERATURES / _DEFAULT_DAILY_PROGRAM in const.py"
      via: "deep-copied import"
      pattern: "DEFAULT_PERIOD_TEMPERATURES|_DEFAULT_DAILY_PROGRAM"
---

<objective>
Remove all hardcoded period-temperature and time-program defaults from the frontend.
Reset buttons must call new backend WebSocket commands that copy the canonical
defaults from `const.py` into `runtime_config`, persist, and re-evaluate — the
existing get_config / subscribe_status push then refreshes the UI naturally.

Purpose: `const.py` becomes the single source of truth for defaults. Today the
frontend duplicates them (with a stale value — frost was 7, backend says 5) and
applies them client-side, which silently diverges from backend behavior.

Output:

- Two new WS commands `reset_period_temperatures` and `reset_time_program`
- Two new typed WsClient methods
- Frontend reset handlers that call those commands; no local DEFAULT\_\*
  constants </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@./CLAUDE.md
@custom_components/climate_manager/const.py
@custom_components/climate_manager/websocket.py
@tests/test_websocket.py
@frontend/src/ws-client.ts
@frontend/src/components/global-settings-tab.ts
@frontend/src/types.ts

<interfaces>
<!-- Backend canonical defaults — already exported from const.py -->

From custom_components/climate_manager/const.py:

- `DEFAULT_PERIOD_TEMPERATURES: dict[str, float]` — {frost_protection: 5.0,
  reduced: 18.0, normal: 20.0, comfort: 22.0}
- `_DEFAULT_DAILY_PROGRAM: dict` — 7-day program, each day =
  [{"start":"00:00","mode":"reduced"},{"start":"06:00","mode":"normal"},{"start":"22:00","mode":"reduced"}]
- `DEFAULT_GLOBAL_MODE` = MODE_TIME_PROGRAM

`_DEFAULT_DAILY_PROGRAM` is module-private (leading underscore). For external
use, either (a) reference it directly from `websocket.py` (same package), or (b)
add a public alias `DEFAULT_GLOBAL_TIME_PROGRAM = _DEFAULT_DAILY_PROGRAM` in
const.py. Choose (a): same-package import, no new public symbol.

Backend write-then-evaluate pattern (every existing write handler follows it):

```
mutate runtime_config[<key>]
await entry.runtime_data.store.async_save(runtime_config)
connection.send_result(msg["id"], {"success": True})
hass.async_create_task(entry.runtime_data.coordinator.async_evaluate())
```

WsClient pattern (one method per backend command, returns sendMessagePromise):

```
methodName(): Promise<{ success: boolean }> {
  return this.hass.connection.sendMessagePromise<{success: boolean}>({
    type: "climate_manager/<command_name>",
  });
}
```

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add reset_period_temperatures and reset_time_program WS handlers + tests</name>
  <files>custom_components/climate_manager/websocket.py, tests/test_websocket.py</files>
  <action>
In `custom_components/climate_manager/websocket.py`:

1. Extend the imports from `.const` to include `DEFAULT_PERIOD_TEMPERATURES` and
   `_DEFAULT_DAILY_PROGRAM`. Also add `import copy` at top of file (needed to
   deep-copy the per-day program before assignment so the runtime_config never
   shares refs with the module-level default).

2. Add two factory functions next to the existing write handlers, modeled
   exactly on `_make_ws_set_period_temperatures` / `_make_ws_set_time_program`:

   - `_make_ws_reset_period_temperatures(entry)` — schema is just
     `{vol.Required("type"): f"{DOMAIN}/reset_period_temperatures"}` (no
     payload). Body:
     `entry.runtime_data.runtime_config["period_temperatures"] = dict(DEFAULT_PERIOD_TEMPERATURES)`
     then standard save / send_result / async_evaluate.
   - `_make_ws_reset_time_program(entry)` — schema is just
     `{vol.Required("type"): f"{DOMAIN}/reset_time_program"}` (no payload).
     Body:
     `entry.runtime_data.runtime_config["global_time_program"] = copy.deepcopy(_DEFAULT_DAILY_PROGRAM)`
     then standard save / send_result / async_evaluate.

3. Register both new handlers in `async_register_commands` next to the existing
   set_period_temperatures / set_time_program registrations.

4. Update the module docstring "Registers 8 WebSocket commands" → "Registers 10
   WebSocket commands" and add the two new commands to the bullet list.

In `tests/test_websocket.py`:

5. Add two tests following the style of
   `test_ws_set_global_mode_persists_and_evaluates`:
   - `test_ws_reset_period_temperatures_writes_defaults` — first mutate
     `entry.runtime_data.runtime_config["period_temperatures"]` to something
     non-default (e.g. {frost_protection: 99, reduced: 99, normal: 99, comfort:
     99}), then send the reset command, assert success and assert
     `runtime_config["period_temperatures"] == DEFAULT_PERIOD_TEMPERATURES`.
   - `test_ws_reset_time_program_writes_defaults` — mutate `global_time_program`
     to a 1-day stub, send the reset command, assert success and assert the
     resulting program has all 7 day keys ("mon".."sun") and that each day has 3
     periods matching the const.py default starts ("00:00", "06:00", "22:00").
     Import `_DEFAULT_DAILY_PROGRAM` and compare with `==`.

Import the new symbols `DEFAULT_PERIOD_TEMPERATURES`, `_DEFAULT_DAILY_PROGRAM`
in the test module's import block.

No mutation of DEFAULT_PERIOD_TEMPERATURES or \_DEFAULT_DAILY_PROGRAM is allowed
— always deep-copy on write (matches T-03-09 pattern: never let a module
constant get reference-shared into runtime_config). </action> <verify>
<automated>cd /home/arnaud/dev/climate_manager && uv run pytest
tests/test_websocket.py -x -q</automated> </verify> <done>All websocket tests
pass; the two new tests are present and green; runtime_config matches const.py
defaults after each reset command.</done> </task>

<task type="auto">
  <name>Task 2: Add WsClient methods and strip frontend hardcoded defaults</name>
  <files>frontend/src/ws-client.ts, frontend/src/components/global-settings-tab.ts</files>
  <action>
In `frontend/src/ws-client.ts`:

1. Add two new methods next to `setPeriodTemperatures` / `setTimeProgram`:
   - `resetPeriodTemperatures(): Promise<{ success: boolean }>` →
     sendMessagePromise with
     `type: "climate_manager/reset_period_temperatures"`.
   - `resetTimeProgram(): Promise<{ success: boolean }>` → sendMessagePromise
     with `type: "climate_manager/reset_time_program"`. No payload arguments.

In `frontend/src/components/global-settings-tab.ts`:

2. DELETE the `DEFAULT_TEMPERATURES` constant (lines 61-67).
3. DELETE the `DEFAULT_GLOBAL_MODE` constant (lines 69-70).
4. DELETE the `DEFAULT_TIME_PROGRAM` constant + its IIFE (lines 72-89).

5. Rewrite `_onResetTemperatures` to call
   `await this.ws.resetPeriodTemperatures()` then
   `await this.panel.reloadConfig()` and show toast. Same try/catch shape as
   before.

6. Rewrite `_onResetConfiguration` to call:

   - `await this.ws.resetTimeProgram()`
   - `await this.ws.setGlobalMode(MODE_TIME_PROGRAM)` — the canonical default;
     MODE_TIME_PROGRAM is already imported as a local const above (line 52).
     This keeps "Reset to default" semantics for the global mode field. Add an
     inline code comment noting this mirrors `DEFAULT_GLOBAL_MODE` in const.py.
   - then `await this.panel.reloadConfig()` and show toast.

7. Remove the `defaultVal: number` parameter from the local `tempField` helper
   inside `_renderTemperaturesCard` and remove the four hardcoded numbers in the
   `tempField(...)` invocations. The `.value=${...}` binding becomes
   `.value=${temps[id] != null ? String(temps[id]) : ""}` — an empty string when
   the backend hasn't sent a value yet is acceptable (will refresh once
   `reloadConfig` completes). Update the helper signature accordingly:
   `(id: string, label: string) => html\`...\``.

8. Verify no other references to the deleted constants remain
   (`grep -n "DEFAULT_TEMPERATURES\\|DEFAULT_TIME_PROGRAM\\|DEFAULT_GLOBAL_MODE" frontend/src/components/global-settings-tab.ts`
   must return 0 lines after the edit; the `MODE_TIME_PROGRAM` constant at the
   top of the file is NOT one of these and must remain).

After edits, build the panel to confirm there are no TypeScript or Lit errors.
</action> <verify> <automated>cd /home/arnaud/dev/climate_manager/frontend &&
npm run build 2>&1 | tail -20 && cd /home/arnaud/dev/climate_manager && grep -nE
"DEFAULT_TEMPERATURES|DEFAULT_TIME_PROGRAM|DEFAULT_GLOBAL_MODE"
frontend/src/components/global-settings-tab.ts | grep -v '^[[:space:]]\*//' | wc
-l</automated> </verify> <done>Frontend builds cleanly; the grep returns 0 (no
non-comment references to deleted constants); the two reset buttons issue
backend commands instead of applying local defaults.</done> </task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify reset flow end-to-end in running HA</name>
  <what-built>
Two new backend WS commands (reset_period_temperatures, reset_time_program) that copy defaults from const.py into runtime_config, plus frontend reset buttons that call them instead of applying hardcoded local values. All DEFAULT_* constants removed from global-settings-tab.ts.
  </what-built>
  <how-to-verify>
1. Reload the Climate Manager integration (or restart HA) so the new WS commands register.
2. Open the panel → Global Settings tab.
3. In the Temperatures card, change all four values to something obviously wrong (e.g. 99). Wait for auto-save toast.
4. Click "Reset to default" under Temperatures.
   - Expected: toast "Reset to defaults"; the four fields refresh to 5 / 18 / 20 / 22 (NOT 7 / 18 / 20 / 22 — frost must be 5).
5. In the Configuration card, change Global mode to "Off", and drag the time-bar to create a custom program for at least one day. Wait for auto-save.
6. Click "Reset to default" under Configuration.
   - Expected: toast "Reset to defaults"; Global mode returns to "Time program"; the time-bar shows 3 blocks per day (00:00 reduced, 06:00 normal, 22:00 reduced) for all 7 days.
7. Refresh the browser (F5). Values must still match the const.py defaults (proves the reset persisted, not just refreshed locally).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any divergence (e.g., values not refreshing, frost still showing 7, etc.).</resume-signal>
</task>

</tasks>

<verification>
- Backend: `uv run pytest tests/test_websocket.py -x -q` passes including two new tests.
- Frontend: `npm run build` succeeds with no TS errors.
- Grep: no non-comment references to `DEFAULT_TEMPERATURES`, `DEFAULT_TIME_PROGRAM`, or `DEFAULT_GLOBAL_MODE` in `global-settings-tab.ts`.
- Human verification of both reset buttons confirms backend-driven defaults reach the UI via the existing reload path.
</verification>

<success_criteria>

- `const.py` is the only place storing default period temperatures and the
  default daily program.
- Both reset buttons issue a single backend command; the UI refreshes via the
  existing `reloadConfig()` path.
- Frost-protection default displayed after reset is 5 °C (matching backend), not
  7 °C.
- No frontend test regressions; backend tests still green. </success_criteria>

<output>
Create `.planning/quick/260525-l0p-there-should-not-have-default-values-har/260525-l0p-SUMMARY.md` when done.
</output>
