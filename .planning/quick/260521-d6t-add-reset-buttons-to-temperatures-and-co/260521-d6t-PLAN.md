---
phase: quick-260521-d6t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/global-settings-tab.ts
autonomous: false
requirements:
  - "QUICK-260521-d6t: Independent reset buttons for Temperatures and
    Configuration cards"

must_haves:
  truths:
    - "Temperatures card displays a 'Reset to default' button below the 4
      temperature fields"
    - "Configuration card displays a 'Reset to default' button below the global
      time program editor"
    - "Clicking the Temperatures reset button restores frost_protection=7,
      reduced=18, normal=20, comfort=22 and leaves global_mode and
      global_time_program unchanged"
    - "Clicking the Configuration reset button restores global_mode=time_program
      and the default 7-day program (00:00 frost_protection / 06:00 normal /
      22:00 frost_protection on every day) and leaves period_temperatures
      unchanged"
    - "Each reset shows a success toast on success and an error toast on failure"
    - "Card UI re-renders with the reset values after the reset completes"
  artifacts:
    - path: "frontend/src/components/global-settings-tab.ts"
      provides: "Two independent reset handlers + two buttons (one per card)"
      contains:
        "_onResetTemperatures, _onResetConfiguration, DEFAULT_GLOBAL_MODE,
        DEFAULT_TIME_PROGRAM"
  key_links:
    - from: "Temperatures card reset button"
      to: "ws.setPeriodTemperatures"
      via: "_onResetTemperatures handler"
      pattern: "setPeriodTemperatures\\(DEFAULT_TEMPERATURES\\)"
    - from: "Configuration card reset button"
      to: "ws.setGlobalMode + ws.setTimeProgram"
      via: "_onResetConfiguration handler"
      pattern: "setGlobalMode.*setTimeProgram|setTimeProgram.*setGlobalMode"
---

<objective>
Add two independent "Reset to default" buttons to the Global Settings tab — one
in the Temperatures card and one in the Configuration card. Each button resets
only the fields owned by its card.

Purpose: Currently the Configuration card contains a "Reset to default" button
that actually resets temperatures (misplaced). Users have no way to reset global
mode or global time program. This quick task fixes the misplacement and adds the
missing Configuration reset.

Output: Updated `global-settings-tab.ts` with two correctly-scoped reset
handlers and buttons. No backend changes — defaults are inlined at the frontend
mirroring `custom_components/climate_manager/const.py`. </objective>

<execution_context> @$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@frontend/src/components/global-settings-tab.ts
@frontend/src/types.ts
@frontend/src/ws-client.ts

<interfaces>
<!-- Key types and contracts the executor needs. Already in context above. -->

Defaults to mirror — sourced from `custom_components/climate_manager/const.py`:

```python
DEFAULT_GLOBAL_MODE = "time_program"

DEFAULT_PERIOD_TEMPERATURES = {
  "frost_protection": 7.0,
  "reduced": 18.0,
  "normal": 20.0,
  "comfort": 22.0,
}

# Default daily schedule applied to every day mon..sun:
#   00:00 frost_protection
#   06:00 normal
#   22:00 frost_protection
_DEFAULT_DAY_PERIODS = [
  {"start": "00:00", "mode": "frost_protection"},
  {"start": "06:00", "mode": "normal"},
  {"start": "22:00", "mode": "frost_protection"},
]
```

WsClient methods already available in `frontend/src/ws-client.ts`:

- `setPeriodTemperatures(temperatures: Record<string, number>): Promise<{success: boolean}>`
- `setGlobalMode(mode: string): Promise<{success: boolean}>`
- `setTimeProgram(program: DailyProgram): Promise<{success: boolean}>`

`DailyProgram` shape (from `frontend/src/types.ts`):

```ts
type DailyProgram = Record<
  "mon" | "tue" | "wed" | "thu" | "fri" | "sat" | "sun",
  Period[]
>;
interface Period {
  start: string;
  mode?: string;
  state?: string;
}
```

Existing constant `DEFAULT_TEMPERATURES` is already declared at the top of
`global-settings-tab.ts` (lines 61-66) and matches the backend defaults. Reuse
it; do not redefine.

HA 2026.x constraint reminder (from MEMORY.md): the file already avoids
`ha-textfield`, `ha-select`, `ha-tabs`. Keep all new UI on native elements
(`<button>`). </interfaces> </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add two scoped reset handlers and a configuration defaults constant</name>
  <files>frontend/src/components/global-settings-tab.ts</files>
  <behavior>
    - A new module-scoped constant `DEFAULT_GLOBAL_MODE` equals `"time_program"` (matching backend `DEFAULT_GLOBAL_MODE` in const.py).
    - A new module-scoped constant `DEFAULT_TIME_PROGRAM` of type `DailyProgram` contains the three-period schedule `[{start:"00:00",mode:"frost_protection"},{start:"06:00",mode:"normal"},{start:"22:00",mode:"frost_protection"}]` repeated for every key `mon..sun` (deep-cloned per key so callers cannot mutate shared arrays).
    - The existing `_onResetToDefault` method is renamed to `_onResetTemperatures`. Its body is unchanged: it calls `this.ws.setPeriodTemperatures(DEFAULT_TEMPERATURES)`, awaits `panel.reloadConfig()`, and shows `"Reset to defaults"` / `"Reset failed — retrying..."` toasts.
    - A new async method `_onResetConfiguration` is added. It calls `this.ws.setGlobalMode(DEFAULT_GLOBAL_MODE)` and `this.ws.setTimeProgram(DEFAULT_TIME_PROGRAM)` (sequential, in that order so a failure on mode does not partially apply the program), then `await this.panel.reloadConfig()`, then `this.panel.showToast("Reset to defaults", false)`. On any thrown error, it calls `this.panel.showToast("Reset failed — retrying...", true)`.
    - Both handlers do NOT touch `period_temperatures` in the other's flow (independence requirement).
  </behavior>
  <action>Edit `frontend/src/components/global-settings-tab.ts`. Just below the existing `DEFAULT_TEMPERATURES` constant (around line 66) add `DEFAULT_GLOBAL_MODE = "time_program"` and a `DEFAULT_TIME_PROGRAM: DailyProgram` built by mapping `DAY_KEYS` to a fresh array of three Period objects per day (use a helper or repeated literal — do NOT share the same array reference across days). Rename `_onResetToDefault` to `_onResetTemperatures` (no body changes). Add a new async method `_onResetConfiguration` next to it that sequentially awaits `this.ws.setGlobalMode(DEFAULT_GLOBAL_MODE)` then `this.ws.setTimeProgram(DEFAULT_TIME_PROGRAM)`, then `await this.panel.reloadConfig()`, then shows `"Reset to defaults"` toast; in the catch block show the failure toast. Do not remove `DEFAULT_TEMPERATURES`; do not change other handlers (`_onModeChange`, `_onTemperatureBlur`, `_onPeriodsChanged`).</action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tee /tmp/tsc-d6t.log; grep -q "error" /tmp/tsc-d6t.log &amp;&amp; exit 1 || true</automated>
    <automated>grep -v '^\s*//' frontend/src/components/global-settings-tab.ts | grep -c "_onResetTemperatures\|_onResetConfiguration" | awk '{ if ($1 &lt; 4) { print "expected at least 4 references (2 decl + 2 callers later), found " $1; exit 1 } }'</automated>
    <automated>grep -v '^\s*//' frontend/src/components/global-settings-tab.ts | grep -c "DEFAULT_GLOBAL_MODE\|DEFAULT_TIME_PROGRAM" | awk '{ if ($1 &lt; 3) { print "expected DEFAULT_GLOBAL_MODE + DEFAULT_TIME_PROGRAM declared and used, found " $1; exit 1 } }'</automated>
  </verify>
  <done>TypeScript compiles. `_onResetTemperatures` (renamed from `_onResetToDefault`) and `_onResetConfiguration` both exist on the class. `DEFAULT_GLOBAL_MODE` and `DEFAULT_TIME_PROGRAM` constants are declared at module scope and used by the handlers.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Move the reset button into Temperatures card and add a new one to Configuration card</name>
  <files>frontend/src/components/global-settings-tab.ts</files>
  <behavior>
    - `_renderTemperaturesCard()` renders a `<button class="reset-btn" @click=${this._onResetTemperatures}>Reset to default</button>` immediately after the `.temp-fields` grid, still inside `.card-content`.
    - `_renderConfigCard()` no longer renders any reset button tied to `_onResetTemperatures`. Instead, after the `.time-program-section` block, it renders a `<button class="reset-btn" @click=${this._onResetConfiguration}>Reset to default</button>` inside `.card-content`.
    - Both buttons reuse the existing `.reset-btn` CSS class (no new styles needed).
    - The Current Status card is unchanged.
    - Clicking the Temperatures reset triggers `_onResetTemperatures` only; clicking the Configuration reset triggers `_onResetConfiguration` only — verified by reading the template bindings, not via runtime test.
  </behavior>
  <action>In `frontend/src/components/global-settings-tab.ts`: (1) In `_renderTemperaturesCard`, append `<button class="reset-btn" @click=${this._onResetTemperatures}>Reset to default</button>` after the closing `</div>` of `.temp-fields` and before the closing `</div>` of `.card-content`. (2) In `_renderConfigCard`, locate the existing `<button class="reset-btn" @click=${this._onResetToDefault}>...</button>` line (now stale after Task 1's rename) and replace its handler binding with `@click=${this._onResetConfiguration}`; keep the label `Reset to default`. Verify the Configuration card no longer references `_onResetTemperatures`. Do NOT change CSS — `.reset-btn` already exists.</action>
  <verify>
    <automated>cd frontend &amp;&amp; npx tsc --noEmit 2>&amp;1 | tee /tmp/tsc-d6t-t2.log; grep -q "error" /tmp/tsc-d6t-t2.log &amp;&amp; exit 1 || true</automated>
    <automated>cd frontend &amp;&amp; npm run build 2>&amp;1 | tail -20</automated>
    <automated>grep -v '^\s*//' frontend/src/components/global-settings-tab.ts | grep -c "@click=\${this._onResetTemperatures}" | awk '{ if ($1 != 1) { print "expected exactly 1 _onResetTemperatures click binding, found " $1; exit 1 } }'</automated>
    <automated>grep -v '^\s*//' frontend/src/components/global-settings-tab.ts | grep -c "@click=\${this._onResetConfiguration}" | awk '{ if ($1 != 1) { print "expected exactly 1 _onResetConfiguration click binding, found " $1; exit 1 } }'</automated>
    <automated>grep -v '^\s*//' frontend/src/components/global-settings-tab.ts | grep -c "_onResetToDefault" | awk '{ if ($1 != 0) { print "stale _onResetToDefault reference remains: " $1; exit 1 } }'</automated>
  </verify>
  <done>TypeScript compiles. Production build (`npm run build`) succeeds. Exactly one click handler bound to `_onResetTemperatures` (inside Temperatures card render) and exactly one bound to `_onResetConfiguration` (inside Configuration card render). No remaining references to the old `_onResetToDefault` name.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of independent resets on a running HA instance</name>
  <what-built>Two independent "Reset to default" buttons: one in the Temperatures card (resets the 4 setpoints), one in the Configuration card (resets global mode + global time program). Each card's reset leaves the other card's fields untouched.</what-built>
  <how-to-verify>
    1. Rebuild the frontend (`make frontend` or `cd frontend &amp;&amp; npm run build`) and reload Home Assistant (or hard-refresh the Climate Manager panel: Ctrl+Shift+R).
    2. Open the Climate Manager panel → Global Settings tab.
    3. Modify all 4 temperature inputs to non-default values (e.g. frost=9, reduced=17, normal=21, comfort=24). Blur the last field — confirm "Saved" toast.
    4. Change global mode to "Off" via the select. Confirm "Saved" toast.
    5. Drag at least one period boundary in the time-bar to shift it away from defaults. Confirm "Saved" toast.
    6. Click the **Temperatures card** "Reset to default" button.
       - Expected: toast says "Reset to defaults". The 4 temperature inputs snap back to 7, 18, 20, 22.
       - Expected: global mode REMAINS "Off". The time-bar shows the MODIFIED program (NOT reset).
    7. Click the **Configuration card** "Reset to default" button.
       - Expected: toast says "Reset to defaults". Global mode snaps to "Time program". The time-bar shows the default schedule (00:00 frost, 06:00 normal, 22:00 frost) on every day Mon–Sun.
       - Expected: the 4 temperature inputs REMAIN at 7, 18, 20, 22 (already-default values — still independent because step 6 left them at default; no extra change).
    8. Re-set temperatures to non-default values (frost=9, etc.) and confirm step 7 again: the Configuration reset must not change them.
    9. Reload the panel (Ctrl+Shift+R) and confirm all reset values persist (proves the backend received and stored them).
  </how-to-verify>
  <resume-signal>Type "approved" if both buttons reset only their own card's fields and persist after reload, or describe the deviation.</resume-signal>
</task>

</tasks>

<verification>
- TypeScript compilation passes (`npx tsc --noEmit` in `frontend/`)
- Production build succeeds (`npm run build` in `frontend/`)
- Both reset handlers exist, are wired to their respective buttons, and use the documented defaults
- Manual UI verification confirms independence of the two resets and post-reload persistence
</verification>

<success_criteria>

- Temperatures card has a "Reset to default" button that restores the 4
  setpoints to backend defaults and nothing else
- Configuration card has a "Reset to default" button that restores
  `global_mode = "time_program"` and the default 7-day program and nothing else
- Both buttons show success/error toasts and trigger a config reload
- Build is green; manual verification approved </success_criteria>

<output>
Create `.planning/quick/260521-d6t-add-reset-buttons-to-temperatures-and-co/260521-d6t-SUMMARY.md` when done
</output>
