---
quick_id: 260520-q5m
slug: room-person-chip-associations
description:
  replace room checkboxes with chip UI in person-card; add persons chip section
  in room-card
date: 2026-05-20
status: ready
---

# Quick Task 260520-q5m: Room/Person Chip Associations

## Changes

### person-card.ts

- Remove ha-checkbox room list
- Add chip UI: [icon] Room Name [×] for each assigned room
- Add "Add room" button → inline <select> of unassigned rooms on click
- Add @state() \_showRoomAdd = false
- Call panel.reloadConfig() after saves so chips refresh

### room-card.ts

- Add "Associated persons" section (currently missing)
- Compute assigned persons: persons in panelConfig.persons whose room_ids
  includes this roomId
- Chip UI: [mdi:account] Person Name [×] per assigned person
- Add "Add person" button → inline <select> of unassigned persons on click
- Add @state() \_showPersonAdd = false
- Save via ws.setPersonConfig(personId, { room_ids: newIds })
- Call panel.reloadConfig() after saves

## Data flow

- Person-room association lives in PersonConfig.room_ids (person-owned)
- room-card reads from panelConfig.persons to find which persons are in this
  room
- Modifying from room side calls ws.setPersonConfig on the targeted person
- Both sides call reloadConfig() to sync UI state after saves
