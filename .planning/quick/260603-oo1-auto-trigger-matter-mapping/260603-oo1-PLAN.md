---
quick_id: 260603-oo1
slug: auto-trigger-matter-mapping
type: quick
autonomous: true
files_modified:
  - custom_components/climate_manager/__init__.py
  - tests/test_init.py
---

<objective>
Auto-trigger Matter→Tado X mapping detection so the integration populates
`matter_mappings` without requiring the user to open Settings.

Purpose: `suggest_matter_mappings` already knows how to find pairings; this
wires it into two existing trigger points — startup (when mappings are absent)
and registry change (always, additive-only) — so mappings appear the moment
devices are registered in HA.

Output:
- `_async_auto_detect_matter_mappings(hass, entry)` helper in `__init__.py`
- Two call sites in `async_setup_entry` and `_async_refresh_rooms`
- Three new tests in `tests/test_init.py`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@custom_components/climate_manager/__init__.py
@custom_components/climate_manager/discovery.py
@tests/test_init.py
@tests/test_discovery.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add `_async_auto_detect_matter_mappings` and wire trigger points</name>
  <files>custom_components/climate_manager/__init__.py</files>
  <behavior>
    - Helper calls `suggest_matter_mappings(hass)` → gets
      `{tado_id: [matter_ids]}`.
    - For each `tado_id` in suggestions: if `tado_id` is NOT already a key
      in `runtime_config["matter_mappings"]`, add it with its `matter_ids`
      list.
    - If `runtime_config` has no `"matter_mappings"` key yet, treat it as
      `{}` (do not crash).
    - Only saves + refreshes listeners when at least one new mapping was
      added (saves = `entry.runtime_data.store.async_save(
      entry.runtime_data.runtime_config)`, refresh =
      `coordinator._async_refresh_matter_listeners()`).
    - `async_setup_entry` calls the helper after
      `await coordinator.async_evaluate()` only when
      `not entry.runtime_data.runtime_config.get("matter_mappings")`.
    - `_async_refresh_rooms` calls the helper unconditionally after updating
      `entry.runtime_data.rooms` and before (or after) calling
      `coordinator.async_evaluate()` — additive-only logic keeps existing
      mappings safe.
  </behavior>
  <action>
    Add the import `from .discovery import suggest_matter_mappings` to the
    existing discovery import line (it already imports `discover_rooms`,
    `discover_room_sensors`, `discover_persons` — extend it).

    Define the module-level async helper immediately before
    `async_unload_entry`:

    ```
    async def _async_auto_detect_matter_mappings(
        hass: HomeAssistant,
        entry: ClimateManagerConfigEntry,
    ) -> None:
    ```

    Implementation logic (prose, not code):
    - Await `suggest_matter_mappings(hass)` → `suggestions`.
    - If `suggestions` is empty, return early.
    - Read `existing = entry.runtime_data.runtime_config.setdefault(
      "matter_mappings", {})`.
    - Iterate suggestions; for each `tado_id` not in `existing`, set
      `existing[tado_id] = matter_ids` and mark `changed = True`.
    - If `changed`: await
      `entry.runtime_data.store.async_save(
      entry.runtime_data.runtime_config)`, then if
      `entry.runtime_data.coordinator is not None` await
      `entry.runtime_data.coordinator._async_refresh_matter_listeners()`.

    In `async_setup_entry`, after `await coordinator.async_evaluate()` and
    before `async_track_time_interval`, add:

    ```
    if not entry.runtime_data.runtime_config.get("matter_mappings"):
        await _async_auto_detect_matter_mappings(hass, entry)
    ```

    In `_async_refresh_rooms`, after updating `entry.runtime_data.rooms` /
    `room_auto_sensors` and before the `coordinator.async_evaluate()` call,
    add:

    ```
    await _async_auto_detect_matter_mappings(hass, entry)
    ```

    No other changes. Do not alter unload, websocket, or scheduler sections.
    Docstring for the helper must note: additive-only (never overwrites an
    existing key), saves only when something changed, safe to call repeatedly.
  </action>
  <verify>
    <automated>make test 2>&1 | tail -20</automated>
  </verify>
  <done>
    `_async_auto_detect_matter_mappings` exists in `__init__.py`, is called
    from both trigger points, and `make test` passes with no regressions in
    `test_init.py` or `test_discovery.py`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Three tests for auto-detect behaviour in `tests/test_init.py`</name>
  <files>tests/test_init.py</files>
  <behavior>
    - test_auto_detect_on_startup_when_empty: fresh entry (no
      `matter_mappings`), registry contains one tado_x climate entity whose
      zone device has a valve sub-device with serial "VA1111111111" and a
      Matter device with identifier ("matter", "serial_VA1111111111") bearing
      a matching matter climate entity → after `async_setup(entry.entry_id)`
      + `async_block_till_done()`,
      `entry.runtime_data.runtime_config["matter_mappings"]` is non-empty and
      the tado climate entity_id is a key.
    - test_auto_detect_skipped_on_startup_when_already_set: entry data
      pre-seeded with `{"matter_mappings": {"climate.tado_x_zone": [
      "climate.matter_x"]}}`, same registry setup → after setup,
      `matter_mappings` still equals the pre-seeded value (helper not called
      / nothing overwritten, no crash).
    - test_auto_detect_on_registry_change_additive: setup with one existing
      manual mapping `{"matter_mappings": {"climate.existing": [
      "climate.manual"]}}`, then fire `er.EVENT_ENTITY_REGISTRY_UPDATED` with
      `action="create"` and a `entity_id` starting with `"climate."` to
      trigger `_async_refresh_rooms` → after `async_block_till_done()`,
      `matter_mappings` still contains the original manual entry AND has the
      auto-detected entry for the tado_x climate entity whose device/serial
      matches (add that device/entity to the registry before setup).

    Use the same scaffold as existing tests in `test_init.py`:
    `MockConfigEntry(domain=DOMAIN, data={})` + `hass` fixture.
    Wire up device/entity registry entries using `dr.async_get(hass)` and
    `er.async_get(hass)` following the pattern from `test_discovery.py`
    (`test_suggest_matter_mappings_matches_by_serial`):
    - `MockConfigEntry` for `tado_x` and `matter` domains, both
      `.add_to_hass(hass)`.
    - Zone device with identifiers `{("tado_x", "zone_X")}`.
    - Valve sub-device with identifiers `{("tado_x", "VA<serial>")}`;
      update `via_device_id` to zone device.
    - Matter device with identifiers `{("matter", "serial_VA<serial>")}`.
    - Tado climate entity on zone device; Matter climate entity on matter
      device.

    For the registry-change test, fire the event via `hass.bus.async_fire`
    with the `EVENT_ENTITY_REGISTRY_UPDATED` constant from
    `homeassistant.helpers.entity_registry`.
  </behavior>
  <action>
    Append the three test functions to `tests/test_init.py`. Add imports at
    the top of the file as needed:
    `from homeassistant.helpers import device_registry as dr,
    entity_registry as er` and the `EVENT_ENTITY_REGISTRY_UPDATED` constant.

    Use unique serial strings per test to prevent cross-test registry
    collisions (e.g. "VA1111111111", "VA2222222222", "VA3333333333").

    For `test_auto_detect_skipped_on_startup_when_already_set`: pass the
    pre-seeded mapping in `MockConfigEntry(domain=DOMAIN, data={})` then
    patch the store's `async_load` to return a config dict that already
    contains `"matter_mappings"` — OR seed the entry store by adding a
    conftest fixture that pre-populates
    `ClimateManagerStore._store._data` before setup. The simpler approach:
    use `unittest.mock.patch` on
    `custom_components.climate_manager.storage.ClimateManagerStore.async_load`
    to return `{"matter_mappings": {"climate.tado_x_zone": [
    "climate.matter_x"]}}`.

    No new conftest.py needed; patch inline with `pytest.MonkeyPatch` or
    `unittest.mock.patch` context manager.
  </action>
  <verify>
    <automated>make test 2>&1 | tail -20</automated>
  </verify>
  <done>
    All three new tests pass. `make test` exits 0. No existing tests broken.
  </done>
</task>

<task type="auto">
  <name>Task 3: Lint</name>
  <files>custom_components/climate_manager/__init__.py, tests/test_init.py</files>
  <action>
    Run `make lint` and fix any editorconfig violations (line length ≤ 80
    chars for `.py`, LF endings, final newline, no trailing whitespace).
    The helper's docstring and inline comments must also comply.
    No logic changes — formatting only.
  </action>
  <verify>
    <automated>make lint</automated>
  </verify>
  <done>
    `make lint` exits 0 with no violations reported for the two modified
    files.
  </done>
</task>

</tasks>

<success_criteria>
- `_async_auto_detect_matter_mappings` exists in `__init__.py` with
  additive-only logic and conditional save.
- Called from `async_setup_entry` (empty-gate) and `_async_refresh_rooms`
  (unconditional).
- Three new tests in `test_init.py` all pass.
- `make test` exits 0 (no regressions).
- `make lint` exits 0.
</success_criteria>

<output>
No SUMMARY.md required for quick tasks.
</output>
