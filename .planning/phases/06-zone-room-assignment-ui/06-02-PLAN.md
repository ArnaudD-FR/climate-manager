---
phase: 06-zone-room-assignment-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - frontend/src/main.ts
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "Tab bar renders in order: Overview | Default Zone | [custom zone tabs] |
      + | Rooms | Persons (UI-01, D-01)"
    - "Custom zone tabs appear immediately after create_zone resolves; the new
      tab becomes active and the zone name field is focused (D-03, UI-02)"
    - "Custom zone tabs disappear immediately after delete_zone resolves;
      _activeTab falls back to 'global' if the deleted zone was active (UI-02)"
    - "Clicking the + button calls ws.createZone with default name 'Zone N'
      where N = Object.keys(config.zones).length + 1 (D-02)"
    - "_activeTab type accepts arbitrary strings; 'zone_default' selects the
      Default Zone tab; 'zone_<uuid>' selects a custom zone tab"
    - "localStorage stores the active tab id; on load, if the stored value is
      'zone_<uuid>' for a non-existent zone, _activeTab falls back to 'global'"
    - "Default Zone tab and custom zone tabs both render
      <climate-manager-zone-tab>; isDefault toggles delete button visibility"
  artifacts:
    - path: "frontend/src/main.ts"
      provides:
        "Dynamic zone tabs in tab-bar, + button, focus-on-new-zone,
        _validateActiveTab() guard"
      contains: "_onCreateZone"
  key_links:
    - from: "frontend/src/main.ts"
      to: "frontend/src/components/zone-tab.ts"
      via:
        "side-effect import + <climate-manager-zone-tab> render in
        _renderTabContent"
      pattern: "<climate-manager-zone-tab"
    - from: "frontend/src/main.ts"
      to: "frontend/src/ws-client.ts"
      via: "this._ws.createZone(...)"
      pattern: "this\\._ws[\\?\\.]?.*createZone"
---

<objective>
Wire the panel root to render dynamic zone tabs, add the + button, and handle activeTab persistence for arbitrary zone ids. Depends on Plan 01 because the zone-tab component and the WsClient.createZone method must exist before main.ts can import or call them.

Purpose: UI-01 (tab order), UI-02 (dynamic tabs), and UI-03 (+ button) are
entirely main.ts concerns — the rest of the integration (per-zone editing, room
assignment, badges) lives in zone-tab.ts and room-card.ts. Without this plan,
the new component is unreachable from the panel.

Output: frontend/src/main.ts gains zone tab rendering, the + button handler,
post-load \_activeTab validation, and an `import "./components/zone-tab.js"`
side-effect import. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-zone-room-assignment-ui/06-CONTEXT.md
@.planning/phases/06-zone-room-assignment-ui/06-PATTERNS.md
@.planning/phases/06-zone-room-assignment-ui/06-01-SUMMARY.md
@frontend/src/main.ts
@frontend/src/types.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dynamic zone tabs and + button in main.ts</name>
  <files>frontend/src/main.ts</files>
  <read_first>
    - frontend/src/main.ts (current implementation — tab-bar at lines 238-251, _renderTabContent at lines 261-289, _setTab at lines 212-215, _activeTab initialization at lines 42-45, _loadConfig at lines 157-165)
    - .planning/phases/06-zone-room-assignment-ui/06-CONTEXT.md (D-01 tab order, D-02 default name "Zone N", D-03 focus on new zone, Claude's Discretion section: _activeTab broadened to string, "zone_default" / "zone_<uuid>" identifiers, localStorage fallback)
    - .planning/phases/06-zone-room-assignment-ui/06-PATTERNS.md (section "main.ts" — lines 274-410: exact tab-bar markup, _onCreateZone handler, _renderTabContent extension)
    - frontend/src/components/zone-tab.ts (after Plan 01 — verify the public properties needed: config, zoneId, zoneConfig, isDefault, ws, panel, hass)
    - frontend/src/types.ts (ClimateConfig.zones, ClimateConfig.default_zone_name, ClimateConfig.global_mode, ClimateConfig.global_time_program — all required for synthesizing the Default Zone view)
    - frontend/src/ws-client.ts (after Plan 01 — verify createZone returns {zone_id, name, mode, time_program})
  </read_first>
  <action>
    Modify `frontend/src/main.ts` to:

    1. Add a side-effect import after the existing `import "./components/persons-tab.js"` (line 31): `import "./components/zone-tab.js"`. Do NOT remove or reorder existing imports.

    2. Replace the `_activeTab` initializer (lines 42-45). Current code constrains to the union `["global", "rooms", "persons"]`. Replace with a single-line reader that accepts any non-null string:
       `@state() private _activeTab: string = localStorage.getItem("climate-manager-tab") ?? "global";`
       The pre-config validation cannot check zone UUIDs (config not loaded yet), so the post-load guard in `_validateActiveTab` (added below) handles stale UUIDs.

    3. Add a new private method `_validateActiveTab()`:
       - No-op if `this._config` is null.
       - Return immediately if `_activeTab` is one of `"global"`, `"rooms"`, `"persons"`, or `"zone_default"` (Default Zone always exists per ZONE-02).
       - Otherwise, if `_activeTab` starts with `"zone_"`, strip the prefix to get the UUID. If `this._config.zones[uuid]` does not exist, set `this._activeTab = "global"` and update localStorage.
       - If `_activeTab` is none of the above (corrupted or unknown), set to `"global"` and update localStorage.

    4. Call `this._validateActiveTab()` from inside `_loadConfig()` IMMEDIATELY after `this._config = await this._ws.getConfig()` succeeds. This ensures every config reload (including post-delete reloads from Plan 01's zone delete handler) re-validates the active tab.

    5. Add a new private method `_onCreateZone()` (async). Body:
       - Return early if `!this._config || !this._ws` (defensive).
       - Compute `newName = \`Zone \${Object.keys(this._config.zones).length + 1}\`` per D-02.
       - try: `const result = await this._ws.createZone(newName);` — result type is `{zone_id, name, mode, time_program}` (from Plan 01).
       - `await this._loadConfig();` (so the new zone shows up in the tabs list before we switch to it).
       - `this._setTab("zone_" + result.zone_id);` per D-03.
       - After updateComplete, focus the zone name element in the newly rendered zone-tab. Implementation: `await this.updateComplete; const zoneTab = this.shadowRoot?.querySelector("climate-manager-zone-tab"); zoneTab?.shadowRoot?.querySelector<HTMLElement>(".zone-name")?.click();` — clicking the .zone-name element triggers zone-tab's click-to-edit handler which focuses the input. (Plan 01's `_onNameClick` does the focus.)
       - `this.showToast("Zone created", false);`
       - catch: `this.showToast("Create zone failed", true);` — do NOT rethrow.

    6. Rewrite the `.tab-bar` markup in `render()` (currently lines 238-251). New markup, in this exact left-to-right order (UI-01 / D-01):
       a. Overview button — unchanged, calls `_setTab("global")`, active when `_activeTab === "global"`.
       b. Default Zone button — `<button class="tab-btn ${this._activeTab === "zone_default" ? "active" : ""}" @click=${() => this._setTab("zone_default")}>${this._config.default_zone_name}</button>`. The button label is the user-editable default zone name.
       c. Custom zone tabs — `Object.entries(this._config.zones).map(([zoneId, zone]) => html\`<button class="tab-btn ${this._activeTab === "zone_" + zoneId ? "active" : ""}" @click=${() => this._setTab("zone_" + zoneId)}>${zone.name}</button>\`)`. Order: the object insertion order (backend uses uuid4 keys; no sort key from backend, so display in iteration order).
       d. + button — `<button class="tab-btn add-zone-btn" title="Add zone" @click=${() => void this._onCreateZone()}>+</button>`. The `.add-zone-btn` extra class is for an optional styling hint but `.tab-btn` is sufficient — per D-01 / 06-PATTERNS.md, no text label, just the + symbol.
       e. Rooms button — unchanged target (`_setTab("rooms")`), active when `_activeTab === "rooms"`.
       f. Persons button — unchanged target (`_setTab("persons")`), active when `_activeTab === "persons"`.

    7. Extend `_renderTabContent()` (lines 261-289). Add two new cases BEFORE the existing switch (so they intercept zone_* before reaching the default):
       - If `this._activeTab === "zone_default"`: render
         ```
         <climate-manager-zone-tab
           .config=${this._config!}
           .zoneId=${"default"}
           .zoneConfig=${{
             name: this._config!.default_zone_name,
             mode: this._config!.global_mode,
             time_program: this._config!.global_time_program,
           }}
           .isDefault=${true}
           .ws=${this._ws!}
           .panel=${this}
           .hass=${this.hass}
         ></climate-manager-zone-tab>
         ```
         The synthesized ZoneConfig binds the Default Zone editor to the global mode + global time program per Phase 4 D-02 (Default Zone mode/program = global_mode/global_time_program). Renaming the Default Zone in this tab calls renameZone("default", name) which the backend routes to `default_zone_name` per Phase 5 D-05.
       - Else if `this._activeTab.startsWith("zone_")`: extract `zoneId = this._activeTab.slice(5)`. If `this._config!.zones[zoneId]` is undefined, return `html\`\`` (defensive — `_validateActiveTab` should have caught this, but render safely). Otherwise render `<climate-manager-zone-tab .config=${this._config!} .zoneId=${zoneId} .zoneConfig=${this._config!.zones[zoneId]} .isDefault=${false} .ws=${this._ws!} .panel=${this} .hass=${this.hass}></climate-manager-zone-tab>`.

       The existing switch (cases "global" / "rooms" / "persons" / default) stays as-is below these zone checks.

    8. Add minimal CSS to the static styles block for the + button (optional but recommended for visual distinction). Add a rule `.add-zone-btn { font-size: 18px; font-weight: 300; padding: 12px 14px; }`. Append at the end of the existing `static styles = css\`...\`` block.

    Do NOT:
    - Change the existing toast / status / reloadConfig / patchConfig methods.
    - Remove the existing "Overview" / "Rooms" / "Persons" buttons or their handlers.
    - Modify the ws-client.ts or zone-tab.ts files (those are Plan 01's deliverables and are now immutable from this plan's perspective).
    - Touch room-card.ts or person-card.ts (those are Plan 03).

  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npx vite build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npx vite build` exits 0
    - `grep -c '^import "\./components/zone-tab\.js";' frontend/src/main.ts` returns `1`
    - `grep -E 'private _validateActiveTab\(\)' frontend/src/main.ts` matches
    - `grep -E 'private (async )?_onCreateZone\(\)' frontend/src/main.ts` matches
    - `grep -E 'this\._ws.*createZone\(' frontend/src/main.ts` matches
    - `grep -E '_setTab\("zone_" \+' frontend/src/main.ts` matches (post-create tab switch uses zone_ prefix)
    - `grep -E '_activeTab === "zone_default"' frontend/src/main.ts` matches (at least one occurrence in tab-bar render and one in _renderTabContent)
    - `grep -E 'this\._config\.zones' frontend/src/main.ts` matches (zone enumeration in tab-bar)
    - `grep -c '<climate-manager-zone-tab' frontend/src/main.ts` returns at least `2` (Default Zone branch + custom zone branch)
    - `grep -E 'title="Add zone"' frontend/src/main.ts` matches (+ button tooltip)
    - `grep -E '@state\(\) private _activeTab: string = localStorage\.getItem\("climate-manager-tab"\)' frontend/src/main.ts` matches (initializer no longer constrains to union)
    - `grep -E '_validateActiveTab\(\)' frontend/src/main.ts | wc -l` returns at least `2` (method declaration + at least one call from _loadConfig)
    - The render() function preserves the original Overview / Rooms / Persons buttons (verify all three labels still appear: `grep -c '>Overview<\|>Rooms<\|>Persons<' frontend/src/main.ts` returns at least `3`)
  </acceptance_criteria>
  <done>main.ts renders the Overview | Default Zone | [custom zones] | + | Rooms | Persons tab order, the + button creates a new zone and switches to it focused, and stale zone IDs in localStorage fall back to "global" cleanly after config load.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                                                                                             |
| -------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| localStorage → \_activeTab | Stored tab id is untrusted (user could edit it via DevTools). \_validateActiveTab() rejects unknown values by falling back to "global". |
| User click → ws.createZone | createZone name is hardcoded client-side ("Zone N"); user does not type at this point. Renames go through Plan 01's renameZone path.    |

## STRIDE Threat Register

| Threat ID | Category          | Component                                 | Disposition | Mitigation Plan                                                                                                                                                                                      |
| --------- | ----------------- | ----------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-03   | Tampering         | localStorage "climate-manager-tab" value  | mitigate    | `_validateActiveTab()` falls back to "global" for any unknown tab id; runs after every config load.                                                                                                  |
| T-06-04   | Denial of Service | rapid + button clicks creating many zones | accept      | Backend create_zone is idempotent per-call (each generates a new UUID); user can delete spurious zones via the inline delete in zone-tab.ts. Throttling adds UX complexity without security benefit. |
| T-06-SC   | Tampering         | npm/pip/cargo installs                    | mitigate    | No new package installs.                                                                                                                                                                             |

</threat_model>

<verification>
- TypeScript compiles (`cd frontend && npx tsc --noEmit` exits 0)
- Vite build succeeds (`cd frontend && npx vite build` exits 0)
- Tab bar renders in the documented order
- + button is positioned after the last custom zone and before Rooms
- Default Zone tab and custom zone tabs both render <climate-manager-zone-tab> with the correct props
- _validateActiveTab() is invoked from _loadConfig() and falls back to "global" for stale zone UUIDs
</verification>

<success_criteria>

- UI-01 ordered tab bar
- UI-02 zone tabs appear/disappear in sync with \_config.zones
- UI-03 + button creates a zone with default name and switches to it focused
- No regression in existing Overview / Rooms / Persons tabs </success_criteria>

<output>
Create `.planning/phases/06-zone-room-assignment-ui/06-02-SUMMARY.md` when done.
</output>
