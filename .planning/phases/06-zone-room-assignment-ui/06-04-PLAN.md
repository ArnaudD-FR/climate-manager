---
phase: 06-zone-room-assignment-ui
plan: 04
type: execute
wave: 1
depends_on:
  - 06-01
  - 06-02
  - 06-03
files_modified:
  - custom_components/climate_manager/storage.py
  - custom_components/climate_manager/websocket.py
  - tests/test_storage.py
  - frontend/src/components/zone-tab.ts
  - frontend/src/components/room-card.ts
  - frontend/src/main.ts
autonomous: true
gap_closure: true
requirements:
  - ASSIGN-01
  - ASSIGN-02
  - UI-01
  - UI-04
  - UI-06

must_haves:
  truths:
    - "Tab bar's first tab is labeled 'Global Settings' (not 'Overview') —
      matches ROADMAP Phase 6 Success Criterion 1 wording (UI-01)"
    - "Default Zone tab mode <select> change persists: clicking an option calls
      ws.setGlobalMode (NOT ws.setZoneMode) — the WS round-trip returns success
      (UI-04 + ASSIGN-01 prerequisite)"
    - "Default Zone tab time-bar change persists: dragging a period calls
      ws.setTimeProgram (NOT ws.setZoneTimeProgram) — the WS round-trip returns
      success (UI-04)"
    - "Removing a room chip from a custom zone tab moves the room to the Default
      Zone — the next get_config shows the room's zone_id key absent from
      rooms[room_id] (ASSIGN-01 + D-08)"
    - "Adding a room to the Default Zone tab via the search-picker moves the
      room to the Default Zone — backend accepts the request and the next
      get_config shows zone_id absent (ASSIGN-01 + WR-01 fix)"
    - "Selecting the empty/Default-Zone option in a room card's zone <select>
      moves that room to the Default Zone — get_config shows zone_id absent on
      that room (ASSIGN-02 + UI-06)"
    - "Backend set_room_config handler treats an incoming {zone_id: null} as
      'pop zone_id key' — validate_zone_assignment never sees a null zone_id and
      never raises (gap fix prerequisite)"
  artifacts:
    - path: "custom_components/climate_manager/storage.py"
      provides:
        "validate_zone_assignment unchanged contract — still rejects literal
        null zone_id stored on a room (defense in depth); WS handler is
        responsible for stripping null before save"
      contains: "validate_zone_assignment"
    - path: "custom_components/climate_manager/websocket.py"
      provides:
        "ws_set_room_config strips zone_id when incoming value is None before
        sparse-merge"
      contains: "ws_set_room_config"
    - path: "tests/test_storage.py"
      provides:
        "regression test asserting validate_zone_assignment still rejects stored
        null (defense-in-depth contract preserved)"
      contains: "test_validate_zone_assignment_rejects_explicit_null"
    - path: "frontend/src/components/zone-tab.ts"
      provides:
        "isDefault branches in _onModeChange and _onPeriodsChanged; null zone_id
        sends for _onRemoveRoom and Default-Zone _onAddRoom"
      contains: "this.isDefault"
    - path: "frontend/src/components/room-card.ts"
      provides:
        "_onZoneChange sends zone_id: null when the empty-string option is
        selected"
      contains: "zone_id: null"
    - path: "frontend/src/main.ts"
      provides: "First tab button labeled 'Global Settings'"
      contains: ">Global Settings<"
  key_links:
    - from: "frontend/src/components/zone-tab.ts"
      to: "frontend/src/ws-client.ts"
      via:
        "ws.setGlobalMode and ws.setTimeProgram for the Default Zone branches"
      pattern: "this\\.ws\\.(setGlobalMode|setTimeProgram)"
    - from: "frontend/src/components/zone-tab.ts"
      to: "custom_components/climate_manager/websocket.py"
      via:
        "set_room_config with {zone_id: null} sent over WS (handler must pop the
        key)"
      pattern: "zone_id: null"
    - from: "frontend/src/components/room-card.ts"
      to: "custom_components/climate_manager/websocket.py"
      via:
        "set_room_config with {zone_id: null} when Default Zone option chosen"
      pattern: "zone_id: null"
---

<objective>
Close the six verified gaps from `06-VERIFICATION.md` so that every interactive zone mutation path lands at the backend. All bugs are surgical wiring errors — no architectural changes, no new components.

Purpose: Phase 6 ships the structural UI (tabs, zone tab component, badges,
pickers) but four code-level bugs in `zone-tab.ts` + one in `room-card.ts` + one
tab-label mismatch in `main.ts` together prevent the phase goal from being
achieved. The Default Zone tab silently fails to save mode or time-program
changes; removing a room from a custom zone is a no-op; adding a room to the
Default Zone fails; and selecting Default Zone from a room card's zone picker is
a no-op. The fix replaces the broken `zone_id: undefined` / `zone_id: "default"`
payloads with an explicit `zone_id: null` and extends the backend
`set_room_config` handler to interpret null as "pop key" (the sparse-D-06
semantics already implemented for the load path).

Output:

- `custom_components/climate_manager/websocket.py` — `ws_set_room_config` strips
  `zone_id` from the incoming config dict when the value is None, BEFORE
  sparse-merge.
- `custom_components/climate_manager/storage.py` — unchanged behavior contract;
  validator still rejects a stored `zone_id: null` as defense in depth (because
  the handler pop happens first, validator never sees null in the happy path).
- `tests/test_storage.py` — explicit regression test that
  `validate_zone_assignment` still raises on a stored `zone_id: null` (locks the
  contract).
- `frontend/src/components/zone-tab.ts` — `_onModeChange` routes Default Zone to
  `ws.setGlobalMode`; `_onPeriodsChanged` routes Default Zone to
  `ws.setTimeProgram`; `_onRemoveRoom` uses `zone_id: null`; `_onAddRoom`
  Default-Zone branch uses `zone_id: null`.
- `frontend/src/components/room-card.ts` — `_onZoneChange` empty-string branch
  sends `zone_id: null` (not `undefined`).
- `frontend/src/main.ts` — first tab button label changes from `Overview` to
  `Global Settings` (line 288). </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/06-zone-room-assignment-ui/06-CONTEXT.md
@.planning/phases/06-zone-room-assignment-ui/06-VERIFICATION.md
@.planning/phases/06-zone-room-assignment-ui/06-01-SUMMARY.md
@.planning/phases/06-zone-room-assignment-ui/06-02-SUMMARY.md
@.planning/phases/06-zone-room-assignment-ui/06-03-SUMMARY.md
@custom_components/climate_manager/websocket.py
@custom_components/climate_manager/storage.py
@frontend/src/components/zone-tab.ts
@frontend/src/components/room-card.ts
@frontend/src/main.ts
@frontend/src/ws-client.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — set_room_config pops zone_id when value is null + lock the contract with a test</name>
  <files>custom_components/climate_manager/websocket.py, tests/test_storage.py</files>
  <read_first>
    - custom_components/climate_manager/websocket.py (lines 336-377 — `_make_ws_set_room_config` handler; note line 365 `.update(msg["config"])` is where the sparse-merge happens, line 360 takes the rooms_backup for CR-01 rollback)
    - custom_components/climate_manager/storage.py (lines 22-56 — `validate_zone_assignment` with `_SENTINEL` distinguishing absent vs null; line 49-52 raises on explicit null; line 135 `validate_zone_assignment(config)` is called from `async_save` BEFORE `_store.async_save`)
    - tests/test_storage.py (lines 180-218 existing validator tests; line 220-onwards `test_save_rejects_unknown_zone_id` integration test — uses ClimateManagerStore + DEFAULT_CONFIG pattern; line 10 imports `validate_zone_assignment`)
    - .planning/phases/06-zone-room-assignment-ui/06-VERIFICATION.md (Gap 3 / CR-03 — describes the JSON.stringify({zone_id: undefined}) === '{}' no-op and prescribes the null-handling fix on the backend)
    - .planning/phases/04-zone-data-model-storage/04-CONTEXT.md (D-06 — sparse model: absent zone_id = Default Zone member; storing literal None is prohibited because async_load merges OVER DEFAULT_CONFIG and None would mask a real default — this is why the validator still rejects null on stored data)
  </read_first>
  <behavior>
    - **Test 1 (regression / defense in depth):** `test_validate_zone_assignment_rejects_explicit_null` — calling `validate_zone_assignment({"zones": {}, "rooms": {"living_room": {"zone_id": None}}})` raises ValueError with message containing `"sparse model prohibits explicit null"`. Locks the storage-layer contract so future refactors cannot silently allow null.
    - **Test 2 (handler behavior, runtime_config inspection):** `test_set_room_config_pops_zone_id_when_null` — when the WS handler is invoked with `msg = {"type": "climate_manager/set_room_config", "room_id": "living_room", "config": {"zone_id": None}}` against an entry whose `runtime_config["rooms"]["living_room"] = {"zone_id": "some-uuid"}`, after the handler completes:
      - `entry.runtime_data.runtime_config["rooms"]["living_room"]` MUST exist
      - the `"zone_id"` key MUST be absent from that dict (not present with value None, not present with value "some-uuid")
      - the connection MUST have received a success result (`send_result(msg["id"], {"success": True})`)
      - `validate_zone_assignment` MUST NOT raise (since the saved config has no rooms with null zone_id)
    - **Test 3 (handler behavior, null is no-op for rooms that already have no zone_id):** `test_set_room_config_null_zone_id_is_idempotent_when_already_absent` — calling the handler with `config: {"zone_id": None}` on a room whose runtime config has no `zone_id` key succeeds and leaves the room dict unchanged (no zone_id key added, no error).
    - **Test 4 (handler behavior, non-zone_id keys are unaffected):** `test_set_room_config_null_zone_id_preserves_other_keys` — `config: {"zone_id": None, "room_mode": "custom"}` results in `runtime_config["rooms"][room_id]` containing `room_mode: "custom"` and NO `zone_id` key. Confirms the pop is targeted (does not eat other keys).
  </behavior>
  <action>
    Two file edits, applied in order.

    **Edit A — `custom_components/climate_manager/websocket.py`, `_make_ws_set_room_config` (lines 336-377):**

    Inside `ws_set_room_config` (the async inner function at line 347), BEFORE the `setdefault(...).setdefault(...).update(...)` chain that begins at line 361, add a single guarded pop:

    Logic to implement (described, NOT a code block — executor writes the lines):
    - Read the incoming patch dict: `incoming_config = msg["config"]` (the dict the panel sent).
    - If `"zone_id"` is a key in `incoming_config` AND its value is `None`:
        - Remove the key from `incoming_config` (use `incoming_config.pop("zone_id")`).
        - Also pop the existing `zone_id` from the live room entry so the room becomes a Default Zone member. The live room entry is reached by `entry.runtime_data.runtime_config.setdefault("rooms", {}).setdefault(msg["room_id"], {})` — pop `"zone_id"` from that dict (use `.pop("zone_id", None)` so absence is a no-op).
    - Leave the existing `rooms_backup = copy.deepcopy(...)` snapshot at line 360 in place — it MUST come before any mutation (CR-01 rollback contract).

    Important ordering: the snapshot at line 360 must execute BEFORE the pop on the live room entry. The pop on `incoming_config` is a local-dict edit and order does not matter, but for clarity keep both pops together right after the snapshot.

    The rest of the handler (the `.update(msg["config"])` sparse-merge, the try/except around `async_save`, the `send_result`, and the `coordinator.async_evaluate()` task) stays IDENTICAL. The merge at line 365 is now a no-op for `zone_id` because the key was stripped from `incoming_config`.

    Add a one-line docstring comment near the pop explaining intent: "zone_id: null from the frontend signals 'move room to Default Zone' — pop the key per D-06 sparse model (gap CR-03 fix from VERIFICATION 06-04)."

    Do NOT:
    - Modify `_make_ws_set_zone_mode`, `_make_ws_set_zone_time_program`, or any other handler.
    - Change `validate_zone_assignment` in storage.py (its contract is preserved by this handler-side strip).
    - Touch the rooms_backup CR-01 snapshot/rollback logic — it remains correct.
    - Use any new imports (the handler already imports `copy` at module scope; no further imports needed).

    **Edit B — `tests/test_storage.py`:**

    Append four new tests after `test_validate_zone_assignment_multiple_rooms_same_zone_accepted` (line 218):

    1. `test_validate_zone_assignment_rejects_explicit_null` — synchronous test (no `async`/`hass` fixture). Build `config = {"zones": {}, "rooms": {"living_room": {"zone_id": None}}}`. Use `pytest.raises(ValueError, match="sparse model prohibits explicit null")` around `validate_zone_assignment(config)`. Locks the existing storage.py:49-52 behavior.

    For tests 2-4, the WS handler is unit-tested by directly invoking the inner `ws_set_room_config` coroutine returned by `_make_ws_set_room_config(entry)`. Look at `tests/test_websocket.py` for the established pattern of invoking a WS handler with a fake entry, fake connection, and msg dict. Apply the same pattern in `tests/test_storage.py` IF and ONLY IF that file already has an async hass fixture available (lines 220+ already use `async def test_save_rejects_unknown_zone_id(hass)`); otherwise place the three handler tests in `tests/test_websocket.py` following its existing handler-test pattern.

    2. `test_set_room_config_pops_zone_id_when_null` — sets `entry.runtime_data.runtime_config["rooms"]["living_room"] = {"zone_id": "some-uuid"}` (and ensure `"some-uuid"` is in `entry.runtime_data.runtime_config["zones"]` so the precondition is valid). Invoke handler with `msg["config"] = {"zone_id": None}`. Assert: `"zone_id"` is NOT in `entry.runtime_data.runtime_config["rooms"]["living_room"]`; `connection.send_result` was called with `{"success": True}`; `connection.send_error` was NOT called.

    3. `test_set_room_config_null_zone_id_is_idempotent_when_already_absent` — set room entry to `{"room_mode": "global"}` (no zone_id). Invoke handler with `msg["config"] = {"zone_id": None}`. Assert: room entry still equals `{"room_mode": "global"}`; success returned.

    4. `test_set_room_config_null_zone_id_preserves_other_keys` — set room entry to `{}`. Invoke handler with `msg["config"] = {"zone_id": None, "room_mode": "custom"}`. Assert: `"zone_id"` NOT in room entry; `room_entry["room_mode"] == "custom"`; success returned.

    Place the three handler tests in the file whose existing patterns most cleanly support them. If `tests/test_websocket.py` has a working fake-entry + fake-connection pattern (it does — see line 367+ test_create_zone), use that file and import `_make_ws_set_room_config` there. The single validator-only test (#1) stays in `tests/test_storage.py`.

    Do NOT modify existing tests — only append new ones.

  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager && uv run pytest tests/test_storage.py::test_validate_zone_assignment_rejects_explicit_null tests/test_websocket.py -k "set_room_config" -x 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - `cd /home/arnaud/dev/climate_manager && uv run pytest tests/test_storage.py::test_validate_zone_assignment_rejects_explicit_null -x` exits 0
    - `cd /home/arnaud/dev/climate_manager && uv run pytest tests/test_websocket.py -k "set_room_config" -x` exits 0 with at least 3 tests collected and passing (the three new handler tests)
    - `grep -n 'incoming_config.*pop\("zone_id"\)\|pop("zone_id", None)' custom_components/climate_manager/websocket.py | grep -v '^#'` returns at least 2 lines (pop on incoming patch AND pop on live room entry)
    - `grep -c 'zone_id: null from the frontend signals' custom_components/climate_manager/websocket.py` returns `1` (the explanatory comment is present)
    - `grep -E 'def test_validate_zone_assignment_rejects_explicit_null' tests/test_storage.py` matches
    - `grep -E 'def test_set_room_config_pops_zone_id_when_null|def test_set_room_config_null_zone_id_is_idempotent_when_already_absent|def test_set_room_config_null_zone_id_preserves_other_keys' tests/test_websocket.py | wc -l` returns `3`
    - `validate_zone_assignment` function body in storage.py is UNCHANGED — `git diff custom_components/climate_manager/storage.py` shows no edits to that function (only test additions to tests/test_storage.py count as storage-test changes; storage.py source is untouched)
    - `cd /home/arnaud/dev/climate_manager && uv run pytest tests/ -x 2>&1 | tail -3` exits 0 (no regression in the existing test suite — Phase 4 / Phase 5 / earlier Phase 6 backend tests all still pass)
  </acceptance_criteria>
  <done>Backend treats {zone_id: null} on set_room_config as "pop key", the storage validator still rejects literal null as defense in depth, and four new tests lock both the validator contract and the three handler behaviors.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Frontend — surgical patches for the five wiring bugs + tab label rename</name>
  <files>frontend/src/components/zone-tab.ts, frontend/src/components/room-card.ts, frontend/src/main.ts</files>
  <read_first>
    - frontend/src/components/zone-tab.ts (lines 352-363 `_onModeChange` — CR-01 site; lines 365-379 `_onPeriodsChanged` — CR-02 site; lines 380-394 `_onAddRoom` — WR-01 site; lines 396-411 `_onRemoveRoom` — CR-03 site; verify `this.isDefault` is the existing boolean property from Plan 01)
    - frontend/src/components/room-card.ts (lines 444-466 — `_getZoneName` + `_onZoneChange`; line 458 is the CR-03 mirror site)
    - frontend/src/main.ts (lines 284-292 — current tab bar markup; line 288 reads `>Overview</button>`; line 286 has the activeTab class condition; the localStorage key value `"global"` for that tab MUST NOT change — only the visible label changes)
    - frontend/src/ws-client.ts (lines 37-42 setGlobalMode signature `setGlobalMode(mode: string): Promise<{success: boolean}>`; lines 55-60 setTimeProgram signature `setTimeProgram(program: DailyProgram): Promise<{success: boolean}>`; both already exist — no WS client changes in this task)
    - .planning/phases/06-zone-room-assignment-ui/06-VERIFICATION.md (gaps section is the authoritative bug list; CR-01, CR-02, CR-03, WR-01 are all referenced by line number in zone-tab.ts and room-card.ts)
  </read_first>
  <action>
    Five surgical edits across three files. Each edit is described by exact site and intended behavior — keep all other code in these files UNCHANGED.

    **Edit 1 — `frontend/src/components/zone-tab.ts` `_onModeChange` (around line 353-363, CR-01):**

    The handler currently unconditionally calls `await this.ws.setZoneMode(this.zoneId, newMode);` at line 357. Add a branch on `this.isDefault` so the Default Zone routes its mode change to the global mode endpoint:
    - If `this.isDefault` is `true`: call `await this.ws.setGlobalMode(newMode);`
    - Else: call `await this.ws.setZoneMode(this.zoneId, newMode);` (existing behavior preserved)

    The early-return guard `if (!newMode || newMode === this.zoneConfig.mode) return;` at line 355 stays unchanged — the comparison is correct for both branches because `zoneConfig.mode` for the Default Zone is synthesized from `global_mode` by main.ts (per Phase 4 D-02 and Plan 02's render branch).

    The surrounding try/catch (write-then-reload pattern with `panel.reloadConfig()` and `panel.showToast`) MUST stay intact — only the single line that picks the WS method changes.

    **Edit 2 — `frontend/src/components/zone-tab.ts` `_onPeriodsChanged` (around line 366-379, CR-02):**

    The handler currently calls `await this.ws.setZoneTimeProgram(this.zoneId, program);` at line 372. Add the same `isDefault` branch:
    - If `this.isDefault` is `true`: call `await this.ws.setTimeProgram(program);`
    - Else: call `await this.ws.setZoneTimeProgram(this.zoneId, program);` (existing behavior preserved)

    The `program` variable (built from `this.zoneConfig.time_program` + `dayIndexToKey(dayIndex)` + the changed `periods`) stays IDENTICAL — only the WS method call changes. `e.stopPropagation()` at line 378 remains the last statement.

    Update the existing JSDoc comment on this handler to note: "When isDefault is true, writes go to the global time program via ws.setTimeProgram (the Default Zone is backed by global_time_program per Phase 4 D-02)."

    **Edit 3 — `frontend/src/components/zone-tab.ts` `_onAddRoom` (around line 386-394, WR-01):**

    The handler currently sends `{ zone_id: this.zoneId }` at line 388. When `this.isDefault` is `true`, `this.zoneId === "default"` and the backend `validate_zone_assignment` rejects `"default"` as an unknown zone UUID. Add a branch so the Default Zone path sends a null zone_id (which Task 1's backend pop translates to "remove the key"):
    - If `this.isDefault` is `true`: send `{ zone_id: null }` (TypeScript: assign through `Partial<RoomConfig>` typed as `{ zone_id: null as unknown as string | undefined }` if the strict typing rejects null — see "Type note" below).
    - Else: send `{ zone_id: this.zoneId }` (existing behavior preserved — custom zone UUID).

    Update the existing block comment on this handler (lines 381-385) to replace the "NOTE: If backend rejects 'default' here, flag it rather than silently dropping the assignment." line with: "When isDefault is true, sends zone_id: null — Task 1's backend handler interprets null as 'pop zone_id' per D-06 sparse model (gap WR-01 fix)."

    **Edit 4 — `frontend/src/components/zone-tab.ts` `_onRemoveRoom` (around line 402-411, CR-03 first site):**

    Line 403 currently reads `const patch: Partial<RoomConfig> = { zone_id: undefined };`. JSON.stringify drops undefined keys, so the backend receives `{}`. Replace with `const patch: Partial<RoomConfig> = { zone_id: null as unknown as string | undefined };` — sending literal null over WS preserves the key in the JSON payload, and Task 1's backend handler pops it.

    Type note: the existing `RoomConfig` type in `frontend/src/types.ts` declares `zone_id?: string;`. TypeScript will reject `{ zone_id: null }` against `Partial<RoomConfig>`. Use the cast `null as unknown as string | undefined` as the minimal type escape hatch (do NOT widen the `RoomConfig.zone_id` type in types.ts — that would propagate `null` semantics into every consumer; the cast scope is local to this single line).

    Alternative if the executor finds the cast aesthetically unacceptable: build the WS payload as a plain object literal sent directly via `this.ws.setRoomConfig(roomId, { zone_id: null } as Partial<RoomConfig>)`. Either form is acceptable provided the JSON serialization of the sent message includes the literal `"zone_id":null` token. The acceptance criteria below assert on the resulting source text using a permissive grep.

    The surrounding try/catch + reload + toast pattern stays unchanged.

    **Edit 5 — `frontend/src/components/room-card.ts` `_onZoneChange` (line 456-466, CR-03 second site):**

    Line 458 currently reads `const patch: Partial<RoomConfig> = newZoneId ? { zone_id: newZoneId } : { zone_id: undefined };`. The empty-string branch (Default Zone option) sends undefined and gets dropped by JSON.stringify. Replace the right-hand side so the empty-string branch sends literal null using the same cast pattern as Edit 4:
    - Truthy `newZoneId` branch: `{ zone_id: newZoneId }` (unchanged)
    - Falsy `newZoneId` branch: `{ zone_id: null as unknown as string | undefined }` (was undefined)

    The surrounding try/catch + setRoomConfig + reloadConfig + showToast pattern stays unchanged.

    **Edit 6 — `frontend/src/main.ts` first tab button label (line 288):**

    Line 288 currently reads `>Overview</button>`. Replace `Overview` with `Global Settings` so the rendered label matches ROADMAP Phase 6 Success Criterion 1.

    The button's `_setTab("global")` call at line 287 and the active-class condition `this._activeTab === "global"` at line 286 MUST NOT change — only the visible text between the `>` and `</button>` changes. The localStorage key `"global"` is internal state; do not touch it. (`_validateActiveTab` and every other reference to `"global"` in main.ts stays as-is.)

    Search for any other occurrence of `>Overview<` in main.ts (defensive — there should be only one); if a second occurrence exists (e.g., in a comment or another render branch), report it but do NOT change additional sites without explicit confirmation. The verification grep enforces zero remaining `>Overview<` strings.

    **Do NOT (across all 6 edits):**
    - Change any WS message type strings (`"climate_manager/set_global_mode"` etc.) — they are wire contracts and Plan 01 / Phase 3 set them.
    - Modify the `RoomConfig.zone_id` declaration in `frontend/src/types.ts` (the cast is a local escape hatch; widening the type would propagate null into every reader and require auditing every `config.zone_id` use site).
    - Touch `ws-client.ts` — no new client methods needed; both `setGlobalMode` and `setTimeProgram` already exist (lines 37 and 55).
    - Modify the tab order, the `+` button, the Rooms button, or the Persons button in `main.ts` — only the first button's text changes.
    - Modify rooms-tab.ts, persons-tab.ts, person-card.ts, global-settings-tab.ts, search-picker.ts, time-bar.ts — they are not implicated by any gap.

  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend && npx tsc --noEmit && npx vite build 2>&1 | tail -8 && cd /home/arnaud/dev/climate_manager && grep -c '>Overview<' frontend/src/main.ts && grep -c 'this.isDefault' frontend/src/components/zone-tab.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npx vite build` exits 0
    - `grep -c '>Overview<' frontend/src/main.ts` returns `0` (no stale Overview label)
    - `grep -c '>Global Settings<' frontend/src/main.ts` returns `1` (new label present exactly once in the tab bar)
    - `grep -n 'this\.ws\.setGlobalMode' frontend/src/components/zone-tab.ts` returns at least one line in the `_onModeChange` handler (verify line is inside a `this.isDefault` branch by reading the surrounding 3 lines)
    - `grep -n 'this\.ws\.setTimeProgram' frontend/src/components/zone-tab.ts` returns at least one line in the `_onPeriodsChanged` handler (verify line is inside a `this.isDefault` branch by reading the surrounding 3 lines)
    - `grep -c 'this\.isDefault' frontend/src/components/zone-tab.ts` returns at least `4` (existing usages + new branches in _onModeChange, _onPeriodsChanged, _onAddRoom — count rises by at least 3 above the pre-edit baseline)
    - `grep -cE 'zone_id:\s*null' frontend/src/components/zone-tab.ts` returns at least `2` (_onRemoveRoom + Default-Zone branch of _onAddRoom)
    - `grep -cE 'zone_id:\s*null' frontend/src/components/room-card.ts` returns at least `1` (the empty-string branch of _onZoneChange)
    - `grep -cE 'zone_id:\s*undefined' frontend/src/components/zone-tab.ts` returns `0` (no stale undefined-payload sites in zone-tab.ts)
    - `grep -cE 'zone_id:\s*undefined' frontend/src/components/room-card.ts` returns `0` (no stale undefined-payload sites in room-card.ts)
    - `grep -E 'this\.ws\.setZoneMode\(this\.zoneId' frontend/src/components/zone-tab.ts` still matches — the custom-zone branch in `_onModeChange` is preserved (this is the non-isDefault branch)
    - `grep -E 'this\.ws\.setZoneTimeProgram\(this\.zoneId' frontend/src/components/zone-tab.ts` still matches — the custom-zone branch in `_onPeriodsChanged` is preserved
    - `grep -E 'this\._activeTab === "global"' frontend/src/main.ts` matches (the internal tab id `"global"` is unchanged; only the visible label moved from "Overview" to "Global Settings")
    - `git diff frontend/src/types.ts` returns empty (RoomConfig.zone_id declaration NOT widened)
    - `git diff frontend/src/ws-client.ts` returns empty (no WS client changes)
    - `git diff frontend/src/components/person-card.ts frontend/src/components/persons-tab.ts frontend/src/components/global-settings-tab.ts frontend/src/components/rooms-tab.ts frontend/src/components/search-picker.ts frontend/src/components/time-bar.ts` returns empty (no collateral damage)
  </acceptance_criteria>
  <done>The five wiring bugs are patched at the exact sites identified in VERIFICATION.md, the tab label says "Global Settings", TypeScript compiles, the Vite build succeeds, and no unintended files are touched.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                                                                                                                                                                                      |
| ------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Panel UI → set_room_config WS handler | The frontend now sends `{zone_id: null}` deliberately. The handler interprets null as "pop key" and the live room dict ends up without the zone_id key. The boundary surface is unchanged — only the semantic interpretation of one specific value (null) gains a meaning.       |
| async_save → validate_zone_assignment | Validator behavior is preserved (still rejects stored null zone_id). The handler-side pop guarantees the validator never sees null in the happy path. Defense in depth: if a future bug bypasses the handler pop, the validator stops a malformed write from reaching the store. |

## STRIDE Threat Register

| Threat ID | Category          | Component                                                        | Disposition                                                       | Mitigation Plan                                                                                                                                                                                                                                                                                      |
| --------- | ----------------- | ---------------------------------------------------------------- | ----------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-06-07   | Tampering         | set_room_config receives `{zone_id: null}` from a hostile client | accept                                                            | Backend already requires HA WebSocket auth (T-03-06); the pop translates null to "Default Zone membership" — the only effect a hostile client can produce is "this room is in the Default Zone", which is the same outcome they could achieve by omitting the key entirely. No privilege escalation. |
| T-06-08   | Tampering         | Frontend cast `null as unknown as string                         | undefined` bypasses TypeScript's RoomConfig.zone_id?: string type | accept                                                                                                                                                                                                                                                                                               | The cast is local to two lines; the WS message is the trust boundary (validated server-side). No type-system-wide weakening. Documented in `<action>` with rationale and alternative form. |
| T-06-09   | Denial of Service | Rapid Default-Zone-tab clicks invoke setGlobalMode in tight loop | accept                                                            | Existing global-settings-tab already calls setGlobalMode the same way; no new attack surface. Backend is idempotent for repeated identical mode writes.                                                                                                                                              |
| T-06-SC   | Tampering         | npm/pip/cargo installs                                           | mitigate                                                          | No new package installs in this plan — only existing devDeps (lit, typescript, vite, pytest, voluptuous, homeassistant) are used. No legitimacy gate needed.                                                                                                                                         |

</threat_model>

<verification>
- `cd /home/arnaud/dev/climate_manager && uv run pytest tests/` exits 0 with the four new tests passing alongside all prior phases' tests
- `cd /home/arnaud/dev/climate_manager/frontend && npx tsc --noEmit && npx vite build` exits 0
- `grep -c '>Overview<' frontend/src/main.ts` returns 0 and `grep -c '>Global Settings<' frontend/src/main.ts` returns 1
- `grep -cE 'zone_id:\s*null' frontend/src/components/zone-tab.ts frontend/src/components/room-card.ts` (combined) returns at least 3
- `grep -cE 'zone_id:\s*undefined' frontend/src/components/zone-tab.ts frontend/src/components/room-card.ts` (combined) returns 0
- Backend handler's incoming-patch pop + live-room pop are both present in websocket.py (verified by Task 1's acceptance criteria)
- No unintended files modified — `git status` post-execution shows exactly the 6 files in `files_modified` frontmatter (plus any planning-doc SUMMARY)
</verification>

<success_criteria>

- All six gaps from `06-VERIFICATION.md` are addressed at the exact files/lines
  identified
- Backend `set_room_config` correctly pops `zone_id` when the panel sends
  `{zone_id: null}`, satisfying the sparse D-06 model end-to-end
- Default Zone tab mode and time-program changes persist (use the global
  endpoints)
- Adding a room to the Default Zone from its tab succeeds (sends null, backend
  pops the key)
- Removing a room from a custom zone moves it back to the Default Zone (chip ×
  works)
- Selecting Default Zone in a room card's zone picker moves the room back to the
  Default Zone
- First tab label reads "Global Settings" (matches ROADMAP wording for Phase 6
  Success Criterion 1)
- The defensive contract `validate_zone_assignment rejects stored zone_id: null`
  is locked by a new test
- All TypeScript compiles, all Python tests pass, no collateral damage to
  unrelated files </success_criteria>

<output>
Create `.planning/phases/06-zone-room-assignment-ui/06-04-SUMMARY.md` when done.
</output>
