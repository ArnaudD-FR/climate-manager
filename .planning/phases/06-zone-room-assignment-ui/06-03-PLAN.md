---
phase: 06-zone-room-assignment-ui
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - frontend/src/components/room-card.ts
  - frontend/src/components/person-card.ts
autonomous: true
requirements:
  - ASSIGN-02
  - ASSIGN-03
  - UI-06

must_haves:
  truths:
    - "Every room card displays a zone badge pill in the collapsed header row
      showing the room's zone display name (ASSIGN-03)"
    - "Rooms with no zone_id display the Default Zone display name
      (panelConfig.default_zone_name) in the zone badge (D-06 phase 4, D-11
      phase 6)"
    - "Expanded room card content includes a Zone <select> placed below the room
      mode picker and above the persons section (D-12, ASSIGN-02)"
    - "Changing the Zone <select> calls setRoomConfig with the new zone_id (or
      unset for Default Zone) and the change auto-saves immediately"
    - "The room card uses the same zone badge in both rooms-tab.ts and any other
      rendering context — no rooms-tab.ts changes are required because the badge
      lives inside room-card.ts (UI-06)"
    - "person-card.ts displays 'HA home tracking' in both the badge text (line
      373) and the mode <option> text (line 411); the backend value 'ha' is
      unchanged (D-13)"
  artifacts:
    - path: "frontend/src/components/room-card.ts"
      provides:
        "Zone badge in collapsed header + Zone <select> in expanded content +
        _onZoneChange handler"
      contains: "_onZoneChange"
    - path: "frontend/src/components/person-card.ts"
      provides:
        "Display label 'HA home tracking' for ha presence mode (badge text +
        option label)"
      contains: "HA home tracking"
  key_links:
    - from: "frontend/src/components/room-card.ts"
      to: "frontend/src/ws-client.ts"
      via: "this.ws.setRoomConfig with zone_id patch"
      pattern: "this\\.ws\\.setRoomConfig.*zone_id"
    - from: "frontend/src/components/room-card.ts"
      to: "frontend/src/types.ts"
      via:
        "ClimateConfig.zones / ClimateConfig.default_zone_name /
        RoomConfig.zone_id reads"
      pattern: "panelConfig\\.(zones|default_zone_name)|config\\?\\.zone_id"
---

<objective>
Add the zone badge and zone picker to every room card, and rename the "HA" presence mode label to "HA home tracking" in person-card. These are the per-card display changes that complete ASSIGN-02 (room-card zone picker), ASSIGN-03 (zone badge on every room card), UI-06 (Rooms tab shows badge + picker because the cards do), and folded todo D-13 (label rename).

Purpose: The Rooms tab already renders room cards (rooms-tab.ts is unchanged),
so wiring the badge and picker inside room-card.ts automatically satisfies UI-06
without any structural change to rooms-tab. The person-card label change is a
tiny 2-line edit grouped with this plan because it is also a card-layer display
change, runs in the same wave, and modifies a disjoint file (no overlap with
room-card.ts).

Output:

- frontend/src/components/room-card.ts gains a `.zone-badge` rule, a
  `_getZoneName()` helper, the badge in the header row, the Zone <select> in
  expanded content, and an `_onZoneChange` handler.
- frontend/src/components/person-card.ts text strings updated at lines 373 and
  411 from "HA" to "HA home tracking". </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-zone-room-assignment-ui/06-CONTEXT.md
@.planning/phases/06-zone-room-assignment-ui/06-PATTERNS.md
@.planning/phases/06-zone-room-assignment-ui/06-01-SUMMARY.md
@frontend/src/components/room-card.ts
@frontend/src/components/person-card.ts
@frontend/src/types.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: room-card zone badge + zone picker + _onZoneChange handler</name>
  <files>frontend/src/components/room-card.ts</files>
  <read_first>
    - frontend/src/components/room-card.ts (existing structure: properties lines 26-37, program-badge CSS lines 121-138, header-top render around line 608, mode select wrapper around lines 629-638, persons section around line 639+)
    - frontend/src/types.ts (ClimateConfig.zones, ClimateConfig.default_zone_name, RoomConfig.zone_id, RoomConfig optional fields)
    - .planning/phases/06-zone-room-assignment-ui/06-CONTEXT.md (D-11 badge in collapsed header, D-12 select below mode picker above persons section, D-06 phase 4 sparse zone_id absent = Default Zone)
    - .planning/phases/06-zone-room-assignment-ui/06-PATTERNS.md (section "room-card.ts" lines 414-491: zone-badge CSS, _getZoneName helper, zone select render, _onZoneChange handler)
    - frontend/src/ws-client.ts (existing setRoomConfig method — accepts Partial<RoomConfig>, already supports zone_id)
    - frontend/src/components/global-settings-tab.ts (CSS for `.select-wrapper`, `.select-label`, `.mode-select` reused — already imported/styled in room-card)
  </read_first>
  <action>
    Modify `frontend/src/components/room-card.ts` to add the zone badge in the collapsed header row and a Zone picker `<select>` in the expanded content area. The persons section, room mode picker, time-bar, and all other existing markup MUST be preserved verbatim.

    1. Add CSS rule `.zone-badge` inside the existing `static styles = css\`...\`` block, immediately after the `.program-badge.global` block (around line 138):
       ```
       .zone-badge {
         display: inline-flex;
         align-items: center;
         padding: 2px 8px;
         border-radius: 12px;
         font-size: 12px;
         font-weight: 400;
         background: var(--secondary-background-color, #f5f5f5);
         color: var(--secondary-text-color, #757575);
         border: 1px solid var(--divider-color, #e0e0e0);
       }
       ```
       This pill matches the visual weight of `.program-badge` (per D-11) but uses neutral background/text colors so it does not compete with the program badge for attention.

    2. Add a private helper method `_getZoneName(): string`:
       - `const zoneId = this.config?.zone_id;`
       - If `!zoneId`: return `this.panelConfig?.default_zone_name ?? "Default Zone"` (sparse zone_id absent = Default Zone member per D-06 phase 4).
       - Otherwise: return `this.panelConfig?.zones?.[zoneId]?.name ?? this.panelConfig?.default_zone_name ?? "Default Zone"` (defensive — if the zone_id references a deleted zone, treat as Default Zone).
       Add the method near the other render-helpers (e.g. next to the existing program-badge helper around lines 449-475).

    3. In `render()`, locate the `.card-header-top` div (around line 608) and add the zone badge after the existing program/period badges, BEFORE the closing div. Markup: `<span class="zone-badge">${this._getZoneName()}</span>`. Do not reorder, remove, or restyle the existing room name, period badge, or program badge — only append the zone badge to the same row.

    4. In `render()`, locate the room mode picker `.select-wrapper` block in the expanded content (around lines 629-638). Insert a NEW `.select-wrapper` block immediately after the existing mode picker and BEFORE the persons section (which currently follows). The new block is the Zone picker per D-12 / ASSIGN-02:
       ```
       <div class="select-wrapper">
         <label class="select-label">Zone</label>
         <select class="mode-select" @change=${this._onZoneChange}>
           <option value="" ?selected=${!this.config?.zone_id}>
             ${this.panelConfig?.default_zone_name ?? "Default Zone"}
           </option>
           ${Object.entries(this.panelConfig?.zones ?? {}).map(([zoneId, zone]) => html`
             <option value=${zoneId} ?selected=${this.config?.zone_id === zoneId}>
               ${zone.name}
             </option>
           `)}
         </select>
       </div>
       ```
       Reuse the `.select-wrapper`, `.select-label`, `.mode-select` classes (already defined and used by the room mode picker).

    5. Add a new private async handler `_onZoneChange(e: Event)`:
       - `const newZoneId = (e.target as HTMLSelectElement).value;`
       - `const patch: Partial<RoomConfig> = newZoneId ? { zone_id: newZoneId } : { zone_id: undefined };` (empty-string value means Default Zone, sent as undefined so the sparse-merge backend strips the key — sparse model D-06 phase 4)
       - Wrap in try/catch using the established write-then-reload pattern from existing room-card handlers (e.g. `_onRoomModeChange` around lines 410-417):
         ```
         try {
           await this.ws.setRoomConfig(this.roomId, patch);
           await this.panel.reloadConfig();
           this.panel.showToast("Saved", false);
         } catch {
           this.panel.showToast("Save failed — retrying...", true);
         }
         ```
       Place the method near `_onRoomModeChange` so all the auto-save handlers are co-located.

    Do NOT:
    - Modify rooms-tab.ts — the badge inside room-card.ts is what satisfies UI-06; rooms-tab already passes the full panelConfig through.
    - Change the persons section, time-bar, custom-mode controls, or expand/collapse behaviour.
    - Add any new dependencies or imports beyond what's already in the file (the RoomConfig type is already imported; Partial is built-in TypeScript).
    - Touch person-card.ts (Task 2's job).

  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -c 'zone-badge\|_getZoneName\|_onZoneChange' src/components/room-card.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0
    - `cd frontend && npx vite build` exits 0
    - `grep -E 'class _getZoneName|private _getZoneName\(\)' frontend/src/components/room-card.ts` matches (the helper method exists)
    - `grep -E 'private (async )?_onZoneChange\(e: Event\)' frontend/src/components/room-card.ts` matches
    - `grep -c '<span class="zone-badge">' frontend/src/components/room-card.ts` returns at least `1`
    - `grep -E '\.select-label[^"]*">Zone<' frontend/src/components/room-card.ts` matches (Zone label on the picker)
    - `grep -E 'this\.ws\.setRoomConfig\(this\.roomId, patch\)' frontend/src/components/room-card.ts` matches
    - `grep -E 'this\.panel\.reloadConfig\(\)' frontend/src/components/room-card.ts | wc -l` returns at least `5` (existing 4 calls + the new one — no regression)
    - `grep -E 'zone_id:\s*undefined' frontend/src/components/room-card.ts` matches (empty option sends undefined sparse-merge)
    - `grep -E 'panelConfig\?\.zones \?\? \{\}' frontend/src/components/room-card.ts` matches (zones enumeration in the select options)
    - The existing room-mode `.select-wrapper` block is preserved: `grep -c 'class="select-wrapper"' frontend/src/components/room-card.ts` returns at least `2` (existing room mode wrapper + new zone wrapper)
  </acceptance_criteria>
  <done>Every room card now shows a zone badge in its collapsed header and a zone picker in its expanded content; changing the picker auto-saves via setRoomConfig.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Rename "HA" → "HA home tracking" in person-card.ts (D-13)</name>
  <files>frontend/src/components/person-card.ts</files>
  <read_first>
    - frontend/src/components/person-card.ts (lines 365-376 _getBadgeInfo with the "HA" badge text at line 373; lines 405-415 mode select with the "HA" option text at line 411; verify PRESENCE_MODE_HA constant at line 28 — backend value MUST remain "ha")
    - .planning/phases/06-zone-room-assignment-ui/06-CONTEXT.md (D-13 — display-only rename, backend value unchanged)
  </read_first>
  <action>
    Make exactly two text-substitution edits in `frontend/src/components/person-card.ts`:

    1. Line 373 — change the badge `text` value from `"HA"` to `"HA home tracking"`:
       - Before: `case PRESENCE_MODE_HA: return { cls: "ha", text: "HA" };`
       - After:  `case PRESENCE_MODE_HA: return { cls: "ha", text: "HA home tracking" };`

    2. Line 411 — change the `<option>` display text from `HA` to `HA home tracking`:
       - Before: `<option value=${PRESENCE_MODE_HA} ?selected=${currentMode === PRESENCE_MODE_HA}>HA</option>`
       - After:  `<option value=${PRESENCE_MODE_HA} ?selected=${currentMode === PRESENCE_MODE_HA}>HA home tracking</option>`

    Do NOT:
    - Change the `PRESENCE_MODE_HA = "ha"` constant at line 28 (backend value must stay "ha" per D-13).
    - Change the CSS class `cls: "ha"` (it drives badge color — class name is internal, not a display string).
    - Touch persons-tab.ts (per the pattern map correction in the planning context, persons-tab.ts has no "HA" label string — all badge rendering is delegated to person-card.ts via `<climate-manager-person-card>`).
    - Modify any other case in the `_getBadgeInfo` switch (Scheduled / Force Present / Force Absent remain unchanged).
    - Modify any other option in the mode select (Scheduled / Force Present / Force Absent remain unchanged).

  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && grep -c '"HA home tracking"\|>HA home tracking<' src/components/person-card.ts</automated>
  </verify>
  <acceptance_criteria>
    - `cd frontend && npx tsc --noEmit` exits 0
    - `grep -E 'case PRESENCE_MODE_HA: return \{ cls: "ha", text: "HA home tracking" \};' frontend/src/components/person-card.ts` matches
    - `grep -E '<option value=\$\{PRESENCE_MODE_HA\}[^>]*>HA home tracking</option>' frontend/src/components/person-card.ts` matches
    - `grep -E '\btext: "HA"\b' frontend/src/components/person-card.ts` returns empty (no stale "HA" badge text)
    - `grep -E '>HA</option>' frontend/src/components/person-card.ts` returns empty (no stale "HA" option text)
    - `grep -E 'const PRESENCE_MODE_HA = "ha";' frontend/src/components/person-card.ts` matches (backend value preserved)
    - `grep -E 'cls: "ha"' frontend/src/components/person-card.ts` matches (CSS class preserved — drives badge color)
    - Other modes unchanged: `grep -c 'Scheduled\|Force Present\|Force Absent' frontend/src/components/person-card.ts` returns the same count as before the edit (each label appears in both badge text and option label, so 6 total: 2 occurrences × 3 modes)
  </acceptance_criteria>
  <done>person-card.ts displays "HA home tracking" everywhere the user sees the ha presence mode; the backend value "ha" is unchanged.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                                             |
| ------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- |
| Room card <select> → ws.setRoomConfig | newZoneId is a UUID string from the select option (populated server-side via get_config); backend rejects unknown zone_ids per ZONE-04. |
| panelConfig.zones → render            | Plain text rendering via Lit html`` — no XSS vector since zone names already exist in the same panel session.                           |

## STRIDE Threat Register

| Threat ID | Category               | Component                                                      | Disposition | Mitigation Plan                                                                                                              |
| --------- | ---------------------- | -------------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------- |
| T-06-05   | Tampering              | Race: user changes zone picker while reloadConfig is in-flight | accept      | setRoomConfig is sparse-merge on the backend; last-write-wins is the desired UX (consistent with existing room mode picker). |
| T-06-06   | Information Disclosure | Zone display name visible on every room card                   | accept      | Already visible in zone tabs; no privacy delta.                                                                              |
| T-06-SC   | Tampering              | npm/pip/cargo installs                                         | mitigate    | No new package installs.                                                                                                     |

</threat_model>

<verification>
- TypeScript compiles cleanly
- Vite build succeeds
- Every room card renders a zone badge in the collapsed header
- Expanded room card renders the Zone <select> below the mode picker, above the persons section
- Changing the zone select auto-saves via setRoomConfig and triggers panel.reloadConfig + showToast
- person-card.ts displays "HA home tracking" in both badge text and mode select option
</verification>

<success_criteria>

- ASSIGN-02: room cards have a working zone picker
- ASSIGN-03: every room card displays a zone badge with the correct zone name
- UI-06: Rooms tab automatically shows badges + picker because they live in
  room-card.ts
- D-13: HA presence label updated; backend "ha" value untouched
- No regression in existing room-card behaviour (mode picker, time-bar, persons
  section, expand/collapse all still work) </success_criteria>

<output>
Create `.planning/phases/06-zone-room-assignment-ui/06-03-SUMMARY.md` when done.
</output>
