---
phase: 12-predictive-pre-heat
plan: 04
type: execute
wave: 4
depends_on: ["12-02", "12-03"]
files_modified:
  - frontend/src/types.ts
  - frontend/src/components/room-card.ts
autonomous: true
requirements: [PREHEAT-01, PREHEAT-04]
must_haves:
  truths:
    - "RoomConfig exposes optional preheat_enabled and preheat_max_lead_minutes"
    - "RoomStatus exposes optional preheat_active, preheat_target, preheat_suppressed"
    - "The room card renders a Pre-heat checkbox that auto-saves preheat_enabled via setRoomConfig"
    - "When pre-heat is enabled the room card renders a Max lead time number input that auto-saves preheat_max_lead_minutes"
    - "The room card shows 'Pre-heating (→ XX.X°C)' when preheat_active and a target are present"
    - "The room card shows the suppression warning only when preheat_enabled and preheat_suppressed are both true"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "Pre-heat fields on RoomConfig + RoomStatus; wakeup_advance_minutes rename on PersonConfig"
      contains: "preheat_max_lead_minutes"
    - path: "frontend/src/components/room-card.ts"
      provides: "Pre-heat config block + status line below the TRV section"
      contains: "_renderPreheatSection"
  key_links:
    - from: "frontend/src/components/room-card.ts"
      to: "ws.setRoomConfig"
      via: "auto-save on toggle/number change"
      pattern: "setRoomConfig\\(this.roomId, \\{ preheat"
    - from: "frontend/src/components/room-card.ts"
      to: "RoomStatus.preheat_active/preheat_target/preheat_suppressed"
      via: "this.roomStatus reads in _renderPreheatSection"
      pattern: "roomStatus\\??\\.preheat_active"
---

<objective>
Add the Phase 12 frontend: extend `RoomConfig` / `RoomStatus` (and rename the
PersonConfig `preheat_lead_minutes` field to `wakeup_advance_minutes`) in
`types.ts`, then add the inline pre-heat config block and status line to the
room card below the TRV list (D-11). This is the user-facing half of PREHEAT-01
(per-room enable/disable + max lead time from the Rooms tab) and PREHEAT-04
(panel pre-heat status display).

Purpose: Plans 12-02 and 12-03 produced and exposed the three pre-heat status
fields and accept the two sparse room-config keys over WebSocket; this plan is
the only consumer that lets a user actually toggle pre-heat and see its status.
Without it PREHEAT-01 and PREHEAT-04 are not observable in the panel.

Output: typed `preheat_enabled` / `preheat_max_lead_minutes` on RoomConfig,
`preheat_active` / `preheat_target` / `preheat_suppressed` on RoomStatus, the
`wakeup_advance_minutes` field rename (D-02 frontend side), and the room-card
pre-heat section with auto-save controls and status line.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-predictive-pre-heat/12-CONTEXT.md
@.planning/phases/12-predictive-pre-heat/12-RESEARCH.md
@.planning/phases/12-predictive-pre-heat/12-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend RoomConfig/RoomStatus + rename PersonConfig field in types.ts</name>
  <files>frontend/src/types.ts</files>
  <read_first>
    - frontend/src/types.ts — read the RoomConfig interface (lines 54-66), the
      PersonConfig interface (lines 69-81, including preheat_lead_minutes at
      line 80), and the RoomStatus interface (lines 117-127, including
      present_person_count at line 124 and has_trv at line 126) to match the
      sparse-optional field style and comment conventions
    - frontend/src/components/person-card.ts — grep for preheat_lead_minutes /
      wakeup_advance to find any reads of the renamed PersonConfig field that
      must be updated in lockstep (the UI label was already changed by quick
      task 260602-wakeup-advance-rename; the typed field key is renamed here)
  </read_first>
  <action>
    In RoomConfig add two sparse optional fields after the existing keys (per
    D-01, absent = default): `preheat_enabled?: boolean;` and
    `preheat_max_lead_minutes?: number;` with a comment noting sparse/absent =
    default 120. In RoomStatus add three optional fields after `has_trv` (per
    D-10): `preheat_active?: boolean;`, `preheat_target?: number | null;`,
    `preheat_suppressed?: boolean;`. In PersonConfig rename the existing
    `preheat_lead_minutes?: number;` field (line 80) to
    `wakeup_advance_minutes?: number;` (D-02) and update its comment to note the
    rename from preheat_lead_minutes. Then update every reader of the old
    PersonConfig field name found in the person-card.ts grep to read
    `wakeup_advance_minutes` (the backend already migrates the stored key in
    12-01; the frontend must send/read the new key). Use 2-space indent, ≤ 80
    chars per line, LF endings, final newline per .editorconfig.
  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager && grep -q "preheat_max_lead_minutes" frontend/src/types.ts && grep -q "preheat_suppressed" frontend/src/types.ts && grep -q "wakeup_advance_minutes" frontend/src/types.ts && ! grep -q "preheat_lead_minutes" frontend/src/types.ts && make build</automated>
  </verify>
  <acceptance_criteria>
    - types.ts RoomConfig contains `preheat_enabled?: boolean` and
      `preheat_max_lead_minutes?: number`
    - types.ts RoomStatus contains `preheat_active?`, `preheat_target?: number | null`,
      and `preheat_suppressed?`
    - types.ts PersonConfig contains `wakeup_advance_minutes?: number` and no
      longer contains `preheat_lead_minutes`
    - No remaining reference to `preheat_lead_minutes` in person-card.ts
      (grep returns nothing) — readers use `wakeup_advance_minutes`
    - `make build` exits 0 (TypeScript compiles with the renamed field)
  </acceptance_criteria>
  <done>RoomConfig/RoomStatus carry the pre-heat fields, PersonConfig field is
  renamed to wakeup_advance_minutes, all readers updated, and the bundle
  compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add pre-heat config block + status line to the room card</name>
  <files>frontend/src/components/room-card.ts</files>
  <read_first>
    - frontend/src/components/room-card.ts — read the component property
      declarations (lines 42-60: `config: RoomConfig`, `roomStatus: RoomStatus
      | null`, `roomId`, `ws`, `panel`), the `_onZoneChange` auto-save handler
      (lines 421-433) as the exact try/catch + setRoomConfig + reloadConfig +
      showToast template, `_renderRoomModeDescription` (lines 625-635) for the
      `schedule-hint` status-line pattern, `_renderTrvSection` (line 530) and
      its render-call site (line 798) where the new section is inserted after
      the TRV list, and an existing `section-label` usage (e.g. line 743) for
      the block heading style
    - frontend/src/types.ts — confirm the RoomConfig/RoomStatus pre-heat field
      names added in Task 1 before reading them in the template
  </read_first>
  <action>
    Add two auto-save handlers mirroring `_onZoneChange` exactly (try { await
    this.ws.setRoomConfig(this.roomId, patch); await this.panel.reloadConfig();
    this.panel.showToast("Saved", false); } catch {
    this.panel.showToast("Save failed — retrying...", true); }): `_onPreheatToggle`
    reads `(e.target as HTMLInputElement).checked` and sends `{ preheat_enabled:
    enabled }`; `_onPreheatMaxLeadChange` parses `parseInt(value, 10)`, returns
    early (no save) when NaN or outside [0, 480], else sends `{
    preheat_max_lead_minutes: val }`. Add a `_renderPreheatSection()` method
    (per D-11) returning a Lit template with: a `section-label` div labeled
    "Pre-heat"; a `<label>` wrapping a native `<input type="checkbox">` bound
    `.checked=${this.config?.preheat_enabled ?? false}` with
    `@change=${this._onPreheatToggle}` and text "Pre-heat this room"; only when
    enabled, a `<label>` "Max lead time (min)" wrapping a native `<input
    type="number">` with `.value=${String(this.config?.preheat_max_lead_minutes
    ?? 120)}`, `min="0" max="480" step="5"`, `@change=${this._onPreheatMaxLeadChange}`;
    a `schedule-hint` `<p>` reading "Pre-heating (→ ${preheatTarget.toFixed(1)}°C)"
    shown only when `this.roomStatus?.preheat_active` is true AND
    `this.roomStatus?.preheat_target` is not null/undefined; and a separate
    `schedule-hint` `<p>` "Pre-heat disabled — presence cannot be scheduled"
    shown only when `this.config?.preheat_enabled` is true AND
    `this.roomStatus?.preheat_suppressed` is true (suppression warning is NOT
    shown when pre-heat is simply disabled — per CONTEXT specifics). Use native
    `<input>` elements only (project memory: ha-textfield / ha-select broken in
    HA 2026.x). Insert `${this._renderPreheatSection()}` immediately after the
    `${this._renderTrvSection()}` call at line 798. Use 2-space indent, ≤ 80
    chars, LF, final newline per .editorconfig.
  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager && grep -q "_renderPreheatSection" frontend/src/components/room-card.ts && grep -q "_onPreheatToggle" frontend/src/components/room-card.ts && grep -q "setRoomConfig(this.roomId, { preheat_enabled" frontend/src/components/room-card.ts && make build && make lint</automated>
  </verify>
  <acceptance_criteria>
    - room-card.ts contains `_renderPreheatSection`, `_onPreheatToggle`, and
      `_onPreheatMaxLeadChange`
    - `_onPreheatToggle` calls `setRoomConfig(this.roomId, { preheat_enabled: ... })`
      and `_onPreheatMaxLeadChange` calls `setRoomConfig(this.roomId, {
      preheat_max_lead_minutes: ... })` inside a try/catch with showToast
    - `_onPreheatMaxLeadChange` rejects NaN and values outside [0, 480] without
      calling setRoomConfig
    - The "Pre-heating (→" status line is gated on `roomStatus?.preheat_active`
      and a non-null `preheat_target`; the suppression line is gated on both
      `config?.preheat_enabled` and `roomStatus?.preheat_suppressed`
    - The max-lead `<input type="number">` is rendered only when
      `preheat_enabled` is true
    - `${this._renderPreheatSection()}` appears after `${this._renderTrvSection()}`
    - `make build` and `make lint` both exit 0
  </acceptance_criteria>
  <done>The room card shows a working Pre-heat toggle and max-lead input that
  auto-save, plus the active and suppressed status lines, using native HTML
  controls; the bundle builds and passes lint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel input → set_room_config WS | User-entered max lead time crosses to the backend; the authoritative clamp lives in 12-03 (ws_set_room_config), the panel adds a defensive [0,480] guard before sending |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-08 | Tampering | _onPreheatMaxLeadChange input | mitigate | Client-side [0,480] int guard drops invalid input before WS send; backend ws_set_room_config (12-03 T-12-06) is the authoritative clamp — defense in depth |
| T-12-09 | Information Disclosure | preheat status line rendering | accept | Status text exposes only the room's own target temperature to the already-authenticated panel session; no cross-tenant data |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs — RESEARCH.md confirms zero new dependencies; only existing Lit/TS build |
</threat_model>

<verification>
- `make build` — TypeScript compiles with the new fields and room-card section
- `make lint` — editorconfig compliance (2-space TS indent, 80-char lines, LF,
  final newline)
- Manual (per 12-VALIDATION.md): enable pre-heat on a room with a scheduled
  person and observe "Pre-heating (→ XX.X°C)"; enable on an ha-mode-only room
  and observe the suppression warning
</verification>

<success_criteria>
- RoomConfig/RoomStatus carry the pre-heat fields and PersonConfig uses
  wakeup_advance_minutes (D-01, D-02, D-10)
- The room card lets a user enable/disable pre-heat and set the max lead time
  from the Rooms tab with auto-save (PREHEAT-01)
- The room card shows the active and suppressed pre-heat status lines
  (PREHEAT-04)
- Native HTML controls only; bundle builds and passes lint
</success_criteria>

<output>
Create `.planning/phases/12-predictive-pre-heat/12-04-SUMMARY.md` when done
</output>
