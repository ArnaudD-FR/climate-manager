---
phase: 12-predictive-pre-heat
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - custom_components/climate_manager/const.py
  - custom_components/climate_manager/schedule.py
  - custom_components/climate_manager/storage.py
  - tests/test_preheat.py
autonomous: true
requirements: [PREHEAT-02, PREHEAT-05]
must_haves:
  truths:
    - "D-03: next_occupied_at() returns the next present datetime for scheduled (single) persons"
    - "D-03: next_occupied_at() respects ISO-week parity per future day for even_odd persons"
    - "D-03: next_occupied_at() returns the calendar event boundary for calendar-mode persons"
    - "D-03: next_occupied_at() returns None for ha / force_present / force_absent modes"
    - "D-01: Pre-heat constants (DEFAULT_PREHEAT_MAX_LEAD_MINUTES + sample tuning) exist as the sparse-key defaults for room preheat_enabled / preheat_max_lead_minutes"
    - "D-05: next_occupied_at() supplies the next-transition value the trigger condition (now >= next_occupied_at - learned_lead) depends on"
    - "D-02: Stored persons[*].preheat_lead_minutes is migrated to wakeup_advance_minutes at load"
  artifacts:
    - path: "custom_components/climate_manager/schedule.py"
      provides: "next_occupied_at() pure-Python next-transition helper"
      contains: "def next_occupied_at"
    - path: "custom_components/climate_manager/const.py"
      provides: "Phase 12 pre-heat constants"
      contains: "DEFAULT_PREHEAT_MAX_LEAD_MINUTES"
    - path: "custom_components/climate_manager/storage.py"
      provides: "wakeup_advance_minutes migration in async_load"
      contains: "wakeup_advance_minutes"
    - path: "tests/test_preheat.py"
      provides: "Unit tests for next_occupied_at + migration"
      contains: "def test_next_occupied"
  key_links:
    - from: "tests/test_preheat.py"
      to: "schedule.next_occupied_at"
      via: "direct import + call"
      pattern: "from custom_components.climate_manager.schedule import"
    - from: "storage.async_load"
      to: "persons[*].wakeup_advance_minutes"
      via: "post-merge migration loop"
      pattern: "wakeup_advance_minutes"
---

<objective>
Build the pure-Python foundation for predictive pre-heat: the
`next_occupied_at()` next-transition helper in `schedule.py`, the Phase 12
constants in `const.py`, and the `preheat_lead_minutes` → `wakeup_advance_minutes`
storage migration in `storage.py`. This is the TDD wave — every behavior has a
defined input/output and is testable without Home Assistant runtime.

Decisions implemented here: D-01 (pre-heat config sparse-key defaults live in
const.py), D-02 (rename preheat_lead_minutes → wakeup_advance_minutes via load
migration), D-03 (next_occupied_at() function + mode dispatch in schedule.py),
and D-05 (next_occupied_at() is the next-transition input the pre-heat trigger
condition consumes).

Purpose: Wave 2 (coordinator pre-heat pass) and Wave 3 (frontend) both depend on
`next_occupied_at()` and the new constants. Locking the contract and migration
first prevents downstream scavenger hunts.

Output: `next_occupied_at()` (PREHEAT-05), pre-heat constants (PREHEAT-02
default lead time), wakeup-advance migration (D-02), and the new
`tests/test_preheat.py` test module covering all of the above.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-predictive-pre-heat/12-CONTEXT.md
@.planning/phases/12-predictive-pre-heat/12-RESEARCH.md
@.planning/phases/12-predictive-pre-heat/12-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 12 pre-heat constants to const.py</name>
  <files>custom_components/climate_manager/const.py</files>
  <read_first>
    - custom_components/climate_manager/const.py — read the existing constant
      block (lines 54-71) where DEFAULT_PREHEAT_LEAD_MINUTES already lives, and
      the room/person sparse-schema comment blocks (around lines 161-163) to
      match the documentation style
  </read_first>
  <behavior>
    - Importing const exposes DEFAULT_PREHEAT_MAX_LEAD_MINUTES == 120
    - Importing const exposes PREHEAT_DEFAULT_SAMPLE_COUNT_THRESHOLD == 3
    - Importing const exposes PREHEAT_MAX_SAMPLES == 5
    - Importing const exposes PREHEAT_CONVERGENCE_THRESHOLD == 0.2
    - Existing DEFAULT_PREHEAT_LEAD_MINUTES (== 60) is unchanged
  </behavior>
  <action>
    D-01: Add four module-level constants to const.py next to the existing
    DEFAULT_PREHEAT_LEAD_MINUTES (line 59), using the same naming and comment
    style: DEFAULT_PREHEAT_MAX_LEAD_MINUTES = 120 (per D-01 / PREHEAT-01),
    PREHEAT_DEFAULT_SAMPLE_COUNT_THRESHOLD = 3 (minimum valid samples before the
    learned lead is used, per D-08), PREHEAT_MAX_SAMPLES = 5 (max stored samples
    per room, per D-08), PREHEAT_CONVERGENCE_THRESHOLD = 0.2 (°C tolerance for
    convergence detection, per D-09). Do NOT change DEFAULT_PREHEAT_LEAD_MINUTES.
    Add the sparse-key documentation comment for the room schema noting
    preheat_enabled and preheat_max_lead_minutes as sparse keys (absent =
    default), matching the existing person-config sparse comment style. Keep
    every line ≤ 80 chars per .editorconfig.
  </action>
  <verify>
    <automated>.venv/bin/python -c "from custom_components.climate_manager import const; assert const.DEFAULT_PREHEAT_MAX_LEAD_MINUTES == 120 and const.PREHEAT_MAX_SAMPLES == 5 and const.PREHEAT_DEFAULT_SAMPLE_COUNT_THRESHOLD == 3 and const.PREHEAT_CONVERGENCE_THRESHOLD == 0.2 and const.DEFAULT_PREHEAT_LEAD_MINUTES == 60"</automated>
  </verify>
  <acceptance_criteria>
    - const.py contains `DEFAULT_PREHEAT_MAX_LEAD_MINUTES = 120`
    - const.py contains `PREHEAT_DEFAULT_SAMPLE_COUNT_THRESHOLD = 3`
    - const.py contains `PREHEAT_MAX_SAMPLES = 5`
    - const.py contains `PREHEAT_CONVERGENCE_THRESHOLD = 0.2`
    - const.py still contains `DEFAULT_PREHEAT_LEAD_MINUTES` with value 60
    - The verify command above exits 0
  </acceptance_criteria>
  <done>All four new constants importable with correct values; existing
  DEFAULT_PREHEAT_LEAD_MINUTES preserved.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement next_occupied_at() in schedule.py (RED→GREEN)</name>
  <files>custom_components/climate_manager/schedule.py, tests/test_preheat.py</files>
  <read_first>
    - custom_components/climate_manager/schedule.py — read resolve_presence()
      (lines 276-367) for the exact mode-dispatch and even/odd parity pattern,
      resolve_calendar_presence() for the calendar event-boundary logic,
      _parse_time() and _parse_calendar_dt() (lines 94-121) for aware-datetime
      construction, the import block (lines 26-45), and WEEKDAY_TO_DAY mapping
    - tests/test_calendar.py — read the pure-Python (no hass fixture) test
      style and person-config dict fixtures (lines 90-115, calendar fixtures)
    - custom_components/climate_manager/const.py — confirm PRESENCE_HA,
      PRESENCE_CALENDAR, PRESENCE_PRESENT, PRESENCE_ABSENT, PRESENCE_AUTOMATIC
      constant names before importing them into schedule.py
  </read_first>
  <behavior>
    - test_next_occupied_scheduled_single: a single-schedule person absent now,
      present at 07:00 → returns today/next 07:00 as an aware datetime > now
    - test_next_occupied_even_odd: even_odd person; the chosen schedule_even vs
      schedule_odd for each future day is selected by that DAY's
      isocalendar().week % 2 (not now's week); crosses a week boundary correctly
    - test_next_occupied_calendar_absent: calendar mode, event_means="absent",
      currently inside an event → returns that event's end datetime (person
      returns home when event ends)
    - test_next_occupied_calendar_present: calendar mode, event_means="present"
      → returns the start datetime of the next event
    - test_next_occupied_ha_returns_none: mode "ha" → None
    - test_next_occupied_force_modes_return_none: "force_present" and
      "force_absent" → None
    - test_next_occupied_no_present_period_returns_none: scheduled person who is
      never present in the next 7 days → None
    - All returned datetimes are timezone-aware and strictly greater than `now`
  </behavior>
  <action>
    First write the listed tests in tests/test_preheat.py importing
    next_occupied_at from custom_components.climate_manager.schedule; run them to
    confirm RED (ImportError / failures). Then implement
    `next_occupied_at(person_config: dict, now: datetime.datetime,
    calendar_cache: dict | None = None, start_of_local_day=None) ->
    datetime.datetime | None` in schedule.py, mirroring resolve_presence()'s
    signature. Mode dispatch per D-03: PRESENCE_HA, PRESENCE_PRESENT
    (force_present), PRESENCE_ABSENT (force_absent) → return None. For
    PRESENCE_CALENDAR: read person_config["calendar_config"] entity_id +
    event_means, look up events in (calendar_cache or {}).get(entity_id, []); for
    event_means=="absent" return the active event's end datetime, for
    event_means=="present" return the next event's start datetime. For scheduled
    modes: walk a 7-day lookahead (day_offset 0..6); for each target_date select
    schedule_even/schedule_odd by `target_date.isocalendar().week % 2` when
    schedule_type=="even_odd" else "schedule" (single); resolve the day's periods
    via WEEKDAY_TO_DAY[target_date.weekday()]; return the start of the first
    period whose state is "present" with a start datetime strictly after `now`.
    Construct aware datetimes via
    `datetime.datetime.combine(target_date, _parse_time(period["start"])).replace(tzinfo=now.tzinfo)`
    — never mix naive/aware (Pitfall 2). Keep schedule.py PURE Python — no hass
    imports, no async, no calendar.get_events calls (the cache is passed in).
    Add PRESENCE_HA and PRESENCE_CALENDAR to the existing const import block.
    Keep all lines ≤ 80 chars.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_preheat.py -k next_occupied -x -q</automated>
  </verify>
  <acceptance_criteria>
    - schedule.py contains `def next_occupied_at(`
    - schedule.py imports PRESENCE_HA and PRESENCE_CALENDAR from .const
    - schedule.py contains no `import homeassistant` and no `get_events` call
      inside next_occupied_at (grep: `grep -n "get_events" custom_components/climate_manager/schedule.py` returns no new occurrence inside next_occupied_at)
    - tests/test_preheat.py contains `def test_next_occupied_even_odd` and
      `def test_next_occupied_calendar_absent` and
      `def test_next_occupied_ha_returns_none`
    - `.venv/bin/python -m pytest tests/test_preheat.py -k next_occupied -x -q` exits 0
  </acceptance_criteria>
  <done>next_occupied_at returns correct next-present datetimes for single,
  even_odd, and calendar modes, and None for ha/force modes; all
  next_occupied tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: wakeup_advance_minutes migration in storage.py (RED→GREEN)</name>
  <files>custom_components/climate_manager/storage.py, tests/test_preheat.py</files>
  <read_first>
    - custom_components/climate_manager/storage.py — read the async_load()
      migration block (lines 129-138) that runs the person mode-rename loop on
      the post-merge `result` dict, and the sparse-merge loop above it, to place
      the new migration AFTER the merge (Pitfall 6)
    - tests/test_storage.py — read how async_load is exercised with a mocked
      Store to assert on the merged result dict
    - tests/test_calendar.py — note the 8+ fixtures still using the string key
      "preheat_lead_minutes"; these are renamed in plan 12-02 (coordinator wave),
      not here — do not edit test_calendar.py in this task
  </read_first>
  <behavior>
    - test_wakeup_advance_migration: stored person config with
      preheat_lead_minutes=90 and no wakeup_advance_minutes → after async_load,
      result persons[*] has wakeup_advance_minutes=90 and no preheat_lead_minutes
    - test_wakeup_advance_migration_noop_when_new_key_present: stored config with
      both keys → wakeup_advance_minutes is kept, preheat_lead_minutes is removed
      (pop), value not overwritten
    - test_wakeup_advance_migration_absent_key: stored config with neither key →
      no wakeup_advance_minutes added
  </behavior>
  <action>
    D-02: First write the three listed tests in tests/test_preheat.py (mirroring
    the test_storage.py async_load harness with a mocked Store). Run to confirm
    RED. Then add a migration loop in storage.py async_load(), placed AFTER the
    existing sparse-merge loop and alongside the existing person mode-rename loop,
    operating on the post-merge `result` dict (per D-02 / Pitfall 6): for each
    person_cfg in result.get("persons", {}).values(), if "preheat_lead_minutes"
    is present AND "wakeup_advance_minutes" is absent, set
    person_cfg["wakeup_advance_minutes"] = person_cfg.pop("preheat_lead_minutes").
    Do not touch the preheat Store (that is a coordinator/init concern). Keep
    lines ≤ 80 chars.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_preheat.py -k wakeup_advance -x -q</automated>
  </verify>
  <acceptance_criteria>
    - storage.py async_load contains a loop referencing both
      `preheat_lead_minutes` and `wakeup_advance_minutes`
    - The migration uses `.pop("preheat_lead_minutes")` (renames, does not copy)
    - tests/test_preheat.py contains `def test_wakeup_advance_migration`
    - `.venv/bin/python -m pytest tests/test_preheat.py -k wakeup_advance -x -q` exits 0
  </acceptance_criteria>
  <done>Stored preheat_lead_minutes is renamed to wakeup_advance_minutes at
  load when the new key is absent; existing wakeup_advance_minutes is never
  overwritten.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored JSON → in-memory config | Persisted Store data is loaded and merged; legacy keys must migrate without corrupting defaults |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | storage.async_load migration | mitigate | Migration runs on post-merge `result` (not DEFAULT_CONFIG) and only renames when new key absent — prevents default absorption (Pitfall 6) |
| T-12-02 | Tampering | next_occupied_at() time math | accept | Pure function; naive/aware mix guarded by combining with now.tzinfo (Pitfall 2); no external input beyond config dicts already validated by WS layer |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — RESEARCH.md confirms zero new dependencies |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_preheat.py -v` — all Wave 1 tests green
- `make lint` — editorconfig compliance (80-char lines, LF, final newline)
- schedule.py remains pure Python (no homeassistant imports added)
</verification>

<success_criteria>
- next_occupied_at() implemented and unit-tested for single, even_odd,
  calendar, ha, and force modes (PREHEAT-05)
- Pre-heat constants present with correct defaults (PREHEAT-02 lead default)
- wakeup_advance_minutes migration renames the legacy key at load (D-02)
- tests/test_preheat.py exists and passes
</success_criteria>

<output>
Create `.planning/phases/12-predictive-pre-heat/12-01-SUMMARY.md` when done
</output>
</content>
</invoke>
