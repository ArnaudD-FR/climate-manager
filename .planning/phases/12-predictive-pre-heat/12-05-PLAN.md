---
phase: 12-predictive-pre-heat
plan: 05
type: execute
wave: 5
depends_on: ["12-04"]
files_modified: []
autonomous: false
gap_closure: true
# Gap closure: visual UI verification only
# PREHEAT-02, PREHEAT-03, PREHEAT-05 fully verified in 12-01..12-04
# PREHEAT-01, PREHEAT-04: pending human visual verification (this plan)
requirements: [PREHEAT-01, PREHEAT-04]
must_haves:
  truths:
    - "PREHEAT-04: The room card renders 'Pre-heating (→ XX.X°C)' with the correct target temperature while a pre-heat window is active"
    - "PREHEAT-04: The suppression warning 'Pre-heat disabled — presence cannot be scheduled' renders only when preheat_enabled AND preheat_suppressed are both true (ha-mode-only room), and disappears when the pre-heat checkbox is turned off"
    - "PREHEAT-01: The Max lead time number input is visible only when the Pre-heat checkbox is checked and collapses when it is unchecked"
  artifacts:
    - path: ".planning/phases/12-predictive-pre-heat/12-HUMAN-UAT.md"
      provides: "Updated UAT log with the 3 pre-heat visual scenarios recorded as passed/issue"
      contains: "passed:"
  key_links:
    - from: "frontend/src/components/room-card.ts"
      to: "deployed HA panel bundle"
      via: "make build && make deploy"
      pattern: "_renderPreheatSection"
    - from: "deployed HA panel"
      to: "browser inspection"
      via: "/verify skill screenshot session"
      pattern: "Pre-heating"
---

<objective>
Close the three pending human-verification (UAT) items from
`12-VERIFICATION.md` (`status: human_needed`). All 14 code must-haves are
already VERIFIED; the only outstanding work is confirming three visual
behaviours of the Phase 12 pre-heat room-card UI in a live HA panel session.

Decisions covered: D-11 (pre-heat UI inline in the room card — status line,
suppression warning, conditional max-lead input). Requirements covered:
PREHEAT-01 (per-room enable/disable + configurable max lead time from the
panel — visible/collapsing input) and PREHEAT-04 (panel shows
"Pre-heating (→ XX.X°C)" and the suppression warning).

Purpose: The implementation (12-04) builds and lints clean, but Lit template
rendering, conditional DOM presence, and the dual-gate suppression branch can
only be confirmed in a browser against a deployed bundle. This plan deploys
the current frontend and runs a screenshot-based verification of the three
behaviours.

Output: Deployed frontend bundle on the HA instance and an updated
`12-HUMAN-UAT.md` recording the result of each of the three scenarios.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-predictive-pre-heat/12-CONTEXT.md
@.planning/phases/12-predictive-pre-heat/12-VERIFICATION.md
@.planning/phases/12-predictive-pre-heat/12-HUMAN-UAT.md
@.planning/phases/12-predictive-pre-heat/12-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and deploy the pre-heat frontend to the HA instance</name>
  <files>
    - frontend/src/components/room-card.ts
    - frontend/src/types.ts
  </files>
  <read_first>
    - Makefile — read the `build`, `deploy` (depends on build), and `logs`
      targets to confirm the exact commands and that `deploy` copies the built
      bundle into the running HA instance
    - .planning/phases/12-predictive-pre-heat/12-04-SUMMARY.md — confirms the
      room-card pre-heat section (_renderPreheatSection) and types are already
      committed; this task only rebuilds and deploys them
  </read_first>
  <action>
    Run `make build && make deploy` from the repo root to compile the Lit/TS
    panel bundle and copy it into the running Home Assistant instance. Do NOT
    edit any source files — 12-04 already shipped the pre-heat UI; this task
    only produces and deploys the bundle so it is loadable in the browser.
    After deploy, the panel JS served by HA must contain the
    `_renderPreheatSection` logic. If `make deploy` reports an auth or path
    error, surface it as a blocking issue rather than editing the Makefile.
  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager && make build && make deploy</automated>
  </verify>
  <acceptance_criteria>
    - `make build` exits 0 (bundle compiles)
    - `make deploy` exits 0 (bundle copied to the HA instance)
    - The deployed panel bundle is the current one — its source
      (frontend/src/components/room-card.ts) contains `_renderPreheatSection`
      (grep returns a match)
  </acceptance_criteria>
  <done>The current Phase 12 pre-heat frontend is built and deployed to the
  running HA instance, ready for browser inspection.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify the three pre-heat visual behaviours in the panel via /verify</name>
  <read_first>
    - .planning/phases/12-predictive-pre-heat/12-HUMAN-UAT.md — the three
      pending scenarios and their expected results; this file is updated with
      the outcome of each scenario
    - .planning/phases/12-predictive-pre-heat/12-VERIFICATION.md — the
      `human_verification` block (test / expected / why_human) for exact
      reproduction steps for each of the three scenarios
  </read_first>
  <what-built>
    The room card (frontend/src/components/room-card.ts `_renderPreheatSection`,
    deployed in Task 1) renders: a Pre-heat checkbox that auto-saves
    `preheat_enabled`; a Max lead time `<input type="number">` shown only when
    pre-heat is enabled; a "Pre-heating (→ XX.X°C)" status line gated on
    `roomStatus.preheat_active && preheat_target != null`; and a suppression
    warning "Pre-heat disabled — presence cannot be scheduled" gated on
    `config.preheat_enabled && roomStatus.preheat_suppressed`.
  </what-built>
  <how-to-verify>
    Invoke the `/verify` skill to launch a screenshot-based inspection of the
    deployed HA panel and confirm all three behaviours. Verify, in order:

    1. PREHEAT-04 — active status line: Enable pre-heat on a room assigned to a
       scheduled (non-ha) person, then bring the system within the pre-heat
       window (advance the clock or wait for a scheduled present period). Open
       the Rooms tab and confirm the room card shows
       "Pre-heating (→ XX.X°C)" with the upcoming target temperature formatted
       to one decimal place.

    2. PREHEAT-04 — suppression warning dual-gate: On a room whose only assigned
       person is in "HA" (ha) presence mode, enable the Pre-heat checkbox. Open
       the Rooms tab and confirm "Pre-heat disabled — presence cannot be
       scheduled" appears (and the active pre-heating line does NOT). Then
       uncheck the Pre-heat checkbox and confirm the warning disappears
       (warning is shown only when preheat_enabled AND preheat_suppressed).

    3. PREHEAT-01 — max-lead input visibility toggle: On any room, check the
       Pre-heat checkbox and confirm the "Max lead time (min)" number input
       appears; uncheck it and confirm the input collapses (hidden when
       preheat_enabled is false).

    Record the result of each scenario in
    .planning/phases/12-predictive-pre-heat/12-HUMAN-UAT.md (set each test's
    `result:` to passed or describe the issue, and update the Summary
    `passed`/`issues`/`pending` counts).
  </how-to-verify>
  <acceptance_criteria>
    - Scenario 1 (PREHEAT-04 active line): The room card displays
      "Pre-heating (→ XX.X°C)" with the correct upcoming target temperature to
      one decimal place during an active pre-heat window — confirmed by
      screenshot
    - Scenario 2 (PREHEAT-04 suppression): "Pre-heat disabled — presence cannot
      be scheduled" is shown on an ha-mode-only room with pre-heat enabled, the
      active pre-heating line is NOT shown, and the warning disappears when the
      Pre-heat checkbox is unchecked — confirmed by screenshot
    - Scenario 3 (PREHEAT-01 input toggle): The Max lead time number input is
      visible when Pre-heat is checked and hidden when unchecked — confirmed by
      screenshot
    - 12-HUMAN-UAT.md is updated: each of the 3 tests has a non-pending
      `result:` and the Summary shows `pending: 0` (with `passed` + `issues`
      summing to 3)
  </acceptance_criteria>
  <resume-signal>Type "approved" once all three scenarios are confirmed in the
  panel and recorded in 12-HUMAN-UAT.md, or describe any scenario that fails so
  a follow-up gap-closure plan can be created.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local build → HA instance deploy | `make deploy` writes the built bundle into the running HA instance's served panel path; the operator-controlled local build crosses into the served frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-10 | Tampering | make deploy bundle copy | accept | Operator-run local build of in-repo, reviewed sources; no remote artifact fetched; deploy target is the operator's own HA instance |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs — verification-and-deploy only; RESEARCH.md confirms zero new dependencies; existing Lit/TS build reused |
</threat_model>

<verification>
- `make build && make deploy` — bundle compiles and is copied to the HA
  instance
- `/verify` skill session — three pre-heat visual behaviours confirmed by
  screenshot against the deployed panel
- 12-HUMAN-UAT.md updated with `pending: 0` and each scenario resolved
</verification>

<success_criteria>
- The Phase 12 pre-heat frontend is deployed to the running HA instance
  (PREHEAT-01, PREHEAT-04 made observable)
- All three pending UAT scenarios are confirmed in a browser via `/verify`:
  active pre-heat status line, dual-gate suppression warning, and conditional
  max-lead input visibility
- 12-HUMAN-UAT.md records the outcome of each scenario with no pending items,
  closing the `human_needed` status from 12-VERIFICATION.md
</success_criteria>

<output>
Create `.planning/phases/12-predictive-pre-heat/12-05-SUMMARY.md` when done
</output>
