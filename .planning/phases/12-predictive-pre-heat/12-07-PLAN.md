---
phase: 12-predictive-pre-heat
plan: 07
type: execute
wave: 2
depends_on: ["12-06"]
gap_closure: true
autonomous: true
requirements: [PREHEAT-01, PREHEAT-04]
files_modified:
  - frontend/src/types.ts
  - frontend/src/ws-client.ts
  - frontend/src/components/zone-tab.ts
  - frontend/src/components/room-card.ts
must_haves:
  truths:
    - "PREHEAT-01: The zone editor (Default and custom) shows a Pre-heat enable toggle bound to the zone's preheat_enabled"
    - "PREHEAT-01: Toggling the zone Pre-heat control saves via the set_zone_preheat WS command"
    - "PREHEAT-01: The room card no longer shows a per-room Pre-heat checkbox"
    - "PREHEAT-01: The room card shows the Max lead time input only when the room's zone has pre-heat enabled"
    - "PREHEAT-04: The room card still shows 'Pre-heating (→ XX.X°C)' and the suppression warning from roomStatus"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "preheat_enabled on ZoneConfig + default_zone_preheat_enabled on ClimateConfig; removed from RoomConfig"
      contains: "preheat_enabled"
    - path: "frontend/src/ws-client.ts"
      provides: "setZonePreheat(zoneId, enabled) method"
      contains: "set_zone_preheat"
    - path: "frontend/src/components/zone-tab.ts"
      provides: "Zone pre-heat toggle with auto-save"
      contains: "setZonePreheat"
    - path: "frontend/src/components/room-card.ts"
      provides: "Removed room toggle; conditional max-lead input + status lines"
      contains: "_renderPreheatSection"
  key_links:
    - from: "frontend/src/components/zone-tab.ts"
      to: "ws.setZonePreheat"
      via: "toggle @change auto-save"
      pattern: "setZonePreheat"
    - from: "frontend/src/components/room-card.ts"
      to: "panelConfig zone preheat_enabled"
      via: "zone_id lookup to decide max-lead input visibility"
      pattern: "preheat_enabled"
---

<objective>
Move the pre-heat enable toggle in the panel UI from the room card to the zone
editor, matching the backend change in 12-06. Closes the frontend half of
GAP-01 (PREHEAT-01): the pre-heat on/off control is now per zone; the room card
keeps the per-room Max lead time input (shown only when the zone has pre-heat
enabled) and the live status lines (PREHEAT-04), which are unchanged in meaning.

Purpose: After 12-06 the backend reads `preheat_enabled` from the zone (or the
top-level `default_zone_preheat_enabled` for Default Zone rooms) and exposes a
`set_zone_preheat` WS command. The room-level toggle and its WS write are now
dead and misleading; this plan rewires the UI to the zone contract.

Output: A zone Pre-heat toggle in the zone editor, a `setZonePreheat` WS client
method, updated types, and a room card with the toggle removed and the max-lead
input gated on the zone's enable state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/12-predictive-pre-heat/12-HUMAN-UAT.md
@.planning/phases/12-predictive-pre-heat/12-06-PLAN.md
@.planning/phases/12-predictive-pre-heat/12-PATTERNS.md
@.planning/phases/12-predictive-pre-heat/12-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Types + WS client — zone-scoped contract</name>
  <files>frontend/src/types.ts, frontend/src/ws-client.ts</files>
  <read_first>
    - frontend/src/types.ts lines 53-125 (RoomConfig 53-76, ZoneConfig 94-102,
      ClimateConfig 104-125 — note `default_zone_name: string` is the analog for
      the new default flag) and RoomStatus 127-150 (unchanged — preheat_*
      status fields stay)
    - frontend/src/ws-client.ts lines 119-188 (setZoneMode 122-129 is the analog
      for the new zone method; setRoomConfig 166-176)
  </read_first>
  <action>
    (a) frontend/src/types.ts:
    - REMOVE `preheat_enabled?: boolean` from `RoomConfig` (lines 67-70 doc +
      field). KEEP `preheat_max_lead_minutes?: number` on `RoomConfig`.
    - ADD `preheat_enabled?: boolean` to `ZoneConfig` (sparse — absent = false),
      with a Phase 12 / GAP-01 doc comment.
    - ADD `default_zone_preheat_enabled?: boolean` to `ClimateConfig` (sparse —
      absent = false), documented beside `default_zone_name`, noting it is the
      Default Zone's pre-heat enable flag (the Default Zone has no entry in
      `zones`).
    - Leave `RoomStatus.preheat_active / preheat_target / preheat_suppressed`
      unchanged.

    (b) frontend/src/ws-client.ts: ADD a `setZonePreheat(zoneId, enabled)`
    method modeled on `setZoneMode`. It sends
    `{ type: "climate_manager/set_zone_preheat", zone_id, enabled }` and returns
    `Promise<{ success: boolean }>`. `zoneId` is `"default"` for the Default
    Zone or a UUID for custom zones (same convention as setZoneMode /
    renameZone).

    All TypeScript ≤ 80 chars, 2-space indent (CLAUDE.md / .editorconfig).
  </action>
  <verify>
    <automated>grep -c "set_zone_preheat" frontend/src/ws-client.ts</automated>
  </verify>
  <acceptance_criteria>
    - `RoomConfig` has no `preheat_enabled`; still has `preheat_max_lead_minutes`.
    - `ZoneConfig` has `preheat_enabled?: boolean`.
    - `ClimateConfig` has `default_zone_preheat_enabled?: boolean`.
    - `ws-client.ts` exports `setZonePreheat(zoneId, enabled)` sending
      `climate_manager/set_zone_preheat`.
    - `make build` typechecks clean (no reference to a removed field).
  </acceptance_criteria>
  <done>
    The TypeScript contract matches the 12-06 backend: enable on the zone /
    default flag, max lead on the room, and a WS method to persist zone enable.
  </done>
</task>

<task type="auto">
  <name>Task 2: Zone editor Pre-heat toggle</name>
  <files>frontend/src/components/zone-tab.ts</files>
  <read_first>
    - frontend/src/components/zone-tab.ts lines 49-86 (props: zoneId, zoneConfig,
      isDefault, ws, panel), 217-256 (_onModeChange / reset handlers — the
      write-then-reload-then-toast auto-save pattern to copy), 507-575 (render —
      Mode picker + description + reset-row, where the toggle should sit)
    - frontend/src/types.ts ZoneConfig (after Task 1 — `preheat_enabled` field)
  </read_first>
  <action>
    In zone-tab.ts add a Pre-heat enable control to the zone editor.

    - Add a handler `_onPreheatToggle` (arrow-fn field, same shape as
      `_onModeChange`): read `(e.target as HTMLInputElement).checked`; call
      `await this.ws.setZonePreheat(this.zoneId, enabled)` then
      `await this.panel.reloadConfig()` then `this.panel.showToast("Saved",
      false)`; on throw `this.panel.showToast("Save failed", true)`. Note:
      `this.zoneId` is already `"default"` for the Default Zone (line 58-61), so
      the same handler works for both — no `isDefault` branch needed.
    - In `render()`, add a labelled `<input type="checkbox">` (native element —
      ha-* form controls are broken in HA 2026.x per project memory) bound with
      `.checked=${this.zoneConfig?.preheat_enabled ?? false}` and
      `@change=${this._onPreheatToggle}`. Place it under the mode description /
      above or below the reset-row, with a `section-label` "Pre-heat" header
      (reuse the existing `section-label` style). Add a short `schedule-hint`
      line explaining the flag governs every room in the zone.

    All TypeScript ≤ 80 chars, 2-space indent.
  </action>
  <verify>
    <automated>grep -c "setZonePreheat" frontend/src/components/zone-tab.ts</automated>
  </verify>
  <acceptance_criteria>
    - zone-tab renders a Pre-heat checkbox bound to
      `zoneConfig.preheat_enabled`.
    - Toggling it calls `ws.setZonePreheat(this.zoneId, enabled)` then reloads
      config and shows a toast (failure toast on error).
    - The control works for both the Default Zone (`zoneId === "default"`) and
      custom zones with no special-casing in the handler.
    - `make build` typechecks clean.
  </acceptance_criteria>
  <done>
    Pre-heat is enabled/disabled per zone from the zone editor for every room in
    the zone (PREHEAT-01).
  </done>
</task>

<task type="auto">
  <name>Task 3: Room card — remove toggle, gate max-lead on zone enable</name>
  <files>frontend/src/components/room-card.ts</files>
  <read_first>
    - frontend/src/components/room-card.ts lines 42-57 (props: config,
      roomStatus, panelConfig), 405-433 (_onZoneChange — how the room reads
      `this.config.zone_id` and resolves the zone from `panelConfig`), 647-727
      (pre-heat handlers + _renderPreheatSection), 875-882 (render call site for
      `_renderPreheatSection`)
    - frontend/src/types.ts ZoneConfig + ClimateConfig (after Task 1)
  </read_first>
  <action>
    In room-card.ts rework the pre-heat block.

    - DELETE `_onPreheatToggle` (lines 651-660). KEEP
      `_onPreheatMaxLeadChange` (writes `preheat_max_lead_minutes` via
      setRoomConfig — max lead stays per-room).
    - In `_renderPreheatSection`: REMOVE the Pre-heat checkbox `<label>` and its
      `<input type="checkbox">`. Replace the `enabled` source: instead of
      `this.config?.preheat_enabled`, derive the zone's enable state:
        - `const zoneId = this.config?.zone_id;`
        - if `zoneId` is falsy: `enabled =
          this.panelConfig?.default_zone_preheat_enabled ?? false`.
        - else: `enabled =
          this.panelConfig?.zones?.[zoneId]?.preheat_enabled ?? false`.
      Show the "Max lead time (min)" `<input type="number">` only when
      `enabled` is true (same conditional render as before, now driven by the
      zone flag). Keep the active status line gate
      (`preheatActive && preheatTarget != null`) and the suppression-warning
      gate (`enabled && suppressed`) reading from `this.roomStatus` — unchanged
      in meaning, just sourcing `enabled` from the zone now.
    - Keep the `section-label` "Pre-heat" header so the per-room status/max-lead
      block still has context.

    Do NOT add a room-level enable control. All TypeScript ≤ 80 chars, 2-space
    indent.
  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager && make build</automated>
  </verify>
  <acceptance_criteria>
    - `_onPreheatToggle` is removed from room-card.ts; no
      `setRoomConfig(..., { preheat_enabled })` call remains.
    - `_renderPreheatSection` derives `enabled` from the room's zone
      (`panelConfig.zones[zoneId].preheat_enabled`) or
      `panelConfig.default_zone_preheat_enabled` for Default Zone rooms.
    - The Max lead time input renders only when the zone's pre-heat is enabled.
    - The "Pre-heating (→ XX.X°C)" line and the suppression warning still render
      from `roomStatus` (PREHEAT-04).
    - `make build` typechecks and bundles clean.
  </acceptance_criteria>
  <done>
    The room card no longer offers a per-room enable; it shows the per-room max
    lead input only when the room's zone has pre-heat on, plus the live status
    lines.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel UI → WS command | User toggle input crosses into set_zone_preheat (validated server-side in 12-06) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-14 | Tampering | setZonePreheat payload | accept | Server-side validation (vol schema + zone existence + rollback) lives in 12-06 ws_set_zone_preheat; the client only sends a bool + zone_id |
| T-12-15 | Spoofing | room reads zone flag from panelConfig | accept | panelConfig is the same authenticated WS get_config payload the panel already trusts; no new trust surface |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No new dependencies — existing Lit/TS/Vite build reused; RESEARCH.md confirms zero PyPI/npm additions for this phase |
</threat_model>

<verification>
- `make build` typechecks and bundles clean with no reference to the removed
  `RoomConfig.preheat_enabled`.
- `grep -n "preheat_enabled" frontend/src/components/room-card.ts` shows reads
  from the zone / default flag, not a room checkbox.
- `grep -c "setZonePreheat" frontend/src/components/zone-tab.ts` ≥ 1.
</verification>

<success_criteria>
- The zone editor (Default and custom) has a Pre-heat enable toggle that saves
  via set_zone_preheat (PREHEAT-01).
- The room card has no per-room enable; the Max lead input is gated on the
  zone's enable state; status lines are preserved (PREHEAT-01, PREHEAT-04).
- The frontend bundle builds clean against the 12-06 backend contract.
</success_criteria>

<output>
Create `.planning/phases/12-predictive-pre-heat/12-07-SUMMARY.md` when done.
</output>
