---
phase: 12-predictive-pre-heat
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - custom_components/climate_manager/websocket.py
  - tests/test_preheat.py
autonomous: true
requirements: [PREHEAT-01, PREHEAT-04]
must_haves:
  truths:
    - "D-10: ws_get_status room entries include preheat_active, preheat_target, preheat_suppressed (parity with the push payload)"
    - "D-01: set_room_config clamps preheat_max_lead_minutes to [0, 480] and drops invalid values, and coerces preheat_enabled to bool (the two sparse room-config keys)"
    - "set_person_config accepts and clamps wakeup_advance_minutes to [0, 480]"
  artifacts:
    - path: "custom_components/climate_manager/websocket.py"
      provides: "Pre-heat status in ws_get_status + room/person config validation"
      contains: "preheat_max_lead_minutes"
    - path: "tests/test_preheat.py"
      provides: "WS validation + status payload tests"
      contains: "def test_ws_set_room_preheat"
  key_links:
    - from: "websocket.ws_get_status"
      to: "coordinator._preheat_active/_preheat_target/_preheat_suppressed"
      via: "room_entry field reads"
      pattern: "_preheat_active"
    - from: "websocket.ws_set_room_config"
      to: "rooms[room_id] preheat keys"
      via: "clamp + sparse update"
      pattern: "preheat_max_lead_minutes"
---

<objective>
Complete the backend trust-boundary work for Phase 12 in websocket.py: add the
three pre-heat status fields to the `ws_get_status` room payload (the second
status builder that the panel reads on initial load), clamp/validate
`preheat_max_lead_minutes` and `preheat_enabled` in the `set_room_config`
handler, and rename the person-config clamp from `preheat_lead_minutes` to
`wakeup_advance_minutes`.

Decisions implemented here: D-10 (ws_get_status surfaces the same
preheat_active / preheat_target / preheat_suppressed fields as the push payload)
and D-01 (validation of the two sparse room-config keys preheat_enabled /
preheat_max_lead_minutes before persist).

Purpose: `_build_status_payload` (push path) was extended in 12-02; the separate
`ws_get_status` read path MUST match it or initial page load shows stale/missing
pre-heat status (RESEARCH Pitfall 1). The room-config clamp is the security
mitigation for malformed max-lead payloads (T-12-06).

Output: ws_get_status parity with the push payload (PREHEAT-04), room-config
validation for the new sparse keys (PREHEAT-01), and the person clamp rename
(D-02).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-predictive-pre-heat/12-CONTEXT.md
@.planning/phases/12-predictive-pre-heat/12-RESEARCH.md
@.planning/phases/12-predictive-pre-heat/12-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pre-heat fields to ws_get_status room payload</name>
  <files>custom_components/climate_manager/websocket.py, tests/test_preheat.py</files>
  <read_first>
    - custom_components/climate_manager/websocket.py — read ws_get_status
      (lines 129-242), specifically the room_entry construction around
      present_person_count (lines 213-228) and has_trv (line 226) where the new
      fields are appended; confirm the handler has a `coordinator` reference in
      scope
    - custom_components/climate_manager/coordinator.py — confirm the
      coordinator instance dicts _preheat_active / _preheat_target /
      _preheat_suppressed added in 12-02
    - tests/test_calendar.py — read the hass_ws_client get_status test pattern
      (sending climate_manager/get_status and asserting on rooms_status entries)
  </read_first>
  <behavior>
    - test_ws_get_status_preheat_fields: calling get_status returns rooms_status
      entries each containing preheat_active (bool), preheat_target (float|None),
      preheat_suppressed (bool), mirroring the coordinator instance dicts
    - When the coordinator has no entry for a room, the fields default to
      False / None / False respectively
  </behavior>
  <action>
    D-10: Write the test in tests/test_preheat.py first (RED). Then in
    ws_get_status, after `room_entry["has_trv"] = ...` (line 226) and before
    `rooms_status.append(room_entry)`, add (per D-10, mirroring 12-02's
    _build_status_payload exactly): `room_entry["preheat_active"] =
    coordinator._preheat_active.get(area_id, False)`,
    `room_entry["preheat_target"] = coordinator._preheat_target.get(area_id,
    None)`, `room_entry["preheat_suppressed"] =
    coordinator._preheat_suppressed.get(area_id, False)`. Use the existing
    `coordinator` variable already resolved in this handler. Keep lines ≤ 80
    chars.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_preheat.py -k ws_get_status_preheat -x -q</automated>
  </verify>
  <acceptance_criteria>
    - websocket.py ws_get_status contains `coordinator._preheat_active.get`,
      `coordinator._preheat_target.get`, `coordinator._preheat_suppressed.get`
    - tests/test_preheat.py contains `def test_ws_get_status_preheat_fields`
    - `.venv/bin/python -m pytest tests/test_preheat.py -k ws_get_status_preheat -x -q` exits 0
  </acceptance_criteria>
  <done>Initial get_status load returns the same three pre-heat fields as the
  push payload; defaults applied when absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clamp room pre-heat config + rename person clamp key</name>
  <files>custom_components/climate_manager/websocket.py, tests/test_preheat.py</files>
  <read_first>
    - custom_components/climate_manager/websocket.py — read ws_set_room_config
      (lines 394-448) where incoming_config is updated into rooms[room_id]
      (the update at line 429-433 has no key allowlist), and ws_set_person_config
      (lines 451-553) with the existing preheat_lead_minutes clamp (lines
      542-548); note the docstring at line 478 that lists accepted keys
    - tests/test_calendar.py — read the existing
      test_ws_persists_preheat_lead_minutes pattern for clamp assertions
  </read_first>
  <behavior>
    - test_ws_set_room_preheat_config: set_room_config with
      preheat_enabled=true + preheat_max_lead_minutes=90 persists both into
      rooms[room_id]
    - test_ws_room_max_lead_clamped: preheat_max_lead_minutes=999 (out of range)
      is dropped; preheat_max_lead_minutes=-5 is dropped; non-int dropped
    - test_ws_room_enabled_coerced_bool: preheat_enabled=1 (truthy non-bool) is
      coerced to a Python bool before persist
    - test_ws_set_person_wakeup_advance: set_person_config with
      wakeup_advance_minutes=90 persists; wakeup_advance_minutes=999 dropped
  </behavior>
  <action>
    D-01: Write the four tests first (RED). In ws_set_room_config, BEFORE the
    setdefault/update that writes incoming_config into rooms[room_id] (line
    429), validate the two D-01 sparse room-config keys (security T-12-06, per
    RESEARCH Security Domain): if "preheat_max_lead_minutes" in incoming_config,
    keep it only when it is an int in [0, 480], else pop it; if "preheat_enabled"
    in incoming_config and it is not already a bool, coerce with
    `incoming_config["preheat_enabled"] = bool(incoming_config["preheat_enabled"])`.
    In ws_set_person_config, rename the existing preheat_lead_minutes clamp block
    (lines 542-548) to operate on "wakeup_advance_minutes" with the same [0, 480]
    int rule (drop if invalid), and update the docstring accepted-keys list
    (line 478) to `wakeup_advance_minutes: int (0-480, clamped; D-02)`. During
    the transition window also accept a legacy `preheat_lead_minutes` by renaming
    it to `wakeup_advance_minutes` in `incoming` before the clamp (so old clients
    keep working). Keep lines ≤ 80 chars.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_preheat.py -k "set_room_preheat or max_lead_clamped or enabled_coerced or wakeup_advance" -x -q</automated>
  </verify>
  <acceptance_criteria>
    - ws_set_room_config validates `preheat_max_lead_minutes` with a `0 <= ... <= 480`
      int check and coerces `preheat_enabled` to bool before the rooms update
    - ws_set_person_config clamps `wakeup_advance_minutes` (not
      `preheat_lead_minutes`) and maps a legacy `preheat_lead_minutes` key onto it
    - tests/test_preheat.py contains `def test_ws_set_room_preheat_config` and
      `def test_ws_room_max_lead_clamped` and `def test_ws_set_person_wakeup_advance`
    - `.venv/bin/python -m pytest tests/test_preheat.py -k "set_room_preheat or max_lead_clamped or enabled_coerced or wakeup_advance" -x -q` exits 0
  </acceptance_criteria>
  <done>Room pre-heat keys are validated/clamped before persist; person clamp
  uses wakeup_advance_minutes with legacy-key compatibility.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel WS client → set_room_config | Untrusted config dict crosses into persisted room state; no key allowlist exists |
| panel WS client → set_person_config | Untrusted wakeup_advance_minutes crosses into persisted person state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-06 | Tampering | ws_set_room_config preheat_max_lead_minutes | mitigate | Clamp to int [0, 480] and drop invalid; coerce preheat_enabled to bool — prevents negative/oversized lead corrupting the pre-heat trigger window (RESEARCH Security Domain V5) |
| T-12-07 | Tampering | ws_set_person_config wakeup_advance_minutes | mitigate | Same [0, 480] int clamp as the prior preheat_lead_minutes clamp; legacy key mapped then validated |
| T-12-SC | Tampering | npm/pip/cargo installs | accept | No package installs — RESEARCH.md confirms zero new dependencies |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_preheat.py -v` — WS tests green
- `make test` — full suite green (websocket + calendar regression check)
- `make lint` — editorconfig compliance
</verification>

<success_criteria>
- ws_get_status returns the three pre-heat fields, matching the push payload
  (PREHEAT-04, Pitfall 1 avoided)
- Room pre-heat config is clamped/validated before persist (PREHEAT-01,
  T-12-06)
- Person clamp renamed to wakeup_advance_minutes with legacy compatibility
  (D-02)
</success_criteria>

<output>
Create `.planning/phases/12-predictive-pre-heat/12-03-SUMMARY.md` when done
</output>
</content>
</invoke>
