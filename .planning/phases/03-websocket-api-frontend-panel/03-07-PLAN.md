---
phase: 03-websocket-api-frontend-panel
plan: 07
type: execute
wave: 6
depends_on: ["03-06"]
files_modified:
  - frontend/src/components/person-card.ts
  - frontend/src/components/persons-tab.ts
  - frontend/src/components/time-bar.ts
autonomous: false
requirements: [UI-04, UI-03]
tags: [phase-03, frontend, person-card, time-bar, d-19, uat-gap, human-verify]

must_haves:
  truths:
    - "The add-room picker in person-card.ts is the shared <search-picker>
      component; room items show floor name as secondary text (from
      hass.floors[area.floor_id].name), omitted if no floor"
    - "Releasing a time-bar boundary drag commits the new boundary silently — NO
      popup appears after pointerup"
    - "End-to-end human verification confirms D-13 Temperatures card placement,
      D-14d room status 4 items, D-19 search pickers in both cards, D-20 3-way
      room mode selector with seeding behavior"
    - "Decisions D-01 (per-day schema), D-02 (visual bar editor), D-03 (period
      colors), D-04 (click-to-split), D-05 (click-block popup), D-06 (drag
      tooltip), D-07 (copy/paste), D-08 (auto-save), D-09
      (save-on-interaction-end), D-10 (toast feedback), D-11 (no applied
      confirmation), D-12 (3-tab navigation), D-14 (rooms expandable cards),
      D-15 (persons expandable cards), D-18 (present persons on global tab only)
      were fully implemented by the prior executed plans (03-01, 03-02, 03-03)
      and are confirmed working by their summaries; this replan adds D-13,
      D-14d, D-19, D-20 only"
  artifacts:
    - path: "frontend/src/components/person-card.ts"
      provides: "Person card with shared search-picker for add-room"
      contains: "search-picker"
    - path: "frontend/src/components/time-bar.ts"
      provides: "Drag-end no longer triggers split popup"
      contains: "_justDragged"
  key_links:
    - from: "frontend/src/components/person-card.ts"
      to: "frontend/src/components/search-picker.ts"
      via: "import './search-picker.js' and <search-picker> in render"
      pattern: "search-picker"
    - from: "frontend/src/components/time-bar.ts"
      to: "(internal — _onBarClick suppresses click after drag)"
      via: "_justDragged flag checked in _onBarClick"
      pattern: "_justDragged"
---

<objective>
Three small but user-visible items close out the Phase 3 replan:
1. D-19 — Integrate the shared `<search-picker>` (delivered by Plan 05) into person-card.ts in place of the current native `<select>`-based add-room picker. Room items show floor name as secondary text.
2. UAT gap fix — Time-bar bug where releasing a boundary drag opens the segment popup (reported in 03-UAT.md test 7). Cause: `_onBarClick` does not honor the `_justDragged` flag that `_onPointerUp` sets, so the synthetic click after pointerup propagates to the bar-wrap and opens a split popup.
3. End-to-end human-verify checkpoint covering the full replanned panel (D-13, D-14d, D-19, D-20, and the time-bar bugfix).

Purpose: Final wave — depends on Plan 06 (room-card overhaul) being live in the
bundled panel so the human-verify exercises all four new decisions together.
Output: Updated person-card.ts + time-bar.ts; verified by human in a deployed HA
instance. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md
@.planning/phases/03-websocket-api-frontend-panel/03-UAT.md
@.planning/phases/03-websocket-api-frontend-panel/03-UI-SPEC.md
@frontend/src/components/person-card.ts
@frontend/src/components/time-bar.ts
@frontend/src/components/search-picker.ts
@frontend/src/components/room-card.ts
@frontend/src/types.ts

<interfaces>
<!-- Contracts the executor needs. -->

From frontend/src/components/search-picker.ts (delivered by Plan 05): public API
documented in Plan 05.

From frontend/src/components/time-bar.ts (lines 104, 446–465, 698–748): the
click/drag interaction model:

- @state() \_drag — non-null while dragging
- private \_justDragged = false — set true in \_onPointerUp; used by
  \_onSegmentClick (line 494) to swallow the synthetic click; NOT consulted by
  \_onBarClick (THIS is the bug)
- \_onBarClick (line 446): if (\_drag) return; — only guards against in-progress
  drag, not against the just-released-drag click

From frontend/src/components/person-card.ts (lines 36–37, 277–284, 322–323,
370–384): current add-room logic: @state() \_showRoomAdd = false;
\_onAddRoomSelect(e: Event) reads e.target.value, calls \_onRoomToggle(roomId,
true) Render block at 370–384 uses the same `<select class="add-select">`
pattern as room-card pre-Plan-06 roomChoices: RoomChoice[] = [{id, name}, ...]
is passed in from persons-tab.ts

From frontend/src/types.ts (Hass interface): hass.areas[area_id].floor_id and
hass.floors[floor_id].name are available — required to derive floor secondary
text per D-19. hass.areas: Record<string, {area_id: string; name: string;
floor_id: string | null}> hass.floors: Record<string, {floor_id: string; name:
string; level: number}>

Person-card currently does NOT receive `hass` as a prop. The parent
persons-tab.ts (line 26) receives hass but does not currently forward it to
person-card. Either: (a) add a hass prop to person-card.ts and forward it from
persons-tab.ts, or (b) pre-compute floor names in persons-tab.ts and expand
RoomChoice to include `secondary?: string`. Approach (b) is cleaner and confines
the change. </interfaces> </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Integrate search-picker into person-card.ts with floor-name secondary text</name>
  <files>frontend/src/components/person-card.ts, frontend/src/components/persons-tab.ts</files>
  <read_first>
    - frontend/src/components/person-card.ts (full file — focus on _showRoomAdd state, _onAddRoomSelect handler, .chips render block at ~365)
    - frontend/src/components/persons-tab.ts (full file — observe how roomChoices is computed at _getRoomChoices(); the RoomChoice interface comes from person-card.ts exporting it at line 27)
    - frontend/src/components/search-picker.ts (verify public API matches Plan 05 contract; especially the picked CustomEvent shape)
    - frontend/src/components/room-card.ts (post-Plan-06: confirm the search-picker integration pattern there so person-card uses the same idiom)
    - .planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md (D-19 — room items show floor name as secondary text; omit secondary when no floor)
  </read_first>
  <action>
    Update both person-card.ts AND persons-tab.ts:

    **In person-card.ts:**
    - Extend the exported RoomChoice interface (line ~27) to include `secondary?: string` (optional floor name). Keep id and name unchanged.
    - Add `import "./search-picker.js";` near the top with other component imports.
    - Replace the `_showRoomAdd ? <select> : <button class="chip-add">` branch (around lines 369–384) with a single `<search-picker>` element rendered whenever `unassignedRooms.length > 0`. Items mapping: `unassignedRooms.map(r => ({id: r.id, label: r.name, secondary: r.secondary, icon: "mdi:home-outline"}))`. Trigger label "Add room", trigger icon "mdi:plus".
    - Bind `@picked=${(e: CustomEvent) => { const {id} = e.detail; void this._onRoomToggle(id, true); }}` on the search-picker element.
    - Remove the `@state() _showRoomAdd = false` field, the `_onAddRoomSelect` method, and the .add-select CSS rule.
    - Hide the search-picker entirely when unassignedRooms is empty (use a conditional render — consistent with the current behavior).

    **In persons-tab.ts:**
    - Extend `_getRoomChoices()` to populate the `secondary` field. For each room: look up `this.hass?.areas?.[roomId]?.floor_id`; if non-null, set `secondary = this.hass?.floors?.[floor_id]?.name` (use `?? undefined` so missing floor entries omit secondary). If floor_id is null or floor not found, leave secondary undefined.
    - No other changes to persons-tab.ts.

    Do NOT change ws-client.ts. Do NOT change room-card.ts (Plan 06 already integrates search-picker there for the add-person flow). Do NOT change the search-picker component itself.

  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'secondary\?:\s*string' frontend/src/components/person-card.ts` shows the RoomChoice interface now includes the optional secondary field
    - `grep -c 'import "./search-picker.js"' frontend/src/components/person-card.ts` returns 1
    - `grep -n "<search-picker" frontend/src/components/person-card.ts` returns at least 1
    - `grep -nE "_showRoomAdd|_onAddRoomSelect" frontend/src/components/person-card.ts` returns NO matches (legacy state and handler removed)
    - `grep -n "mdi:home-outline" frontend/src/components/person-card.ts` shows the icon assigned to picker items
    - In persons-tab.ts: `grep -n "this.hass?.floors" frontend/src/components/persons-tab.ts` and `grep -n "floor_id" frontend/src/components/persons-tab.ts` both return at least one match each (floor-name lookup wired in _getRoomChoices)
    - `grep -n "secondary" frontend/src/components/persons-tab.ts` shows the field being set in the RoomChoice object literal
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - `npx vite build` exits 0; resulting panel.js bundles the new search-picker integration (verifiable: `grep -c "mdi:home-outline" custom_components/climate_manager/www/panel.js` returns at least 1)
  </acceptance_criteria>
  <done>
    person-card.ts uses search-picker for add-room with floor-name secondary; persons-tab.ts computes the secondary from hass.floors; legacy `<select>` machinery removed; build passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Fix time-bar boundary-drag commits opening segment popup (UAT gap test 7)</name>
  <files>frontend/src/components/time-bar.ts</files>
  <read_first>
    - frontend/src/components/time-bar.ts (full file — focus on lines 446–465 (_onBarClick), 492–511 (_onSegmentClick), 698–748 (_onPointerUp), 104 (_justDragged))
    - .planning/phases/03-websocket-api-frontend-panel/03-UAT.md (test 7 gap: "Releasing the drag handle after resizing a period boundary should commit the change silently — no popup")
  </read_first>
  <action>
    Add a `_justDragged` guard to `_onBarClick` matching the existing guard in `_onSegmentClick`. Currently `_onSegmentClick` (lines 492–511) honors `_justDragged` and stops propagation; `_onBarClick` (lines 446–465) only checks the active `_drag` state but not the post-drag flag, so the synthetic click after pointerup propagates and opens a split popup.

    Modification: at the top of `_onBarClick`, immediately after the existing `if (this._drag) return;` guard at line 448, add:

      if (this._justDragged) {
        this._justDragged = false;
        e.stopPropagation();
        return;
      }

    This makes `_onBarClick` behave consistently with `_onSegmentClick`: both swallow the synthetic click that follows a pointerup-on-boundary-drag.

    Do NOT change `_onPointerUp` — the `_justDragged = true` assignment at lines 735 and 744 is correct. Do NOT change `_onSegmentClick`. Do NOT change the drag tooltip or drag preview rendering. Do NOT introduce a setTimeout-based suppression — the synchronous flag pattern is sufficient and matches the pre-existing _onSegmentClick implementation.

    Optional: if while reading the file you notice the `_justDragged = true` assignment at the early-return branch (line 744, when `leftSeg` or `rightSeg` is missing — the no-op exit from pointerup), keep it unchanged. Both code paths must set the flag so the swallow works whether or not a boundary was actually moved.

    No type changes. No other files touched. This is a 5-line behavioral fix.

  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - In time-bar.ts, the `_onBarClick` method body now checks `_justDragged` before opening the split popup. Verifiable: `grep -B2 -A2 "if (this._justDragged)" frontend/src/components/time-bar.ts` shows the guard appearing TWICE — once in `_onSegmentClick` (pre-existing) and once in `_onBarClick` (new). Therefore `grep -c "if (this._justDragged)" frontend/src/components/time-bar.ts` returns at least 2.
    - The new guard appears between the existing `if (this._drag) return;` line and the existing `const barEl = (e.currentTarget as HTMLElement);` line (verifiable by line number ordering)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - `npx vite build` exits 0
  </acceptance_criteria>
  <done>
    time-bar.ts `_onBarClick` honors `_justDragged`; tsc + vite build pass.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify end-to-end — D-13 / D-14d / D-19 / D-20 + UAT gap closure</name>
  <files>(no file modification — user runs HA against deployed integration)</files>
  <what-built>
    All four locked decisions from the latest /gsd:discuss-phase round, plus the time-bar UAT gap fix:
    - D-13: Global Settings tab has THREE ha-cards in order: Current Status, Temperatures, Configuration. The Temperatures card holds the 4 period temperature inputs.
    - D-14d: Each room card's header status line shows 4 items in order: thermometer / water-percent / clock-outline / account-group(count). The 4th item is always present (shows "0" when no persons assigned).
    - D-19: Both add-person picker (room card) and add-room picker (person card) use the shared <search-picker> component (native input + floating list). Person items show presence state (Home/Away) as secondary text; room items show floor name as secondary text.
    - D-20: Each room card has a 3-way native <select> mode selector (Global program / Frost protection / Custom program) replacing the old ha-switch override toggle. Switching to Custom for the first time copies the global program into the room's time_program. The collapsed card badge text matches the selected mode. The time-bar appears only when Custom is selected.
    - UAT gap (test 7): Releasing a time-bar boundary drag commits silently — no popup opens.
  </what-built>
  <how-to-verify>
    1. Build and deploy the integration:
       ```
       cd /home/arnaud/dev/climate_manager &amp;&amp; make deploy
       ```
       Wait for HA to reload the integration (or restart HA if needed). Open the Climate Manager panel from the sidebar.

    2. **D-13 verification** — Go to the Global Settings tab. Confirm the page shows THREE separate ha-cards top-to-bottom, in this order:
       (a) **Current Status** — read-only, with current global mode label, active period, present persons.
       (b) **Temperatures** — card header text is exactly "Temperatures" (not "Default temperatures"). Contains 4 numeric inputs labelled Frost protection, Reduced, Normal, Comfort, each with a °C suffix.
       (c) **Configuration** — contains the global mode dropdown, the global time-program 7-bar editor, and the "Reset to default" button. The 4 temperature inputs are NOT present in this card.
       Change a temperature value and tab out. A "Saved" toast appears. Reload the page — the new value persists.

    3. **D-14d verification** — Switch to the Rooms tab. For each room card (collapsed or expanded), confirm the header status line below the room name shows 4 items separated by spacing, in this order:
       (a) Thermometer icon + temperature (or "—" if no sensor data)
       (b) Water-percent icon + humidity (or "—")
       (c) Clock-outline icon + active period name (or "—")
       (d) Account-group icon + integer count (shows "0" when no persons assigned to the room)
       Assign at least one person to a room (via the search-picker in the next step) and confirm the count increases without a page reload.

    4. **D-19 verification (room card add-person picker)** — Expand any room card. Find the "Add person" pill button under "Associated persons". Click it. A floating popup appears with:
       - A search input (placeholder "Search…" or similar)
       - A scrollable list of person rows; each row shows the person name in bold and presence state ("Home" or "Away") as secondary text in lighter color
       Type a partial name — the list filters case-insensitively. Click a person — the popup closes and a chip appears in the room's associated persons section. A "Saved" toast appears.

    5. **D-19 verification (person card add-room picker)** — Switch to the Persons tab. Expand any person card. Find "Add room" under "Room associations". Click it. The picker shows rooms with floor name (e.g. "Ground floor", "First floor") as secondary text; rooms without a floor show no secondary text. Click a room — chip appears, "Saved" toast fires.

    6. **D-20 verification (room mode selector)** — Go back to the Rooms tab. Expand a room card. Confirm:
       (a) There is NO "Override global time program" toggle anywhere in the card.
       (b) Instead, there is a "Mode" label and a native dropdown with three options: "Global program", "Frost protection", "Custom program".
       (c) Default selection (for a fresh room): "Global program". The collapsed card badge reads "Global program".
       (d) Select "Frost protection" — toast "Saved" appears. The collapsed card badge updates to "Frost protection" (in deep-blue / #1565C0 color). The time-bar editor is NOT shown. Inspect the backend storage (`/config/.storage/climate_manager` or via WS get_config in dev tools) — `rooms.<area_id>.room_mode === "frost_protection"`.
       (e) Select "Custom program" — toast "Saved" appears. The time-bar editor appears below the selector. The badge updates to "Custom program". Inspect storage — `room_mode === "custom"` AND `time_program` is now present in the room's config (seeded from global). Edit one day's program (e.g., split a period or drag a boundary) — only that room's program changes; the global program in Global Settings tab is unchanged.
       (f) Switch back to "Global program" and then back to "Custom program" — the previously-edited time_program is preserved (not re-seeded from global). Inspect storage to confirm.
       (g) Backend behavior: with one room set to "Frost protection", confirm the coordinator pushes 7.0 °C to that room's TRV regardless of the global program. (Check the climate entity's setpoint in HA Developer Tools → States, or watch the Climate Manager logs.)

    7. **UAT gap fix verification (time-bar)** — In the Global Settings tab → Configuration card → time-bar, hover the boundary between two segments on any day (Mon–Sun). A drag handle appears. Drag the boundary left or right — a tooltip shows the snapped HH:MM. Release the mouse. The boundary commits to the new position AND no popup appears. (Previously, releasing opened a split popup — the bug from 03-UAT.md test 7.)

    8. Refresh the panel page once and confirm all changes from steps 2–7 persisted.

    If any of (2)–(8) fail, type the failure description rather than "approved" so the gap can be addressed before phase close.

  </how-to-verify>
  <resume-signal>Type "approved" or describe issues observed during steps 2–8</resume-signal>
  <acceptance_criteria>
    - User explicitly types "approved" (or equivalent affirmative) covering all D-13, D-14d, D-19, D-20, and UAT-test-7 items
    - If any step fails, the failure is recorded with which decision letter / step number; the failure becomes input to a follow-up gap-closure plan
  </acceptance_criteria>
  <done>
    Human confirms all four decisions land correctly in the deployed panel and the UAT test 7 gap is closed.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                   | Description                                                    |
| -------------------------- | -------------------------------------------------------------- |
| panel → ws.setPersonConfig | Untrusted person_id + config; backend sparse-merges (existing) |
| time-bar pointer events    | DOM events; no external input                                  |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                   | Disposition | Mitigation Plan                                                                                                                                |
| ---------- | ---------------------- | ----------------------------------------------------------- | ----------- | ---------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-07-01 | Information Disclosure | Floor name shown as picker secondary                        | accept      | Floor data already visible in HA's main UI; no new exposure                                                                                    |
| T-03-07-02 | Tampering              | search-picker XSS via room label or floor name              | mitigate    | Lit auto-escapes interpolations; persons-tab passes plain strings from hass.floors[].name                                                      |
| T-03-07-03 | DoS                    | Time-bar pointer event handlers fire frequently during drag | accept      | Already mitigated in existing code via \_dragPreviewDays caching; no save fires until pointerup; this plan does not change the drag/move logic |

</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` exits 0 after both Tasks 1 and 2
- `cd frontend && npx vite build` exits 0
- `grep -c "if (this._justDragged)" frontend/src/components/time-bar.ts` returns ≥ 2
- `grep -n "<search-picker" frontend/src/components/person-card.ts` returns ≥ 1
- Human-verify checkpoint approved by user
</verification>

<success_criteria>

- person-card.ts uses <search-picker> for add-room with floor-name secondary
- persons-tab.ts populates RoomChoice.secondary from hass.floors
- time-bar.ts no longer opens a popup when a boundary drag ends
- All TypeScript and Vite builds pass
- Human-verify checkpoint confirms D-13, D-14d, D-19, D-20, and UAT test 7 gap
  closure </success_criteria>

<output>
Create `.planning/phases/03-websocket-api-frontend-panel/03-07-SUMMARY.md` when done. Include user's verification result and any follow-up gaps surfaced during the checkpoint.
</output>
