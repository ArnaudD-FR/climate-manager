---
phase: 03-websocket-api-frontend-panel
plan: 11
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types.ts
  - frontend/src/components/room-card.ts
  - frontend/src/components/global-settings-tab.ts
  - frontend/src/components/time-bar.ts
  - frontend/src/components/person-card.ts
  - frontend/src/components/search-picker.ts
  - frontend/src/components/persons-tab.ts
  - frontend/src/components/rooms-tab.ts
  - frontend/src/toast.ts
  - frontend/src/main.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]
tags: [phase-03, gap-closure, frontend, styling, d-26, d-27]

must_haves:
  truths:
    - "frontend/src/types.ts `PERIOD_COLORS` equals `{ frost_protection:
      '#1565C0', reduced: '#0277BD', normal: '#2E7D32', comfort: '#E65100' }` —
      byte-for-byte per D-26"
    - "frontend/src/types.ts `PRESENCE_COLORS` equals `{ present: '#2E7D32',
      absent: '#9E9E9E' }` — byte-for-byte per D-26"
    - "No file under `frontend/src/` other than `types.ts` contains a hex color
      literal of the form `#RRGGBB` or `#RGB` — search `grep -nE
      '#[0-9a-fA-F]{3,8}' frontend/src/ -r` shows matches only inside types.ts
      (the constants) and inside CSS custom-property fallback expressions of the
      form `var(--token, #fallback)`. NO bare hex literals outside fallbacks
      remain."
    - "All `.period-badge.*` and similar period/presence-badge CSS rules read
      their color from `PERIOD_COLORS` / `PRESENCE_COLORS` via inline `style=`
      attributes or Lit `styleMap`, NOT from hardcoded hex in static styles"
    - "All non-period-badge styling uses HA CSS variables — every
      previously-hardcoded color value (e.g., the `#388E3C` person-dot, the
      `#1565C0` frost program badge) has been replaced with the appropriate HA
      token (or sourced from PRESENCE_COLORS/PERIOD_COLORS when the color is a
      semantic period/presence color)"
    - "The Vite build (`npx vite build`) and TypeScript check (`npx tsc
      --noEmit`) both succeed with no errors after the changes"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "Updated PERIOD_COLORS and PRESENCE_COLORS to D-26 exact values"
      contains: "#0277BD"
    - path: "frontend/src/components/room-card.ts"
      provides:
        "Period badge backgrounds sourced from PERIOD_COLORS; program badge
        frost color sourced from PERIOD_COLORS.frost_protection; no inline hex
        literals"
      contains: "PERIOD_COLORS"
    - path: "frontend/src/components/global-settings-tab.ts"
      provides:
        "Person-dot color sourced from PRESENCE_COLORS.present (or HA
        --success-color via CSS variable); no inline hex literal"
      contains: "PRESENCE_COLORS"
  key_links:
    - from: "frontend/src/components/room-card.ts"
      to: "frontend/src/types.ts"
      via: "import { PERIOD_COLORS } from '../types.js'"
      pattern: "import.*PERIOD_COLORS"
    - from: "frontend/src/components/global-settings-tab.ts"
      to: "frontend/src/types.ts"
      via: "import { PRESENCE_COLORS } from '../types.js'"
      pattern: "import.*PRESENCE_COLORS"
---

<objective>
Apply D-26 (single source of truth for period/presence colors) and D-27 (HA CSS variables everywhere else). This is a global frontend cleanup pass — single concern, no logic changes, only style and color-source changes.

- **D-26:** Make `PERIOD_COLORS` and `PRESENCE_COLORS` in `types.ts` the only
  place a period/presence hex color is written. Update their values to the
  D-26-mandated spec (some current values differ — see Interfaces). All inline
  hex literals that encode a period or presence color get replaced with imports
  from `types.ts`, applied via inline `style=` on the relevant element.
- **D-27:** All other hardcoded colors (the `#388E3C` person-dot in
  global-settings-tab, the `#1565C0` frost program badge in room-card, etc.) get
  replaced with appropriate HA CSS variables (`--primary-color`,
  `--secondary-text-color`, `--success-color`, etc.). The only acceptable hex
  literals remaining outside `types.ts` are CSS variable fallbacks of the form
  `var(--token, #fallback)` — already present throughout the codebase and
  explicitly permitted by D-27 because they are not the primary color source.

Purpose: Establishes the styling discipline before Plan 03-12 ships the D-23
binding fix. The CONTEXT.md decisions stipulate "All non-period-badge styling
uses HA CSS custom properties" — this plan enforces that rule across the whole
panel codebase.

Output: types.ts contains the canonical color tables. No bare hex literal
remains in any other frontend source file (CSS variable fallbacks excluded).
Vite build succeeds. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md
@CLAUDE.md
@frontend/src/types.ts
@frontend/src/components/room-card.ts
@frontend/src/components/global-settings-tab.ts
@frontend/src/components/time-bar.ts
@frontend/src/components/person-card.ts
@frontend/src/components/search-picker.ts
@frontend/src/components/rooms-tab.ts
@frontend/src/components/persons-tab.ts
@frontend/src/toast.ts
@frontend/src/main.ts

<interfaces>
<!-- Current vs target color values, file-by-file. Sourced from `grep -rn '#[0-9a-fA-F]\{3,6\}' frontend/src/`. -->

**Current `frontend/src/types.ts` (lines 91-103) — values disagree with D-26:**

```
PERIOD_COLORS:
  frost_protection: "#1565C0"   ← matches D-26
  reduced:          "#64B5F6"   ← D-26 mandates "#0277BD" (MUST CHANGE)
  normal:           "#F57C00"   ← D-26 mandates "#2E7D32" (MUST CHANGE)
  comfort:          "#D32F2F"   ← D-26 mandates "#E65100" (MUST CHANGE)

PRESENCE_COLORS:
  present:          "#388E3C"   ← D-26 mandates "#2E7D32" (MUST CHANGE)
  absent:           "#9E9E9E"   ← matches D-26
```

Note: D-26 deliberately uses the SAME value (`#2E7D32`) for `normal` and
`present` (both semantically green). Do not "correct" this — it is the D-26
spec.

**Inline hex literals encoding period or presence colors (MUST become
PERIOD_COLORS / PRESENCE_COLORS references):**

- `frontend/src/components/room-card.ts:124-125` — `.program-badge.frost` uses
  `background: #1565C0` (frost program badge — same color as the frost period;
  route through `PERIOD_COLORS.frost_protection`).
- `frontend/src/components/room-card.ts:139-142` —
  `.period-badge.frost-protection` `.reduced` `.normal` `.comfort` use the four
  period hexes inline; CURRENT values are
  `#1565C0 / #0277BD / #2E7D32 / #E65100` — these happen to already match the
  D-26 spec, so the colors visually stay the same in this file even though
  `types.ts` is updating. Replace with PERIOD_COLORS source.
- `frontend/src/components/global-settings-tab.ts:171` — `.person-dot` uses
  `background: #388E3C` — currently the PRESENCE_COLORS.present value but it
  will mismatch after types.ts updates to `#2E7D32`. Replace with
  `PRESENCE_COLORS.present` to keep them coupled.

**Inline hex literals encoding non-period-badge colors (D-27 — MUST become HA
CSS variables, removing the bare hex; fallback hex within
`var(--token, #fallback)` is allowed):**

- `frontend/src/components/room-card.ts:157` — `.no-trv-badge color: #e65100` —
  replace with `color: var(--warning-color, #e65100)` (HA warning palette).
- `frontend/src/components/room-card.ts:156` —
  `background: rgba(255, 152, 0, 0.12)` (warning-tint background) — replace with
  `background: var(--warning-color, #ff9800)` plus `opacity: 0.12` is NOT
  correct; instead use
  `background: rgba(var(--warning-color-rgb, 255 152 0) / 0.12)` IF the variable
  exists, otherwise prefer a thin border + transparent background, OR fall back
  to `var(--secondary-background-color)` (use whichever HA token is closer to
  the original visual). Document the choice in the task action.
- `frontend/src/components/room-card.ts:238`,
  `frontend/src/components/search-picker.ts:75`,
  `frontend/src/components/global-settings-tab.ts:284`,
  `frontend/src/components/person-card.ts:255` — all four files contain
  `background: rgba(3, 169, 244, 0.08)` for `.chip-add:hover` style. Replace
  with `background: var(--primary-color)` plus `opacity: 0.08` is wrong because
  opacity affects children; use
  `background: color-mix(in srgb, var(--primary-color) 8%, transparent)` (modern
  CSS, supported in Lit-rendered shadow DOM) OR fall back to
  `background: var(--secondary-background-color)` if `color-mix` is unsupported
  in the target HA frontend. Document the choice once and apply consistently to
  all four sites.
- `frontend/src/components/time-bar.ts:162` — `color: rgba(255, 255, 255, 0.9)`
  for segment text — replace with `color: var(--text-primary-color, white)` (HA
  token for on-colored-background text).
- `frontend/src/components/time-bar.ts:235-236` — tooltip styling
  `background: rgba(0,0,0,0.75); color: #fff;` — replace with HA tokens
  (`var(--app-header-background-color, rgba(0,0,0,0.75))` and
  `var(--text-primary-color, white)`) OR keep the `rgba` semi-transparent black
  for tooltip readability since HA does not expose a tooltip-bg token; pick one
  approach and apply.
- `frontend/src/components/time-bar.ts:256` —
  `box-shadow: 0 4px 16px rgba(0, 0, 0, 0.25)` — shadow tokens are not
  standardized; replace with
  `box-shadow: var(--ha-card-box-shadow, 0 4px 16px rgba(0,0,0,0.25))` (HA
  defines this for cards).
- `frontend/src/components/search-picker.ts:96` —
  `box-shadow: 0 4px 16px rgba(0, 0, 0, 0.15)` — same
  `var(--ha-card-box-shadow, ...)` treatment.
- `frontend/src/toast.ts:37` — `box-shadow: 0 2px 8px rgba(0,0,0,0.2)` — same
  `var(--ha-card-box-shadow, ...)` treatment.
- `frontend/src/main.ts:84` — `color: #fff` for the error badge — replace with
  `color: var(--text-primary-color, white)`.

**CSS variable fallbacks (e.g., `var(--primary-text-color, #212121)`) are
explicitly permitted — D-27 only forbids hex as the PRIMARY color source;
fallbacks for missing HA variables are correct defensive code.** Do not strip
the fallbacks.

**Period-badge background colors are the ONLY hardcoded color values that can
survive outside `types.ts` — BUT they must be sourced from `PERIOD_COLORS`
(D-26), so even they are not literal hex in component files. The PERIOD_COLORS /
PRESENCE_COLORS objects ARE the single source of truth.**

**Pattern for replacing `.period-badge.<mode> { background: #hex }` static
styles with PERIOD_COLORS:** Two acceptable patterns — pick one and apply
consistently:

PATTERN A (inline style on the element):

```
<span class="period-badge" style="background: ${PERIOD_COLORS[period]}; color: white;">...</span>
```

Remove all `.period-badge.<mode>` CSS class selectors from static styles.

PATTERN B (CSS custom properties set per-element, classes drive default):

```
:host { --period-frost: ${PERIOD_COLORS.frost_protection}; ... }
.period-badge.frost-protection { background: var(--period-frost); }
```

This is more complex; PATTERN A is recommended.

For the frost `.program-badge.frost` rule (room-card.ts:123-126), apply the same
pattern: render `style="background: ${PERIOD_COLORS.frost_protection}"` inline
on the badge element when room_mode === "frost_protection". Remove the
`.program-badge.frost { background: #1565C0; ... }` static rule. </interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: D-26 — update PERIOD_COLORS/PRESENCE_COLORS values in types.ts</name>
  <files>frontend/src/types.ts</files>
  <read_first>
    - frontend/src/types.ts (full file — lines 90-103 are the target)
    - .planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md (D-26 section — confirms the exact spec values: frost #1565C0, reduced #0277BD, normal #2E7D32, comfort #E65100, present #2E7D32, absent #9E9E9E)
  </read_first>
  <action>
    Update the values of the two const objects in `frontend/src/types.ts` (lines 92-103) to the EXACT D-26 spec.

    Replace the PERIOD_COLORS object body to be:
    ```
    {
      frost_protection: "#1565C0",
      reduced: "#0277BD",
      normal: "#2E7D32",
      comfort: "#E65100",
    }
    ```

    Replace the PRESENCE_COLORS object body to be:
    ```
    {
      present: "#2E7D32",
      absent: "#9E9E9E",
    }
    ```

    Do not change the object keys, types, exports, or comments — only the four value literals listed above. Do not touch PERIOD_LABELS or PERIOD_DISPLAY_NAMES.

  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; grep -nE '"#0277BD"|"#1565C0"|"#2E7D32"|"#E65100"|"#9E9E9E"' src/types.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'frost_protection:\s*"#1565C0"' frontend/src/types.ts` returns 1 match
    - `grep -nE 'reduced:\s*"#0277BD"' frontend/src/types.ts` returns 1 match
    - `grep -nE 'normal:\s*"#2E7D32"' frontend/src/types.ts` returns 1 match
    - `grep -nE 'comfort:\s*"#E65100"' frontend/src/types.ts` returns 1 match
    - `grep -nE 'present:\s*"#2E7D32"' frontend/src/types.ts` returns 1 match
    - `grep -nE 'absent:\s*"#9E9E9E"' frontend/src/types.ts` returns 1 match
    - `grep -nE '#64B5F6|#F57C00|#D32F2F|#388E3C' frontend/src/types.ts` returns 0 matches (legacy values removed)
    - `cd frontend && npx tsc --noEmit` exits with code 0
  </acceptance_criteria>
  <done>
    types.ts contains exactly the D-26 spec values. No legacy color hex remains.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: D-26 — route all period/presence colors through PERIOD_COLORS / PRESENCE_COLORS imports</name>
  <files>frontend/src/components/room-card.ts, frontend/src/components/global-settings-tab.ts</files>
  <read_first>
    - frontend/src/components/room-card.ts (full file — focus on static styles block lines 47-290 and the render template lines 546-609; the relevant inline hex lines are 124, 139-142)
    - frontend/src/components/global-settings-tab.ts (locate the `.person-dot` CSS rule — line 171 contains `background: #388E3C`)
    - frontend/src/components/time-bar.ts (lines 30-35 + lines 350-435 — already imports PERIOD_COLORS/PRESENCE_COLORS; reuse the same import statement shape for the two files this task modifies)
    - frontend/src/types.ts (confirm the export names PERIOD_COLORS and PRESENCE_COLORS are accessible)
  </read_first>
  <action>
    Route the four period-badge backgrounds AND the frost program-badge AND the person-dot color through the central color tables. NO inline hex literal for any period or presence color may remain in these two files.

    **A. `frontend/src/components/room-card.ts`:**

    1. Add `PERIOD_COLORS` to the existing named import on line 17:
       Change `import { PERIOD_DISPLAY_NAMES } from "../types.js";`
       To `import { PERIOD_DISPLAY_NAMES, PERIOD_COLORS } from "../types.js";`

    2. **Delete** these four static-style rules in the styles block (lines 139-142):
       ```
       .period-badge.frost-protection { background: #1565C0; color: #fff; }
       .period-badge.reduced           { background: #0277BD; color: #fff; }
       .period-badge.normal            { background: #2E7D32; color: #fff; }
       .period-badge.comfort           { background: #E65100; color: #fff; }
       ```
       Replace with a single class rule that sets only the shared properties (no color):
       ```
       .period-badge { color: white; }
       ```
       (The base `.period-badge` rule at lines 129-137 already sets the layout; just ensure `color: white` is present so the inline `background` from PERIOD_COLORS contrasts. The base rule may already set `color`, so adjust accordingly — do not duplicate.)

    3. **Delete** the `.program-badge.frost` rule (lines 123-126):
       ```
       .program-badge.frost {
         background: #1565C0;
         color: #fff;
       }
       ```
       Keep the `.program-badge.custom` and `.program-badge.global` rules unchanged.

    4. Modify the render template in `_renderHeaderStatus()` (line 428) — the period badge:
       Change `html\`<span class="period-badge ${period.replace(/_/g, "-")}">${periodText}</span>\``
       To `html\`<span class="period-badge" style="background: ${PERIOD_COLORS[period] ?? PERIOD_COLORS.frost_protection}">${periodText}</span>\``
       (Remove the dynamic class suffix; rely on the inline style backed by PERIOD_COLORS. Fallback to frost color when period key is unknown — consistent with time-bar.ts:357.)

    5. Modify the program-badge render block in `render()` (lines 561-562 — the badge before the expand icon).
       Change the existing `<span class="program-badge ${badgeClass}">${badgeText}</span>` to also conditionally add an inline style when badgeClass is "frost":
       ```
       <span
         class="program-badge ${badgeClass}"
         style=${badgeClass === "frost" ? `background: ${PERIOD_COLORS.frost_protection}; color: white;` : ""}
       >${badgeText}</span>
       ```
       This preserves the existing CSS for custom/global badges (which already use `var(--primary-color)` / `var(--secondary-background-color)` — D-27 compliant) and injects PERIOD_COLORS for the frost badge.

    **B. `frontend/src/components/global-settings-tab.ts`:**

    1. Add `PRESENCE_COLORS` to the existing named import on line 22:
       Change `import { PERIOD_DISPLAY_NAMES } from "../types.js";`
       To `import { PERIOD_DISPLAY_NAMES, PRESENCE_COLORS } from "../types.js";`

    2. Delete the `background: #388E3C;` line inside `.person-dot` (line 171) and replace with a CSS custom property reference. Since CSS-in-JS Lit static styles cannot directly interpolate a JS object value into a `static styles = css\`\`` block (the css tag is static), use this approach:
       - Add a `:host` style rule that sets a CSS variable: `:host { --present-color: ${PRESENCE_COLORS.present}; }`
       - Change `.person-dot { background: #388E3C; }` to `.person-dot { background: var(--present-color); }`
       - To inject the JS value into the Lit `css` tag, use the `unsafeCSS` helper from Lit: `import { LitElement, html, css, unsafeCSS } from "lit"` at the top of the file. Then write the rule as:
         ```
         :host { --present-color: ${unsafeCSS(PRESENCE_COLORS.present)}; }
         ```
       - `unsafeCSS` is the canonical Lit pattern for interpolating dynamic values into `css\`\`` template literals. It is safe here because PRESENCE_COLORS.present is a constant string controlled by our codebase, not user input.

    Do not touch any other inline hex in these two files — the next task handles D-27 fallback hex rewrites. Period/presence colors are this task's only scope.

  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '#1565C0|#0277BD|#2E7D32|#E65100' frontend/src/components/room-card.ts` returns 0 matches (all four period hex literals removed from this file)
    - `grep -nE 'background:\s*#388E3C' frontend/src/components/global-settings-tab.ts` returns 0 matches
    - `grep -n 'PERIOD_COLORS' frontend/src/components/room-card.ts` returns at least 2 matches (import + usage)
    - `grep -n 'PRESENCE_COLORS' frontend/src/components/global-settings-tab.ts` returns at least 2 matches (import + usage)
    - `grep -nE 'unsafeCSS' frontend/src/components/global-settings-tab.ts` returns at least 1 match
    - `grep -nE '\.period-badge\.frost-protection\s*\{' frontend/src/components/room-card.ts` returns 0 matches (the per-mode static rules are deleted)
    - `grep -nE '\.program-badge\.frost\s*\{' frontend/src/components/room-card.ts` returns 0 matches
    - The Vite build (`npx vite build`) and TS check (`npx tsc --noEmit`) both exit with code 0
  </acceptance_criteria>
  <done>
    Period and presence colors are exclusively sourced from `types.ts` constants. The period and program badges still render visually identical (because the D-26 spec values match the previously-inlined values for room-card period badges; the person-dot becomes slightly darker green per D-26's `#2E7D32` vs the legacy `#388E3C`).
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: D-27 — replace remaining bare hex literals with HA CSS variables</name>
  <files>frontend/src/components/room-card.ts, frontend/src/components/time-bar.ts, frontend/src/components/search-picker.ts, frontend/src/components/person-card.ts, frontend/src/components/global-settings-tab.ts, frontend/src/components/persons-tab.ts, frontend/src/components/rooms-tab.ts, frontend/src/toast.ts, frontend/src/main.ts</files>
  <read_first>
    - The output of `grep -rnE '#[0-9a-fA-F]{3,8}|rgba?\(' frontend/src/` — this is the full list of remaining color literals; the executor must work through every match.
    - frontend/src/components/room-card.ts (lines 156-157, 238 — the warning badge and chip-add hover)
    - frontend/src/components/time-bar.ts (lines 162, 235-236, 256 — segment text color, tooltip styling, popup shadow)
    - frontend/src/components/search-picker.ts (lines 75, 96 — chip-add hover, popup shadow)
    - frontend/src/components/person-card.ts (line 255 — chip-add hover)
    - frontend/src/components/global-settings-tab.ts (line 284 — chip-add hover)
    - frontend/src/toast.ts (line 37 — toast box-shadow)
    - frontend/src/main.ts (line 84 — error badge text color; line 72 + 92 — divider fallback uses rgba which is acceptable as a fallback inside `var(--divider-color, rgba(0,0,0,0.12))`)
    - .planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md (D-27 mandates these HA tokens: `--primary-text-color`, `--secondary-text-color`, `--primary-color`, `--card-background-color`, `--secondary-background-color`, `--ha-card-border-radius`, `--divider-color`. Additional tokens available in HA: `--warning-color`, `--error-color`, `--success-color`, `--text-primary-color`, `--ha-card-box-shadow`. Choose the closest semantic match for each call site.)
  </read_first>
  <action>
    Replace every bare hex/rgba literal that is NOT a CSS variable fallback. The acceptable form is `var(--ha-token, #fallback)` — hex inside parentheses-after-comma is allowed as a defensive fallback. Bare hex (`background: #abc`) and bare rgba (`background: rgba(...)`) outside fallbacks are forbidden.

    Work through the files in this exact order; do not skip any match found by the grep:

    **1. `frontend/src/components/room-card.ts`:**
    - Line 156 — `.no-trv-badge` `background: rgba(255, 152, 0, 0.12)` → `background: var(--secondary-background-color)` (closest HA token for a subtle tinted background; the warning color tint is not available via HA CSS variables).
    - Line 157 — `.no-trv-badge` `color: #e65100` → `color: var(--warning-color, #e65100)` (HA exposes `--warning-color`).
    - Line 238 — `.chip-add:hover` `background: rgba(3, 169, 244, 0.08)` → `background: var(--secondary-background-color)` (HA does not standardize a primary-color-tint token; the existing border + color on `.chip-add` already conveys interactivity, so use the neutral hover bg).

    **2. `frontend/src/components/time-bar.ts`:**
    - Line 162 — `.segment color: rgba(255, 255, 255, 0.9)` → `color: var(--text-primary-color, white)`.
    - Line 235 — `.popup-tooltip` (or the tooltip rule containing it) `background: rgba(0, 0, 0, 0.75)` → `background: var(--app-header-background-color, rgba(0, 0, 0, 0.75))`. Use the rgba as the fallback inside var.
    - Line 236 — adjacent `color: #fff` → `color: var(--text-primary-color, white)`.
    - Line 256 — `box-shadow: 0 4px 16px rgba(0, 0, 0, 0.25)` → `box-shadow: var(--ha-card-box-shadow, 0 4px 16px rgba(0,0,0,0.25))`.

    **3. `frontend/src/components/search-picker.ts`:**
    - Line 75 — `.chip-add:hover background: rgba(3, 169, 244, 0.08)` → `background: var(--secondary-background-color)`.
    - Line 96 — `box-shadow: 0 4px 16px rgba(0, 0, 0, 0.15)` → `box-shadow: var(--ha-card-box-shadow, 0 4px 16px rgba(0,0,0,0.15))`.

    **4. `frontend/src/components/person-card.ts`:**
    - Line 255 — `.chip-add:hover background: rgba(3, 169, 244, 0.08)` → `background: var(--secondary-background-color)`.

    **5. `frontend/src/components/global-settings-tab.ts`:**
    - Line 284 — `.chip-add:hover background: rgba(3, 169, 244, 0.08)` → `background: var(--secondary-background-color)`.

    **6. `frontend/src/toast.ts`:**
    - Line 37 — `box-shadow: 0 2px 8px rgba(0, 0, 0, 0.2)` → `box-shadow: var(--ha-card-box-shadow, 0 2px 8px rgba(0,0,0,0.2))`.

    **7. `frontend/src/main.ts`:**
    - Line 84 — `color: #fff` → `color: var(--text-primary-color, white)`.

    **8. Audit `frontend/src/components/persons-tab.ts` and `frontend/src/components/rooms-tab.ts`:**
    - Run `grep -nE '#[0-9a-fA-F]{3,8}|rgba?\(' frontend/src/components/persons-tab.ts frontend/src/components/rooms-tab.ts`. For every match that is NOT inside a `var(--token, #fallback)` form, replace with the appropriate HA token following the same patterns used in the files above. If no matches: no changes needed for these two files (and they can be omitted from this task's commit).

    **CSS variable fallback hex INSIDE `var(--token, #fallback)` parentheses-after-comma is ALLOWED to remain** — these are existing defensive defaults (e.g., `var(--primary-color, #03a9f4)`, `var(--secondary-text-color, #757575)`). Do not remove or "clean up" the fallback hex; they protect against missing HA tokens in non-HA contexts.

    **Hex inside `PERIOD_COLORS` / `PRESENCE_COLORS` const objects in types.ts are also allowed** (already changed in Task 1).

  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - The audit `grep -rnE '^\s*background:\s*#[0-9a-fA-F]{3,8}\s*;|^\s*color:\s*#[0-9a-fA-F]{3,8}\s*;|^\s*background:\s*rgba?\([^)]*\)\s*;' frontend/src/ | grep -v types.ts` returns 0 matches (no bare hex or rgba as a primary value outside types.ts)
    - `grep -rnE 'rgba\(3, 169, 244, 0.08\)' frontend/src/` returns 0 matches (the four chip-add:hover rules are all cleaned up)
    - `grep -rnE 'rgba\(255, 152, 0, 0.12\)' frontend/src/` returns 0 matches (room-card no-trv-badge cleaned)
    - `grep -rn 'box-shadow: 0' frontend/src/` either returns 0 matches OR only matches lines inside `var(--ha-card-box-shadow, ...)` fallback form
    - The Vite build (`npx vite build`) and TS check (`npx tsc --noEmit`) both exit with code 0
    - Visual no-regression check: the `.period-badge` and `.program-badge.frost` backgrounds use PERIOD_COLORS exclusively (from Task 2), and all other colors come from HA tokens. Period-badge values are byte-equal between types.ts and the prior inline literals, so the user-visible appearance of badges does NOT change after this plan ships.
  </acceptance_criteria>
  <done>
    No bare hex/rgba color value remains as a primary `background:` or `color:` value anywhere under `frontend/src/`. All non-period/presence colors route through HA CSS variables; period/presence colors route through `PERIOD_COLORS` / `PRESENCE_COLORS`. The panel is fully styled by HA tokens + one product-specific palette.
  </done>
</task>

</tasks>

<verification>
- Vite build succeeds: `cd frontend && npx vite build` exits with code 0 and produces `dist/panel.js`.
- TypeScript check passes: `cd frontend && npx tsc --noEmit` exits with code 0.
- Audit greps in each task's acceptance_criteria all pass.
- Manual visual check (deferred to Plan 03-12 human-verify, not blocking here): when the panel is opened, period badges render in their D-26 colors (frost dark blue, reduced sky blue, normal green, comfort orange-red); the person-dot in Overview is the same green as the `normal` period badge (intentional per D-26 spec).
</verification>

<success_criteria>

- `frontend/src/types.ts` PERIOD_COLORS and PRESENCE_COLORS match the D-26 spec
  exactly.
- No file under `frontend/src/` except `types.ts` contains a hex literal that is
  NOT inside a CSS variable fallback.
- Build artifacts produced; integration deploys cleanly via `make deploy`
  (assumed unchanged — not exercised in this plan). </success_criteria>

<output>
Create `.planning/phases/03-websocket-api-frontend-panel/03-11-SUMMARY.md` when done. Summary must record:
- Final list of files modified
- The chosen replacement for each rgba/hex literal (so reviewers can audit semantic intent)
- Whether `color-mix` was used anywhere (it was discouraged in this plan; if used, document why)
- Confirmation that the Vite build output size did not regress materially
</output>
