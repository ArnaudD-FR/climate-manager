---
phase: 03-websocket-api-frontend-panel
plan: 06
type: execute
wave: 5
depends_on: ["03-04", "03-05"]
files_modified:
  - frontend/src/types.ts
  - frontend/src/components/room-card.ts
autonomous: true
requirements: [UI-03]
tags: [phase-03, frontend, ui, room-card, d-14d, d-19, d-20]

must_haves:
  truths:
    - "Room card header status line always renders 4 items in order:
      temperature, humidity, active period, person count — each shows '—' (or 0
      for person count) when data is unavailable"
    - "Person count uses mdi:account-group icon and reads
      _getAssignedPersonIds().length"
    - "The 'Override global time program' ha-switch is REMOVED from
      room-card.ts; a native <select> with 3 options (Global program / Frost
      protection / Custom program) replaces it"
    - "The native <select> @change handler calls ws.setRoomConfig with
      {room_mode: '<value>'} and (only when switching TO custom for the first
      time, i.e. no existing room time_program) also includes time_program
      copied from panelConfig.global_time_program in the same call"
    - "Selecting 'Custom program' reveals the inline 7-bar time-bar editor below
      the selector; 'Global program' and 'Frost protection' hide the editor"
    - "Collapsed card badge text is exactly 'Frost protection' / 'Global
      program' / 'Custom program' depending on the resolved room_mode"
    - "The add-person picker (formerly a native <select>) is replaced by the
      shared <search-picker> component from Plan 05; person items show name
      (bold) + presence state ('Home'/'Away') as secondary text"
  artifacts:
    - path: "frontend/src/components/room-card.ts"
      provides:
        "Room card with 4-item header status, 3-way mode selector, search-picker
        integration"
      contains: "search-picker"
  key_links:
    - from: "frontend/src/components/room-card.ts"
      to: "frontend/src/components/search-picker.ts"
      via: "import './search-picker.js' and <search-picker> in render"
      pattern: "search-picker"
    - from: "frontend/src/components/room-card.ts"
      to: "frontend/src/ws-client.ts"
      via: "ws.setRoomConfig({room_mode: ..., time_program?: ...})"
      pattern: "setRoomConfig"
    - from: "frontend/src/components/room-card.ts"
      to: "frontend/src/types.ts"
      via: "RoomConfig type — needs an optional room_mode field"
      pattern: "room_mode"
---

<objective>
Apply three decisions to the Room Card:
1. D-14d — Add person count as a 4th item in the header status line (always rendered; '0' when no persons).
2. D-19 — Replace the add-person `<select>` dropdown with the shared `<search-picker>` component from Plan 05; secondary text is the person's presence state from hass.states.
3. D-20 — Replace the boolean "Override global time program" ha-switch with a 3-way native `<select>` mode selector (Global program / Frost protection / Custom program). The select drives `room_mode` in the backend via ws.setRoomConfig. Custom mode reveals the inline time-bar; the first switch to Custom seeds time_program from the global program in the same WS call.

Purpose: The single biggest user-visible Phase 3 replan change. All three
decisions live in the same component (room-card.ts), so they ship together in
one plan. Output: Restructured room-card.ts. types.ts gains an optional
room_mode field on RoomConfig. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md
@.planning/phases/03-websocket-api-frontend-panel/03-RESEARCH.md
@.planning/phases/03-websocket-api-frontend-panel/03-UI-SPEC.md
@frontend/src/components/room-card.ts
@frontend/src/components/search-picker.ts
@frontend/src/components/global-settings-tab.ts
@frontend/src/types.ts
@frontend/src/ws-client.ts

<interfaces>
<!-- Contracts the executor needs. -->

From frontend/src/components/search-picker.ts (delivered by Plan 05):

- customElements.define("search-picker", SearchPicker)
- @property items: Array<{id: string; label: string; secondary?: string; icon?:
  string}>
- @property placeholder: string
- @property triggerLabel: string
- @property triggerIcon: string
- Fires CustomEvent "picked" with detail {id: string} on selection

From frontend/src/ws-client.ts (lines 61-71): setRoomConfig signature
setRoomConfig(roomId: string, config: Partial<RoomConfig>): Promise<{success:
boolean}> // POSTs to climate_manager/set_room_config which sparse-merges config
into rooms[room_id]

From frontend/src/types.ts (lines 25-27): current RoomConfig type export
interface RoomConfig { time_program?: DailyProgram | null; } Needs to be
extended with: room_mode?: "global" | "frost_protection" | "custom";

From custom_components/climate_manager/const.py (delivered by Plan 04 Task 1):
ROOM_MODE_GLOBAL = "global" ROOM_MODE_FROST = "frost_protection"
ROOM_MODE_CUSTOM = "custom" These are the exact wire strings the backend expects
in room_mode.

From frontend/src/components/room-card.ts current shape (study before
modifying):

- \_getAssignedPersonIds(): string[] — already present at line ~293
- \_getAllPersonIds(): string[] — already present at line ~300
- \_getPersonName(personId: string): string — already present at line ~308
- \_onOverrideToggle(e) — line ~351 — to be removed/replaced
- \_renderHeaderStatus() — line ~400 — currently renders 3 items
  (temp/humidity/period); add 4th item
- \_renderPersonsSection() — line ~459 — uses a native <select> for add-person;
  replace with <search-picker>
- render() body — at line ~500; the override-row block at ~528 holds the
  ha-switch to be removed

HA 2026.x: continue using native <select> for the 3-way mode selector
(consistent with the existing global mode selector in global-settings-tab.ts
that also uses a native <select>). </interfaces> </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend RoomConfig type with optional room_mode field</name>
  <files>frontend/src/types.ts</files>
  <read_first>
    - frontend/src/types.ts (full file — confirm exact RoomConfig location and existing field, then extend)
    - .planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md (D-20 — confirms the three legal string values: "global", "frost_protection", "custom")
  </read_first>
  <action>
    Add an optional field `room_mode?: "global" | "frost_protection" | "custom"` to the existing RoomConfig interface in frontend/src/types.ts. Place it as the first field (above `time_program?`). Add a JSDoc comment explaining: default sparse — absent key implies "global"; legal values per D-20. Do NOT change any other type. Do NOT add a runtime const map (the three string literals live in code via comparisons; no enum needed).
  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'room_mode\?:\s*"global"\s*\|\s*"frost_protection"\s*\|\s*"custom"' frontend/src/types.ts` returns exactly one line
    - `grep -c "interface RoomConfig" frontend/src/types.ts` returns 1 (interface still exists once)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - The existing `time_program?: DailyProgram | null` field is preserved unchanged
  </acceptance_criteria>
  <done>
    RoomConfig has an optional room_mode union type; tsc passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Refactor room-card.ts — header status (4 items), 3-way mode selector, search-picker for persons</name>
  <files>frontend/src/components/room-card.ts</files>
  <read_first>
    - frontend/src/components/room-card.ts (full file — large file ~560 lines, read in full before editing; understand _renderHeaderStatus, _renderPersonsSection, _onOverrideToggle, _onPeriodsChanged, render() ordering)
    - frontend/src/components/search-picker.ts (delivered by Plan 05 — verify the public API matches before importing)
    - frontend/src/types.ts (post Task 1 — verify room_mode field present on RoomConfig)
    - frontend/src/components/global-settings-tab.ts (lines 370–385 — observe the existing native `<select>` styling pattern; reuse the same .mode-select CSS approach in room-card.ts for the new 3-way selector)
    - .planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md (D-14d, D-19, D-20 — confirm wire values, badge text, secondary text for persons, mdi:account-group icon)
  </read_first>
  <action>
    Restructure room-card.ts to apply D-14d, D-19, D-20:

    **D-14d — Header status 4-item line:**
    - Modify `_renderHeaderStatus()`. Currently it emits 3 `<span class="status-item">` blocks for temperature / humidity / active period. Add a 4th `<span class="status-item">` after the active period: `<ha-icon icon="mdi:account-group"></ha-icon>${count}` where `count` is `this._getAssignedPersonIds().length` (use the existing helper unchanged — D-14d explicitly references it). The count must always render — show "0" when the array is empty (not "—"; per D-14d "always shows '0' when no persons assigned"). All 4 items remain present whether the card is collapsed or expanded.

    **D-19 — Replace add-person `<select>` with `<search-picker>`:**
    - Add `import "./search-picker.js";` at the top of the file with the other component imports.
    - In `_renderPersonsSection()`, replace the current `_showPersonAdd ? <select>... : <button class="chip-add">...` branch with a single `<search-picker>` element. The picker is always rendered (no toggle button needed — its trigger button is built into the component). Items prop is built from `unassignedPersonIds` mapped to `{id: personId, label: this._getPersonName(personId), secondary: this._getPersonPresenceState(personId), icon: "mdi:account"}`. The trigger button shows label "Add person" and icon "mdi:plus".
    - Add a new private helper `_getPersonPresenceState(personId: string): string`: returns the friendly presence string from hass — `this.hass?.states[personId]?.state === "home" ? "Home" : this.hass?.states[personId]?.state === "not_home" ? "Away" : "—"`. Capitalize first letter for unknown states for consistency.
    - Listen for the picker's `picked` CustomEvent on the search-picker element via `@picked=${this._onPersonPicked}`. The handler `_onPersonPicked(e: CustomEvent)` reads `e.detail.id`, calls the existing `_onAddPerson(personId)` flow, then stopPropagation.
    - Remove the `@state() _showPersonAdd` field and the `_onAddPersonSelect` method — both are no longer used.
    - Hide the picker entirely when `unassignedPersonIds.length === 0` (consistent with current behavior).

    **D-20 — Replace boolean override ha-switch with 3-way native `<select>`:**
    - Delete the entire `.override-row` block in render() (the ha-switch labelled "Override global time program") and remove the `.override-row` and `.override-label` CSS rules from `static styles`.
    - Delete the `_onOverrideToggle` method entirely.
    - Add a new section before the time-bar in the expanded card body, structured like the global mode selector in global-settings-tab.ts: a `.section-label` "Mode" + a wrapping `<div class="select-wrapper">` containing a `<select class="mode-select">` with three `<option>` elements:
        - value="global" — text "Global program" — selected when room_mode is "global" OR absent
        - value="frost_protection" — text "Frost protection" — selected when room_mode is "frost_protection"
        - value="custom" — text "Custom program" — selected when room_mode is "custom"
    - The select's @change handler is a new method `_onRoomModeChange(e: Event)`. Read the new mode from `(e.target as HTMLSelectElement).value`. Build the WS payload:
        - If `newMode === "custom"` AND `this.config.time_program` is absent/null/empty: include `time_program` copied from `this.panelConfig.global_time_program` (use `JSON.parse(JSON.stringify(this.panelConfig.global_time_program))` to deep-copy and avoid shared references). The payload is `{room_mode: "custom", time_program: <copied>}`.
        - If `newMode === "custom"` AND `this.config.time_program` already exists: payload is just `{room_mode: "custom"}` — preserve existing time_program (no seeding on subsequent switches).
        - Otherwise: payload is `{room_mode: newMode}` (no time_program touched — leaves any prior custom program intact in storage so the user can flip back to Custom without losing it).
    - Call `await this.ws.setRoomConfig(this.roomId, payload); await this.panel.reloadConfig(); this.panel.showToast("Saved", false)` (with the existing try/catch + showToast("Save failed — retrying...", true) error path).
    - Add CSS for `.mode-select`, `.select-wrapper`, `.section-label` to the static styles block, copied from global-settings-tab.ts so the look matches the global-mode selector.

    **Badge text update (collapsed header):**
    - Update the render() block that computes `badgeText`. Resolve the room mode: `const resolvedMode = this.config?.room_mode ?? "global";` (treat absent key as "global" per D-20). Map to badge text/class:
        - "global" → text "Global program", class "global"
        - "frost_protection" → text "Frost protection", class "frost"
        - "custom" → text "Custom program", class "custom"
    - Add a `.program-badge.frost` CSS rule with background `#1565C0` (frost protection color from D-03) and white text. Keep the existing `.program-badge.global` and `.program-badge.custom` rules.

    **Time-bar visibility:**
    - The inline `<climate-manager-time-bar mode="schedule" .days=...>` block is now shown only when `resolvedMode === "custom"` (NOT when `hasCustomProgram` — that legacy boolean is replaced). The block's @periods-changed handler `_onPeriodsChanged` and its existing ws.setRoomConfig({time_program: ...}) call are unchanged.

    **Default expansion state:**
    - In connectedCallback(), expand the card by default when the room is in custom mode (matches the old behavior of expanding rooms with a custom program). Update: `this._expanded = (this.config?.room_mode === "custom");`

    **Code cleanup:**
    - Remove the now-unused @state() _showPersonAdd field and the _onAddPersonSelect method.
    - Remove the .add-select CSS rule if it's no longer referenced (the search-picker has its own styling).
    - Keep all existing person-removal chip behavior (× button on each .chip) unchanged.

    Do NOT modify ws-client.ts. Do NOT modify the backend. Do NOT touch person-card.ts (Plan 07's job). Do NOT change the search-picker.ts file (Plan 05 delivered it).

  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "mdi:account-group" frontend/src/components/room-card.ts` shows at least one match inside `_renderHeaderStatus`
    - `grep -c "_getAssignedPersonIds().length" frontend/src/components/room-card.ts` returns at least 1 (the helper is called from the status-line render)
    - `grep -nE "ha-switch|_onOverrideToggle|_showPersonAdd|_onAddPersonSelect" frontend/src/components/room-card.ts` returns NO matches (legacy override toggle and select-picker artifacts removed)
    - `grep -nE 'value="global"|value="frost_protection"|value="custom"' frontend/src/components/room-card.ts | wc -l` returns at least 3 (three `<option>` values)
    - `grep -n "_onRoomModeChange" frontend/src/components/room-card.ts` returns both a method declaration and at least one @change binding
    - `grep -nE 'badge.*"Frost protection"|"Frost protection".*badge' frontend/src/components/room-card.ts` — the literal "Frost protection" badge text appears in the render path
    - `grep -c 'import "./search-picker.js"' frontend/src/components/room-card.ts` returns 1
    - `grep -n "<search-picker" frontend/src/components/room-card.ts` returns at least 1 (search-picker used in render)
    - `grep -n "_getPersonPresenceState" frontend/src/components/room-card.ts` returns both a declaration and at least one usage
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - `npx vite build` exits 0; the produced `custom_components/climate_manager/www/panel.js` exists and contains the string "Frost protection" (verifiable via `grep -c "Frost protection" custom_components/climate_manager/www/panel.js` returning at least 1)
    - `grep -n "panelConfig.global_time_program" frontend/src/components/room-card.ts` returns at least 1 match (the D-20 seeding path deep-copies global_time_program when first switching to custom)
    - When the executor inspects the rendered structure (manual smoke test deferred to Plan 07 verification), the room card header status line shows 4 items in order: thermometer / water-percent / clock-outline / account-group
  </acceptance_criteria>
  <done>
    Room card emits 4-item header status line including person count; ha-switch removed; 3-way <select> drives ws.setRoomConfig with correct payloads including time_program seeding on first Custom switch; add-person picker uses <search-picker>; badge text matches D-20 wire values; tsc + vite build both pass.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                 | Description                                                                                                                                                    |
| ------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| panel → ws.setRoomConfig | Untrusted room_id + config dict; backend handler accepts arbitrary keys via setdefault+update (existing — Plan 04 coordinator validates room_mode defensively) |
| hass.states[personId]    | Read-only HA state; presence string already shown elsewhere in HA UI                                                                                           |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                 | Disposition | Mitigation Plan                                                                                                                                                                                        |
| ---------- | ---------------------- | --------------------------------------------------------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| T-03-06-01 | Tampering              | Select value spoofed to unknown string                    | mitigate    | Plan 04 coordinator treats unknown room_mode as "global" (defensive default); native <select> only emits the three declared option values in normal use                                                |
| T-03-06-02 | Information Disclosure | Person presence state shown in picker secondary text      | accept      | This data is already visible in HA's main UI; no new exposure                                                                                                                                          |
| T-03-06-03 | Data integrity         | Time program lost when switching Custom → Global → Custom | mitigate    | Per action spec: switching away from Custom does NOT delete the stored time_program (only room_mode changes); re-selecting Custom preserves the existing time_program and does NOT re-seed from global |
| T-03-06-04 | XSS                    | Search-picker label/secondary                             | mitigate    | search-picker.ts uses Lit html templates which auto-escape interpolation; the helper \_getPersonName already returns plain text                                                                        |

</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` exits 0
- `cd frontend && npx vite build` exits 0
- All grep-based acceptance assertions in Task 2 pass
- `grep -E "ha-select|ha-textfield|ha-tabs" frontend/src/components/room-card.ts` returns nothing
</verification>

<success_criteria>

- Header status line renders 4 items (D-14d): thermometer / water-percent /
  clock-outline / account-group(count), in that order, always present
- 3-way `<select>` drives room_mode (D-20) with options Global program / Frost
  protection / Custom program
- Switching to Custom for the first time bundles time_program (deep-copied from
  global) in the same WS call
- Time-bar editor visible only in Custom mode
- Badge text: "Global program" / "Frost protection" / "Custom program"
- Add-person picker is a <search-picker> with presence-state secondary text
  (D-19)
- ha-switch and legacy \_onOverrideToggle removed
- types.ts RoomConfig has optional room_mode union type
- TypeScript and Vite build both pass </success_criteria>

<output>
Create `.planning/phases/03-websocket-api-frontend-panel/03-06-SUMMARY.md` when done.
</output>
