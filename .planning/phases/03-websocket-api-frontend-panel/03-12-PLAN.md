---
phase: 03-websocket-api-frontend-panel
plan: 12
type: execute
wave: 2
depends_on: ["03-10", "03-11"]
files_modified:
  - frontend/src/types.ts
  - frontend/src/components/room-card.ts
autonomous: false
requirements: [UI-02, UI-03]
tags: [phase-03, gap-closure, frontend, d-23, d-24, d-25, human-verify]

must_haves:
  truths:
    - "frontend/src/types.ts `RoomStatus` interface includes a new field
      `present_person_count: number` (non-optional)"
    - "frontend/src/types.ts `ClimateConfig` interface includes a new field
      `climate_entities: string[]` (non-optional — backend always emits it per
      Plan 03-10)"
    - "frontend/src/components/room-card.ts no longer computes `presentCount`
      via `assignedIds.filter(id =>
      this.status?.present_persons?.includes(id)).length` — that expression is
      gone from `_renderHeaderStatus`"
    - "frontend/src/components/room-card.ts binds the present count directly
      from `this.roomStatus?.present_person_count ?? 0`"
    - "The 4-item room card header status line (D-14d) continues to display
      assignment-count vs presence-count correctly: in non-presence modes it
      shows `${totalPersons}`; in time_program_presences mode it shows
      `${presentCount}/${totalPersons}`, where `presentCount` comes from the
      backend field, not from a TS-side array intersection"
    - "Vite build and TypeScript check both succeed"
    - "Human verification confirms: opening the panel, switching to
      time_program_presences mode, marking a person as Force Present, and
      verifying that the corresponding room card header shows the expected `1/N`
      count without page refresh (the value comes from the backend push)"
  artifacts:
    - path: "frontend/src/types.ts"
      provides:
        "RoomStatus.present_person_count and ClimateConfig.climate_entities
        typed contracts"
      contains: "present_person_count"
    - path: "frontend/src/components/room-card.ts"
      provides:
        "_renderHeaderStatus binds present_person_count directly from
        roomStatus; the legacy `assignedIds.filter(...).length` intersection is
        removed"
      contains: "present_person_count"
  key_links:
    - from: "frontend/src/components/room-card.ts"
      to: "custom_components/climate_manager/coordinator.py:_build_status_payload"
      via: "roomStatus.present_person_count populated by Plan 03-10 backend"
      pattern: "present_person_count"
    - from: "frontend/src/types.ts"
      to: "custom_components/climate_manager/websocket.py:ws_get_config"
      via: "climate_entities field added in Plan 03-10"
      pattern: "climate_entities"
---

<objective>
Close out D-23 — the frontend-logic-boundary cleanup. With Plan 03-10 having shipped `present_person_count` and `climate_entities` on the backend, this plan:

1. Updates `frontend/src/types.ts` to declare the new contract fields on
   `RoomStatus` and `ClimateConfig`.
2. Removes the inline array-intersection in `room-card.ts:435`
   (`assignedIds.filter(id => this.status?.present_persons?.includes(id)).length`)
   and replaces it with the direct binding
   `this.roomStatus?.present_person_count ?? 0` — moving the
   present-person-count computation from frontend to backend per D-23.
3. Runs the end-to-end human-verify checkpoint that exercises the round-trip:
   change a person's mode → backend recomputes → status push updates
   roomStatus.present_person_count → room card header updates without manual
   refresh.

Purpose: This is the final D-23/D-24-binding plan; after it ships, every
CONTEXT.md decision (D-23 through D-30) is fully implemented and all logic lives
in the backend per the architectural rule.

Output: TypeScript types reflect the new backend contract; room-card.ts contains
no array intersection for presence-count; visual round-trip works. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md
@.planning/phases/03-websocket-api-frontend-panel/03-10-PLAN.md
@.planning/phases/03-websocket-api-frontend-panel/03-11-PLAN.md
@CLAUDE.md
@frontend/src/types.ts
@frontend/src/components/room-card.ts
@custom_components/climate_manager/coordinator.py
@custom_components/climate_manager/websocket.py

<interfaces>
<!-- Contracts the executor needs. -->

From frontend/src/types.ts (current state — lines 50-58):

```
export interface RoomStatus {
  area_id: string;
  name: string;
  entity_ids?: string[];
  temperature?: number | null;
  humidity?: number | null;
  active_period?: string | null;
}
```

After Plan 03-10 ships backend, EVERY rooms_status entry contains
`present_person_count: int` — non-optional. Add it as
`present_person_count: number` (non-optional) to mirror the backend contract.

From frontend/src/types.ts (current state — lines 41-48):

```
export interface ClimateConfig {
  global_mode: string;
  period_temperatures: Record<string, number>;
  global_time_program: DailyProgram;
  rooms: Record<string, RoomConfig>;
  persons: Record<string, PersonConfig>;
}
```

After Plan 03-10 ships backend, `get_config` response includes
`climate_entities: list[str]`. Add it as `climate_entities: string[]`
(non-optional — backend always emits per Plan 03-10's tests).

From frontend/src/components/room-card.ts (current state — lines 413-465,
\_renderHeaderStatus method): The relevant block (lines 430-439) currently
reads:

```
const assignedIds = this._getAssignedPersonIds();
const totalPersons = assignedIds.length;
const globalMode = this.status?.global_mode ?? this.panelConfig?.global_mode ?? "";
const isPresenceMode = globalMode === "time_program_presences";
const presentCount = isPresenceMode
  ? assignedIds.filter(id => this.status?.present_persons?.includes(id)).length
  : null;
const personDisplay = presentCount != null
  ? `${presentCount}/${totalPersons}`
  : `${totalPersons}`;
```

The target behavior after this plan:

- `totalPersons` = `assignedIds.length` (still computed in TS — this is a SIMPLE
  LOOKUP per D-23 "Acceptable in frontend: filtering picker items").
- `presentCount` = `this.roomStatus?.present_person_count ?? 0` when
  `isPresenceMode === true`, else `null`.
- The expression
  `assignedIds.filter(id => this.status?.present_persons?.includes(id)).length`
  is removed in its entirety.
- The behavior visible to the user is unchanged:
  `${presentCount}/${totalPersons}` in presence mode, `${totalPersons}`
  otherwise.

CRITICAL: `assignedIds` is still needed for the `title` tooltip text (line 459:
`${presentCount} present / ${totalPersons} assigned`). Do not remove
`assignedIds` or `totalPersons` — only the `.filter(...).length` intersection.

From custom_components/climate_manager/coordinator.py and websocket.py (after
Plan 03-10): `rooms_status[].present_person_count: int` is always present, value
≥ 0. The frontend can rely on this contract.

From the existing room-card.ts (line 30):

```
@property({ attribute: false }) roomStatus: RoomStatus | null = null;
```

The roomStatus property already exists; this plan extends the TypeScript
interface (which roomStatus references) and reads the new field via
`this.roomStatus?.present_person_count`.

Human-verify scenario (end-to-end):

- A clean HA dev environment with the integration installed and `make deploy`
  having delivered both backend (Plan 03-10) and frontend (Plans 03-11 + 03-12)
  changes.
- At least one person and one room with the person assigned to the room.
- Steps documented in the checkpoint task. </interfaces> </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add new fields to RoomStatus and ClimateConfig in types.ts</name>
  <files>frontend/src/types.ts</files>
  <read_first>
    - frontend/src/types.ts (full file — lines 41-66 contain the two interfaces being modified)
    - .planning/phases/03-websocket-api-frontend-panel/03-10-SUMMARY.md (confirm the backend contract shipped — present_person_count is non-optional int, climate_entities is non-optional list[str])
    - .planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md (D-23, D-24, D-25 sections)
  </read_first>
  <action>
    Add two non-optional fields to two interfaces in `frontend/src/types.ts`.

    **1. Extend `RoomStatus` (lines 50-58):**
    Add a new non-optional field `present_person_count: number;` after the existing `active_period?: string | null;` line. Final shape:
    ```
    export interface RoomStatus {
      area_id: string;
      name: string;
      entity_ids?: string[];
      temperature?: number | null;
      humidity?: number | null;
      active_period?: string | null;
      present_person_count: number;
    }
    ```
    Do NOT make this optional (no `?`). The backend always emits it (Plan 03-10 contract).

    **2. Extend `ClimateConfig` (lines 41-48):**
    Add a new non-optional field `climate_entities: string[];` after `persons: Record<string, PersonConfig>;`. Final shape:
    ```
    export interface ClimateConfig {
      global_mode: string;
      period_temperatures: Record<string, number>;
      global_time_program: DailyProgram;
      rooms: Record<string, RoomConfig>;
      persons: Record<string, PersonConfig>;
      climate_entities: string[];
    }
    ```
    Non-optional per Plan 03-10's contract: the backend always emits the key, even when the list is empty.

    Do not touch any other interface, export, or comment. Do not change `Period`, `DailyProgram`, `RoomConfig`, `PersonConfig`, `StatusPayload`, `Hass`, `PERIOD_COLORS`, `PRESENCE_COLORS`, `PERIOD_LABELS`, or `PERIOD_DISPLAY_NAMES`.

    Note: this may break the compile-time type-check in `panel.ts` / `main.ts` if those files load a config object from disk/storage without a `climate_entities` key. Search for those usages with `grep -n 'ClimateConfig' frontend/src/`. If any constructor expression instantiates a ClimateConfig literal without `climate_entities`, the TS compiler will fail. In that case, add `climate_entities: []` to the literal. (Practical note: the only construction of ClimateConfig in the codebase is the WebSocket response binding; literal constructions are rare. Investigate via grep first.)

  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'present_person_count:\s*number;' frontend/src/types.ts` returns exactly 1 match
    - `grep -nE 'climate_entities:\s*string\[\];' frontend/src/types.ts` returns exactly 1 match
    - The new fields are non-optional (no `?` before the colon) — `grep -nE 'present_person_count\?:' frontend/src/types.ts` returns 0 matches and `grep -nE 'climate_entities\?:' frontend/src/types.ts` returns 0 matches
    - `cd frontend && npx tsc --noEmit -p tsconfig.json` exits with code 0
  </acceptance_criteria>
  <done>
    Type definitions for the two new backend contract fields are in place. Compile passes.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: room-card.ts — replace frontend array intersection with direct backend binding</name>
  <files>frontend/src/components/room-card.ts</files>
  <read_first>
    - frontend/src/components/room-card.ts (full file — focus on `_renderHeaderStatus` at lines 413-465)
    - frontend/src/types.ts (after Task 1 — confirm `RoomStatus.present_person_count` is declared)
    - .planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md (D-23 paragraph — defines the boundary rule: "display-computed values that require backend data joins (e.g., per-room present-person count)" belong in backend)
  </read_first>
  <action>
    Modify ONLY the `_renderHeaderStatus` method (around lines 413-465) in `frontend/src/components/room-card.ts`. No other method or section of this file changes.

    **Find this block (lines 430-439 in the current file):**
    ```
    const assignedIds = this._getAssignedPersonIds();
    const totalPersons = assignedIds.length;
    const globalMode = this.status?.global_mode ?? this.panelConfig?.global_mode ?? "";
    const isPresenceMode = globalMode === "time_program_presences";
    const presentCount = isPresenceMode
      ? assignedIds.filter(id => this.status?.present_persons?.includes(id)).length
      : null;
    const personDisplay = presentCount != null
      ? `${presentCount}/${totalPersons}`
      : `${totalPersons}`;
    ```

    **Replace with:**
    ```
    const assignedIds = this._getAssignedPersonIds();
    const totalPersons = assignedIds.length;
    const globalMode = this.status?.global_mode ?? this.panelConfig?.global_mode ?? "";
    const isPresenceMode = globalMode === "time_program_presences";
    // D-23: present count comes from backend (rooms_status.present_person_count) — no TS-side intersection.
    const presentCount = isPresenceMode
      ? (this.roomStatus?.present_person_count ?? 0)
      : null;
    const personDisplay = presentCount != null
      ? `${presentCount}/${totalPersons}`
      : `${totalPersons}`;
    ```

    Keep everything else in `_renderHeaderStatus` unchanged — including the `title` attribute on the `mdi:account-group` status item (line 459) which uses `presentCount` and `totalPersons` for the tooltip text. The variable names and shapes are identical, so the tooltip and display work without further edits.

    Do NOT remove `this.status` from the imports, properties, or render template — `globalMode` and `isPresenceMode` still read from `this.status?.global_mode` so the prop remains in use.

    Do NOT touch `_getAssignedPersonIds` (lines 296-301) — it is still called to compute `totalPersons` (assignment count, which IS a "simple lookup" per D-23 and stays in the frontend).

    Do NOT touch `_getAllPersonIds`, `_getPersonName`, `_getPersonPresenceState`, `_onAddPerson`, `_onRemovePerson`, `_onPersonPicked`, or any other method.

    Do NOT touch the TRV section, the persons section, the mode select, the time-bar section, or any CSS in this file.

  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'assignedIds\.filter\(id\s*=>\s*this\.status\?\.present_persons\?\.includes' frontend/src/components/room-card.ts` returns 0 matches (the legacy intersection is gone)
    - `grep -nE 'this\.roomStatus\?\.present_person_count\s*\?\?\s*0' frontend/src/components/room-card.ts` returns exactly 1 match
    - `grep -nE 'this\.status\?\.present_persons\?\.includes' frontend/src/components/room-card.ts` returns 0 matches (no remaining usage of the present_persons array for this purpose; status.present_persons may still be used elsewhere by other code paths — confirm via context whether any remain, but the room-card array-intersection use is fully removed)
    - `grep -n '_getAssignedPersonIds' frontend/src/components/room-card.ts` returns at least 2 matches (declaration + at least 1 caller — the method is preserved)
    - `cd frontend && npx tsc --noEmit -p tsconfig.json` exits with code 0
    - `cd frontend && npx vite build` exits with code 0 and produces `dist/panel.js`
  </acceptance_criteria>
  <done>
    `_renderHeaderStatus` binds `presentCount` directly from `this.roomStatus?.present_person_count ?? 0`. The frontend no longer joins persons.room_ids against status.present_persons — that work is the backend's per D-23.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <files>(no file modification — user runs HA against deployed integration)</files>
  <name>Task 3: End-to-end human verification of D-23/D-24/D-25 round-trip</name>
  <what-built>
    Backend (Plan 03-10) emits `present_person_count` on every rooms_status entry and `climate_entities` on every get_config response. Frontend (Plans 03-11 + 03-12) styles all colors from `types.ts` + HA tokens, and reads `present_person_count` directly from `roomStatus` without computing it.
  </what-built>
  <how-to-verify>
    Run `make deploy` to push backend + frontend to the dev HA instance, restart HA, then verify in the HA UI:

    **Step 1 — Build and deploy.**
    `cd /home/arnaud/dev/climate_manager && make deploy`
    Confirm both the Python module copy and the frontend build copy succeed (the Makefile output should show panel.js being deployed). If `make deploy` fails, stop and report the error — do not continue.

    **Step 2 — Restart HA and open the Climate Manager panel.**
    Restart Home Assistant (via Developer Tools → YAML → Reload Config, or full restart if the integration is not hot-reloadable). Open the Climate Manager panel from the sidebar.

    Confirm:
    - The panel loads without a JS console error (open browser devtools).
    - The Overview tab shows the current global mode and present persons (no regression).
    - The Rooms tab shows at least one room card with the 4-item header status line: thermometer + humidity + period badge + person count.

    **Step 3 — Verify present-person-count binding (D-23/D-24).**
    a. On the Overview tab, set the global mode to "Time program & presences" (top-level mode selector).
    b. Go to the Persons tab. Expand any one person card. Set their mode to "Force Present". Verify the save toast appears.
    c. Note which rooms that person is assigned to (room association chips in the expanded card).
    d. Switch to the Rooms tab. For each room the person is assigned to, the header status line should now show `1/N` for the person count (where N is the total assignment count for that room). For rooms with no person assignments, the count should be `0`.
    e. Without refreshing the page, return to the Persons tab and switch that person's mode to "Force Absent". Switch back to the Rooms tab — the count should now be `0/N` for those same rooms. The update should appear within a minute (the coordinator re-evaluates and pushes status).

    **Step 4 — Verify backend-only logic boundary.**
    Open browser devtools → Network tab → filter "websocket". Trigger a `get_status` (refresh the panel). Inspect the response payload. For every entry in `rooms_status`, confirm a `present_person_count` integer key is present.

    Also send a `get_config` (refresh the panel triggers this). Confirm the response payload contains a `climate_entities` array of strings, each starting with `climate.`.

    **Step 5 — Visual styling no-regression (D-26/D-27).**
    Compare the panel's visual appearance with a screenshot from before this gap-closure cycle (or the previous plan's human-verify). Confirm:
    - Period badges (Frost/Reduced/Normal/Comfort) render in the D-26 colors. `normal` and `present` both display as the same green (`#2E7D32`) — this is intentional per D-26.
    - The "Frost protection" program badge on a Rooms tab card uses the deep blue color, not a generic gray.
    - No element has lost its color or layout vs. the prior state.
    - In dark mode (if the HA theme supports it), backgrounds and text use HA tokens that adapt correctly — no white/light-only colors leak through.

    Report success only if ALL of Steps 1-5 pass.

  </how-to-verify>
  <action>Human-only verification — no automated action. Follow the steps in   <resume-signal>Type "approved" to mark Phase 3 fully complete, or describe any visual or functional regression seen so a follow-up fix plan can be created.</resume-signal>lt;how-to-verify  <resume-signal>Type "approved" to mark Phase 3 fully complete, or describe any visual or functional regression seen so a follow-up fix plan can be created.</resume-signal>gt;. The executor pauses for the user to perform the checks and respond with "approved" or describe regressions.</action>
  <verify>Manual verification per   <resume-signal>Type "approved" to mark Phase 3 fully complete, or describe any visual or functional regression seen so a follow-up fix plan can be created.</resume-signal>lt;how-to-verify  <resume-signal>Type "approved" to mark Phase 3 fully complete, or describe any visual or functional regression seen so a follow-up fix plan can be created.</resume-signal>gt; steps; no automated command runs for this task. The user reports the outcome in their resume signal.</verify>
  <done>User has responded "approved" indicating all 5 verification steps passed. Any reported regression triggers a follow-up gap-closure plan instead of marking this task done.</done>
  <resume-signal>Type "approved" to mark Phase 3 fully complete, or describe any visual or functional regression seen so a follow-up fix plan can be created.</resume-signal>
</task>

</tasks>

<verification>
- TypeScript check passes after Task 1+2 (`cd frontend && npx tsc --noEmit` exits 0).
- Vite build succeeds after Task 2 (`cd frontend && npx vite build` exits 0).
- Human-verify Task 3 confirms the full round-trip and no visual regressions.
</verification>

<success_criteria>

- `frontend/src/types.ts` declares `RoomStatus.present_person_count: number` and
  `ClimateConfig.climate_entities: string[]`, both non-optional.
- `frontend/src/components/room-card.ts` reads `present_person_count` directly
  from `roomStatus`; the legacy `.filter(...).length` intersection is gone.
- Human-verify checkpoint passes (Steps 1-5 of Task 3).
- After this plan ships, every CONTEXT.md decision D-23 through D-30 is fully
  implemented across backend + frontend. </success_criteria>

<output>
Create `.planning/phases/03-websocket-api-frontend-panel/03-12-SUMMARY.md` when done. Summary must record:
- The exact diff applied to types.ts and room-card.ts
- The human-verify result (approved or list of regressions)
- A note that D-23 through D-30 are now end-to-end implemented (referencing each decision and which plan delivered it)
- Any follow-up issues discovered during human-verify (to feed into a new gap-closure if needed)
</output>
