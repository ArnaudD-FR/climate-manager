---
phase: 03-websocket-api-frontend-panel
plan: 05
type: execute
wave: 4
depends_on: []
files_modified:
  - frontend/src/components/search-picker.ts
  - frontend/src/components/global-settings-tab.ts
autonomous: true
requirements: [UI-01, UI-02, UI-03, UI-04]
tags: [phase-03, frontend, ui, d-13, d-19, search-picker, temperatures-card]

must_haves:
  truths:
    - "A new shared search-picker Lit component exists at frontend/src/components/search-picker.ts using native input + custom list (no ha-select, no ha-textfield, no ha-tabs — all broken in HA 2026.x)"
    - "The Global Settings tab renders exactly THREE ha-card sections in this order: 'Current Status', 'Temperatures', 'Configuration'"
    - "The 'Temperatures' card (card-header text exactly 'Temperatures') contains the 4 period temperature inputs (Frost protection, Reduced, Normal, Comfort)"
    - "The 'Configuration' card no longer contains the 4 temperature inputs (only the global mode <select>, the global time-bar editor, and the Reset to default button)"
    - "search-picker exposes a `<search-picker>` custom element accepting `items: Array<{id, label, secondary?, icon?}>` and `placeholder: string`; fires `picked` CustomEvent with detail {id} on selection"
  artifacts:
    - path: "frontend/src/components/search-picker.ts"
      provides: "Shared SearchPicker Lit component"
      contains: "customElements.define(\"search-picker\""
    - path: "frontend/src/components/global-settings-tab.ts"
      provides: "Restructured Global Settings tab with Temperatures card separated"
      contains: "Temperatures"
  key_links:
    - from: "frontend/src/components/global-settings-tab.ts"
      to: "frontend/src/ws-client.ts"
      via: "ws.setPeriodTemperatures call from Temperatures card input"
      pattern: "setPeriodTemperatures"
    - from: "frontend/src/components/search-picker.ts"
      to: "frontend/src/types.ts"
      via: "no direct import needed; component is generic over its own item shape"
      pattern: "SearchPickerItem"
---

<objective>
Two parallel UI changes:
1. Restructure the Global Settings tab into THREE cards (D-13): Current Status → **Temperatures** → Configuration. The Temperatures card holds the 4 period temperature inputs that previously lived inside the Configuration card.
2. Create the shared `<search-picker>` Lit component (D-19) — a native-input + floating custom-list popup that replaces both the existing `<select>`-based add-person picker (in room-card.ts) and add-room picker (in person-card.ts). This plan only delivers the component itself; downstream plans (06 + 07) integrate it into room-card and person-card.

Purpose: Establishes both UI building blocks needed for the rest of Phase 3 replan. Independent of the backend room_mode plan (04) so can run in the same wave.
Output: New search-picker.ts file + restructured global-settings-tab.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md
@.planning/phases/03-websocket-api-frontend-panel/03-RESEARCH.md
@.planning/phases/03-websocket-api-frontend-panel/03-UI-SPEC.md
@frontend/src/components/global-settings-tab.ts
@frontend/src/components/room-card.ts
@frontend/src/components/person-card.ts
@frontend/src/types.ts

<interfaces>
<!-- Key contracts the executor needs. Extracted from current codebase. -->

From frontend/src/components/global-settings-tab.ts (lines 14-21): the file currently imports
  import { LitElement, html, css } from "lit";
  import { property } from "lit/decorators.js";
  import type { ClimateConfig, StatusPayload, DailyProgram, Period } from "../types.js";
  import type { WsClient } from "../ws-client.js";
  import type { ClimateManagerPanel } from "../main.js";
  import "./time-bar.js";

Current Global Settings tab renders TWO cards in its render(): _renderStatusCard() and _renderConfigCard(). The _renderConfigCard contains: global-mode `<select>`, temperatures grid, time-bar editor, reset button.

From frontend/src/components/room-card.ts (lines 482-487): existing add-person `<select>` UI uses
  <select class="add-select" @change=${this._onAddPersonSelect}>
    <option value="">Select person…</option>
    ${unassignedPersonIds.map((id) => html`<option value=${id}>${this._getPersonName(id)}</option>`)}
  </select>

From frontend/src/components/person-card.ts (lines 370-376): existing add-room `<select>` UI uses the same `<select>` pattern with roomChoices.

The new <search-picker> component will REPLACE both `<select>` blocks in plans 06 and 07. This plan only defines the component contract; plans 06/07 wire it in.

HA 2026.x CRITICAL CONSTRAINTS (project memory):
  - ha-select is BROKEN — never use it. Native `<select>` is OK as a transitional fallback but search-picker must use a native `<input type="text">` + custom `<ul>` list rendered as a floating popup.
  - ha-tabs/paper-tab REMOVED — not relevant to this plan but reinforces "use native HTML".
  - ha-textfield renders NOTHING — never use it. Use native `<input>` with surrounding `<label>` and a suffix span.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create shared search-picker.ts Lit component</name>
  <files>frontend/src/components/search-picker.ts</files>
  <read_first>
    - frontend/src/components/room-card.ts (lines 280–500 — observe the .add-select / .chip-add styles and add-person dropdown interaction that this component replaces; visual style of the new picker should align with the existing chip-add button as its trigger)
    - frontend/src/components/person-card.ts (lines 230–250 and 365–380 — same .add-select pattern for the add-room dropdown)
    - frontend/src/components/global-settings-tab.ts (lines 160–230 — observe HA CSS variable usage for the existing native `<select>` so search-picker visual styling is consistent with the global mode dropdown)
    - .planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md (D-19 section — defines item shape and secondary text semantics)
  </read_first>
  <action>
    Create a new Lit element file frontend/src/components/search-picker.ts. The component name (customElements.define string) is exactly "search-picker". Public API:

    - `@property({ type: Array }) items: Array<{id: string; label: string; secondary?: string; icon?: string}>` — list of selectable rows; consumer pre-filters to exclude already-assigned items
    - `@property({ type: String }) placeholder: string` — input placeholder text (default: "Search…")
    - `@property({ type: String }) triggerLabel: string` — text shown on the trigger button when popup is closed (default: "Add")
    - `@property({ type: String }) triggerIcon: string` — mdi icon for the trigger button (default: "mdi:plus")
    - Internal `@state() _open: boolean` — popup open/closed
    - Internal `@state() _query: string` — current search text
    - On click of the trigger button: opens the popup, focuses the search input, resets _query
    - On search input event: updates _query
    - Items shown filter case-insensitively on label OR secondary (substring match)
    - On click of a list row: dispatches `new CustomEvent("picked", {detail: {id: row.id}, bubbles: true, composed: true})`, closes popup, clears _query
    - On Escape key: closes popup without dispatching
    - On click outside the picker: closes popup (use a document-level click listener registered when popup opens, removed when closed)
    - List rendering: each row shows `<ha-icon>` (if icon prop present), bold label, secondary text in `var(--secondary-text-color)` (font-size 12px); rows have hover background `var(--secondary-background-color)` and a pointer cursor
    - Popup is positioned absolutely below the trigger button (CSS `position: absolute; top: 100%; left: 0;` inside a wrapper with `position: relative;`)
    - Use native `<input type="text">`, native `<ul>` + `<li>`, native `<button>` — NO ha-textfield, NO ha-select, NO ha-list-item (per HA 2026.x memory)
    - Export the class as `SearchPicker` from the module (named export)
    - Add a TypeScript declare global block extending HTMLElementTagNameMap so consumers get type completion when using `<search-picker>` in Lit html templates

    Component CSS uses HA CSS variables: `var(--primary-color)`, `var(--primary-text-color)`, `var(--secondary-text-color)`, `var(--card-background-color)`, `var(--secondary-background-color)`, `var(--divider-color)`. No hardcoded colors except where falling back via the same `, #default` pattern already used in room-card.ts/person-card.ts.

    Trigger button is styled to match the existing `.chip-add` look in room-card.ts: rounded pill, border `1px solid var(--primary-color)`, transparent background, primary-color text, mdi icon + label.

    Do NOT wire search-picker into room-card.ts or person-card.ts in this task — that is plans 06 and 07. This task delivers ONLY the standalone component file and verifies it loads.
  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - File frontend/src/components/search-picker.ts exists
    - `grep -n 'customElements.define("search-picker"' frontend/src/components/search-picker.ts` returns exactly one match
    - `grep -n "@property" frontend/src/components/search-picker.ts | grep -c -E "items|placeholder|triggerLabel|triggerIcon"` returns at least 4 (one @property line per public input)
    - `grep -n "new CustomEvent" frontend/src/components/search-picker.ts | grep -c '"picked"'` returns at least 1 (picked event dispatched)
    - `grep -E "ha-select|ha-textfield|ha-tabs|ha-list-item|paper-tab" frontend/src/components/search-picker.ts` returns no matches (HA 2026.x broken components must not appear)
    - `grep -c "var(--primary-color)" frontend/src/components/search-picker.ts` returns at least 1 (uses HA theme variables)
    - `npx tsc --noEmit -p tsconfig.json` exits 0 from the frontend directory (no type errors introduced)
    - Component bundles cleanly into panel.js (verified by Task 2 build step or by `npx vite build` if run independently; non-blocking here — plan 06 will fail-fast if the file is unreadable)
  </acceptance_criteria>
  <done>
    Standalone search-picker.ts file exists with the documented public API; passes tsc --noEmit; no HA 2026.x broken components referenced.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Restructure Global Settings tab into three cards (Current Status / Temperatures / Configuration)</name>
  <files>frontend/src/components/global-settings-tab.ts</files>
  <read_first>
    - frontend/src/components/global-settings-tab.ts (full file — current structure renders 2 cards via _renderStatusCard + _renderConfigCard; must split _renderConfigCard into _renderTemperaturesCard + _renderConfigCard)
    - .planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md (D-13 section — exact card title is "Temperatures", order is Current Status → Temperatures → Configuration)
    - .planning/phases/03-websocket-api-frontend-panel/03-UI-SPEC.md (Component Inventory — Global Settings Tab — confirms field labels and types)
  </read_first>
  <action>
    Refactor global-settings-tab.ts so render() outputs exactly three ha-card elements in this order:
    1. Current Status card — UNCHANGED, produced by existing _renderStatusCard().
    2. **Temperatures card** — NEW. Card-header text is exactly the string `Temperatures` (not "Default temperatures", not "Default Temperatures"). Contains the existing 4 temperature inputs grid (Frost protection / Reduced / Normal / Comfort), keeping their data-key attributes ("frost_protection", "reduced", "normal", "comfort"), `<input type="number" step="0.5" min="5" max="30">`, `°C` suffix span, and the existing @change handler `_onTemperatureChange`. Move the entire `.temp-fields` block and its 4 `.temp-field` children from the Configuration card into this new card. Remove the `<div class="section-divider">Default temperatures</div>` sub-heading — the card title "Temperatures" replaces it.
    3. Configuration card — Contains ONLY the global mode `<select>`, the global time-bar editor (`<climate-manager-time-bar mode="schedule" .days=... @periods-changed=...>`), the "Global time program" section divider, and the "Reset to default" button. The temperature fields must NOT appear here.

    Create a new private method `_renderTemperaturesCard()` that returns the Temperatures card html template. Method signature: no parameters, returns the ha-card html.

    Update render() to: `${this._renderStatusCard()} ${this._renderTemperaturesCard()} ${this._renderConfigCard()}` in that exact order.

    Update the _renderConfigCard() method to remove the temperature fields entirely; keep the global mode `<select>`, the time-bar, and the Reset button. Remove the "Default temperatures" `.section-divider` div. The "Global time program" `.section-divider` div MAY remain inside the Configuration card.

    The "Reset to default" button (_onResetToDefault) MUST continue to reset temperatures back to {frost_protection:7, reduced:18, normal:20, comfort:22} via ws.setPeriodTemperatures — it stays in the Configuration card unchanged (it resets all three card payloads: mode, temperatures, program). After clicking Reset, both the Temperatures card AND the Configuration card UI must reflect the new defaults (handled automatically by the existing patchConfig + reloadConfig flow).

    Keep all existing CSS in static styles. The `.temp-fields`, `.temp-field`, `.temp-input`, `.temp-suffix`, `.temp-input-row`, `.temp-label` styles remain — they're now used inside the Temperatures card.

    Do NOT change WebSocket commands. Do NOT change types.ts. Do NOT touch room-card.ts or person-card.ts in this task.
  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "_renderTemperaturesCard" frontend/src/components/global-settings-tab.ts` shows at least one declaration line and at least one usage in render()
    - `grep -nE "Temperatures|Configuration" frontend/src/components/global-settings-tab.ts | grep -E "card-header" | grep -c '"Temperatures"\|>Temperatures<'` is at least 1 (the literal string "Temperatures" appears as a card header value)
    - In render() the three card calls appear in source order: a regex match `_renderStatusCard\\(\\).*_renderTemperaturesCard\\(\\).*_renderConfigCard\\(\\)` is found via `grep -Pzo` across whitespace
    - `grep -c "section-divider.*Default temperatures" frontend/src/components/global-settings-tab.ts` returns 0 (the "Default temperatures" sub-heading is gone)
    - The temperature `<input>` elements appear inside the Temperatures card render branch, NOT inside the Configuration card render branch (verifiable by inspecting the source: _renderConfigCard does not contain any `class="temp-input"`)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - `npx vite build` exits 0 and produces custom_components/climate_manager/www/panel.js
    - The compiled panel.js contains the string "Temperatures" (verified with `grep -c "Temperatures" custom_components/climate_manager/www/panel.js` returning at least 1)
  </acceptance_criteria>
  <done>
    Global Settings tab renders 3 cards in the order Current Status → Temperatures → Configuration; the Temperatures card holds all 4 temperature inputs; the Configuration card no longer holds temperature inputs; tsc + vite build pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel → ws.setPeriodTemperatures | Untrusted numeric input; backend already validates via vol.Coerce(float) (existing) |
| search-picker items prop | Consumer-supplied list; rendered as DOM via Lit (auto-escapes — no XSS) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05-01 | Tampering | search-picker XSS via item.label or item.secondary | mitigate | Lit's html template auto-escapes interpolated values; consumer is expected to pass plain strings (no html templates); component documentation enforces this expectation |
| T-03-05-02 | Information Disclosure | search-picker item.secondary may show presence state ("Home"/"Away") | accept | this data is already visible to the panel user via the HA frontend — no new exposure |
| T-03-05-03 | DoS | search-picker filter is O(n) over items[] on every keystroke | accept | items[] is bounded to rooms or persons count (~dozens); negligible cost |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` exits 0
- `cd frontend && npx vite build` exits 0 producing custom_components/climate_manager/www/panel.js
- `grep -E "ha-select|ha-textfield|ha-tabs|ha-list-item" frontend/src/components/search-picker.ts` returns nothing
- Manually loading the panel (executed in Plan 07 verification) shows the Temperatures card as a separate ha-card between Current Status and Configuration
</verification>

<success_criteria>
- search-picker.ts exists with the documented contract (items, placeholder, triggerLabel, triggerIcon, picked event)
- Global Settings tab renders Current Status / Temperatures / Configuration in that order
- Temperatures card holds the 4 temperature inputs with their existing data-keys and ws.setPeriodTemperatures handler
- Configuration card no longer renders the temperature inputs
- TypeScript and Vite build both pass
- No HA 2026.x broken components introduced
</success_criteria>

<output>
Create `.planning/phases/03-websocket-api-frontend-panel/03-05-SUMMARY.md` when done.
</output>
