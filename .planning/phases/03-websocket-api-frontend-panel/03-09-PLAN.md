---
phase: 03-websocket-api-frontend-panel
plan: 09
type: execute
wave: 8
depends_on: ["03-08"]
files_modified:
  - frontend/src/components/person-card.ts
  - frontend/src/components/persons-tab.ts
autonomous: false
requirements: [UI-04]
tags:
  [
    phase-03,
    frontend,
    person-card,
    presence-modes,
    d-15-updated,
    d-21,
    d-22,
    human-verify,
  ]

must_haves:
  truths:
    - "person-card.ts defines the four wire-string constants
      PRESENCE_MODE_SCHEDULED='scheduled',
      PRESENCE_MODE_FORCE_PRESENT='force_present',
      PRESENCE_MODE_FORCE_ABSENT='force_absent', PRESENCE_MODE_HA='ha'
      (replacing the legacy PRESENCE_MODE_PRESENT='present' /
      PRESENCE_MODE_ABSENT='absent' literals)"
    - "The presence mode selector renders FOUR <option> elements in this exact
      order: Scheduled (value='scheduled'), HA (value='ha'), Force Present
      (value='force_present'), Force Absent (value='force_absent')"
    - "Mode badge text is 'Scheduled' / 'HA' / 'Force Present' / 'Force Absent'
      per D-21, replacing the legacy 'Present' / 'Absent' / 'Scheduled'
      three-text mapping"
    - "When _onModeChange fires with newMode='scheduled' AND
      this.config.schedule is empty/absent (no day has any periods), the WS save
      payload includes a default schedule object per D-22 (Mon–Fri: 00:00
      present → 08:00 absent → 18:00 present; Sat–Sun: 00:00 present) bundled
      into the same set_person_config call"
    - "person-card receives a new @property `status: StatusPayload | null` and
      renders a presence dot (green or gray) in the card header (collapsed AND
      expanded), driven by `status.present_persons.includes(this.personId)`"
    - "person-card.connectedCallback no longer expands non-default cards — ALL
      cards are always collapsed by default per D-15 (updated)"
    - "persons-tab.ts passes .status=${this.status} to every
      <climate-manager-person-card>"
    - "The presence schedule time-bar is visible only when the selected mode is
      'scheduled' (was: 'automatic'/'scheduled' — same semantic, refreshed wire
      value)"
  artifacts:
    - path: "frontend/src/components/person-card.ts"
      provides:
        "Four-mode presence selector, default schedule seeding on switch to
        scheduled, presence dot in card header, always-collapsed default"
      contains: "force_present"
    - path: "frontend/src/components/persons-tab.ts"
      provides: "Forwards status prop to every person-card"
      contains: ".status=${this.status}"
  key_links:
    - from: "frontend/src/components/person-card.ts"
      to: "frontend/src/ws-client.ts"
      via: "ws.setPersonConfig({mode, schedule?})"
      pattern: "setPersonConfig"
    - from: "frontend/src/components/person-card.ts"
      to: "frontend/src/types.ts"
      via: "StatusPayload type import for the new status prop"
      pattern: "StatusPayload"
    - from: "frontend/src/components/persons-tab.ts"
      to: "frontend/src/components/person-card.ts"
      via: ".status=${this.status} binding"
      pattern: "\\.status="
---

<objective>
Implement D-21 frontend (four presence modes incl. HA), D-22 (default schedule seeding when switching TO `scheduled` mode), and the D-15 update (always-collapsed person cards + presence dot in card header). Backend wire strings and HA-mode evaluation already shipped in Plan 03-08, so this plan focuses entirely on `person-card.ts` and `persons-tab.ts`.

Purpose: Closes out the 2026-05-21 CONTEXT.md additions. After this plan ships,
every CONTEXT.md decision is implemented end-to-end.

Output: person-card.ts has 4 mode options, badge text per D-21, presence dot
bound to `status.present_persons`, default-collapsed always, and seeds a default
presence schedule on first switch to `scheduled` mode. persons-tab.ts forwards
the status prop down. A human-verify checkpoint confirms the round-trip
including the storage migration from Plan 03-08. </objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md
@.planning/phases/03-websocket-api-frontend-panel/03-RESEARCH.md
@.planning/phases/03-websocket-api-frontend-panel/03-UI-SPEC.md
@.planning/phases/03-websocket-api-frontend-panel/03-08-PLAN.md
@frontend/src/components/person-card.ts
@frontend/src/components/persons-tab.ts
@frontend/src/components/room-card.ts
@frontend/src/types.ts
@frontend/src/ws-client.ts

<interfaces>
<!-- Contracts the executor needs. -->

From frontend/src/components/person-card.ts (current state — lines 23-26): const
PRESENCE_MODE_AUTOMATIC = "scheduled"; const PRESENCE_MODE_PRESENT = "present";
// ← VALUE must change to "force_present" (D-21) const PRESENCE_MODE_ABSENT =
"absent"; // ← VALUE must change to "force_absent" (D-21) // ADD: const
PRESENCE_MODE_HA = "ha";

Suggested rename (clearer intent, matches backend): rename
PRESENCE_MODE_AUTOMATIC → PRESENCE_MODE_SCHEDULED PRESENCE_MODE_PRESENT →
PRESENCE_MODE_FORCE_PRESENT PRESENCE_MODE_ABSENT → PRESENCE_MODE_FORCE_ABSENT
add PRESENCE_MODE_HA = "ha"

All five usages of these constants live inside person-card.ts (see lines 24-26,
55, 292-296, 302-303, 328-330). No external file imports them.

From frontend/src/components/person-card.ts (current state — lines 45-49):
connectedCallback expands on \_isNonDefault(). D-15 (updated) removes this —
always collapsed.

From frontend/src/components/person-card.ts (current state — lines 241-251):
\_onModeChange currently saves only `{mode: newMode}`. D-22 requires it to ALSO
include `schedule: <default>` in the SAME payload when newMode === "scheduled"
AND no existing schedule.

From frontend/src/components/persons-tab.ts (lines 22-27): persons-tab already
has `status: StatusPayload | null` as a property AND `hass: Hass`. The tab does
NOT currently forward `status` to person-card. This plan wires that prop
binding.

From frontend/src/types.ts: StatusPayload has shape
`{global_mode, active_period, present_persons: string[], rooms_status: RoomStatus[]}`.
The `present_persons` array contains person.\* entity ids. Import is
`import type { StatusPayload } from "../types.js"` (already imported in
persons-tab.ts).

From frontend/src/ws-client.ts (existing signature):
`setPersonConfig(personId: string, config: Partial<PersonConfig>): Promise<{success: boolean}>`
— accepts a partial PersonConfig dict; sparse-merges into persons[person_id] in
the backend. Calling setPersonConfig with `{mode: "scheduled", schedule: {...}}`
in one call sends both keys in a single payload — exactly what D-22 needs.

D-22 default schedule literal (from CONTEXT.md — reproduce exactly): { mon:
[{start: "00:00", state: "present"}, {start: "08:00", state: "absent"}, {start:
"18:00", state: "present"}], tue: [{start: "00:00", state: "present"}, {start:
"08:00", state: "absent"}, {start: "18:00", state: "present"}], wed: [{start:
"00:00", state: "present"}, {start: "08:00", state: "absent"}, {start: "18:00",
state: "present"}], thu: [{start: "00:00", state: "present"}, {start: "08:00",
state: "absent"}, {start: "18:00", state: "present"}], fri: [{start: "00:00",
state: "present"}, {start: "08:00", state: "absent"}, {start: "18:00", state:
"present"}], sat: [{start: "00:00", state: "present"}], sun: [{start: "00:00",
state: "present"}], }

HA 2026.x compatibility: native `<select>` is REQUIRED; never use ha-select.
Native HTML for the new dot indicator is preferable — use a
`<span class="presence-dot present|absent">●</span>` or equivalent CSS-styled
element. </interfaces> </context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: D-21 + D-22 + D-15 update — rewrite person-card.ts presence semantics</name>
  <files>frontend/src/components/person-card.ts</files>
  <read_first>
    - frontend/src/components/person-card.ts (FULL file — 397 lines; understand the current 3-mode constant block, _isNonDefault, connectedCallback expansion logic, _onModeChange handler, _getBadgeInfo, render() badge classes, mode-select <option> render block, schedule visibility predicate)
    - frontend/src/components/persons-tab.ts (lines 22-30 — confirm persons-tab already has status: StatusPayload | null available)
    - frontend/src/types.ts (locate StatusPayload — needed for the new status import)
    - frontend/src/ws-client.ts (setPersonConfig signature — verify Partial<PersonConfig> accepts both mode and schedule in a single call)
    - .planning/phases/03-websocket-api-frontend-panel/03-CONTEXT.md (D-15 updated, D-21, D-22 sections — wire values, badge text strings, default schedule literal, presence dot semantics)
    - .planning/phases/03-websocket-api-frontend-panel/03-08-PLAN.md (verify the backend constants are now PRESENCE_HA="ha", PRESENCE_PRESENT="force_present", PRESENCE_ABSENT="force_absent" so frontend wire values match)
  </read_first>
  <action>
    Rewrite the presence-mode semantics in `person-card.ts`. All edits are confined to this single file. Apply the four changes in order:

    **1. Constants block (lines 23-26):**
    - Rename `PRESENCE_MODE_AUTOMATIC` → `PRESENCE_MODE_SCHEDULED` (value stays `"scheduled"`)
    - Rename `PRESENCE_MODE_PRESENT` → `PRESENCE_MODE_FORCE_PRESENT` and change its value to `"force_present"`
    - Rename `PRESENCE_MODE_ABSENT` → `PRESENCE_MODE_FORCE_ABSENT` and change its value to `"force_absent"`
    - Add `const PRESENCE_MODE_HA = "ha"`
    Update every usage of the old identifier names throughout the file (lines 55, 292-296, 302-303, 328-330) to the new names. There are no external importers — this is a closed rename.

    **2. Add `status` property + new imports:**
    - Add `import type { StatusPayload } from "../types.js"` at the top of the file (StatusPayload is already exported from types.ts and used by persons-tab.ts).
    - Add a new public property `@property({ attribute: false }) status: StatusPayload | null = null;` next to the existing `panel` property (around line 41).

    **3. D-15 update — always-collapsed default:**
    - In `connectedCallback()` (lines 45-49), remove the call to `this._isNonDefault()`. Replace the body with: `super.connectedCallback();` followed by `this._expanded = false;` (or simply remove the connectedCallback override since `_expanded` already defaults to `false` via its @state decorator).
    - Delete the now-unused `_isNonDefault()` method (lines 51-59) and the `_hasSchedulePeriods()` helper (lines 61-63) — neither is referenced elsewhere after the rule is removed.

    **4. D-21 — 4-option mode selector + badge text:**
    - Update `_getBadgeInfo()` (lines 291-298). The switch must now have four arms (in this exact mapping):
        - `PRESENCE_MODE_FORCE_PRESENT` → `{ cls: "force-present", text: "Force Present" }`
        - `PRESENCE_MODE_FORCE_ABSENT` → `{ cls: "force-absent", text: "Force Absent" }`
        - `PRESENCE_MODE_HA` → `{ cls: "ha", text: "HA" }`
        - default (including `PRESENCE_MODE_SCHEDULED` and unknown values) → `{ cls: "scheduled", text: "Scheduled" }`
    - In `render()` (lines 327-331), replace the three `<option>` elements with FOUR in this exact order:
        1. `<option value=${PRESENCE_MODE_SCHEDULED} ?selected=${currentMode === PRESENCE_MODE_SCHEDULED}>Scheduled</option>`
        2. `<option value=${PRESENCE_MODE_HA} ?selected=${currentMode === PRESENCE_MODE_HA}>HA</option>`
        3. `<option value=${PRESENCE_MODE_FORCE_PRESENT} ?selected=${currentMode === PRESENCE_MODE_FORCE_PRESENT}>Force Present</option>`
        4. `<option value=${PRESENCE_MODE_FORCE_ABSENT} ?selected=${currentMode === PRESENCE_MODE_FORCE_ABSENT}>Force Absent</option>`
    - Update the CSS `static styles` block (lines 103-117): rename `.mode-badge.automatic` → `.mode-badge.scheduled` (same color rules), rename `.mode-badge.present` → `.mode-badge.force-present`, rename `.mode-badge.absent` → `.mode-badge.force-absent`, and add a new `.mode-badge.ha` rule. Use the same gray-on-light-gray pattern for `.scheduled`/`.force-absent`/`.ha` as the prior `.automatic`/`.absent`; keep the `.force-present` styled with a primary-color border (carry the `.present` rule's existing colors).
    - Update the schedule visibility predicate (line 303): `const isAutomatic = currentMode === PRESENCE_MODE_SCHEDULED;` — rename the local variable to `isScheduled` for clarity; the time-bar is rendered iff `isScheduled` is true.

    **5. D-22 — default schedule seeding on switch TO scheduled:**
    - Modify `_onModeChange(e)` (lines 241-251). After computing `newMode`, check whether to seed a default schedule:
        - Compute `const hasSchedule = !!this.config?.schedule && Object.values(this.config.schedule).some((day) => day.length > 0);`
        - If `newMode === PRESENCE_MODE_SCHEDULED && !hasSchedule`: build the default schedule payload (literal copied EXACTLY from the interfaces section of this plan — do not paraphrase the time/state values) and include it alongside `mode` in the save call: `await this.ws.setPersonConfig(this.personId, { mode: newMode, schedule: <DEFAULT_SCHEDULE> })`.
        - Otherwise: call `await this.ws.setPersonConfig(this.personId, { mode: newMode })` (single-key payload, as today).
    - The default schedule literal MUST match the structure in the interfaces section byte-for-byte (per-day arrays with the documented start/state pairs). Use a module-level `const DEFAULT_SCHEDULE: DailyProgram = {...}` declared near the top of the file (after the PRESENCE_MODE_* constants) so the literal is a single source of truth. Type it as `DailyProgram` so TypeScript validates the day-period structure (DailyProgram is already imported from "../types.js" at line 15).
    - Keep the existing `await this.panel.reloadConfig()` and `this.panel.showToast("Saved", false)` and try/catch + error toast — unchanged from today.

    **6. D-15 update — presence dot in card header:**
    - Add a private helper `_isCurrentlyPresent(): boolean` that returns `this.status?.present_persons?.includes(this.personId) ?? false`.
    - In `render()`, inside the `.card-header-left` div (around line 311), add a third inline element AFTER the mode badge: `<span class="presence-dot ${this._isCurrentlyPresent() ? "present" : "absent"}" title="Currently ${this._isCurrentlyPresent() ? "present" : "absent"}">●</span>`. The dot is always rendered (both collapsed and expanded card states — D-15 explicitly says "Presence status dot — green (●) if currently present, gray (●) if absent").
    - Add CSS rules in `static styles`:
        ```
        .presence-dot { font-size: 12px; line-height: 1; }
        .presence-dot.present { color: var(--success-color, #4caf50); }
        .presence-dot.absent { color: var(--secondary-text-color, #9e9e9e); }
        ```
      Use HA CSS variables with sensible fallbacks (green / muted gray).

    Cleanup:
    - The `panel: ClimateManagerPanel` import path stays unchanged.
    - Do NOT touch the existing _onRoomToggle, _onSchedulePeriodsChanged, search-picker integration, time-bar block — these are unchanged.
    - Do NOT modify types.ts. The PersonConfig.mode field is typed loosely (string or union) and accepts the new wire values transparently. If TypeScript flags a narrowing issue on the schedule property, look at `frontend/src/types.ts` to confirm the existing PersonConfig.schedule type accepts the default schedule literal shape (DailyProgram). No type widening should be needed.
    - Do NOT touch room-card.ts, global-settings-tab.ts, time-bar.ts, search-picker.ts, ws-client.ts, or any backend file.

  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE 'PRESENCE_MODE_SCHEDULED\s*=\s*"scheduled"' frontend/src/components/person-card.ts` returns exactly one line
    - `grep -nE 'PRESENCE_MODE_FORCE_PRESENT\s*=\s*"force_present"' frontend/src/components/person-card.ts` returns exactly one line
    - `grep -nE 'PRESENCE_MODE_FORCE_ABSENT\s*=\s*"force_absent"' frontend/src/components/person-card.ts` returns exactly one line
    - `grep -nE 'PRESENCE_MODE_HA\s*=\s*"ha"' frontend/src/components/person-card.ts` returns exactly one line
    - `grep -nE 'PRESENCE_MODE_AUTOMATIC|PRESENCE_MODE_PRESENT\b|PRESENCE_MODE_ABSENT\b' frontend/src/components/person-card.ts` returns 0 matches (legacy identifier names fully renamed; FORCE_PRESENT/FORCE_ABSENT do not match the \b-bounded grep)
    - `grep -c '<option value=\${PRESENCE_MODE_' frontend/src/components/person-card.ts` returns exactly 4
    - The `<option>` order in render() is Scheduled → HA → Force Present → Force Absent (verifiable by reading the source in order)
    - `grep -nE '"Force Present"|"Force Absent"|"Scheduled"|"HA"' frontend/src/components/person-card.ts` shows each badge text appearing at least once
    - `grep -n 'isNonDefault\|_hasSchedulePeriods' frontend/src/components/person-card.ts` returns 0 matches (both methods deleted)
    - `grep -n 'this._expanded = this._isNonDefault' frontend/src/components/person-card.ts` returns 0 matches (D-15 update applied — always collapsed)
    - `grep -nE 'status:\s*StatusPayload\s*\|\s*null' frontend/src/components/person-card.ts` returns at least 1 match (new property declared)
    - `grep -n 'StatusPayload' frontend/src/components/person-card.ts` returns at least 1 import line and 1 property declaration line
    - `grep -n '_isCurrentlyPresent' frontend/src/components/person-card.ts` returns both a declaration and a call site
    - `grep -n 'presence-dot' frontend/src/components/person-card.ts` returns at least 3 matches (CSS rule + 2 class bindings in render — present/absent)
    - `grep -nE 'present_persons\?\.includes' frontend/src/components/person-card.ts` returns at least 1 match (status binding)
    - `grep -n 'DEFAULT_SCHEDULE' frontend/src/components/person-card.ts` returns at least 2 matches (declaration + use in _onModeChange)
    - `grep -c '"start": "08:00", "state": "absent"' frontend/src/components/person-card.ts` returns at least 5 (Mon–Fri have an 08:00 absent entry) — verifies the default schedule literal is present and complete
    - `grep -nE 'setPersonConfig\(this\.personId,\s*\{\s*mode:\s*newMode,\s*schedule' frontend/src/components/person-card.ts` returns at least 1 match (D-22 seeding payload includes schedule alongside mode in one call)
    - `npx tsc --noEmit -p tsconfig.json` exits 0 (no type errors introduced)
    - `npx vite build` exits 0 and produces `custom_components/climate_manager/www/panel.js`
    - `grep -c "Force Present\|Force Absent" custom_components/climate_manager/www/panel.js` returns at least 2 (badge text bundled into the production artifact)
    - `grep -c '"force_present"\|"force_absent"' custom_components/climate_manager/www/panel.js` returns at least 2 (wire values reach the bundle)
  </acceptance_criteria>
  <done>
    person-card.ts: four constants, four-option selector in Scheduled/HA/Force Present/Force Absent order, presence dot bound to status.present_persons, always-collapsed default, D-22 default schedule seeded on first switch to Scheduled. tsc + vite build pass.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Forward status prop from persons-tab to every person-card</name>
  <files>frontend/src/components/persons-tab.ts</files>
  <read_first>
    - frontend/src/components/persons-tab.ts (full file — focus on lines 97-117 where the person-card element is composed; the `.status` binding goes here)
    - frontend/src/components/person-card.ts (after Task 1 — verify the new `status` @property is declared as `attribute: false`, so it's bound via `.status=...` not `status=...`)
  </read_first>
  <action>
    In `persons-tab.ts`, modify the render template that composes `<climate-manager-person-card>` (around lines 108-116). Add a `.status=${this.status}` binding to every person-card element so each card receives the live present_persons list. Place the binding next to the existing `.panel=${this.panel}` binding for clarity.

    Do NOT change any other persons-tab.ts logic (sort order, room choices, hass forwarding, _isNonDefault local helper — that's used by persons-tab's OWN sort logic and may or may not stay; LEAVE IT in persons-tab.ts even though it was removed from person-card.ts — persons-tab uses it for sort ordering, not for default-expansion).

    Actually, re-evaluate: persons-tab._isNonDefault references `mode != null && c.mode !== "scheduled"`. After D-21 the modes "force_present", "force_absent", "ha" are all non-default. The existing condition already returns true for any mode other than "scheduled" — so it continues to work for the new modes WITHOUT any change. Leave persons-tab._isNonDefault as-is.

    Do NOT touch any other file. This is a single one-line binding addition plus a sanity re-read of persons-tab to confirm nothing else needs adjustment for the renamed wire values (the existing `c.mode !== "scheduled"` check is exact-string-match against the unchanged "scheduled" wire value, so it continues to work).

  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager/frontend &amp;&amp; npx tsc --noEmit -p tsconfig.json &amp;&amp; npx vite build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE '\.status=\$\{this\.status\}' frontend/src/components/persons-tab.ts` returns at least 1 match (the binding is wired)
    - `grep -nE 'c\.mode\s*!==\s*"scheduled"' frontend/src/components/persons-tab.ts` returns 1 match (still references the "scheduled" wire value — confirms the rename in person-card.ts did not break persons-tab's sort logic)
    - `npx tsc --noEmit -p tsconfig.json` exits 0
    - `npx vite build` exits 0; `custom_components/climate_manager/www/panel.js` exists and is non-empty
  </acceptance_criteria>
  <done>
    persons-tab forwards .status to every person-card; build passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify end-to-end — D-21, D-22, D-15 update + storage migration</name>
  <files>(no file modification — user runs HA against deployed integration)</files>
  <what-built>
    The remaining 2026-05-21 CONTEXT.md decisions land together:
    - D-21: Four presence modes in person card — Scheduled, HA, Force Present, Force Absent. Wire values: "scheduled" / "ha" / "force_present" / "force_absent". HA mode delegates presence to HA's own person.* entity (state == "home" iff present).
    - D-22: Switching a person to Scheduled mode for the first time (no existing schedule) auto-seeds a default schedule (Mon–Fri present except 08:00–18:00; Sat–Sun present all day).
    - D-15 (updated): Person cards are always collapsed by default. Card header shows person name + mode badge + presence status dot (green when present, gray when absent), all visible in both collapsed and expanded states.
    - Storage migration (Plan 03-08): any pre-existing stored config with persons[*].mode == "present" or "absent" is automatically renamed to "force_present" or "force_absent" on next HA restart — no user action required.
    - HA-mode coordinator branch (Plan 03-08): the Global Settings "Present persons" row in the Current Status card now reflects HA-mode persons whose linked person.* entity is currently at home.
  </what-built>
  <how-to-verify>
    1. Build and deploy:
       ```
       cd /home/arnaud/dev/climate_manager && make deploy
       ```
       Restart HA (or wait for the integration auto-reload). Open the Climate Manager panel from the sidebar.

    2. **Storage migration verification** — Before opening the Persons tab, check that any pre-existing person with `mode: "present"` or `mode: "absent"` (from before D-21) was migrated to `"force_present"` / `"force_absent"`. Method: open the HA Developer Tools → Services → call `climate_manager/get_config` via WS dev tools (or inspect `/config/.storage/climate_manager`). Confirm the JSON `persons.<id>.mode` values are now either `"scheduled"`, `"ha"`, `"force_present"`, or `"force_absent"` — never `"present"` or `"absent"`. If no person had a legacy mode set, this check is trivially satisfied.

    3. **D-15 updated — collapsed by default + presence dot** — Switch to the Persons tab. Confirm:
       (a) EVERY person card is collapsed on initial render — none open by default, even persons with non-default mode or room associations.
       (b) Each card's header (collapsed) shows three elements left-to-right: person name (bold) → mode badge → presence status dot.
       (c) The presence dot is green for persons whose mode is "Force Present" OR (mode "HA" AND their HA person.* entity state is "home") OR (mode "Scheduled" AND the current time falls in a scheduled "present" period). Dot is gray otherwise.
       (d) Expand a card by clicking the header. The presence dot stays visible in the (now-expanded) header.

    4. **D-21 — Four mode options** — Inside any expanded person card, find the "Presence mode" dropdown. Confirm it has exactly four `<option>` entries in this order:
       - Scheduled
       - HA
       - Force Present
       - Force Absent

    5. **D-21 — Badge text per mode** — For each of the four modes, select it from the dropdown and confirm:
       (a) A "Saved" toast appears.
       (b) The collapsed header badge text matches: "Scheduled" / "HA" / "Force Present" / "Force Absent" (exact strings).
       (c) Selecting "Scheduled" reveals the presence schedule time-bar below the dropdown. Selecting any other mode hides it.

    6. **D-22 — Default schedule on first switch to Scheduled** — Pick a person whose `schedule` is empty/absent (use an as-yet-unconfigured person, or check via WS dev tools). With that person in any non-Scheduled mode, change the dropdown to "Scheduled". Confirm:
       (a) A "Saved" toast appears.
       (b) The presence schedule time-bar appears below the dropdown with pre-populated periods, NOT empty/gray bars.
       (c) Each weekday Mon–Fri shows: green (present) from 00:00 → 08:00, gray (absent) from 08:00 → 18:00, green (present) from 18:00 → 24:00.
       (d) Sat and Sun show: green (present) all day (00:00 → 24:00).
       (e) Refresh the panel — the seeded schedule persists. Switching to a different mode and back to Scheduled does NOT re-seed (the existing schedule is preserved).

    7. **D-21 — HA mode behavior** — Pick a person whose linked HA `person.<slug>` entity exists and is currently in state "home" (verify in HA Developer Tools → States). In the Persons tab, set that person's mode to "HA". Confirm:
       (a) The presence dot in the card header turns green within ~1 minute (next coordinator tick) or immediately after panel reload.
       (b) Switch to the Global Settings tab → Current Status card. The person's name appears under "Present persons".
       (c) Manually change the linked person.* entity state to "not_home" (HA Developer Tools → States → set state). Within ~1 minute, the dot turns gray and the person disappears from "Present persons".
       (d) Restore the state to "home"; dot turns green again.

    8. **D-21 — Force modes** — Set a person's mode to "Force Present". Confirm the dot turns green regardless of any HA state and the person appears in "Present persons". Set another person to "Force Absent" — dot stays gray, person never appears in "Present persons".

    9. **Regression — global mode behaviors** — With global mode = "Time program & presences" and at least one "Force Present" person assigned to a room, confirm the coordinator pushes Normal/Comfort temperatures to that room's TRVs during the occupied window (HA Developer Tools → States, check the climate entity setpoint). Setting the same person to "Force Absent" causes the room to revert to Reduced.

    10. **Refresh resilience** — Refresh the panel once. All changes from steps 3–9 persist. The collapsed default rule holds (no card auto-expands).

    If any step fails, type the failure description (which step and which sub-bullet) so the gap can be addressed in a follow-up plan. Otherwise type "approved".

  </how-to-verify>
  <resume-signal>Type "approved" or describe issues observed during steps 2–10</resume-signal>
  <acceptance_criteria>
    - User explicitly types "approved" (or equivalent affirmative) covering all D-21 / D-22 / D-15-update / storage-migration items
    - If any step fails, the failure is recorded with the step number and sub-bullet letter; the failure becomes input to a follow-up gap-closure plan
  </acceptance_criteria>
  <done>
    Human confirms the four presence modes, the default schedule seeding behavior, the always-collapsed default with header presence dot, and the storage migration all work end-to-end against a deployed HA instance.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                              | Description                                                                                                                         |
| ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| panel → ws.setPersonConfig            | Untrusted person_id + config dict; backend sparse-merges (existing); Plan 03-08 coordinator handles unknown mode values defensively |
| hass.states[personId] in person-card  | Read-only HA state; secondary text already shown in HA's main UI (no new exposure)                                                  |
| status.present_persons → presence dot | StatusPayload broadcast over a WS subscription Plan 03-02 established; no new trust surface                                         |

## STRIDE Threat Register

| Threat ID  | Category               | Component                                                              | Disposition | Mitigation Plan                                                                                                                                                                                                                                                                                                                        |
| ---------- | ---------------------- | ---------------------------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-03-09-01 | Tampering              | Select value spoofed to unknown string                                 | mitigate    | Plan 03-08 coordinator treats unknown person mode by falling through to resolve_presence (returns False — default-absent); no crash on tampered payload. Native <select> only emits the four declared option values in normal use.                                                                                                     |
| T-03-09-02 | Information Disclosure | Presence dot reveals presence via panel                                | accept      | The same information is already shown in HA's frontend via the person.\* entity state; no new exposure                                                                                                                                                                                                                                 |
| T-03-09-03 | Data integrity         | Default schedule seeding overwrites a user's manually-cleared schedule | mitigate    | The seeding only fires when `hasSchedule === false` (no day has any periods); a user-deleted-all-periods state means the schedule is effectively empty and seeding is acceptable per D-22 semantics. Switching away from Scheduled mode does NOT clear the schedule — it remains in storage and is preserved for the next switch back. |
| T-03-09-04 | XSS                    | Badge text or default-schedule values                                  | mitigate    | Lit html templates auto-escape interpolated values; all literals are plain ASCII strings from a closed enum                                                                                                                                                                                                                            |

</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit -p tsconfig.json` exits 0
- `cd frontend && npx vite build` exits 0; the bundle contains "Force Present" and "force_present"
- All grep-based acceptance assertions in Tasks 1 and 2 pass
- Plan 03-08 was completed first (the backend wire values match the frontend values "force_present" / "force_absent" / "ha")
- Human-verify checkpoint approved by user covering migration, D-15-update, D-21, D-22
</verification>

<success_criteria>

- person-card.ts exports four PRESENCE*MODE*\* constants with values
  "scheduled", "ha", "force_present", "force_absent"
- 4-option mode selector in order: Scheduled, HA, Force Present, Force Absent
- Badge text: "Scheduled" / "HA" / "Force Present" / "Force Absent"
- Default schedule seeded on first switch TO Scheduled when no existing schedule
  (D-22)
- Person cards always collapsed by default (D-15 updated) — \_isNonDefault
  expansion logic removed from person-card
- Presence status dot rendered in card header (green/gray) bound to
  `status.present_persons.includes(personId)`
- persons-tab forwards .status=${this.status} to every person-card
- TypeScript and Vite builds pass
- Human-verify confirms D-21, D-22, D-15-update, and the Plan 03-08 storage
  migration round-trip end-to-end
- No HA 2026.x broken components introduced (native <select> retained)
  </success_criteria>

<output>
Create `.planning/phases/03-websocket-api-frontend-panel/03-09-SUMMARY.md` when done. Include the user's human-verify result and any failures observed during checkpoint steps 2–10.
</output>
