---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on: [1, 2]
files_modified:
  - custom_components/climate_manager/__init__.py
  - tests/conftest.py
  - tests/test_init.py
autonomous: true
requirements: [INFRA-01, INFRA-02]
must_haves:
  truths:
    - "async_setup_entry loads the Store, runs room+person discovery, and stores both plus the store in entry.runtime_data"
    - "async_unload_entry exists and returns cleanly (no ghost listeners)"
    - "The integration loads to the 'loaded' state under the pytest hass fixture without errors"
    - "Discovered rooms/persons and merged config are reachable from entry.runtime_data after setup"
  artifacts:
    - path: "custom_components/climate_manager/__init__.py"
      provides: "async_setup_entry, async_unload_entry, ClimateManagerData dataclass, typed ConfigEntry alias"
      contains: "async_setup_entry"
    - path: "tests/conftest.py"
      provides: "pytest fixtures enabling custom integrations"
      min_lines: 1
    - path: "tests/test_init.py"
      provides: "Smoke test asserting the integration reaches loaded state"
      contains: "async_setup"
  key_links:
    - from: "__init__.py async_setup_entry"
      to: "storage.ClimateManagerStore + discovery.discover_rooms/discover_persons"
      via: "entry.runtime_data = ClimateManagerData(...)"
      pattern: "runtime_data"
    - from: "tests/test_init.py"
      to: "hass.config_entries.async_setup"
      via: "MockConfigEntry add_to_hass then async_setup"
      pattern: "async_setup"
---

<objective>
Wire the foundation together: implement async_setup_entry / async_unload_entry in __init__.py using the modern entry.runtime_data pattern, integrating the Store (plan 02), discovery (plan 02), and config flow (plan 02). Add the pytest conftest and the smoke test that proves the integration loads in HA without errors.

Purpose: This is the integration's spine — the single place where storage + discovery come together at setup. The smoke test is the gate proving Phase 1's primary success criterion (the integration installs and loads cleanly).

Output: A fully loadable integration; passing smoke test confirming "loaded" state.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md

<interfaces>
<!-- Contracts from plans 01 and 02. Use directly, no exploration. -->

# plan 01 — const.py
const.DOMAIN = "climate_manager"

# plan 01 — __init__.py placeholder (this plan replaces it)
PLATFORMS = []   # already present; keep it

# plan 02 — storage.py
class ClimateManagerStore:
    def __init__(self, hass): ...
    async def async_load(self) -> dict        # returns DEFAULT_CONFIG merged with sparse stored data
    async def async_save(self, config) -> None

# plan 02 — discovery.py
async def discover_rooms(hass) -> dict[str, list[str]]    # {area_id: [climate_entity_ids]}
async def discover_persons(hass) -> list[str]             # [person.* entity_ids]

# HA APIs (RESEARCH Pattern 2, HIGH confidence):
homeassistant.config_entries.ConfigEntry        # generic: ConfigEntry[ClimateManagerData]
entry.runtime_data = <dataclass instance>        # modern in-memory state (NOT hass.data[DOMAIN])
hass.config_entries.async_unload_platforms(entry, PLATFORMS)
pytest_homeassistant_custom_component.common.MockConfigEntry
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement __init__.py setup/unload with runtime_data</name>
  <files>custom_components/climate_manager/__init__.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 2 entry.runtime_data, Pitfall 1 missing async_unload_entry, Anti-Patterns: no hass.data[DOMAIN], System Architecture Diagram async_setup_entry flow)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-09 schema, code_context Integration Points: Phase 1 establishes storage+discovery that Phase 2 reads)
    - custom_components/climate_manager/__init__.py (the plan-01 placeholder — PLATFORMS = []; preserve it)
    - custom_components/climate_manager/storage.py (ClimateManagerStore — plan 02)
    - custom_components/climate_manager/discovery.py (discover_rooms, discover_persons — plan 02)
    - custom_components/climate_manager/const.py (DOMAIN — plan 01)
  </read_first>
  <behavior>
    - Test 1: after async_setup_entry, entry.runtime_data is a ClimateManagerData instance with .store, .runtime_config, .rooms, .persons attributes populated
    - Test 2: runtime_config equals what ClimateManagerStore.async_load() returned (DEFAULT_CONFIG on fresh install)
    - Test 3: async_unload_entry returns True and does not raise
    - Test 4: hass.data[DOMAIN] is NOT used as the state store (state lives on entry.runtime_data)
  </behavior>
  <action>
    Replace the plan-01 __init__.py placeholder. Keep the module docstring and PLATFORMS = [].

    Define @dataclass ClimateManagerData with fields: store: ClimateManagerStore, runtime_config: dict, rooms: dict[str, list[str]], persons: list[str]. Define the type alias: type ClimateManagerConfigEntry = ConfigEntry[ClimateManagerData] (Pattern 2 — modern typed runtime_data).

    async def async_setup_entry(hass, entry: ClimateManagerConfigEntry) -> bool: construct store = ClimateManagerStore(hass); runtime_config = await store.async_load(); rooms = await discover_rooms(hass); persons = await discover_persons(hass); set entry.runtime_data = ClimateManagerData(store=store, runtime_config=runtime_config, rooms=rooms, persons=persons); return True. Do NOT write to hass.data[DOMAIN] (Anti-Pattern — use runtime_data only). Do NOT start any coordinator, scheduler, or time-interval listener (Phase 2 scope; Pitfall 1 — Phase 1 has no scheduled callbacks).

    async def async_unload_entry(hass, entry: ClimateManagerConfigEntry) -> bool: return await hass.config_entries.async_unload_platforms(entry, PLATFORMS) (Pitfall 1 — must exist even though PLATFORMS is empty so HA marks the integration reloadable).

    Use only the modern registry/runtime patterns; no async_get_registry, no async_track_state_change.
  </action>
  <verify>
    <automated>grep -q 'async def async_setup_entry' custom_components/climate_manager/__init__.py && grep -q 'async def async_unload_entry' custom_components/climate_manager/__init__.py && grep -q 'runtime_data' custom_components/climate_manager/__init__.py && ! grep -q 'hass.data\[' custom_components/climate_manager/__init__.py && .venv/bin/python -c "import ast; ast.parse(open('custom_components/climate_manager/__init__.py').read()); print('syntax OK')"</automated>
  </verify>
  <acceptance_criteria>
    - __init__.py defines async_setup_entry and async_unload_entry
    - __init__.py defines @dataclass ClimateManagerData with store, runtime_config, rooms, persons fields
    - async_setup_entry sets entry.runtime_data (grep finds "runtime_data") and does NOT use hass.data[ (grep confirms absent)
    - async_setup_entry calls discover_rooms and discover_persons and ClimateManagerStore.async_load
    - async_unload_entry calls hass.config_entries.async_unload_platforms (Pitfall 1)
    - No coordinator/scheduler/time-interval listener started (Phase 2 scope)
  </acceptance_criteria>
  <done>__init__.py wires Store + discovery into entry.runtime_data and provides unload; syntax check prints "syntax OK"</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add conftest.py and the integration smoke test</name>
  <files>tests/conftest.py, tests/test_init.py</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 6 pytest smoke test setup, "tests/conftest.py" + "tests/test_init.py" code examples, Environment Availability)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-08 pytest from day one — smoke test sufficient for Phase 1)
    - custom_components/climate_manager/__init__.py (task 1 — async_setup_entry sets runtime_data)
    - custom_components/climate_manager/const.py (DOMAIN)
    - pyproject.toml (asyncio_mode = "auto" — created plan 01)
  </read_first>
  <behavior>
    - Test 1: setting up a MockConfigEntry for DOMAIN reaches state "loaded" (Phase 1 primary success criterion: integration installs and loads without errors)
    - Test 2: after setup, entry.runtime_data is populated (store + runtime_config + rooms + persons present)
    - Test 3: unloading the entry succeeds (entry transitions out of loaded without error)
  </behavior>
  <action>
    Create tests/conftest.py. pytest-homeassistant-custom-component supplies the `hass` fixture and the `enable_custom_integrations` fixture; add an autouse fixture that requests `enable_custom_integrations` so custom_components/climate_manager is loadable in tests (Pattern 6). Add nothing else — Phase 1 needs no extra fixtures.

    Create tests/test_init.py with an async test: build entry = MockConfigEntry(domain=DOMAIN, data={}); entry.add_to_hass(hass); await hass.config_entries.async_setup(entry.entry_id); await hass.async_block_till_done(); assert entry.state.value == "loaded" (Pattern 6 — proves the integration loads without errors, Phase 1 success criterion 1). Add assertions that entry.runtime_data is not None and has store / runtime_config / rooms / persons attributes (proves the wiring from task 1). Add a second test that unloads the entry via hass.config_entries.async_unload(entry.entry_id) and asserts it returns True (proves Pitfall 1 unload works).

    Write the tests, then run them. If the integration fails to load, fix __init__.py / manifest.json until "loaded" is reached.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_init.py -v</automated>
  </verify>
  <acceptance_criteria>
    - tests/conftest.py exists and enables custom integrations for the test session
    - tests/test_init.py exists and all tests pass under .venv/bin/python -m pytest
    - The smoke test asserts entry.state.value == "loaded" (Phase 1 success criterion 1)
    - The smoke test asserts entry.runtime_data has store, runtime_config, rooms, persons
    - The unload test asserts async_unload returns True
    - `.venv/bin/python -m pytest tests/ -v` (full suite) passes
  </acceptance_criteria>
  <done>conftest.py and test_init.py exist; the integration reaches "loaded" state in tests; full test suite green; INFRA-01/INFRA-02 verified end-to-end</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HA config-entry lifecycle → integration | HA invokes async_setup_entry / async_unload_entry; integration must not leak listeners |
| In-memory runtime state | entry.runtime_data holds the merged config + discovery results for the process lifetime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-10 | Denial of Service | Missing async_unload_entry → integration non-reloadable, ghost listeners accumulate on reload | mitigate | async_unload_entry implemented and tested (Pitfall 1, PITFALLS M5); unload test asserts it returns True |
| T-01-11 | Tampering | hass.data[DOMAIN] global mutable state shared across integrations | mitigate | State lives only on entry.runtime_data (typed dataclass, auto-cleaned on unload); acceptance criteria assert hass.data[ is not used |
| T-01-12 | Denial of Service | Blocking call in async_setup_entry stalls HA startup | accept | async_setup_entry awaits only Store.async_load (non-blocking per plan 02 Pitfall 2 mitigation) and registry reads (in-memory, indexed per RESEARCH Don't Hand-Roll); no synchronous I/O introduced here |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/ -v` — entire suite passes (storage, discovery, trv, init)
- Smoke test confirms entry.state.value == "loaded"
- `grep -n 'hass.data\[' custom_components/climate_manager/__init__.py` returns nothing
- `grep -n 'async_unload_entry' custom_components/climate_manager/__init__.py` returns a match
</verification>

<success_criteria>
- __init__.py implements async_setup_entry (Store + discovery → runtime_data) and async_unload_entry
- Integration reaches "loaded" state under the pytest hass fixture (Phase 1 ROADMAP success criterion 1)
- Room config schema and discovery are reachable from entry.runtime_data (foundation for Phase 2)
- Full test suite green; INFRA-01 and INFRA-02 verified end-to-end
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
