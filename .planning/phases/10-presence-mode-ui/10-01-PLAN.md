---
phase: 10-presence-mode-ui
plan: 01
type: tdd
wave: 0
depends_on: []
files_modified:
  - frontend/src/components/presence-mode.ts
  - frontend/src/components/presence-mode.test.ts
autonomous: true
requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "Badge/label text for the ha mode resolves to the string Live tracking"
    - "The ha option is reported visible only when device trackers exist"
    - "Stuck-mode warning text is returned when mode is ha and no trackers"
    - "hasDeviceTrackers treats absent attribute and empty array both as false"
  artifacts:
    - path: "frontend/src/components/presence-mode.ts"
      provides: "Pure, Lit-free helpers for ha-mode label, option visibility, hint text, and device-tracker detection"
      exports: ["MODE_LABEL_HA", "computeHasDeviceTrackers", "shouldShowHaOption", "presenceModeHint"]
    - path: "frontend/src/components/presence-mode.test.ts"
      provides: "Failing-then-passing unit tests for UI-01 and UI-02 behaviors"
      contains: "Live tracking"
  key_links:
    - from: "frontend/src/components/presence-mode.test.ts"
      to: "frontend/src/components/presence-mode.ts"
      via: "import of exported helpers"
      pattern: "from \"./presence-mode"
---

<objective>
Create a Lit-free helper module `presence-mode.ts` that encapsulates every
decision behind the Phase 10 UI changes — the "Live tracking" label (D-01),
the device-tracker detection logic (D-02), the conditional option visibility
(D-04), and the schedule-hint text including the stuck-mode warning (D-05) —
plus a `presence-mode.test.ts` covering all five behaviors from the validation
architecture.

`person-card.ts` and `persons-tab.ts` carry Lit `@property` decorators that
`node --experimental-strip-types` cannot parse, so the testable logic is
extracted into a pure module exactly as `week-parity.ts` was extracted from
`person-card.ts` for the same reason. Plan 02 wires these helpers into the Lit
components.

Purpose: Make UI-01 and UI-02 logic genuinely unit-testable (Nyquist) and give
Plan 02 a defined contract to consume rather than re-deriving string/boolean
logic inline.

Output: `presence-mode.ts` (helpers) and `presence-mode.test.ts` (tests).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-presence-mode-ui/10-CONTEXT.md
@.planning/phases/10-presence-mode-ui/10-RESEARCH.md
@.planning/phases/10-presence-mode-ui/10-PATTERNS.md
@frontend/src/components/week-parity.ts
@frontend/src/components/week-parity.test.ts
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Create presence-mode.ts pure helpers with failing tests (RED → GREEN)</name>
  <files>frontend/src/components/presence-mode.ts, frontend/src/components/presence-mode.test.ts</files>
  <read_first>
    - frontend/src/components/week-parity.ts (the precedent: pure helper module extracted to allow node --experimental-strip-types tests without Lit decorators; copy its module-doc + export style)
    - frontend/src/components/week-parity.test.ts (exact test-file structure: SPDX header, `node:test` + `node:assert/strict` imports, `.ts` import specifier, one `test(desc, () => assert.equal(...))` per behavior)
    - frontend/src/components/person-card.ts lines 39-43 (mode constants: PRESENCE_MODE_SCHEDULED="scheduled", PRESENCE_MODE_HA="ha", PRESENCE_MODE_FORCE_PRESENT="force_present", PRESENCE_MODE_FORCE_ABSENT="force_absent")
    - frontend/src/components/person-card.ts lines 434-446 (_getBadgeInfo current switch — ha case returns text "HA home tracking")
    - frontend/src/components/person-card.ts lines 530-538 (existing schedule-hint ternary chain to reproduce)
    - .planning/phases/10-presence-mode-ui/10-RESEARCH.md lines 333-345 (verified hasDeviceTrackers computation), 377-394 (verified stuck-mode hint chain)
  </read_first>
  <behavior>
    presence-mode.ts MUST export (pure functions/constants, no Lit imports):
    - `MODE_LABEL_HA = "Live tracking"` — the display string for the "ha" mode (D-01, UI-02).
    - `computeHasDeviceTrackers(trackers: unknown): boolean` — returns
      `(Array.isArray(trackers) ? trackers.length : 0) > 0`; returns false for
      undefined, null, and empty array (D-02).
    - `shouldShowHaOption(hasDeviceTrackers: boolean): boolean` — returns
      `hasDeviceTrackers` (D-04: option rendered only when trackers exist).
    - `presenceModeHint(mode: string, hasDeviceTrackers: boolean): string` —
      returns the schedule-hint text:
        force_present → "Always considered present, regardless of schedule."
        force_absent  → "Always absent. Rooms are not heated for presence."
        ha && !hasDeviceTrackers → "Live tracking requires a device tracker linked to this person in HA." (D-05)
        ha (with trackers) → "Presence mirrors Home Assistant home/away tracking."
        default → "Presence follows a weekly schedule."

    presence-mode.test.ts MUST assert (one test() per row — five behaviors):
    - UI-02: `MODE_LABEL_HA` equals `"Live tracking"`.
    - UI-01: `computeHasDeviceTrackers(undefined)` is false; `computeHasDeviceTrackers([])` is false; `computeHasDeviceTrackers(["device_tracker.phone"])` is true.
    - UI-01: `shouldShowHaOption(false)` is false; `shouldShowHaOption(true)` is true.
    - UI-01/D-05: `presenceModeHint("ha", false)` equals the stuck-mode warning string above.
    - UI-02: `presenceModeHint("ha", true)` equals "Presence mirrors Home Assistant home/away tracking." (proves the ha label/hint split is correct).
  </behavior>
  <action>
    Write presence-mode.test.ts FIRST importing from "./presence-mode.ts"; run
    the test command and confirm it fails (RED — module does not yet exist).
    Commit the failing test as `test(10-01): add failing presence-mode helper
    tests`.

    Then write presence-mode.ts with the four exports above. Use the exact
    constant string values listed in <behavior> — `MODE_LABEL_HA` is exactly
    "Live tracking", the stuck-mode warning is exactly "Live tracking requires
    a device tracker linked to this person in HA.", and the present-tracker
    hint is exactly "Presence mirrors Home Assistant home/away tracking." Start
    the file with the `// SPDX-License-Identifier: MIT` header and a module
    docstring noting it is Lit-free for strip-types testing (mirror
    week-parity.ts lines 1-17). Do NOT import lit or any decorator. Re-run the
    test command and confirm GREEN. Commit as `feat(10-01): implement
    presence-mode pure helpers`.

    All lines max 80 chars, LF endings, final newline (.editorconfig). Run
    `make lint` after writing.
  </action>
  <verify>
    <automated>node --test --experimental-strip-types frontend/src/components/presence-mode.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `node --test --experimental-strip-types frontend/src/components/presence-mode.test.ts` exits 0 with 5+ passing tests
    - presence-mode.ts contains `Live tracking` and the exact string `Live tracking requires a device tracker linked to this person in HA.`
    - presence-mode.ts contains no `import` of `lit` (grep -v '^#' frontend/src/components/presence-mode.ts | grep -c "from \"lit" returns 0)
    - presence-mode.test.ts imports from "./presence-mode.ts"
    - `make lint` exits 0
  </acceptance_criteria>
  <done>
    The five UI-01/UI-02 behaviors are encoded as pure exported helpers and
    proven green by a strip-types unit test; no Lit dependency is introduced
    into the test path.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hass.states → panel | Read-only HA state consumed by frontend; no mutation crosses here in this plan |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | `device_trackers` attribute shape | mitigate | `computeHasDeviceTrackers` uses `Array.isArray` guard; non-array/absent values return false rather than throwing |
| T-10-02 | Information Disclosure | helper strings | accept | All strings are static UI labels; no PII or secrets |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `node --test --experimental-strip-types frontend/src/components/presence-mode.test.ts` green
- `make lint` green
</verification>

<success_criteria>
- presence-mode.ts exports MODE_LABEL_HA, computeHasDeviceTrackers, shouldShowHaOption, presenceModeHint
- All five validation behaviors (UI-01 ×3, UI-02 ×2) pass as unit tests
- No Lit import in the tested module
</success_criteria>

<output>
Create `.planning/phases/10-presence-mode-ui/10-01-SUMMARY.md` when done
</output>
