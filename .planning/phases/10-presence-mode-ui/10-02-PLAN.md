---
phase: 10-presence-mode-ui
plan: 02
type: execute
wave: 1
depends_on: ["10-01"]
files_modified:
  - frontend/src/components/person-card.ts
  - frontend/src/components/persons-tab.ts
autonomous: false

requirements: [UI-01, UI-02]

must_haves:
  truths:
    - "D-04: Person whose person.* entity has no device_trackers never renders the HA home tracking option"
    - "D-04: Person with at least one device tracker still shows and can select HA home tracking"
    - "D-01: The ha mode label stays HA home tracking — no rename; MODE_LABEL_HA is imported for consistency"
    - "D-05: A person stuck on mode=ha with no trackers sees the inline warning, no WS call fires on render"
    - "D-03: PersonsTab forwards .hasDeviceTrackers boolean prop to each PersonCard via property binding"
    - "D-02: hasDeviceTrackers computation reads hass.states[personId]?.attributes?.device_trackers"
  artifacts:
    - path: "frontend/src/components/person-card.ts"
      provides: "hasDeviceTrackers prop, MODE_LABEL_HA via import, conditional ha option, stuck-mode hint"
      contains: "hasDeviceTrackers"
    - path: "frontend/src/components/persons-tab.ts"
      provides: "Per-person hasDeviceTrackers computation from hass.states, forwarded to PersonCard"
      contains: "device_trackers"
  key_links:
    - from: "frontend/src/components/persons-tab.ts"
      to: "frontend/src/components/person-card.ts"
      via: ".hasDeviceTrackers property binding"
      pattern: "\\.hasDeviceTrackers="
    - from: "frontend/src/components/person-card.ts"
      to: "frontend/src/components/presence-mode.ts"
      via: "import of MODE_LABEL_HA / shouldShowHaOption / presenceModeHint"
      pattern: "from \"./presence-mode"
    - from: "frontend/src/components/persons-tab.ts"
      to: "frontend/src/components/presence-mode.ts"
      via: "import of computeHasDeviceTrackers"
      pattern: "computeHasDeviceTrackers"
---

<objective>
Wire the Plan 01 helpers into the two Lit components so the user-visible
behavior changes: rename the "HA home tracking" label to "Live tracking"
(D-01/UI-02), compute `hasDeviceTrackers` per person in `PersonsTab` and
forward it to each card (D-02/D-03), conditionally render the "Live tracking"
option only when device trackers exist (D-04/UI-01), and show the inline
stuck-mode warning when a person is on mode=ha with no trackers (D-05) without
firing any WS call on render.

Purpose: Deliver the actual UI-01 and UI-02 behavior to the panel, backed by
the already-tested helpers from Plan 01.

Output: Edited `person-card.ts` and `persons-tab.ts`; built panel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-presence-mode-ui/10-CONTEXT.md
@.planning/phases/10-presence-mode-ui/10-RESEARCH.md
@.planning/phases/10-presence-mode-ui/10-PATTERNS.md
@frontend/src/components/presence-mode.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add hasDeviceTrackers prop + conditional option + stuck-mode hint in person-card.ts</name>
  <files>frontend/src/components/person-card.ts</files>
  <read_first>
    - frontend/src/components/person-card.ts (full file — current mode constants, _getBadgeInfo, the four-option select, the schedule-hint ternary)
    - frontend/src/components/presence-mode.ts (the helpers to import: MODE_LABEL_HA, shouldShowHaOption, presenceModeHint)
    - frontend/src/components/presence-mode.test.ts (the contract the rendered output must match)
    - .planning/phases/10-presence-mode-ui/10-PATTERNS.md lines 37-110 (exact before/after for the @property, badge, conditional option, hint)
  </read_first>
  <behavior>
    - Badge: when `config.mode === "ha"`, `_getBadgeInfo()` text uses `MODE_LABEL_HA` (which equals "HA home tracking" — no rename, import for consistency).
    - Prop: `PersonCard` exposes `@property({ type: Boolean }) hasDeviceTrackers = false;`.
    - Option visibility: the `<option value="ha">` is rendered only when `shouldShowHaOption(this.hasDeviceTrackers)` is true; its label text is `${MODE_LABEL_HA}`; it keeps `?selected=${currentMode === PRESENCE_MODE_HA}`. The other three options (Scheduled, Force Present, Force Absent) always render.
    - Hint: the `<p class="schedule-hint">` text equals `presenceModeHint(currentMode, this.hasDeviceTrackers)`, so mode=ha && !hasDeviceTrackers shows the stuck-mode warning (D-05).
    - No `setPersonConfig` / `ws` call fires from render or from the stuck-mode path (D-05); `_onModeChange` remains the only writer, fired only on user `@change`.
  </behavior>
  <action>
    Add `import { MODE_LABEL_HA, shouldShowHaOption, presenceModeHint } from
    "./presence-mode.js";` (use the `.js` specifier — matches the existing
    `./week-parity.js` import style at line 79 for Vite build resolution).

    Declare `@property({ type: Boolean }) hasDeviceTrackers = false;`
    immediately after the `autoExpand` property (after line 102).

    In `_getBadgeInfo()`, update the PRESENCE_MODE_HA case to use `MODE_LABEL_HA`
    instead of the inline string literal "HA home tracking" (same value, but
    now sourced from the shared constant).

    In the mode `<select>` (lines 510-515) replace the unconditional ha
    `<option>` with a conditional: render it only when
    `shouldShowHaOption(this.hasDeviceTrackers)` is true, label text
    `${MODE_LABEL_HA}`, preserving
    `?selected=${currentMode === PRESENCE_MODE_HA}`. Use the
    `${cond ? html\`...\` : ""}` form (empty string for nothing, matching the
    file's existing conditional blocks). Do NOT use `disabled` or
    `display:none` — remove the element from the DOM (D-04 anti-pattern).

    Replace the schedule-hint ternary body (lines 531-537) with a single call
    to `presenceModeHint(currentMode, this.hasDeviceTrackers)`.

    Do not change `PRESENCE_MODE_HA = "ha"` or any config/WS value. All lines
    max 80 chars, LF, final newline. Run `make lint`.
  </action>
  <verify>
    <automated>node --test --experimental-strip-types frontend/src/components/presence-mode.test.ts && cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - person-card.ts contains `hasDeviceTrackers` and `shouldShowHaOption`
    - person-card.ts no longer contains the inline literal `"HA home tracking"` string (uses MODE_LABEL_HA constant instead)
    - person-card.ts contains `presenceModeHint(currentMode`
    - The ha `<option>` is wrapped in `shouldShowHaOption(this.hasDeviceTrackers)` guard (grep finds `shouldShowHaOption` adjacent to the option block)
    - person-card.ts still contains `PRESENCE_MODE_HA = "ha"` (constant unchanged)
    - `cd frontend && npx tsc --noEmit` exits 0
    - `make lint` exits 0
  </acceptance_criteria>
  <done>
    The ha mode badge and option both use MODE_LABEL_HA ("HA home tracking"),
    the option is gated on hasDeviceTrackers, and the hint delegates to
    presenceModeHint — all with no new render-time WS calls.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Compute hasDeviceTrackers per person in persons-tab.ts and forward to each card</name>
  <files>frontend/src/components/persons-tab.ts</files>
  <read_first>
    - frontend/src/components/persons-tab.ts (full render(): hass.states access at lines 91-96, sortedIds.map at 118-138, the existing prop-forwarding block at 127-136)
    - frontend/src/components/presence-mode.ts (computeHasDeviceTrackers to import)
    - frontend/src/types.ts §Hass (hass.states typed Record<string,{state;attributes:Record<string,unknown>}>; device_trackers is unknown and must be cast)
    - .planning/phases/10-presence-mode-ui/10-PATTERNS.md lines 140-174 (exact computation + forwarding pattern)
  </read_first>
  <behavior>
    - For each `personId` in `allPersonIds`, a `hasDeviceTrackersMap` entry is
      computed as `computeHasDeviceTrackers(hass.states[personId]?.attributes?.device_trackers)`.
    - Each `<climate-manager-person-card>` receives
      `.hasDeviceTrackers=${hasDeviceTrackersMap.get(personId) ?? false}`.
    - A person present in config but absent from `hass.states` forwards `false`
      (option hidden), via the `?? false` fallback.
  </behavior>
  <action>
    Add `import { computeHasDeviceTrackers } from "./presence-mode.js";` (use
    the `.js` specifier).

    In `render()`, after `const roomChoices = this._getRoomChoices();`
    (line 115), build `const hasDeviceTrackersMap = new Map<string, boolean>();`
    and loop over `allPersonIds`, setting each entry to
    `computeHasDeviceTrackers(this.hass?.states[personId]?.attributes?.device_trackers)`.
    Pass the raw attribute value straight into the helper — the helper owns the
    Array.isArray / empty-array logic (D-02), so no cast or length check is
    duplicated here.

    In the `<climate-manager-person-card>` instantiation, append
    `.hasDeviceTrackers=${hasDeviceTrackersMap.get(personId) ?? false}` after
    the existing `.autoExpand=...` binding, matching the dot-prefix property
    binding pattern.

    All lines max 80 chars, LF, final newline. Run `make lint`.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && cd .. && make build</automated>
  </verify>
  <acceptance_criteria>
    - persons-tab.ts contains `computeHasDeviceTrackers` and `hasDeviceTrackersMap`
    - persons-tab.ts contains `.hasDeviceTrackers=${hasDeviceTrackersMap.get(personId) ?? false}`
    - persons-tab.ts references `device_trackers`
    - `cd frontend && npx tsc --noEmit` exits 0
    - `make build` exits 0 (Vite bundles the panel without error)
    - `make lint` exits 0
  </acceptance_criteria>
  <done>
    PersonsTab computes a per-person device-tracker boolean from hass.states
    and forwards it to every PersonCard, completing the UI-01 data path.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visually verify label rename, conditional option, and stuck-mode warning in the live panel</name>
  <what-built>
    The Persons tab now renames "HA home tracking" to "Live tracking"
    everywhere, hides the "Live tracking" option for persons with no linked
    device trackers, and shows an inline warning for persons stuck on the ha
    mode without trackers. The panel was rebuilt with `make build`.
  </what-built>
  <how-to-verify>
    1. Run `make deploy` (or your usual deploy step), then hard-refresh the
       Home Assistant panel (Ctrl-Shift-R) to bust the cached panel.js.
    2. Open the Climate Manager panel → Persons tab.
    3. For a person WITH a linked device tracker (person.* has a
       device_trackers attribute with ≥1 entry): expand the card. Confirm the
       presence-mode dropdown shows "Live tracking" (not "HA home tracking")
       as one of the four options, and that selecting it shows the hint
       "Presence mirrors Home Assistant home/away tracking."
    4. For a person WITHOUT any device tracker: expand the card. Confirm the
       dropdown shows only three options (Scheduled, Force Present, Force
       Absent) and "Live tracking" is absent.
    5. If you have a person currently stored as mode "ha" but with no device
       tracker, confirm the inline warning reads exactly: "Live tracking
       requires a device tracker linked to this person in HA." and that the
       mode did NOT silently change (no toast fired on load).
    6. Collapse a person on ha mode and confirm the badge reads "Live tracking".
  </how-to-verify>
  <resume-signal>Type "approved" or describe any mismatch (wrong label, option still visible, missing warning).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hass.states → PersonsTab | Read-only HA entity attributes consumed to decide option visibility |
| PersonCard render → ws/setPersonConfig | Config-mutation boundary; must NOT be crossed at render time (D-05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-03 | Tampering | device_trackers attribute of unexpected shape | mitigate | computeHasDeviceTrackers (Plan 01) guards with Array.isArray; persons-tab passes raw value, never indexes it directly |
| T-10-04 | Elevation of Privilege | silent setPersonConfig on render | mitigate | render path calls no ws methods; only `_onModeChange` (user @change) writes config (D-05) |
| T-10-05 | Repudiation | hidden-but-selected ha value (stuck mode) | accept | Native select retains "ha" value even with option removed; surfaced to user via inline warning rather than silent reset (D-05 decision) |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (RESEARCH §Package Legitimacy Audit: not applicable) |
</threat_model>

<verification>
- `node --test --experimental-strip-types frontend/src/components/presence-mode.test.ts` green
- `cd frontend && npx tsc --noEmit` clean
- `make build` succeeds
- `make lint` green
- Human checkpoint confirms label, option visibility, and warning in live panel
</verification>

<success_criteria>
- Inline literal "HA home tracking" replaced by MODE_LABEL_HA import in person-card.ts
- ha option renders only when hasDeviceTrackers is true (UI-01)
- Badge and option both read MODE_LABEL_HA ("HA home tracking") (UI-02)
- Stuck-mode warning shows for mode=ha && no trackers, no WS call on render (D-05)
- Panel builds and type-checks clean
</success_criteria>

<output>
Create `.planning/phases/10-presence-mode-ui/10-02-SUMMARY.md` when done
</output>
