---
phase: 13-matter-tado-x-real-time-calibration
plan: 02
type: tdd
wave: 2
depends_on: ["13-01"]
files_modified:
  - custom_components/climate_manager/websocket.py
  - tests/test_websocket.py
autonomous: true
requirements: [MCALIB-01, MCALIB-02]
must_haves:
  truths:
    - "Sending set_matter_mapping with a valid payload stores the mapping under matter_mappings[tado_entity_id]"
    - "Sending set_matter_mapping with an empty matter_entity_ids list removes (pops) the mapping (sparse model)"
    - "Non-climate entity_ids in the payload are filtered out before storage"
    - "After persisting, the coordinator's Matter listeners are refreshed atomically"
    - "get_config returns matter_entities and tado_x_entities so the frontend can build the pairing UI"
  artifacts:
    - path: "custom_components/climate_manager/websocket.py"
      provides: "set_matter_mapping command + registration; get_config matter_entities/tado_x_entities derivation (A2: Option A/C)"
      contains: "set_matter_mapping"
    - path: "tests/test_websocket.py"
      provides: "set_matter_mapping handler tests + get_config entity-list tests"
      contains: "set_matter_mapping"
  key_links:
    - from: "websocket.ws_set_matter_mapping"
      to: "coordinator._async_refresh_matter_listeners"
      via: "hass.async_create_task after store.async_save (D-16)"
      pattern: "_async_refresh_matter_listeners"
    - from: "websocket.ws_get_config"
      to: "entity_reg platform == matter / tado_x"
      via: "derived matter_entities / tado_x_entities lists"
      pattern: "matter_entities"
---

<objective>
Add the `set_matter_mapping` WebSocket command (D-15/D-16) that persists the
sparse `matter_mappings` config and triggers atomic listener refresh in the
coordinator, plus extend `get_config` to surface `matter_entities` and
`tado_x_entities` derived lists so the frontend can build the pairing UI
without direct entity-registry access (resolves RESEARCH Assumption A2 via
Option A/C — backend-derived entity lists).

Purpose: the panel needs a mutation channel for mappings and a way to know
which climate entities are Matter vs Tado X.
Output: tested WS command + get_config extension. Depends on plan 13-01's
`_async_refresh_matter_listeners`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-matter-tado-x-real-time-calibration/13-CONTEXT.md
@.planning/phases/13-matter-tado-x-real-time-calibration/13-RESEARCH.md
@.planning/phases/13-matter-tado-x-real-time-calibration/13-PATTERNS.md
@.planning/phases/13-matter-tado-x-real-time-calibration/13-01-SUMMARY.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: set_matter_mapping WS command + registration (RED→GREEN→REFACTOR)</name>
  <files>custom_components/climate_manager/websocket.py, tests/test_websocket.py</files>
  <read_first>
    - custom_components/climate_manager/websocket.py (_make_ws_set_global_mode ~300-326 factory analog; async_register_commands ~78-123; _make_ws_get_calibration_status ~1208-1345 for entity_reg platform checks)
    - .planning/phases/13-matter-tado-x-real-time-calibration/13-CONTEXT.md (D-15, D-16)
    - .planning/phases/13-matter-tado-x-real-time-calibration/13-RESEARCH.md (Pitfall 7 — climate. prefix filter; Pattern 3)
    - .planning/phases/13-matter-tado-x-real-time-calibration/13-PATTERNS.md (websocket.py section — full _make_ws_set_matter_mapping template)
    - tests/test_websocket.py (_setup_entry helper ~62-71; existing set_* handler tests as analogs)
  </read_first>
  <behavior>
    - Test (test_set_matter_mapping_stores_mapping): send
      climate_manager/set_matter_mapping with tado_entity_id="climate.tado_lr"
      and matter_entity_ids=["climate.valve1","climate.valve2"]; result
      success=true; runtime_config["matter_mappings"]["climate.tado_lr"] ==
      ["climate.valve1","climate.valve2"].
    - Test (test_set_matter_mapping_empty_list_pops_key): first store a
      mapping, then send matter_entity_ids=[]; the tado_entity_id key is absent
      from matter_mappings (sparse — never stored as []).
    - Test (test_set_matter_mapping_filters_non_climate_entity_ids): send
      matter_entity_ids=["sensor.foo","climate.valve1"]; only "climate.valve1"
      is stored (Pitfall 7 / T-13-01).
    - Test (test_set_matter_mapping_triggers_listener_refresh): after a
      successful send, coordinator._async_refresh_matter_listeners is invoked
      (spy/monkeypatch on the coordinator method; allow for
      hass.async_create_task scheduling — await hass.async_block_till_done()).
  </behavior>
  <action>
    RED: write the four tests in tests/test_websocket.py using the existing
    _setup_entry scaffold and the websocket client fixture pattern from
    neighbouring set_* tests. Confirm failing.

    GREEN, then implement in websocket.py:
    - Add factory `_make_ws_set_matter_mapping(entry)` returning an
      `@websocket_api.websocket_command` + `@websocket_api.async_response`
      handler. Schema: vol.Required("type")=f"{DOMAIN}/set_matter_mapping",
      vol.Required("tado_entity_id"): str, vol.Required("matter_entity_ids"):
      list. Handler body: filter matter_eids = [e for e in
      msg["matter_entity_ids"] if isinstance(e, str) and
      e.startswith("climate.")] (Pitfall 7). mappings =
      entry.runtime_data.runtime_config.setdefault("matter_mappings", {}). If
      matter_eids: mappings[msg["tado_entity_id"]] = matter_eids; else
      mappings.pop(msg["tado_entity_id"], None) (D-01 sparse: never store []).
      await entry.runtime_data.store.async_save(runtime_config).
      connection.send_result(msg["id"], {"success": True}). Then D-16:
      coordinator = entry.runtime_data.coordinator; if coordinator is not None:
      hass.async_create_task(coordinator._async_refresh_matter_listeners()).
    - Register in async_register_commands alongside existing commands:
      websocket_api.async_register_command(hass,
      _make_ws_set_matter_mapping(entry)).

    REFACTOR: keep handler ≤80 chars/line; align docstring with the
    set_global_mode style.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - websocket.py defines `_make_ws_set_matter_mapping` and registers it in
      async_register_commands
    - Behavior: valid payload stores
      matter_mappings[tado_entity_id] = filtered matter_eids
    - Behavior: empty matter_entity_ids pops the key (no `[]` stored)
    - Behavior: non-`climate.` entity_ids are filtered out before storage
    - Behavior: a successful call invokes
      coordinator._async_refresh_matter_listeners (after async_block_till_done)
    - `make test` exits 0
    - `make lint` exits 0
  </acceptance_criteria>
  <done>
    set_matter_mapping persists the sparse mapping, validates input, and
    atomically refreshes coordinator listeners — all covered by green tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: get_config surfaces matter_entities + tado_x_entities (A2 Option A/C)</name>
  <files>custom_components/climate_manager/websocket.py, tests/test_websocket.py</files>
  <read_first>
    - custom_components/climate_manager/websocket.py (_make_ws_get_config ~260-292 — current climate_entities derivation)
    - .planning/phases/13-matter-tado-x-real-time-calibration/13-RESEARCH.md (Assumption A2; Code Examples §frontend)
    - .planning/phases/13-matter-tado-x-real-time-calibration/13-PATTERNS.md ("No Analog Found" — A2 option table: Option A/C chosen)
    - tests/test_websocket.py (existing get_config test as analog)
  </read_first>
  <behavior>
    - Test (test_get_config_includes_matter_entities): register a climate
      entity in the entity registry with platform "matter"; get_config payload
      contains "matter_entities" listing that entity_id and excluding
      non-matter climate entities.
    - Test (test_get_config_includes_tado_x_entities): register a climate
      entity with platform "tado_x"; get_config payload "tado_x_entities"
      lists it and excludes non-tado_x climate entities.
    - Test (test_get_config_entity_lists_do_not_pollute_storage): after
      get_config, runtime_config / persisted store does NOT contain
      matter_entities or tado_x_entities keys (derived-only, mirrors the
      existing climate_entities D-25 invariant).
  </behavior>
  <action>
    This is glue/derivation, not new business logic — standard task, no RED
    cycle required, but the three behavior tests above MUST be written and
    pass. In _make_ws_get_config's ws_get_config: alongside the existing
    climate_entities sort, derive two more sorted lists from
    entity_reg.entities.values() filtered to entity_id.split(".")[0] ==
    "climate": matter_entities where reg_entry.platform == "matter";
    tado_x_entities where reg_entry.platform == "tado_x". Merge both into the
    NEW payload dict (alongside climate_entities) — never mutate
    runtime_config (preserve the D-25 no-pollution invariant). Add tests to
    tests/test_websocket.py.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - get_config payload contains `matter_entities` (climate entities with
      registry platform == "matter") and `tado_x_entities` (platform ==
      "tado_x")
    - Behavior: derived lists exclude climate entities of other platforms
    - Behavior: runtime_config / persisted store does not gain matter_entities
      or tado_x_entities keys
    - `make test` exits 0
    - `make lint` exits 0
  </acceptance_criteria>
  <done>
    Frontend can distinguish Matter and Tado X climate entities purely from
    the get_config payload, with no storage pollution.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel (browser) → WS command | set_matter_mapping payload is untrusted client input |
| WS handler → Store persistence | mutated config is persisted to disk |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-04 | Tampering | matter_entity_ids carrying non-climate entity_ids (e.g. sensor.foo) | mitigate | Filter `[e for e in ids if isinstance(e,str) and e.startswith("climate.")]` before storage (Pitfall 7); test_set_matter_mapping_filters_non_climate_entity_ids enforces it. |
| T-13-05 | Tampering | tado_entity_id pointing at an arbitrary / non-tado_x entity | accept | No service call is issued from the WS handler; the coordinator resolves the real platform at runtime (plan 01 T-13-01). A bogus key is inert — it maps to no real tado_x entity and is skipped in dispatch. |
| T-13-06 | Information Disclosure | get_config leaking derived entity lists into persistent storage | mitigate | Derived lists merged into a NEW payload dict only; runtime_config never mutated (D-25 invariant); test_get_config_entity_lists_do_not_pollute_storage enforces it. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs (RESEARCH §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `make test` green (new WS tests + existing suite)
- `make lint` green
- No new external packages
</verification>

<success_criteria>
- set_matter_mapping persists sparse mappings, filters input, refreshes
  listeners (MCALIB-01 mutation channel; MCALIB-02 atomic refresh)
- get_config exposes matter_entities + tado_x_entities for the pairing UI
  with no storage pollution
</success_criteria>

<output>
Create `.planning/phases/13-matter-tado-x-real-time-calibration/13-02-SUMMARY.md` when done
</output>
