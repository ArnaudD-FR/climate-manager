---
phase: 13-matter-tado-x-real-time-calibration
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - frontend/src/types.ts
  - frontend/src/ws-client.ts
  - frontend/src/components/room-card.ts
autonomous: false
requirements: [MCALIB-01]
user_setup: []
must_haves:
  truths:
    - "A room with at least one Tado X climate entity shows a Matter pairing row per tado_x entity"
    - "Each pairing row lets the user pick a Matter climate entity (or none) from a native select"
    - "Selecting/clearing a Matter entity auto-saves via set_matter_mapping with no Save button"
    - "Rooms with no Tado X entity do not show the pairing section"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "matter_mappings, matter_entities, tado_x_entities on ClimateConfig"
      contains: "matter_mappings"
    - path: "frontend/src/ws-client.ts"
      provides: "setMatterMapping(tadoEntityId, matterEntityIds) method"
      contains: "setMatterMapping"
    - path: "frontend/src/components/room-card.ts"
      provides: "Matter pairing section + auto-save handler"
      contains: "Matter"
  key_links:
    - from: "room-card._onMatterMappingChange"
      to: "ws-client.setMatterMapping"
      via: "auto-save on select change then panel.reloadConfig"
      pattern: "setMatterMapping"
    - from: "room-card Matter picker"
      to: "config.matter_entities"
      via: "select options from backend-derived matter entity list"
      pattern: "matter_entities"
---

<objective>
Add the Matter pairing UI to the room card (D-12..D-14): per tado_x entity, a
native `<select>` to pick a Matter climate entity (or "(none)"), populated
from the backend-derived `matter_entities` list, auto-saving via
`set_matter_mapping`. Section is hidden when the room has no Tado X entity.
This completes MCALIB-01 by giving users a way to configure mappings.

Purpose: user-facing configuration of Matter→Tado X pairings.
Output: room-card pairing section + types + ws-client method. Depends on
plan 13-02 (set_matter_mapping command + get_config matter_entities/
tado_x_entities). Ends with a human visual-verification checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/13-matter-tado-x-real-time-calibration/13-CONTEXT.md
@.planning/phases/13-matter-tado-x-real-time-calibration/13-PATTERNS.md
@.planning/phases/13-matter-tado-x-real-time-calibration/13-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: types.ts + ws-client.ts — matter mapping types and method</name>
  <files>frontend/src/types.ts, frontend/src/ws-client.ts</files>
  <read_first>
    - frontend/src/types.ts (ClimateConfig interface ~104-132 — climate_entities, calibration fields)
    - frontend/src/ws-client.ts (setCalibrationConfig ~217-223 — method analog)
    - .planning/phases/13-matter-tado-x-real-time-calibration/13-CONTEXT.md (D-01, D-15)
    - .planning/phases/13-matter-tado-x-real-time-calibration/13-PATTERNS.md (types.ts + ws-client.ts sections)
  </read_first>
  <action>
    types.ts: extend ClimateConfig with optional `matter_mappings?:
    Record<string, string[]>` (D-01 sparse — key tado_x entity_id, value list
    of Matter entity_ids), and the two backend-derived read-only lists from
    plan 02 get_config: `matter_entities?: string[]` and `tado_x_entities?:
    string[]`. Add a doc comment on matter_mappings noting absent key = no
    mapping.

    ws-client.ts: add `setMatterMapping(tadoEntityId: string,
    matterEntityIds: string[]): Promise<{ success: boolean }>` that calls
    sendMessagePromise with type "climate_manager/set_matter_mapping",
    tado_entity_id: tadoEntityId, matter_entity_ids: matterEntityIds (matches
    plan 02 D-15 schema exactly). Pass an empty array to remove a mapping.
    Mirror the setCalibrationConfig style.
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - types.ts ClimateConfig contains `matter_mappings?: Record<string,
      string[]>`, `matter_entities?: string[]`, `tado_x_entities?: string[]`
    - ws-client.ts defines `setMatterMapping(tadoEntityId, matterEntityIds)`
      sending type "climate_manager/set_matter_mapping"
    - `cd frontend && npm run build` exits 0 (TypeScript compiles)
    - `make lint` exits 0 (editorconfig: 2-space, ≤80 char TS)
  </acceptance_criteria>
  <done>
    Type contract and WS client method exist and compile; payload shape matches
    the backend set_matter_mapping schema.
  </done>
</task>

<task type="auto">
  <name>Task 2: room-card Matter pairing section + auto-save handler</name>
  <files>frontend/src/components/room-card.ts</files>
  <read_first>
    - frontend/src/components/room-card.ts (_renderTrvSection ~542-581; render method ~750-880 where TRV/preheat sections are composed; _onZoneChange ~421-433 auto-save analog; how config + roomStatus.entity_ids are accessed)
    - frontend/src/components/person-card.ts (calendar entity picker native <select> ~521-554; hass.states domain filter ~796-799)
    - .planning/phases/13-matter-tado-x-real-time-calibration/13-CONTEXT.md (D-12, D-13, D-14; Claude's Discretion — section heading)
    - .planning/phases/13-matter-tado-x-real-time-calibration/13-PATTERNS.md (room-card.ts section — Analog 1/2/3, auto-save handler, section visibility)
  </read_first>
  <action>
    Add a `_renderMatterPairingSection()` method and compose it into render()
    next to `_renderTrvSection()` / `_renderPreheatSection()`.

    Visibility (D-12): determine the room's tado_x entities by intersecting
    `this.roomStatus?.entity_ids ?? []` with the backend-derived
    `config.tado_x_entities` (from plan 02 get_config). If the intersection is
    empty, render nothing (no section).

    Options source (D-13, A2 resolved): Matter options come from
    `config.matter_entities` (backend-derived list of climate entities with
    platform "matter"); display each option's friendly_name via
    `this.hass?.states[id]?.attributes?.friendly_name ?? id`. Include a
    "(none)" option (value "") at the top, selected when the tado_x entity has
    no mapping.

    Rows (D-14): render one row per tado_x entity in the room. Each row shows
    the tado_x entity friendly name + a native `<select class="...">` (copy the
    person-card calendar `<select>` structure — native element per Phase 10
    D-04, not ha-select). Current selection: read
    `config.matter_mappings?.[tadoEntityId]?.[0] ?? ""` (single Matter entity
    per row in the picker; the schema stays a list).

    Auto-save (D-12, Phase 9 D-09): add `private async
    _onMatterMappingChange(tadoEntityId: string, e: Event)`: read selected =
    (e.target as HTMLSelectElement).value; matterEntityIds = selected ?
    [selected] : []; try { await this.ws.setMatterMapping(tadoEntityId,
    matterEntityIds); await this.panel.reloadConfig();
    this.panel.showToast("Saved", false); } catch {
    this.panel.showToast("Save failed — retrying...", true); }. No Save button.

    Section heading (Claude's discretion): use "Real-time calibration" to stay
    consistent with the existing calibration terminology in the card; add a one
    line sub-label like "Pair each Tado X valve to its Matter entity for
    sub-minute calibration."
  </action>
  <verify>
    <automated>cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - room-card.ts defines `_renderMatterPairingSection` and
      `_onMatterMappingChange`
    - Behavior: section renders only when the room has ≥1 entity present in
      config.tado_x_entities (hidden otherwise)
    - Behavior: one pairing row per tado_x entity; options sourced from
      config.matter_entities plus a "(none)" option
    - Behavior: changing the select calls
      ws.setMatterMapping(tadoEntityId, [selected]) (or []) then
      panel.reloadConfig and shows the Saved toast (no Save button)
    - Picker uses a native `<select>` (not ha-select), per Phase 10 D-04
    - `cd frontend && npm run build` exits 0
    - `make lint` exits 0
  </acceptance_criteria>
  <done>
    Room card shows per-valve Matter pairing rows for Tado X rooms, auto-saving
    selections through set_matter_mapping; hidden for non-Tado-X rooms.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Matter pairing section on the room card: per Tado X valve, a Matter entity
    picker that auto-saves and refreshes coordinator listeners for sub-minute
    calibration.
  </what-built>
  <how-to-verify>
    1. Run `make build && make deploy` (or the project's deploy target), then
       reload the Climate Manager panel in Home Assistant.
    2. Open a room card for a room that has a Tado X climate entity. Confirm a
       "Real-time calibration" pairing section appears with one row per Tado X
       valve and a Matter entity `<select>`.
    3. Open a room card for a room with NO Tado X entity. Confirm the pairing
       section is absent.
    4. Pick a Matter entity in a row; confirm a "Saved" toast appears and the
       selection persists after a panel reload.
    5. Set the row back to "(none)"; confirm the mapping is removed (selection
       returns to "(none)" after reload).
    6. (If live Tado X + Matter hardware is available) change the room
       temperature and confirm calibration fires within seconds rather than
       waiting for the next minute tick (see 13-VALIDATION.md manual checks).
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user selection (browser) → setMatterMapping WS call | user-chosen entity_ids leave the panel |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-13-07 | Tampering | UI lets a user pair a non-Matter / non-climate entity | mitigate | Options are sourced only from backend-derived config.matter_entities (platform == "matter"); the WS handler (plan 02 T-13-04) re-filters to climate.* server-side regardless of UI. |
| T-13-08 | Spoofing | n/a — WS auth handled by HA session | accept | Panel uses the authenticated HA WebSocket connection; no separate auth surface introduced. |
| T-13-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (frontend uses existing deps; RESEARCH §Package Legitimacy Audit: not applicable). |
</threat_model>

<verification>
- `cd frontend && npm run build` green (TypeScript compiles)
- `make lint` green
- Human checkpoint: pairing section visibility, auto-save, and removal verified
  in the live panel
</verification>

<success_criteria>
- MCALIB-01 (UI): users can configure a room-level Matter→Tado X mapping; the
  pairing section appears only for Tado X rooms; selections auto-save and
  trigger backend listener refresh
</success_criteria>

<output>
Create `.planning/phases/13-matter-tado-x-real-time-calibration/13-03-SUMMARY.md` when done
</output>
