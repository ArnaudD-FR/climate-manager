---
phase: 07-even-odd-week-scheduling-backend
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - custom_components/climate_manager/schedule.py
  - custom_components/climate_manager/const.py
  - frontend/src/types.ts
  - tests/test_schedule.py
autonomous: true
requirements: [SCHED-01, SCHED-02, SCHED-03]
must_haves:
  truths:
    - "During an even ISO week, an even_odd person's presence is evaluated from schedule_even"
    - "During an odd ISO week, an even_odd person's presence is evaluated from schedule_odd"
    - "A person with no schedule_type field behaves exactly as before (uses schedule)"
    - "A person with schedule_type=single behaves identically to absent schedule_type"
    - "An even_odd person missing the selected week schedule resolves to absent (no crash)"
  artifacts:
    - path: "custom_components/climate_manager/schedule.py"
      provides: "resolve_presence() week-parity schedule selection"
      contains: "isocalendar().week"
    - path: "custom_components/climate_manager/const.py"
      provides: "persons sub-schema documentation for schedule_type/even/odd"
      contains: "schedule_type"
    - path: "frontend/src/types.ts"
      provides: "PersonConfig optional even/odd fields for Phase 8"
      contains: "schedule_type"
    - path: "tests/test_schedule.py"
      provides: "even/odd resolve_presence test coverage"
      contains: "even_odd"
  key_links:
    - from: "schedule.py resolve_presence()"
      to: "person_config schedule_type / schedule_even / schedule_odd"
      via: "now.date().isocalendar().week % 2 selection"
      pattern: "isocalendar\\(\\)\\.week"
---

<objective>
Extend `resolve_presence()` in `schedule.py` so an `even_odd` person's presence
is evaluated from `schedule_even` during even ISO weeks and `schedule_odd` during
odd ISO weeks, while every person without a `schedule_type` field (or with
`schedule_type == "single"`) keeps the exact current single-schedule behaviour.
Document the three new person fields in `const.py` and add forward-compatible
optional fields to the `PersonConfig` TypeScript interface for Phase 8.

Purpose: This is the evaluator core of even/odd week scheduling (SCHED-02) plus
the additive schema documentation (SCHED-01, SCHED-03). It must be 100%
backward-compatible — no migration, no behaviour change for existing persons.
Output: Modified `schedule.py`, documented `const.py`, extended `types.ts`, new
test cases in `tests/test_schedule.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-even-odd-week-scheduling-backend/07-CONTEXT.md
@.planning/phases/07-even-odd-week-scheduling-backend/07-RESEARCH.md
@.planning/phases/07-even-odd-week-scheduling-backend/07-PATTERNS.md
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: RED+GREEN — even/odd week selection in resolve_presence()</name>
  <files>tests/test_schedule.py, custom_components/climate_manager/schedule.py</files>
  <read_first>
    - tests/test_schedule.py (existing resolve_presence tests ~lines 257-317;
      module imports already include ALL_DAYS, PRESENCE_AUTOMATIC, resolve_presence;
      reuse the PERSON_SCHEDULE fixture)
    - custom_components/climate_manager/schedule.py (resolve_presence() lines
      117-158 — the automatic branch reads schedule on line 139)
    - .planning/phases/07-even-odd-week-scheduling-backend/07-RESEARCH.md
      (Pattern 1, Code Examples "Exact Insertion Point in resolve_presence()",
      Pitfall 3 isocalendar on date vs datetime)
    - .planning/phases/07-even-odd-week-scheduling-backend/07-PATTERNS.md
      (schedule.py section: exact replacement block and the five test bodies
      T-07-S1..S5)
  </read_first>
  <behavior>
    - T-07-S1: schedule_type="even_odd" on an even ISO week (e.g. 2026-01-05 Mon,
      ISO week 2, parity 0) evaluates from schedule_even → present when
      schedule_even is always-present and schedule_odd is always-absent
    - T-07-S2: schedule_type="even_odd" on an odd ISO week (e.g. 2026-01-12 Mon,
      ISO week 3, parity 1) evaluates from schedule_odd → absent when
      schedule_even is always-present and schedule_odd is always-absent
    - T-07-S3: no schedule_type key → uses schedule (PERSON_SCHEDULE) → present
      Monday 10:00 (unchanged single behaviour)
    - T-07-S4: explicit schedule_type="single" → identical to T-07-S3 → present
    - T-07-S5: schedule_type="even_odd" with NO schedule_even/schedule_odd keys →
      selected week schedule defaults to {} → absent (no crash)
  </behavior>
  <action>
    RED: Add module-level fixtures ALWAYS_PRESENT_SCHEDULE and
    ALWAYS_ABSENT_SCHEDULE alongside the existing PERSON_SCHEDULE fixture, each a
    dict comprehension over ALL_DAYS producing a single 00:00 period with state
    "present" / "absent" respectively. Add the five test functions named exactly
    test_resolve_presence_even_odd_even_week_uses_schedule_even,
    test_resolve_presence_even_odd_odd_week_uses_schedule_odd,
    test_resolve_presence_no_schedule_type_uses_schedule,
    test_resolve_presence_explicit_single_uses_schedule,
    test_resolve_presence_even_odd_missing_week_schedule_returns_false. Use
    timezone-aware datetimes with datetime.timezone.utc. Include the sanity
    assertion `assert now.date().isocalendar().week % 2 == <0|1>` in S1 and S2.
    Run `make test` and confirm the even_odd tests FAIL (S1/S2/S5 fail; S3/S4
    pass since they exercise existing behaviour).

    GREEN: In schedule.py resolve_presence(), replace the single line
    `schedule = person_config.get("schedule", {})` (currently line 139, inside the
    automatic branch after the force_present/force_absent guards) with the
    week-parity selection block: read `schedule_type = person_config.get(
    "schedule_type", "single")`; if it equals "even_odd", compute
    `week_parity = now.date().isocalendar().week % 2`, pick schedule_key
    "schedule_even" when parity == 0 else "schedule_odd", and assign
    `schedule = person_config.get(schedule_key, {})`; else keep
    `schedule = person_config.get("schedule", {})`. Use the named attribute
    `.isocalendar().week` (not `[1]`) per Pitfall 3, and `now.date()` explicitly.
    Add an inline `# SCHED-02:` comment. Do NOT add any HomeAssistant imports —
    schedule.py is pure Python; `datetime` is already imported. Keep the
    subsequent day_name/periods/period-walk logic unchanged. Run `make test` and
    confirm all five tests pass.
  </action>
  <verify>
    <automated>make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "isocalendar().week" custom_components/climate_manager/schedule.py`
      returns a match inside resolve_presence()
    - `grep -c "import homeassistant\|from homeassistant" custom_components/climate_manager/schedule.py`
      returns 0 (pure-Python constraint preserved)
    - The five new test functions exist in tests/test_schedule.py:
      `grep -c "def test_resolve_presence_even_odd\|def test_resolve_presence_no_schedule_type\|def test_resolve_presence_explicit_single" tests/test_schedule.py`
      returns at least 4
    - `make test` passes with no NEW failures versus the documented baseline
      (127 passed, 1 pre-existing unrelated failure in test_phase06_acceptance.py)
  </acceptance_criteria>
  <done>
    even_odd persons evaluate from schedule_even on even ISO weeks and
    schedule_odd on odd ISO weeks; single/absent-schedule_type persons are
    unchanged; missing-week-schedule resolves to absent; all five new tests pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document new person fields in const.py and extend PersonConfig types</name>
  <files>custom_components/climate_manager/const.py, frontend/src/types.ts</files>
  <read_first>
    - custom_components/climate_manager/const.py (persons sub-schema comment,
      lines 130-147 — the block to extend)
    - frontend/src/types.ts (PersonConfig interface and the existing
      DailyProgram type — both already defined in this file)
    - .planning/phases/07-even-odd-week-scheduling-backend/07-PATTERNS.md
      (const.py section: exact replacement comment block; types.ts section:
      exact extended PersonConfig)
  </read_first>
  <action>
    In const.py, extend the persons sub-schema comment block (lines 130-147)
    so the "schedule" entry is annotated `# used when schedule_type == "single"
    (default)` and three new documented keys appear after the schedule block:
    `schedule_type` documented as `"single" | "even_odd"` with `SCHED-01/03;
    absent = "single"`, `schedule_even` documented as `SCHED-03; same structure
    as schedule; used during even ISO weeks (parity == 0)`, and `schedule_odd`
    documented as `SCHED-03; same structure as schedule; used during odd ISO
    weeks (parity == 1)`. This is a comment-only change — add no constants, no
    logic. Keep 4-space indent and max 80 chars per line per .editorconfig.

    In frontend/src/types.ts, add three optional fields to the PersonConfig
    interface after the existing `schedule?: DailyProgram;` line:
    `schedule_type?: "single" | "even_odd";`, `schedule_even?: DailyProgram;`,
    `schedule_odd?: DailyProgram;`, preceded by the comment
    `// Phase 7: even/odd week scheduling (SCHED-01, SCHED-03)`. Reuse the
    existing DailyProgram type (already defined in the file — no new import).
    Keep 2-space indent and max 80 chars per line per .editorconfig.
  </action>
  <verify>
    <automated>grep -c "schedule_type" custom_components/climate_manager/const.py frontend/src/types.ts && cd frontend && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "schedule_type\|schedule_even\|schedule_odd" custom_components/climate_manager/const.py`
      returns at least 3
    - `grep -c "schedule_type\|schedule_even\|schedule_odd" frontend/src/types.ts`
      returns at least 3
    - PersonConfig in types.ts uses `DailyProgram` for schedule_even and
      schedule_odd: `grep -n "schedule_even?: DailyProgram" frontend/src/types.ts`
      returns a match
    - `cd frontend && npm run build` completes with no TypeScript errors
    - `make lint` passes (editorconfig compliance for both modified files)
  </acceptance_criteria>
  <done>
    const.py persons sub-schema documents schedule_type/schedule_even/
    schedule_odd; PersonConfig in types.ts exposes the three optional fields
    typed against DailyProgram; frontend build and lint pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator → resolve_presence() | person_config dict (from runtime_config) crosses into the pure evaluator |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-07-01 | Tampering | resolve_presence() schedule_type value | low | mitigate | Unknown schedule_type values fall through to the `else` branch (single behaviour) — the `if schedule_type == "even_odd"` check is exact-match, so any other string is safe; verified by test T-07-S4 and the implicit fall-through |
| T-07-02 | Denial of Service | resolve_presence() missing week schedule | low | mitigate | `person_config.get(schedule_key, {})` defaults to empty dict; the existing `if not periods: return False` guard prevents any crash — verified by T-07-S5 |
| T-07-03 | Information Disclosure | const.py / types.ts comments | low | accept | Documentation only; no runtime data exposure |

No high-severity threats. This phase adds no external input surface — `set_person_config`
input validation is handled in plan 07-02. No package installs (Python stdlib only).
</threat_model>

<verification>
- `make test` passes (no new failures vs baseline 127 passed / 1 pre-existing
  unrelated failure)
- `cd frontend && npm run build` produces no TypeScript errors
- `make lint` passes for all modified files
- schedule.py contains no HomeAssistant imports (pure-Python constraint)
</verification>

<success_criteria>
- An even_odd person is evaluated from schedule_even on even ISO weeks and
  schedule_odd on odd ISO weeks (SCHED-02)
- A person with no schedule_type, or schedule_type="single", behaves exactly as
  the v1.1 single-schedule person (SCHED-03 backward compatibility)
- The three new person fields are documented in const.py and typed in
  PersonConfig (SCHED-01, SCHED-03)
- All five new resolve_presence tests pass
</success_criteria>

<output>
Create `.planning/phases/07-even-odd-week-scheduling-backend/07-01-SUMMARY.md` when done
</output>
