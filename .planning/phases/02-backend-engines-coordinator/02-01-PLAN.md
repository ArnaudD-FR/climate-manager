---
phase: 02-backend-engines-coordinator
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/climate_manager/const.py
  - custom_components/climate_manager/schedule.py
  - tests/test_schedule.py
autonomous: true
requirements:
  - GLOBAL-03
  - SCHED-01
  - SCHED-02
  - SCHED-03
  - SCHED-04
  - PERSON-01
  - PERSON-02
  - PERSON-03
  - PERSON-04
  - PERSON-05
  - PERSON-07
  - PERSON-08
  - PERSON-09

must_haves:
  truths:
    - "evaluate_schedule returns the active period mode for a given weekday_groups + datetime"
    - "resolve_presence returns True/False per PERSON-01 through PERSON-05 mode rules"
    - "compute_occupied_temp implements the PERSON-07/08/09 occupied-window gap-fill correctly"
    - "validate_7day_coverage rejects missing or duplicate days (D-06)"
    - "Presence mode constants exist in const.py and are imported (not string literals) by schedule.py"
  artifacts:
    - path: "custom_components/climate_manager/const.py"
      provides: "PRESENCE_AUTOMATIC, PRESENCE_PRESENT, PRESENCE_ABSENT constants"
      contains: "PRESENCE_AUTOMATIC"
    - path: "custom_components/climate_manager/schedule.py"
      provides: "Pure-Python schedule + presence evaluation functions"
      exports: ["evaluate_schedule", "resolve_presence", "compute_occupied_temp", "validate_7day_coverage", "DAY_TO_WEEKDAY", "ALL_DAYS"]
    - path: "tests/test_schedule.py"
      provides: "Unit tests covering all schedule/presence branches"
      min_lines: 80
  key_links:
    - from: "custom_components/climate_manager/schedule.py"
      to: "custom_components/climate_manager/const.py"
      via: "from .const import PERIOD_*, PRESENCE_*"
      pattern: "from \\.const import"
    - from: "tests/test_schedule.py"
      to: "custom_components/climate_manager/schedule.py"
      via: "direct function import (no hass fixture)"
      pattern: "from custom_components.climate_manager.schedule import"
---

<objective>
Implement the pure-Python schedule and presence evaluation engine: the algorithms that decide which period mode is active and whether a person is present at any given wall-clock time. This is the core decision logic of the entire integration, isolated in `schedule.py` with no Home Assistant imports so it is trivially unit-testable.

Purpose: Phase 2's most complex logic (PERSON-07/08/09 occupied-window gap-fill) lives here. Isolating it from HA integration concerns lets it be tested exhaustively by passing `datetime` objects directly, with no `hass` fixture and no `freezegun`.

Output: `const.py` extended with 3 presence constants; new `schedule.py` with 4 functions; `test_schedule.py` with full branch coverage.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-backend-engines-coordinator/02-CONTEXT.md
@.planning/phases/02-backend-engines-coordinator/02-RESEARCH.md
@.planning/phases/02-backend-engines-coordinator/02-PATTERNS.md

<interfaces>
<!-- Extracted from custom_components/climate_manager/const.py — executor uses these directly -->

From const.py (existing, do not redefine):
- MODE_OFF = "off", MODE_TIME_PROGRAM = "time_program", MODE_TIME_PROGRAM_PRESENCES = "time_program_presences"
- PERIOD_FROST_PROTECTION = "frost_protection", PERIOD_REDUCED = "reduced", PERIOD_NORMAL = "normal", PERIOD_COMFORT = "comfort"
- DEFAULT_PERIOD_TEMPERATURES: dict[str, float] keyed by the four PERIOD_* constants

Storage schema shapes (from const.py docstring, canonical):
- weekday_group: {"days": ["mon","tue",...], "periods": [{"start": "HH:MM", "mode": "<period_mode>"}, ...]}
- person config: {"mode": "automatic"|"present"|"absent", "room_ids": ["<area_id>"], "schedule": {"weekday_groups": [{"days":[...], "periods":[{"start":"HH:MM","state":"present"|"absent"}]}]}}
- Day strings are lowercase 3-letter: mon tue wed thu fri sat sun (RESEARCH Open Question 3 — canonical form)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add presence mode constants to const.py</name>
  <files>custom_components/climate_manager/const.py</files>
  <read_first>
    - custom_components/climate_manager/const.py (see current section ordering and comment-header block style)
    - .planning/phases/02-backend-engines-coordinator/02-PATTERNS.md (section "custom_components/climate_manager/const.py (modified — add 3 constants)")
  </read_first>
  <action>
    Insert a new constant block into const.py addressing Pitfall 7 (presence modes were string values in the schema but never defined as constants). Add a comment header block titled for presence mode constants (PERSON-01) and define exactly three module-level constants: PRESENCE_AUTOMATIC with value "automatic", PRESENCE_PRESENT with value "present", PRESENCE_ABSENT with value "absent". Place this block after the "Period mode name constants" block (after the PERIOD_COMFORT definition, currently line 31) and before the "Default values" block (currently line 33-35), matching the file's existing `# ---` comment-header grouping convention. Do not modify DEFAULT_CONFIG, DEFAULT_PERIOD_TEMPERATURES, or any existing constant. Keep the "No Home Assistant imports — pure constants only" invariant.
  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager && python -c "from custom_components.climate_manager.const import PRESENCE_AUTOMATIC, PRESENCE_PRESENT, PRESENCE_ABSENT; assert (PRESENCE_AUTOMATIC, PRESENCE_PRESENT, PRESENCE_ABSENT) == ('automatic','present','absent')"</automated>
  </verify>
  <acceptance_criteria>
    - `python -c "from custom_components.climate_manager.const import PRESENCE_AUTOMATIC, PRESENCE_PRESENT, PRESENCE_ABSENT"` exits 0
    - PRESENCE_AUTOMATIC == "automatic", PRESENCE_PRESENT == "present", PRESENCE_ABSENT == "absent"
    - const.py still contains no `import homeassistant` or `from homeassistant` lines: `grep -L "homeassistant" custom_components/climate_manager/const.py` returns the file path
    - DEFAULT_CONFIG and DEFAULT_PERIOD_TEMPERATURES values are unchanged from their pre-edit content
  </acceptance_criteria>
  <done>Three presence constants importable from const.py with the exact string values; no other constant changed; no HA imports introduced.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement schedule.py pure-Python evaluation engine</name>
  <files>custom_components/climate_manager/schedule.py, tests/test_schedule.py</files>
  <read_first>
    - custom_components/climate_manager/const.py (PERIOD_* and new PRESENCE_* constants — import these, never string literals)
    - .planning/phases/02-backend-engines-coordinator/02-RESEARCH.md (Pattern 4 evaluate_schedule, Pattern 5 compute_occupied_temp, Pattern 6 validate_7day_coverage, Pattern 7 resolve_presence — these are the authoritative algorithms; Pitfall 4 boundary >=)
    - .planning/phases/02-backend-engines-coordinator/02-PATTERNS.md (section "custom_components/climate_manager/schedule.py" and "tests/test_schedule.py")
    - tests/test_trv.py (test file structure, module-level constants, direct-call style)
    - custom_components/climate_manager/trv.py (module docstring + decision-ref docstring style to replicate)
  </read_first>
  <behavior>
    - evaluate_schedule: Monday 08:30 with group days=[mon..fri] periods [(07:00,normal),(22:00,reduced)] returns "normal"; before first period returns PERIOD_FROST_PROTECTION; period boundary uses >= so exactly 07:00 returns "normal" not the prior mode (Pitfall 4); no group covers today returns PERIOD_FROST_PROTECTION
    - resolve_presence: mode "present" returns True regardless of time (PERSON-02); mode "absent" returns False regardless of time (PERSON-03); mode "automatic" with empty weekday_groups returns False (PERSON-05); mode "automatic" with a schedule evaluates state=="present" for the active period (PERSON-04); missing "mode" key defaults to automatic
    - compute_occupied_temp: is_present False returns period_temperatures[PERIOD_REDUCED] (PERSON-09); present with no Normal/Comfort periods today returns PERIOD_REDUCED temp (D-05); present before first N/C period returns Reduced temp; present after end of last N/C period returns Reduced temp; present inside occupied window during a Normal period returns Normal temp (PERSON-07); present during a Reduced period sandwiched between two Normal/Comfort periods returns the preceding N/C period's temperature, not Reduced (PERSON-08)
    - validate_7day_coverage: all 7 days across groups with no duplicates returns (True, ""); a missing day returns (False, message naming the missing day) (D-06/SCHED-04); a duplicated day returns (False, duplicate message); an unknown day token returns (False, unknown-days message)
  </behavior>
  <action>
    Create schedule.py as a pure-Python module (only stdlib `datetime` and imports from `.const` — no Home Assistant imports, enforcing the const.py convention). Add a module docstring listing the REQ-IDs and D-xx decisions addressed (SCHED-01..04, PERSON-01..05, PERSON-07..09, GLOBAL-03, D-05, D-06), matching trv.py docstring style.

    Define module constants DAY_TO_WEEKDAY mapping the seven lowercase day tokens to Python datetime.weekday() integers (mon=0 through sun=6) and ALL_DAYS as the set of those seven tokens.

    Implement a private helper `_parse_time(value: str) -> datetime.time` that splits "HH:MM" into hour and minute and returns a datetime.time.

    Implement `evaluate_schedule(weekday_groups: list[dict], now: datetime.datetime) -> str` per RESEARCH Pattern 4: resolve now.weekday() to today, find the weekday_group whose mapped days contain today, sort that group's periods by start, walk forward keeping the last period whose parsed start is <= now.time() (use >= comparison per Pitfall 4 — exact boundary minute belongs to the new period), return that period's "mode"; if no period has started yet or no group covers today, return PERIOD_FROST_PROTECTION (imported from const).

    Implement `resolve_presence(person_config: dict, now: datetime.datetime) -> bool` per RESEARCH Pattern 7: read mode via person_config.get("mode", PRESENCE_AUTOMATIC); return True for PRESENCE_PRESENT, False for PRESENCE_ABSENT; for PRESENCE_AUTOMATIC read person_config.get("schedule", {}).get("weekday_groups", []) — empty returns False (PERSON-05); otherwise evaluate today's group like evaluate_schedule but on per-period "state" keys, returning True only when the active period's state == PRESENCE_PRESENT; no group covers today returns False.

    Implement `compute_occupied_temp(weekday_groups: list[dict], now: datetime.datetime, is_present: bool, period_temperatures: dict[str, float]) -> float` per RESEARCH Pattern 5: if not is_present return period_temperatures[PERIOD_REDUCED] (PERSON-09); find today's sorted periods; collect Normal/Comfort periods (nc_modes = {PERIOD_NORMAL, PERIOD_COMFORT}); if none return period_temperatures[PERIOD_REDUCED] (D-05); compute occupied window as [first N/C start, end of last N/C period] where the end is the start of the period immediately after the last N/C period or datetime.time(23,59,59) if it is the last period of the day; before window start or after window end return period_temperatures[PERIOD_REDUCED]; inside the window, walk periods forward tracking active_mode and the last Normal/Comfort mode seen, and if the active period's mode is not Normal/Comfort apply gap-fill by substituting the last seen N/C mode (PERSON-08); return period_temperatures for the resolved mode, defaulting to the Reduced temperature if the mode key is absent.

    Implement `validate_7day_coverage(weekday_groups: list[dict]) -> tuple[bool, str]` per RESEARCH Pattern 6: flatten all groups' "days"; if the flattened list length differs from its set length return (False, a message stating duplicate day assignment); if the set != ALL_DAYS return (False, a message naming sorted missing days and/or sorted unknown days); otherwise (True, ""). This validates both global and per-room programs (D-06 extends SCHED-04 to exactly-one).

    Create tests/test_schedule.py with no hass fixture. Import the four functions directly from custom_components.climate_manager.schedule. Use module-level weekday_groups fixtures (mirror test_trv.py module-constant style). Build period_temperatures from DEFAULT_PERIOD_TEMPERATURES (import from const). Pass timezone-aware datetime objects directly (datetime.datetime(..., tzinfo=datetime.UTC)) — do not use freezegun. Cover every branch listed in the behavior block above, including the PERSON-08 sandwiched-gap case (a schedule like [(07:00,normal),(12:00,reduced),(14:00,comfort),(22:00,reduced)] evaluated at 12:30 must return the Normal temperature, and at 22:30 must return the Reduced temperature).
  </action>
  <verify>
    <automated>cd /home/arnaud/dev/climate_manager && python -m pytest tests/test_schedule.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - `python -m pytest tests/test_schedule.py -x -q` exits 0 with all tests passing
    - schedule.py has zero HA imports: `grep -c "homeassistant" custom_components/climate_manager/schedule.py` returns 0
    - schedule.py exports evaluate_schedule, resolve_presence, compute_occupied_temp, validate_7day_coverage, DAY_TO_WEEKDAY, ALL_DAYS (all importable)
    - schedule.py imports PERIOD_* and PRESENCE_* from .const (no bare string literals for modes/states): `grep -q "from .const import" custom_components/climate_manager/schedule.py`
    - A test asserts evaluate_schedule returns "normal" at the exact 07:00 boundary minute (Pitfall 4 regression)
    - A test asserts compute_occupied_temp returns the Normal temperature during a Reduced period sandwiched between two Normal/Comfort periods (PERSON-08)
    - A test asserts validate_7day_coverage returns (False, ...) for both a missing-day case and a duplicate-day case (D-06)
    - A test asserts resolve_presence returns False for automatic mode with empty weekday_groups (PERSON-05)
  </acceptance_criteria>
  <done>schedule.py implements all four functions per the RESEARCH algorithms; test_schedule.py covers every behavior branch (including PERSON-08 gap-fill and D-06 validation) and the full suite passes with no HA imports in schedule.py.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored config → schedule.py | weekday_groups / period data read from the Store crosses into pure-Python evaluation. Data originates from Phase 3 panel input (untrusted until validated). |
| time source → evaluation | The caller supplies `now`; schedule.py does not read the clock itself. Correctness depends on the caller passing a valid timezone-aware datetime (enforced by coordinator in Plan 02). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | weekday_groups with overlapping/missing days | mitigate | validate_7day_coverage rejects duplicate and missing days (D-06); called at config save time and exercised by unit tests |
| T-02-02 | Denial of Service | Malformed period "start" string (non "HH:MM") raising in _parse_time | accept | Config is written only via the Phase 3 panel which constrains time input to HH:MM; backend is single-instance, no untrusted external callers; a parse exception is logged by the coordinator tick (Plan 02) and the next tick recovers. Low value target, no PII. |
| T-02-03 | Information Disclosure | Schedule evaluation has no I/O, no logging of secrets | accept | Pure functions; no PII, no secrets, no external surface. |
</threat_model>

<verification>
- `python -m pytest tests/test_schedule.py -x -q` passes
- `grep -c "homeassistant" custom_components/climate_manager/schedule.py` returns 0
- Presence constants importable from const.py with exact values
- Existing Phase 1 tests still pass: `python -m pytest tests/test_trv.py tests/test_storage.py tests/test_discovery.py -q`
</verification>

<success_criteria>
- const.py exposes PRESENCE_AUTOMATIC/PRESENT/ABSENT with values automatic/present/absent
- schedule.py provides evaluate_schedule, resolve_presence, compute_occupied_temp, validate_7day_coverage as pure functions with no HA imports
- The PERSON-07/08/09 occupied-window gap-fill and D-06 7-day validation are implemented exactly per RESEARCH Patterns 5 and 6 and covered by passing tests
- No Phase 1 test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/02-backend-engines-coordinator/02-01-SUMMARY.md`
</output>
