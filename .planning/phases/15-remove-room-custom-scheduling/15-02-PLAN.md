---
phase: 15-remove-room-custom-scheduling
plan: 02
type: execute
wave: 2
depends_on: ["15-01"]
files_modified:
  - custom_components/climate_manager/websocket.py
  - tests/test_websocket.py
autonomous: true
requirements: [ARCH-02]

must_haves:
  truths:
    - "Sending climate_manager/reset_room_to_default_zone_program returns success: False"
    - "set_room_config silently drops a room_mode key from incoming payloads"
    - "make test passes with no reference to room_mode in test_websocket.py"
  artifacts:
    - path: "custom_components/climate_manager/websocket.py"
      provides: "set_room_config room_mode silent drop; reset command removed"
      contains: "incoming_config.pop(\"room_mode\", None)"
    - path: "tests/test_websocket.py"
      provides: "Removed-command test for reset_room_to_default_zone_program"
      contains: "reset_room_to_default_zone_program"
  key_links:
    - from: "ws_set_room_config"
      to: "room record persistence"
      via: "pop room_mode before merge"
      pattern: "incoming_config\\.pop\\(\"room_mode\""
---

<objective>
Remove the `reset_room_to_default_zone_program` WebSocket command (factory +
registration + docstring entry, per D-06) and add a silent `room_mode` drop in
`ws_set_room_config` so no legacy or residual frontend send can re-persist the
field (D-07). Update the websocket tests: delete the obsolete reset test,
rewrite the removed-command assertion as a Phase 15 equivalent, and clean
incidental `room_mode` fixtures.

Purpose: ARCH-02 requires the `reset_room_to_default_zone_program` command
deleted and the backend to never write `room_mode`.
Output: websocket.py and test_websocket.py. `make test` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-remove-room-custom-scheduling/15-CONTEXT.md
@.planning/phases/15-remove-room-custom-scheduling/15-RESEARCH.md
@.planning/phases/15-remove-room-custom-scheduling/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove reset command + add room_mode silent drop in websocket.py</name>
  <files>custom_components/climate_manager/websocket.py</files>
  <read_first>
    - custom_components/climate_manager/websocket.py (lines 10–40 module
      docstring command list; lines 95–145 `async_register_commands`; lines
      350–380 `ws_set_room_config` near the existing `preheat_enabled` drop;
      lines 550–591 `_make_ws_reset_room_to_default_zone_program` factory)
    - .planning/phases/15-remove-room-custom-scheduling/15-PATTERNS.md
      (§websocket.py — exact registration line, factory, docstring, and the
      silent-drop snippet)
    - .planning/phases/15-remove-room-custom-scheduling/15-RESEARCH.md
      (§Detailed Touch Point Map → Backend websocket.py rows; Pitfall 4)
  </read_first>
  <action>
    (1) Delete the registration call
    `websocket_api.async_register_command(hass,
    _make_ws_reset_room_to_default_zone_program(entry))` at lines ~120–122.
    (2) Delete the entire factory function
    `_make_ws_reset_room_to_default_zone_program(entry)` at lines ~550–591
    (including its nested `ws_reset_room_to_default_zone_program` handler and
    the `return ws_reset_room_to_default_zone_program`).
    (3) In the module-level docstring, remove the
    `reset_room_to_default_zone_program` bullet (line ~14) from the active
    command list and add a "Removed in Phase 15 (D-06)" note mirroring the
    Phase 14 removal note style, stating rooms now follow their zone
    exclusively.
    (4) In `ws_set_room_config`, immediately AFTER the existing
    `incoming_config.pop("preheat_enabled", None)` line (~374), add
    `incoming_config.pop("room_mode", None)` with a comment referencing D-07.
    Do not add vol schema rejection — silent drop only (Don't Hand-Roll).
  </action>
  <verify>
    <automated>grep -c '_make_ws_reset_room_to_default_zone_program\|async_register_command(\s*hass,\s*_make_ws_reset_room' custom_components/climate_manager/websocket.py | grep -qx 0 && grep -q 'incoming_config.pop("room_mode", None)' custom_components/climate_manager/websocket.py && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - websocket.py contains zero occurrences of `_make_ws_reset_room_to_default_zone_program`
    - websocket.py contains `incoming_config.pop("room_mode", None)`
    - The factory function definition is fully removed (no orphan handler body)
    - `make lint` reports no unused-name/F811 errors for the removed factory
  </acceptance_criteria>
  <done>The reset command is unregistered and its factory deleted; set_room_config drops room_mode silently.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Update websocket tests for removed command + clean fixtures</name>
  <files>tests/test_websocket.py</files>
  <read_first>
    - tests/test_websocket.py (lines 512–530 existing
      `test_ws_reset_room_to_global_program_is_removed`; lines 533–590
      `test_ws_reset_room_to_default_zone_program_copies_into_room`; lines
      1140–1200 fixtures/assertions referencing room_mode)
    - .planning/phases/15-remove-room-custom-scheduling/15-PATTERNS.md
      (§tests/test_websocket.py — the removed-command test pattern and the
      rewritten Phase 15 test body, plus fixture-line cleanups)
  </read_first>
  <behavior>
    - Sending `{DOMAIN}/reset_room_to_default_zone_program` with a room_id over
      the WS client yields a response where `msg.get("success") is False`
      (command is not registered).
    - The obsolete `..._copies_into_room` test is gone.
    - No surviving test fixture or assertion references `room_mode`.
  </behavior>
  <action>
    (1) Delete `test_ws_reset_room_to_default_zone_program_copies_into_room`
    entirely (lines ~533–590).
    (2) Rewrite `test_ws_reset_room_to_global_program_is_removed` as
    `test_ws_reset_room_to_default_zone_program_is_removed`: send
    `{"type": f"{DOMAIN}/reset_room_to_default_zone_program", "room_id":
    "room-a"}` and assert `msg.get("success") is False`. Use the exact body in
    15-PATTERNS.md §tests/test_websocket.py "Test to rewrite".
    (3) Remove the `"room_mode": "global"` key from the fixture dict at line
    ~1147 and update the line-~1164 assertion to drop any `room_mode` field
    check (assert only the surviving fields).
    (4) Remove the `"room_mode": "custom"` key from the test payload at line
    ~1186 and remove the `room_mode` assertion at line ~1197.
    After this task there must be ZERO occurrences of `room_mode` in
    test_websocket.py.
  </action>
  <verify>
    <automated>grep -c 'room_mode' tests/test_websocket.py | grep -qx 0 && make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'room_mode' tests/test_websocket.py` outputs `0`
    - tests/test_websocket.py contains `def test_ws_reset_room_to_default_zone_program_is_removed`
    - tests/test_websocket.py does NOT contain `_copies_into_room`
    - `make test` exits 0
  </acceptance_criteria>
  <done>WS tests confirm the reset command errors and carry no room_mode references; full suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel/automation → WebSocket API | Untrusted command payloads cross into `ws_set_room_config` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | ws_set_room_config payload | mitigate | `incoming_config.pop("room_mode", None)` silently drops the deprecated key so a residual frontend or automation cannot re-persist room_mode (D-07) |
| T-15-02 | Elevation | removed reset command | accept | Command deletion strictly reduces attack surface; no new path introduced |
</threat_model>

<verification>
- `grep -c '_make_ws_reset_room_to_default_zone_program' custom_components/climate_manager/websocket.py` → `0`
- `grep -c 'incoming_config.pop("room_mode", None)' custom_components/climate_manager/websocket.py` ≥ 1
- `grep -c 'room_mode' tests/test_websocket.py` → `0`
- `make test` exits 0
</verification>

<success_criteria>
- reset_room_to_default_zone_program WS command removed (success criterion 5)
- set_room_config no longer accepts/persists room_mode (D-07)
- Websocket tests pass with no room_mode reference (success criterion 6, slice)
</success_criteria>

<output>
Create `.planning/phases/15-remove-room-custom-scheduling/15-02-SUMMARY.md` when done
</output>
