---
phase: 15-remove-room-custom-scheduling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - custom_components/climate_manager/const.py
  - custom_components/climate_manager/coordinator.py
  - custom_components/climate_manager/storage.py
  - tests/test_storage.py
  - tests/test_coordinator.py
  - tests/test_preheat.py
autonomous: true
requirements: [ARCH-02]

must_haves:
  truths:
    - "D-01: After async_load(), no room record contains room_mode or time_program (lazy read-time compat shim)"
    - "D-02: Storage shim is silent — no log emitted for stripped room_mode fields"
    - "D-03: Zone time_program survives the shim untouched (shim strips only from rooms, not zones)"
    - "D-04: Every room resolves temperature via _resolve_zone_config with no ROOM_MODE_FROST or ROOM_MODE_CUSTOM branch"
    - "D-05: Preheat logic has no room_config.get(room_mode) checks (coordinator lines ~784 and ~869 removed)"
    - "D-14: test_room_mode_* coordinator tests deleted; make test passes with no ROOM_MODE_* imports in coordinator tests"
    - "D-15: Incidental room_mode fixture data removed from surviving coordinator and preheat tests"
  artifacts:
    - path: "custom_components/climate_manager/storage.py"
      provides: "Phase 15 compat shim stripping room_mode + time_program from rooms"
      contains: "room_cfg.pop(\"room_mode\", None)"
    - path: "custom_components/climate_manager/coordinator.py"
      provides: "Zone-only temperature resolution (no room_mode branches)"
    - path: "custom_components/climate_manager/const.py"
      provides: "const.py with ROOM_MODE_* deleted"
    - path: "tests/test_storage.py"
      provides: "Phase 15 shim test"
      contains: "test_load_strips_room_mode_from_room_records"
  key_links:
    - from: "coordinator.py _compute_desired_temps"
      to: "_resolve_zone_config"
      via: "unconditional zone resolution"
      pattern: "_resolve_zone_config\\("
    - from: "storage.py async_load"
      to: "result['rooms'] room records"
      via: "pop room_mode/time_program"
      pattern: "room_cfg\\.pop\\(\"(room_mode|time_program)\""
---

<objective>
Remove the `room_mode` concept from the Python backend core: delete the three
`ROOM_MODE_*` constants, strip every `room_mode`/`time_program` branch from the
coordinator so all rooms resolve through their zone, and add the lazy read-time
storage compat shim that strips `room_mode` and `time_program` from every room
record on load (per D-01/D-02/D-03). Clean the coordinator and preheat tests of
all `room_mode` references and add the Wave 0 storage shim test.

Purpose: ARCH-02 requires `room_mode` removed from coordinator and storage with
a migration shim. The Default Zone / zone model already covers every surviving
case (custom schedule → dedicated zone; frost → zone in MODE_OFF).
Output: const.py, coordinator.py, storage.py, plus test_storage.py (new shim
test), test_coordinator.py and test_preheat.py (cleaned). `make test` green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-remove-room-custom-scheduling/15-CONTEXT.md
@.planning/phases/15-remove-room-custom-scheduling/15-RESEARCH.md
@.planning/phases/15-remove-room-custom-scheduling/15-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Wave 0 storage shim test (RED)</name>
  <files>tests/test_storage.py</files>
  <read_first>
    - tests/test_storage.py (read the Phase 14 shim test
      `test_load_legacy_flat_keys_builds_default_zone` and the `_ALL_DAYS`
      helper near the top to mirror exact fixture style and store API)
    - .planning/phases/15-remove-room-custom-scheduling/15-PATTERNS.md
      (§tests/test_storage.py — the full new-test source to add)
    - custom_components/climate_manager/storage.py (lines 100–200 — confirm
      `async_load` return shape and existing shim chain)
  </read_first>
  <behavior>
    - Test stores config with three room records: room-a has
      {room_mode:"custom", time_program: sentinel, zone_id:"uuid-1"}, room-b
      has {room_mode:"frost_protection"}, room-c has {zone_id:"uuid-1"} only.
    - Also stores a zone "uuid-1" with a time_program equal to the same
      sentinel program.
    - After `await store.async_load()`: every room record has neither
      "room_mode" nor "time_program" keys.
    - The zone "uuid-1" time_program is UNCHANGED (== sentinel) — proves the
      shim scopes to result["rooms"], not result.values() (Pitfall 2).
    - zone_id on room-a and room-c survives.
  </behavior>
  <action>
    Add async test `test_load_strips_room_mode_from_room_records` in
    tests/test_storage.py, inserted after the Phase 14 shim test section. Use
    the exact body provided in 15-PATTERNS.md §tests/test_storage.py "New test
    to add". Use the existing `ClimateManagerStore(hass)` fixture pattern and
    `store._store.async_save(...)` followed by `await store.async_load()`.
    Assert "room_mode" not in room_cfg and "time_program" not in room_cfg for
    every room in result["rooms"], assert
    result["zones"]["uuid-1"]["time_program"] == sentinel, and assert zone_id
    survives on room-a and room-c. This test MUST FAIL initially (the shim does
    not exist yet).
  </action>
  <verify>
    <automated>make test 2>&1 | grep -q "test_load_strips_room_mode_from_room_records" && echo "test present"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_storage.py contains `def test_load_strips_room_mode_from_room_records`
    - Running `make test` collects the test (no collection/import error)
    - The new test FAILS before the storage shim (Task 3) is added
  </acceptance_criteria>
  <done>The Wave 0 storage shim test exists and fails because the shim is not yet implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Delete ROOM_MODE_* constants from const.py</name>
  <files>custom_components/climate_manager/const.py</files>
  <read_first>
    - custom_components/climate_manager/const.py (lines 45–55 — the
      "Per-room mode constants" section header and the three constants)
    - .planning/phases/15-remove-room-custom-scheduling/15-PATTERNS.md
      (§const.py — exact block to delete)
  </read_first>
  <action>
    Delete the section header comment block "Per-room mode constants (D-20)"
    (the `# ---` framed comment) and the three constants `ROOM_MODE_GLOBAL =
    "global"`, `ROOM_MODE_FROST = "frost_protection"`, `ROOM_MODE_CUSTOM =
    "custom"` at lines 50–52. No replacement. Do NOT touch the unrelated
    `(D-20)` reference in the presence section header. This deletion breaks
    coordinator imports until Task 3 runs — Tasks 2 and 3 must land together.
  </action>
  <verify>
    <automated>grep -c 'ROOM_MODE' custom_components/climate_manager/const.py | grep -qx 0 && echo "constants gone"</automated>
  </verify>
  <acceptance_criteria>
    - const.py contains zero occurrences of the string `ROOM_MODE`
    - `grep -c 'ROOM_MODE' custom_components/climate_manager/const.py` outputs `0`
  </acceptance_criteria>
  <done>The three ROOM_MODE_* constants and their section header are removed from const.py.</done>
</task>

<task type="auto">
  <name>Task 3: Strip room_mode branches from coordinator + add storage shim (GREEN)</name>
  <files>custom_components/climate_manager/coordinator.py, custom_components/climate_manager/storage.py</files>
  <read_first>
    - custom_components/climate_manager/coordinator.py (lines 60–75 import
      block; lines 355–425 `_compute_desired_temps`; lines 510–525
      `_apply_presence_overrides`; lines 775–800 and 860–880 preheat steps)
    - custom_components/climate_manager/storage.py (lines 148–200 — GAP-01
      shim, Phase 14 shim, the `else:` day-fill block, and `return result`)
    - .planning/phases/15-remove-room-custom-scheduling/15-PATTERNS.md
      (§coordinator.py and §storage.py — every block to delete/replace)
    - .planning/phases/15-remove-room-custom-scheduling/15-RESEARCH.md
      (§Code Examples — `_compute_desired_temps` after removal, and the shim)
  </read_first>
  <action>
    coordinator.py:
    (1) Remove `ROOM_MODE_FROST` and `ROOM_MODE_CUSTOM` (and `ROOM_MODE_GLOBAL`
    if present) from the `from .const import (...)` block at lines ~68–69.
    (2) In `_compute_desired_temps`: delete the `room_mode = room_config.get(
    "room_mode", "global")` preamble and the entire `if room_mode ==
    ROOM_MODE_FROST:` block (line ~376) and the entire `if room_mode ==
    ROOM_MODE_CUSTOM:` block (line ~404). Delete the now-unused `room_config =
    config.get("rooms", {}).get(area_id, {})` line that fed them. KEEP the
    `if zone_mode == MODE_OFF:` block intact. Every room must fall through to
    `_resolve_zone_config(area_id, config)` and the zone schedule path.
    (3) Delete the docstring bullet points referencing `room_mode` priorities
    (EVAL-05 / SCHED-05) at lines ~360–366.
    (4) In `_apply_presence_overrides`: delete the
    `if room_config.get("room_mode", "global") == ROOM_MODE_CUSTOM: continue`
    guard at line ~517 (and its now-unused `room_config` lookup if that lookup
    serves nothing else).
    (5) In `_async_preheat_room` step 2 (line ~784): replace the
    `if room_cfg_step2.get("room_mode") == ROOM_MODE_CUSTOM: ... else: _,
    tp_step2 = self._resolve_zone_config(...)` fork with the single line
    `_, tp_step2 = self._resolve_zone_config(area_id, config)`.
    (6) In `_async_preheat_room` step 4 (line ~869): replace the analogous
    `if room_cfg.get("room_mode") == ROOM_MODE_CUSTOM: ... else: _,
    time_program = self._resolve_zone_config(...)` fork with the single line
    `_, time_program = self._resolve_zone_config(area_id, config)`.
    Do NOT add any defensive `if room_cfg.get("room_mode")` guards anywhere
    (RESEARCH Anti-Patterns) — the shim guarantees absence.

    storage.py: In `async_load`, insert the Phase 15 compat shim AFTER the
    Phase 14 `else:` day-fill block (after line ~199) and BEFORE
    `return result` (line ~200). The shim iterates
    `result.get("rooms", {}).values()` (NEVER `result.values()` — Pitfall 2)
    and calls `room_cfg.pop("room_mode", None)` and
    `room_cfg.pop("time_program", None)`. Silent — no log (D-02). Use the exact
    snippet from 15-PATTERNS.md §storage.py "Phase 15 shim to insert".
  </action>
  <verify>
    <automated>grep -rc 'ROOM_MODE\|room_mode' custom_components/climate_manager/coordinator.py | grep -qx 0 && grep -q 'room_cfg.pop("room_mode", None)' custom_components/climate_manager/storage.py && make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ROOM_MODE\|room_mode' custom_components/climate_manager/coordinator.py` outputs `0`
    - storage.py contains `room_cfg.pop("room_mode", None)` and `room_cfg.pop("time_program", None)`
    - storage.py shim iterates `result.get("rooms", {}).values()` (the iteration line contains `.get("rooms"`)
    - `make test` exits 0 (the Task 1 storage shim test now passes)
  </acceptance_criteria>
  <done>Coordinator has no room_mode branches, every room resolves via zone; storage shim strips room_mode/time_program on load; the Wave 0 shim test passes.</done>
</task>

<task type="auto">
  <name>Task 4: Clean room_mode references from coordinator + preheat tests</name>
  <files>tests/test_coordinator.py, tests/test_preheat.py</files>
  <read_first>
    - tests/test_coordinator.py (lines 28–48 import block + ALL_DAYS_COMFORT
      fixture; the nine room_mode test functions enumerated in PATTERNS)
    - tests/test_preheat.py (line 34 import block; line ~531 fixture)
    - .planning/phases/15-remove-room-custom-scheduling/15-PATTERNS.md
      (§tests/test_coordinator.py and §tests/test_preheat.py — exact deletions)
    - .planning/phases/15-remove-room-custom-scheduling/15-RESEARCH.md
      (§Detailed Touch Point Map → Tests, and Open Questions 2 & 3 confirming
      delete-not-rewrite for frost/global/absent tests)
  </read_first>
  <action>
    tests/test_coordinator.py:
    (1) Delete the three imports `ROOM_MODE_GLOBAL`, `ROOM_MODE_FROST`,
    `ROOM_MODE_CUSTOM` from the const import block (lines 39–41).
    (2) Delete these nine test functions entirely:
    `test_room_mode_frost_protection_pushes_frost_temp`,
    `test_room_mode_custom_uses_room_time_program`,
    `test_room_mode_global_explicit_key_uses_global_program`,
    `test_room_mode_absent_key_uses_global_program`,
    `test_room_mode_frost_wins_over_stale_time_program`,
    `test_room_mode_frost_wins_over_presence`,
    `test_zone_off_overrides_room_mode_custom_default_zone`,
    `test_zone_off_overrides_room_mode_custom_custom_zone`,
    `test_room_mode_custom_wins_over_active_zone_schedule`.
    (3) Update/remove the line-45 comment referencing custom room_mode. Keep
    `ALL_DAYS_COMFORT_PROGRAM` only if a surviving test still references it;
    if no surviving test references it after deletions, remove it too.
    (4) Remove any remaining incidental `room_mode` keys in surviving fixtures.
    After this task there must be ZERO occurrences of `room_mode` or
    `ROOM_MODE` in test_coordinator.py.

    tests/test_preheat.py:
    (5) Remove the `ROOM_MODE_FROST` import (line 34).
    (6) Delete the `"room_mode": ROOM_MODE_FROST,` fixture line (~531) entirely
    — frost-locked preheat rooms are gated by `self._frost_locked_rooms`, not
    room_mode, so the test still validates the same behavior without it.
  </action>
  <verify>
    <automated>grep -rc 'ROOM_MODE\|room_mode' tests/test_coordinator.py tests/test_preheat.py | grep -v ':0$' | grep -q . && echo "FAIL refs remain" || make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'ROOM_MODE\|room_mode' tests/test_coordinator.py` outputs `0`
    - `grep -c 'ROOM_MODE\|room_mode' tests/test_preheat.py` outputs `0`
    - `make test` exits 0
  </acceptance_criteria>
  <done>No room_mode/ROOM_MODE references remain in test_coordinator.py or test_preheat.py; full suite passes.</done>
</task>

</tasks>

<verification>
- `grep -rc 'ROOM_MODE' custom_components/climate_manager/const.py custom_components/climate_manager/coordinator.py` → all `0`
- `grep -c 'room_cfg.pop("room_mode", None)' custom_components/climate_manager/storage.py` ≥ 1
- `make test` exits 0
- `make lint` passes (no unused imports / F401)
</verification>

<success_criteria>
- ROOM_MODE_* constants deleted from const.py (success criterion 2)
- room_mode absent from coordinator runtime paths (success criterion 1)
- Storage shim strips room_mode + time_program on load (success criterion 4)
- All backend/coordinator/preheat tests pass with no room_mode reference
  (success criterion 6, this plan's slice)
</success_criteria>

<output>
Create `.planning/phases/15-remove-room-custom-scheduling/15-01-SUMMARY.md` when done
</output>
