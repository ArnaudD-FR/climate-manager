---
phase: 15-remove-room-custom-scheduling
plan: 03
type: execute
wave: 3
depends_on: ["15-02"]
files_modified:
  - frontend/src/types.ts
  - frontend/src/components/room-card.ts
  - frontend/src/ws-client.ts
autonomous: true
requirements: [ARCH-02]

must_haves:
  truths:
    - "D-13: RoomConfig TypeScript interface has no room_mode or time_program field"
    - "D-08: room-card.ts renders no mode select element (_onModeChange and _onResetToGlobal deleted)"
    - "D-09: room-card.ts renders no inline time-bar for resolvedMode === custom"
    - "D-10: room-card.ts has no _renderRoomModeDescription method"
    - "D-11: room-card.ts renders no mode badge; active period badge and zone name badge survive unchanged"
    - "D-12: room-card.ts _expanded no longer defaults to true for custom mode"
    - "D-06: ws-client.ts has no resetRoomToDefaultZoneProgram method"
    - "make build succeeds with no TypeScript errors"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "RoomConfig without room_mode/time_program"
    - path: "frontend/src/components/room-card.ts"
      provides: "Room card with mode UI removed, zone/period badges intact"
    - path: "frontend/src/ws-client.ts"
      provides: "ws-client without the reset-room method"
  key_links:
    - from: "room-card.ts render()"
      to: "zone-badge + _renderPeriodBadge"
      via: "surviving badges read zone_id and active_period, not room_mode"
      pattern: "zone-badge|_renderPeriodBadge"
---

<objective>
Remove the `room_mode` UI from the frontend: drop the `room_mode` and
`time_program` fields from the `RoomConfig` TS interface (D-13), delete the
mode `<select>`, inline time-bar, mode description method, mode badge, and the
mode/reset handlers from `room-card.ts` (D-08..D-12), and delete the now-orphan
`resetRoomToDefaultZoneProgram()` from `ws-client.ts`. Keep the active period
status badge and zone name badge (D-11) — they read `StatusPayload`/`zone_id`,
not `room_mode`.

Purpose: ARCH-02 requires the room mode picker and inline time-bar removed from
the Rooms tab and the type contract cleaned.
Output: types.ts, room-card.ts, ws-client.ts. `make build` succeeds.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-remove-room-custom-scheduling/15-CONTEXT.md
@.planning/phases/15-remove-room-custom-scheduling/15-RESEARCH.md
@.planning/phases/15-remove-room-custom-scheduling/15-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove room_mode/time_program from RoomConfig and the reset ws-client method</name>
  <files>frontend/src/types.ts, frontend/src/ws-client.ts</files>
  <read_first>
    - frontend/src/types.ts (lines 50–71 — the `RoomConfig` interface; the
      `room_mode?` field at line 59 and the `time_program?` field)
    - frontend/src/ws-client.ts (lines 143–166 — the surviving
      `resetZoneTimeProgram` method and the orphan
      `resetRoomToDefaultZoneProgram` method at 158–166)
    - .planning/phases/15-remove-room-custom-scheduling/15-PATTERNS.md
      (§frontend/src/types.ts and §frontend/src/ws-client.ts — exact lines)
  </read_first>
  <action>
    types.ts: In the `RoomConfig` interface, delete the `room_mode?: "global" |
    "frost_protection" | "custom";` field (line ~59) together with its JSDoc
    comment block, and delete the `time_program?: DailyProgram | null;` field
    (line ~60). Keep `zone_id?` and `preheat_max_lead_minutes?`. If
    `DailyProgram` becomes an unused import after this, leave the import only if
    other types in the file still reference it — verify with a grep before
    removing.

    ws-client.ts: Delete the entire `resetRoomToDefaultZoneProgram(roomId:
    string)` method and its JSDoc (lines ~158–166). KEEP `resetZoneTimeProgram`
    (the zone-level method) untouched.
  </action>
  <verify>
    <automated>grep -rc 'room_mode\|resetRoomToDefaultZoneProgram' frontend/src/types.ts frontend/src/ws-client.ts | grep -v ':0$' | grep -q . && echo "FAIL refs remain" || echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'room_mode' frontend/src/types.ts` outputs `0`
    - `grep -c 'resetRoomToDefaultZoneProgram' frontend/src/ws-client.ts` outputs `0`
    - frontend/src/ws-client.ts still contains `resetZoneTimeProgram`
  </acceptance_criteria>
  <done>RoomConfig has no room_mode/time_program; ws-client has no reset-room method (zone reset preserved).</done>
</task>

<task type="auto">
  <name>Task 2: Strip mode select, time-bar, description, badge, and handlers from room-card.ts</name>
  <files>frontend/src/components/room-card.ts</files>
  <read_first>
    - frontend/src/components/room-card.ts (line 60 `_expanded` JSDoc; lines
      337–363 `_onRoomModeChange`; lines 399–407 `_onResetToGlobal`; lines
      454–458 `_renderPeriodBadge` head; lines 642–652
      `_renderRoomModeDescription`; lines 1027–1041 render() computed values;
      lines 1054–1059 mode badge; lines 1085–1118 Mode section; lines
      1145–1168 inline time-bar conditional)
    - .planning/phases/15-remove-room-custom-scheduling/15-PATTERNS.md
      (§frontend/src/components/room-card.ts — every block to delete, with the
      surviving zone-badge and period-badge analogs)
    - .planning/phases/15-remove-room-custom-scheduling/15-RESEARCH.md
      (Pitfall 5 — remove the dead frost guard in `_renderPeriodBadge`)
  </read_first>
  <action>
    (1) Delete the `_onRoomModeChange()` method (lines ~337–363).
    (2) Delete the `_onResetToGlobal()` method (lines ~399–407). This is the
    room-card method only — do NOT touch the unrelated `_onResetToGlobal` in
    zone-tab.ts.
    (3) Delete the `_renderRoomModeDescription()` method (lines ~642–652).
    (4) In `_renderPeriodBadge()` (lines ~454–456): delete the
    `const resolvedMode = this.config?.room_mode ?? "global";` line and the
    `if (resolvedMode === "frost_protection") return html\`\`;` guard. The
    method body continues at the existing `globalMode` check.
    (5) In `render()`: delete the `resolvedMode`, `badgeClass`, and `badgeText`
    computed consts (lines ~1027–1040); delete the mode badge `<span
    class="program-badge ${badgeClass}">...</span>` element (lines ~1054–1059);
    delete the Mode section (section-label "Mode" + `select-wrapper` with the
    `.mode-select` and its three `<option>`s + the
    `${this._renderRoomModeDescription(resolvedMode)}` call) at lines
    ~1085–1118; delete the inline time-bar conditional block
    `${resolvedMode === "custom" ? html\`...reset-btn...\` : ""}` at lines
    ~1145–1168 (including the Reset button calling `_onResetToGlobal`).
    (6) Update the `_expanded` JSDoc (line 60) from "Defaults to true when
    room_mode is 'custom'." to "Defaults to collapsed." The `_expanded = false`
    default stays. Do NOT touch the unrelated `autoExpand` property.
    KEEP the zone-badge `<span class="zone-badge">` and the
    `_renderPeriodBadge()` call — these survive unchanged.
    After this task there must be ZERO occurrences of `room_mode`,
    `resolvedMode`, `_onRoomModeChange`, `_renderRoomModeDescription`, or
    `_onResetToGlobal` in room-card.ts.
  </action>
  <verify>
    <automated>grep -c 'room_mode\|resolvedMode\|_onRoomModeChange\|_renderRoomModeDescription\|_onResetToGlobal' frontend/src/components/room-card.ts | grep -qx 0 && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'room_mode\|resolvedMode\|_onRoomModeChange\|_renderRoomModeDescription\|_onResetToGlobal' frontend/src/components/room-card.ts` outputs `0`
    - room-card.ts still contains `zone-badge` and `_renderPeriodBadge`
    - room-card.ts does NOT contain `mode-select`
  </acceptance_criteria>
  <done>Room card has no mode picker, inline time-bar, mode description, mode badge, or mode/reset handlers; zone and period badges remain.</done>
</task>

<task type="auto">
  <name>Task 3: Build frontend and confirm zero TypeScript errors</name>
  <files>frontend/dist/panel.js</files>
  <read_first>
    - Makefile (read the `build` target to confirm it compiles TS via Vite)
    - frontend/src/components/room-card.ts (confirm no dangling references to
      deleted members after Tasks 1–2)
  </read_first>
  <action>
    Run `make build`. Resolve any TypeScript compile error surfaced by the
    deletions (e.g. an unused import of `DailyProgram` in types.ts, or a
    leftover reference to a deleted method). Do NOT add new behavior — only
    remove dead references the compiler flags. Do not run `make deploy` or
    `make screenshots` here; deployment and human verification are Plan 04.
  </action>
  <verify>
    <automated>make build</automated>
  </verify>
  <acceptance_criteria>
    - `make build` exits 0 with no TypeScript errors
    - frontend/dist/panel.js is regenerated (newer mtime than the source edits)
  </acceptance_criteria>
  <done>The frontend bundle builds cleanly with the room_mode UI removed.</done>
</task>

</tasks>

<verification>
- `grep -rc 'room_mode' frontend/src/types.ts frontend/src/components/room-card.ts` → all `0`
- `grep -c 'resetRoomToDefaultZoneProgram' frontend/src/ws-client.ts` → `0`
- room-card.ts retains `zone-badge` and `_renderPeriodBadge`
- `make build` exits 0
</verification>

<success_criteria>
- room_mode field removed from RoomConfig (success criterion implied by ARCH-02)
- Room mode picker and inline time-bar removed from Rooms tab (success criterion 3)
- Active period status and zone name preserved (D-11)
- Frontend builds with no TypeScript errors
</success_criteria>

<output>
Create `.planning/phases/15-remove-room-custom-scheduling/15-03-SUMMARY.md` when done
</output>
