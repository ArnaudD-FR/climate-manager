---
context: phase
phase: 15-remove-room-custom-scheduling
task: 2
total_tasks: 2
status: in_progress
last_updated: 2026-06-04T15:49:57
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: BAD-MERGE-BASE — Phase 15 worktree was branched from pre-Phase-14 commit (e21deb5, 813-line room-card.ts). Merge conflict resolved with `git checkout --theirs` silently discarded ALL Phase 14 additions to frontend/src/types.ts, frontend/src/ws-client.ts, frontend/src/components/room-card.ts. Both regressions (missing preheat section, missing climate entities group) were caused by this. Fix was deployed.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Wrong worktree base commit | Phase 15 worktree branched from formatter commit (e21deb5, 2026-05-29) instead of Phase 14 final commit (bc28490, 2026-06-04). Phase 14 had 4 days of additions not in the worktree base. | blocking | Always verify worktree base is HEAD of main before branching. Check line counts of key files. |
| Blanket --theirs merge resolution | Using `git checkout --theirs` on frontend files accepted the worktree (old) version, discarding main (newer) changes. | advisory | Never use --theirs/--ours on files with legitimate changes on both sides without diffing first. |

<current_state>
Phase 15 Plan 04 (15-04), Task 2 of 2: human-verify checkpoint.

Task 1 completed: room_mode sweep clean, tests green, build+deploy done.
Task 2 regression fixes deployed at 14:32 CEST June 4:
- FIXED: types.ts, ws-client.ts, room-card.ts rebuilt from Phase 14 base (bc28490) with Phase 15 removals.
- Fixes restore: preheat section in room cards, Tado X / Matter entity grouping with DnD.
- make build: 186.88 kB bundle clean.
- make deploy: HA restarted at 14:32.

User then asked "Why bureau is not pre heating?" — investigated and found root cause:
NOT a code bug. Preheat for home_office (bureau) fires based on safa's scheduled arrival (midnight) as next_occupied, NOT the zone's 15:15 comfort period. The fallback to next_setpoint_increase_at only runs when next_arrival=None. Since safa has a non-None arrival time, the 13:15 trigger window for the 15:15 period never fires.

User has NOT yet typed "approved" to complete Task 2.
</current_state>

<completed_work>

Completed Tasks:
- Task 1: Repo-wide room_mode sweep + full suite + deploy — Done (commit 78c2b13)
- Task 2: In progress — regressions found and fixed, deployed

Regression fixes applied this session:
- frontend/src/types.ts: restored Phase 14 fields (default_zone, preheat_*, zones dict, matter_*); removed room_mode/time_program (Phase 15)
- frontend/src/ws-client.ts: removed setGlobalMode/resetTimeProgram; added setZonePreheat, setMatterMapping, suggestMatterMappings
- frontend/src/components/room-card.ts: restored _renderClimateSection (Tado X/Matter DnD grouping) and _renderPreheatSection; removed mode select/time-bar/mode badge/mode handlers
</completed_work>

<remaining_work>

- Task 2: User needs to type "approved" (visual verification sign-off)
- Write 15-04-SUMMARY.md
- Run phase verifier (gsd-verify-phase or equivalent)
- Update .planning/STATE.md and ROADMAP.md to mark phase 15 complete
- Commit all uncommitted files (frontend fixes + STATE + PATTERNS)
</remaining_work>

<decisions_made>

- Chose to rebuild 3 frontend files from bc28490 (Phase 14 final) as base, then reapply Phase 15 removals on top — correct approach since Phase 15's actual changes were only deletions, not additions
- Bureau preheat root cause: not a code bug, not a Phase 15 regression. Preheat logic uses person arrival priority over zone schedule fallback. Safa's midnight arrival dominates.
</decisions_made>

<blockers>
- Waiting for user to type "approved" on Task 2 visual checkpoint before phase can be completed
</blockers>

## Required Reading (in order)
1. `.planning/phases/15-remove-room-custom-scheduling/15-04-PLAN.md` — Task 2 verification steps
2. `.planning/phases/15-remove-room-custom-scheduling/15-VALIDATION.md` — phase success criteria

## Infrastructure State
- HA instance: running at homeassistant.local, restarted 14:32 CEST June 4
- panel.js deployed: /config/custom_components/climate_manager/www/panel.js (186.88 kB, 2026-06-04 14:32:12)
- climate_manager coordinator: running, first eval at 14:32:45 (calendar entity warnings confirm)
- Uncommitted: frontend/src/components/room-card.ts, types.ts, ws-client.ts, .planning/STATE.md, 15-PATTERNS.md, deleted www/.gitkeep

<context>
Phase 15 is functionally complete — all code changes were done in Plans 01-03. Plan 04 is the gate plan (sweep + test + deploy + human sign-off). The human sign-off was delayed by two regressions found after deploy, both caused by a bad merge. Regressions are now fixed and redeployed. Waiting for user confirmation.

The bureau preheat question was investigated and answered: it's a config/design behavior, not a Phase 15 regression. Safa's scheduled midnight arrival takes priority over the zone schedule's 15:15 comfort period trigger. The user may want to remove safa from home_office room_ids if the office isn't her room, which would let the schedule-based fallback fire at 13:15.
</context>

<next_action>
Start with: "Have you had a chance to verify the panel? Check that the preheat section and climate entities group are back in room cards. Type 'approved' to complete Phase 15, or describe any remaining issue."
Then: write 15-04-SUMMARY.md, run verifier, update STATE.md/ROADMAP.md, commit all.
</next_action>
