---
phase: 15-remove-room-custom-scheduling
plan: 04
type: execute
wave: 4
depends_on: ["15-03"]
files_modified: []
autonomous: false
requirements: [ARCH-02]

must_haves:
  truths:
    - "Full test suite passes with no room_mode reference anywhere in the repo"
    - "The deployed panel Rooms tab shows no mode picker, inline time-bar, or mode badge"
    - "The deployed panel Rooms tab still shows active period status and zone name"
  artifacts: []
  key_links:
    - from: "deployed panel.js"
      to: "Home Assistant Rooms tab"
      via: "make deploy"
      pattern: "deploy"
---

<objective>
Final gate for Phase 15: confirm the whole repository is free of `room_mode`
references, run the full test suite, deploy the rebuilt panel, and have the
developer visually verify the Rooms tab. This plan is isolated because it
contains a blocking human-verify checkpoint (no implementation tasks mixed in).

Purpose: ARCH-02 success criterion 6 ("all tests pass with no reference to
room_mode") and the two manual visual verifications recorded in 15-VALIDATION.md.
Output: deployed panel + developer sign-off.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-remove-room-custom-scheduling/15-CONTEXT.md
@.planning/phases/15-remove-room-custom-scheduling/15-VALIDATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Repo-wide room_mode sweep + full suite + deploy</name>
  <files>frontend/dist/panel.js</files>
  <read_first>
    - Makefile (confirm the `test`, `build`, and `deploy` targets)
    - .planning/phases/15-remove-room-custom-scheduling/15-VALIDATION.md
      (Sampling Rate + Manual-Only Verifications)
  </read_first>
  <action>
    (1) Run a repo-wide sweep for any residual references:
    `grep -rn 'room_mode\|ROOM_MODE\|reset_room_to_default_zone_program\|resetRoomToDefaultZoneProgram'
    custom_components/ frontend/src/ tests/`. The only acceptable matches are
    intentional "Removed in Phase 15" docstring/comment notes in websocket.py.
    If any live code/test reference remains, fix it (it indicates a missed
    deletion from Plans 01–03) before proceeding.
    (2) Run `make test` and confirm the full suite is green.
    (3) Run `make build` then `make deploy` to push the rebuilt panel to the
    running Home Assistant instance. Use the Makefile targets only (project
    convention — never hand-roll build/deploy commands).
  </action>
  <verify>
    <automated>grep -rn 'room_mode\|ROOM_MODE\|resetRoomToDefaultZoneProgram' custom_components/ frontend/src/ tests/ | grep -v 'Removed in Phase 15' | grep -v '# .*room_mode' ; test $? -eq 1 && make test</automated>
  </verify>
  <acceptance_criteria>
    - The sweep returns no live-code matches (only intentional removal-note comments allowed)
    - `make test` exits 0 (full suite green)
    - `make deploy` completes without error and panel.js is published
  </acceptance_criteria>
  <done>No residual room_mode references in code/tests; full suite green; panel deployed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Rooms tab</name>
  <files>frontend/src/components/room-card.ts</files>
  <action>
    This is a blocking human-verify checkpoint — no code is written. After Task 1
    has deployed the rebuilt panel, present the verification steps below to the
    developer and pause for their sign-off. Do not proceed to phase completion
    until the developer types "approved" or reports an issue to be fixed.
  </action>
  <what-built>
    The room_mode picker, inline time-bar, mode description, and mode badge were
    removed from the room card. The active period status badge and the zone name
    badge were preserved. The backend strips room_mode/time_program on load and
    no longer reads the field.
  </what-built>
  <how-to-verify>
    1. Open Home Assistant and navigate to the Climate Manager panel → Rooms tab.
    2. Expand a room that previously used a custom or frost room mode.
    3. Confirm there is NO mode `<select>` dropdown (no "Zone program" /
       "Off (frost)" / "Custom program" options).
    4. Confirm there is NO inline time-bar / schedule editor inside the room
       card and NO "Reset to global configuration" button.
    5. Confirm there is NO mode badge chip in the room header (no "custom" /
       "frost" / "Zone program" chip).
    6. Confirm the room header STILL shows the active period status (e.g.
       "Normal — 20°C") and the zone name chip.
    7. Confirm rooms now follow their zone schedule (a room formerly on a custom
       schedule should reflect its zone's program).
  </how-to-verify>
  <verify>
    <human-check>Developer confirms the Rooms tab shows no mode UI and retains the period status + zone name badges.</human-check>
  </verify>
  <resume-signal>Type "approved" or describe any UI issue observed</resume-signal>
  <done>Developer has visually confirmed the room card has no mode picker, inline time-bar, or mode badge, and that the active period status and zone name remain.</done>
</task>

</tasks>

<verification>
- Repo-wide sweep returns no live room_mode references (removal-note comments only)
- `make test` exits 0
- Panel deployed via `make deploy`
- Developer confirms Rooms tab has no mode UI and retains period/zone badges
</verification>

<success_criteria>
- All tests pass with no reference to room_mode (success criterion 6, full)
- Room card UI verified: no mode picker / time-bar / mode badge; period status
  and zone name intact (success criterion 3)
</success_criteria>

<output>
Create `.planning/phases/15-remove-room-custom-scheduling/15-04-SUMMARY.md` when done
</output>
