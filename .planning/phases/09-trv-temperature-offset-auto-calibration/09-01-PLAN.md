---
phase: 09-trv-temperature-offset-auto-calibration
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - custom_components/climate_manager/trv.py
  - tests/test_trv.py
autonomous: true
requirements: [CALIB-02, CALIB-03]
must_haves:
  truths:
    - "supports_offset_calibration returns True when a TRV exposes a temperature_offset attribute"
    - "supports_offset_calibration returns True when the tado_x.set_temperature_offset service is registered even if the attribute is absent"
    - "supports_offset_calibration returns False when neither attribute nor service is present, and when entity state is None"
    - "set_trv_offset issues exactly one tado_x.set_temperature_offset service call with the correct entity_id and offset"
    - "set_trv_offset silently returns for missing or unavailable entities (no service call)"
  artifacts:
    - path: "custom_components/climate_manager/trv.py"
      provides: "supports_offset_calibration capability guard and set_trv_offset service helper"
      contains: "def supports_offset_calibration"
    - path: "tests/test_trv.py"
      provides: "Unit tests for both new functions (7 cases)"
      contains: "supports_offset_calibration"
  key_links:
    - from: "set_trv_offset"
      to: "tado_x.set_temperature_offset"
      via: "hass.services.async_call(blocking=True)"
      pattern: "async_call\\(\\s*\"tado_x\""
    - from: "supports_offset_calibration"
      to: "hass.services.has_service"
      via: "service registry check (secondary guard)"
      pattern: "has_service\\(\"tado_x\""
---

<objective>
Add the two TRV-level primitives the calibration engine depends on:
`supports_offset_calibration()` (capability guard, D-08/CALIB-03) and
`set_trv_offset()` (service-call helper, CALIB-02) to `trv.py`. Both follow
existing analogs in the same file exactly (`supports_hvac_off`, `set_trv_off`).

This is the contract-definition plan: Plan 02 (coordinator) consumes both
functions. They are built first so the coordinator has stable signatures.

Purpose: Provide brand-agnostic offset capability detection and a single-call
offset applier, so the coordinator can calibrate without knowing TRV brand
specifics.
Output: Two new functions in `trv.py` plus 7 passing unit tests in
`tests/test_trv.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-trv-temperature-offset-auto-calibration/09-CONTEXT.md
@.planning/phases/09-trv-temperature-offset-auto-calibration/09-RESEARCH.md
@.planning/phases/09-trv-temperature-offset-auto-calibration/09-PATTERNS.md
@.planning/phases/09-trv-temperature-offset-auto-calibration/09-VALIDATION.md

# Existing analogs to mirror exactly:
@custom_components/climate_manager/trv.py
@tests/test_trv.py
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Write failing tests for both new functions</name>
  <files>tests/test_trv.py</files>
  <action>
Add 7 failing test functions to tests/test_trv.py that import
supports_offset_calibration and set_trv_offset from
custom_components.climate_manager.trv (the import itself will fail until Task 2,
which is the RED state). Mirror the existing supports_hvac_off / set_trv_off
test structure in this file. Use async_mock_service(hass, "tado_x",
"set_temperature_offset") to capture offset calls, the same way existing tests
use async_mock_service(hass, "climate", "set_hvac_mode").

Tests for supports_offset_calibration (CALIB-03):
  - returns True when a TRV state has a temperature_offset attribute present
  - returns True when the attribute is absent but tado_x.set_temperature_offset
    service is registered (register via async_mock_service)
  - returns False when neither the attribute nor the service is present
  - returns False when hass.states.get returns None

Tests for set_trv_offset (CALIB-02):
  - issues exactly one tado_x.set_temperature_offset call with the correct
    entity_id and offset value (assert len(calls) == 1 and call data matches)
  - silently skips (zero calls) when the entity state is "unavailable"
  - silently skips (zero calls) when hass.states.get returns None

Do NOT implement the production functions in this task. Run the suite and
confirm these tests FAIL (RED) due to ImportError. Commit:
test(09-01): add failing tests for offset calibration TRV helpers
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_trv.py -q 2>&1 | grep -qE "ImportError|cannot import name|[1-9][0-9]* (failed|error)" && echo RED-OK</automated>
  </verify>
  <done>7 new tests exist and the file fails to import supports_offset_calibration / set_trv_offset (RED state confirmed).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement supports_offset_calibration and set_trv_offset</name>
  <files>custom_components/climate_manager/trv.py</files>
  <behavior>
    - supports_offset_calibration(hass, entity_id) -> bool: None state -> False;
      temperature_offset attribute present -> True; else
      hass.services.has_service("tado_x", "set_temperature_offset")
    - set_trv_offset(hass, entity_id, offset): None/"unavailable" -> return;
      else one tado_x.set_temperature_offset call with {entity_id, offset}
  </behavior>
  <action>
Implement both functions in custom_components/climate_manager/trv.py.

supports_offset_calibration (sync, mirrors supports_hvac_off at lines 80-94 per
09-PATTERNS.md, D-08/CALIB-03):
  - Signature: def supports_offset_calibration(hass: HomeAssistant, entity_id:
    str) -> bool
  - First guard: state = hass.states.get(entity_id); if state is None: return
    False (ROOM-03 parity — no state.state == "unavailable" check, matching
    supports_hvac_off which only guards None; capability can be detected while
    unavailable)
  - Per D-08 and Pitfall 3: check the attribute FIRST (brand-agnostic), service
    SECOND (Tado X specific). if "temperature_offset" in state.attributes:
    return True; then return hass.services.has_service("tado_x",
    "set_temperature_offset")
  - Docstring documents every False-return condition (None state; attribute
    absent AND service unregistered)
  - Never raises

set_trv_offset (async, mirrors set_trv_off at lines 97-116 per 09-PATTERNS.md,
CALIB-02):
  - Signature: async def set_trv_offset(hass: HomeAssistant, entity_id: str,
    offset: float) -> None
  - Availability guard: state = hass.states.get(entity_id); if state is None or
    state.state == "unavailable": return
  - Single call: await hass.services.async_call("tado_x",
    "set_temperature_offset", {"entity_id": entity_id, "offset": offset},
    blocking=True)
  - Service data parameter name is "offset" per 09-RESEARCH.md A1 (inferred from
    417-SUMMARY). Keep "offset" as named in the test from Task 1 — do not guess
    alternatives; if live HA testing in Plan 04 reveals a different param name,
    fix it there.
  - Docstring notes: caller (coordinator) is responsible for the capability
    guard before calling; silently skips None/"unavailable" (ROOM-03 parity).

Comply with .editorconfig: 4-space indent, max 80 chars/line, LF, final newline,
no trailing whitespace. Run make lint before commit. Commit:
feat(09-01): add supports_offset_calibration and set_trv_offset
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_trv.py -q 2>&1 | tail -3</automated>
  </verify>
  <done>All 7 new tests pass (GREEN). make lint passes. Both functions exported from trv.py with correct signatures.</done>
</task>

</tasks>

<verification>
- `.venv/bin/python -m pytest tests/test_trv.py -v` — all tests green
- `make lint` — editorconfig clean
- `grep -n "def supports_offset_calibration\|async def set_trv_offset" custom_components/climate_manager/trv.py` returns both
</verification>

<success_criteria>
- supports_offset_calibration and set_trv_offset exist in trv.py with the exact
  signatures Plan 02 consumes
- 7 new unit tests pass; full test_trv.py suite remains green
- Attribute-first / service-second guard order (D-08, Pitfall 3) is implemented
- set_trv_offset makes exactly one service call and skips unavailable entities
</success_criteria>

<output>
Create `.planning/phases/09-trv-temperature-offset-auto-calibration/09-01-SUMMARY.md` when done.
</output>
