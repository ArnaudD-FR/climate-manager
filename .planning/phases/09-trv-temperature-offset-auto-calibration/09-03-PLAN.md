---
phase: 09-trv-temperature-offset-auto-calibration
plan: 03
type: tdd
wave: 1
depends_on: []
files_modified:
  - custom_components/climate_manager/websocket.py
  - tests/test_websocket.py
autonomous: true
requirements: [CALIB-01]
must_haves:
  truths:
    - "Sending climate_manager/set_calibration_config {enabled: true} persists calibration_enabled=True in runtime_config and saves the store"
    - "Sending {enabled: false} persists calibration_enabled=False"
    - "A non-bool enabled payload is rejected by the voluptuous schema before the handler runs"
    - "The handler does NOT trigger coordinator.async_evaluate after saving (Pitfall 4)"
  artifacts:
    - path: "custom_components/climate_manager/websocket.py"
      provides: "_make_ws_set_calibration_config factory and its registration"
      contains: "set_calibration_config"
    - path: "tests/test_websocket.py"
      provides: "WS command persistence tests (2 cases)"
      contains: "set_calibration_config"
  key_links:
    - from: "_make_ws_set_calibration_config"
      to: "runtime_config[calibration_enabled]"
      via: "single-key mutation then store.async_save"
      pattern: "calibration_enabled.*=.*msg\\[.enabled.\\]"
    - from: "async_register_commands"
      to: "_make_ws_set_calibration_config"
      via: "websocket_api.async_register_command"
      pattern: "_make_ws_set_calibration_config\\(entry\\)"
---

<objective>
Add the WebSocket command `climate_manager/set_calibration_config` that persists
the calibration toggle (D-10, CALIB-01). It follows the simplest write-command
analog (`_make_ws_set_global_mode`) but deliberately OMITS the
`async_evaluate()` trigger — calibration config affects the next scheduled cycle
only (Pitfall 4).

This plan owns `websocket.py` exclusively and has no dependency on Plans 01/02,
so it runs in Wave 1 alongside Plan 01.

Purpose: Give the frontend (Plan 04) a backend endpoint to flip
calibration_enabled and have it survive restarts.
Output: One new WS command factory + registration, 2 passing unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-trv-temperature-offset-auto-calibration/09-CONTEXT.md
@.planning/phases/09-trv-temperature-offset-auto-calibration/09-RESEARCH.md
@.planning/phases/09-trv-temperature-offset-auto-calibration/09-PATTERNS.md
@.planning/phases/09-trv-temperature-offset-auto-calibration/09-VALIDATION.md

# Analogs to mirror exactly:
@custom_components/climate_manager/websocket.py
@tests/test_websocket.py
</context>

<tasks>

<task type="auto">
  <name>Task 1 (Wave 0): Write failing tests for set_calibration_config</name>
  <files>tests/test_websocket.py</files>
  <action>
Add 2 failing tests to tests/test_websocket.py following the existing WS test
pattern (send a WS message via the test client, then assert runtime_config
mutation), modeled on the existing set_global_mode test.

  1. Send {"type": "climate_manager/set_calibration_config", "enabled": true};
     assert result success and runtime_config["calibration_enabled"] is True
     and store.async_save was called (persisted).
  2. Send {"enabled": false}; assert runtime_config["calibration_enabled"] is
     False.

These will fail RED because the command is not registered yet (the server will
return an unknown_command error). Confirm RED. Commit:
test(09-03): add failing tests for set_calibration_config WS command
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_websocket.py -q -k calibration 2>&1 | grep -qE "[1-9][0-9]* (failed|error)" && echo RED-OK</automated>
  </verify>
  <done>2 calibration WS tests exist and fail RED (command not yet registered).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2 (GREEN): Implement and register _make_ws_set_calibration_config</name>
  <files>custom_components/climate_manager/websocket.py</files>
  <behavior>
    - Schema: type = climate_manager/set_calibration_config, vol.Required("enabled"): bool
    - Handler: runtime_config["calibration_enabled"] = msg["enabled"]; await
      store.async_save(runtime_config); send_result success
    - NO async_create_task(coordinator.async_evaluate())
  </behavior>
  <action>
Add _make_ws_set_calibration_config(entry) following _make_ws_set_global_mode
(websocket.py lines 275-301) exactly, with these differences (09-PATTERNS.md
lines 254-259):
  - Schema: vol.Required("type"): f"{DOMAIN}/set_calibration_config",
    vol.Required("enabled"): bool (V5 input validation, T-03-04 parity — rejects
    non-bool before handler runs)
  - Body: entry.runtime_data.runtime_config["calibration_enabled"] =
    msg["enabled"]; await entry.runtime_data.store.async_save(
    entry.runtime_data.runtime_config); connection.send_result(msg["id"],
    {"success": True})
  - CRITICAL (Pitfall 4 / D-10): do NOT add
    hass.async_create_task(entry.runtime_data.coordinator.async_evaluate()).
    Add a comment: "# NOTE: no async_evaluate trigger — calibration runs on the
    next scheduled cycle (RESEARCH Pitfall 4)". Unlike every other write command,
    this one is fire-and-forget on config.
  - No rollback needed (single boolean top-level key; no ValueError risk).

Register it in async_register_commands by appending, after the last existing
registration (currently around line 110):
  websocket_api.async_register_command(hass,
  _make_ws_set_calibration_config(entry))

Comply with .editorconfig. Run make lint. Commit:
feat(09-03): add set_calibration_config WebSocket command
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_websocket.py -q -k calibration 2>&1 | tail -3</automated>
  </verify>
  <done>Both WS tests pass (GREEN). Command registered. Handler persists the bool and does NOT call async_evaluate. make lint clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend panel -> WS backend | Untrusted `enabled` payload crosses here over the authenticated HA WebSocket connection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | set_calibration_config payload | mitigate | `vol.Required("enabled"): bool` in the WS schema rejects non-bool/missing values before the handler runs (T-03-04 parity) |
| T-09-02 | Spoofing/Tampering | offset target selection | accept | Sensor and TRV entity_ids are read server-side from runtime_config["rooms"], never from the WS payload — no injection surface introduced by this command |
| T-09-03 | Elevation | WS command access | accept | HA WebSocket auth gate (T-03-06) already protects all climate_manager commands; this command adds no new auth surface |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_websocket.py -v` — green
- `grep -n "set_calibration_config" custom_components/climate_manager/websocket.py` shows the schema, factory, and registration
- `grep -n "async_evaluate" custom_components/climate_manager/websocket.py` confirms NO async_evaluate inside ws_set_calibration_config
- `make lint` clean
</verification>

<success_criteria>
- climate_manager/set_calibration_config registered and persists the toggle
- voluptuous schema rejects non-bool enabled (T-09-01)
- Handler omits the async_evaluate trigger (Pitfall 4)
- Both WS tests green; full test_websocket.py suite remains green
</success_criteria>

<output>
Create `.planning/phases/09-trv-temperature-offset-auto-calibration/09-03-SUMMARY.md` when done.
</output>
