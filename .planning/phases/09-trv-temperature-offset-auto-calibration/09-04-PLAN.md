---
phase: 09-trv-temperature-offset-auto-calibration
plan: 04
type: execute
wave: 2
depends_on: ["09-03"]
files_modified:
  - frontend/src/types.ts
  - frontend/src/ws-client.ts
  - frontend/src/components/global-settings-tab.ts
autonomous: false
requirements: [CALIB-01]
must_haves:
  truths:
    - "ClimateConfig type carries optional calibration_enabled and calibration_threshold fields"
    - "ws-client exposes setCalibrationConfig(enabled) calling the set_calibration_config command"
    - "Global Settings tab renders an Options card (third card) with a calibration toggle"
    - "Toggling the switch auto-saves via the WS command and the state survives a page reload"
  artifacts:
    - path: "frontend/src/types.ts"
      provides: "calibration_enabled / calibration_threshold on ClimateConfig"
      contains: "calibration_enabled"
    - path: "frontend/src/ws-client.ts"
      provides: "setCalibrationConfig method"
      contains: "setCalibrationConfig"
    - path: "frontend/src/components/global-settings-tab.ts"
      provides: "_renderOptionsCard with calibration toggle, wired into render()"
      contains: "_renderOptionsCard"
  key_links:
    - from: "global-settings-tab _onCalibrationToggle"
      to: "ws.setCalibrationConfig"
      via: "change event handler -> WS call -> reloadConfig -> toast"
      pattern: "setCalibrationConfig\\("
    - from: "setCalibrationConfig"
      to: "climate_manager/set_calibration_config"
      via: "sendMessagePromise"
      pattern: "set_calibration_config"
---

<objective>
Wire the calibration toggle into the panel UI (CALIB-01, D-11/D-12/D-13).
Extend `ClimateConfig`, add `ws-client.setCalibrationConfig`, and add a new
"Options" ha-card (third card) to the Global Settings tab containing the
auto-saving calibration switch. Then build, deploy, and verify on live HA.

Consumes the `set_calibration_config` command from Plan 03.

Purpose: Make the feature user-controllable from the dashboard. The toggle is
the only user-facing surface of the whole phase.
Output: Frontend changes built and deployed, plus a human-verified Options card
with the ha-switch fallback decision resolved (Pitfall 6).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-trv-temperature-offset-auto-calibration/09-CONTEXT.md
@.planning/phases/09-trv-temperature-offset-auto-calibration/09-RESEARCH.md
@.planning/phases/09-trv-temperature-offset-auto-calibration/09-PATTERNS.md

# Plan 03 provides the WS command this frontend calls:
@.planning/phases/09-trv-temperature-offset-auto-calibration/09-03-PLAN.md

# Analogs to mirror exactly:
@frontend/src/types.ts
@frontend/src/ws-client.ts
@frontend/src/components/global-settings-tab.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend types and ws-client</name>
  <files>frontend/src/types.ts, frontend/src/ws-client.ts</files>
  <action>
types.ts (D-09, 09-PATTERNS.md lines 398-404): Append two OPTIONAL fields before
the closing brace of the ClimateConfig interface:
  /** CALIB-01: global calibration on/off. Absent = false (sparse config). */
  calibration_enabled?: boolean;
  /** CALIB-04: jitter threshold in degrees C. Absent = 0.5 (sparse config). */
  calibration_threshold?: number;
Optional (?) because the backend uses the sparse-config model — an absent key in
the get_config payload means the backend default applies.

ws-client.ts (D-10, 09-PATTERNS.md lines 358-373): Append a setCalibrationConfig
method after the last existing method (subscribeStatus, ~line 193), mirroring
setGlobalMode:
  /** Enable or disable TRV offset auto-calibration globally. */
  setCalibrationConfig(enabled: boolean): Promise<{ success: boolean }> {
    return this.hass.connection.sendMessagePromise<{ success: boolean }>({
      type: "climate_manager/set_calibration_config",
      enabled,
    });
  }
Use property shorthand (enabled,) and an explicit return type. JSDoc above the
method (all existing methods have one).

Comply with .editorconfig: 2-space indent for TS, max 80 cols, LF, final
newline. Commit: feat(09-04): add calibration types and ws-client method
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5 && grep -q "setCalibrationConfig" src/ws-client.ts && grep -q "calibration_enabled" src/types.ts && echo OK</automated>
  </verify>
  <done>ClimateConfig has both optional fields; ws-client exposes setCalibrationConfig; tsc --noEmit reports no errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add Options card with calibration toggle to Global Settings tab</name>
  <files>frontend/src/components/global-settings-tab.ts</files>
  <action>
Add the Options card (D-11/D-12/D-13, 09-PATTERNS.md lines 272-337).

1. _renderOptionsCard() method, mirroring _renderTemperaturesCard structure
   (ha-card > card-header > card-content):
     - card-header text: "Options" (D-11 — general-purpose card, not
       "Calibration"/"Configuration")
     - const enabled = this.config.calibration_enabled ?? false;
     - One .option-row containing an .option-label span "Auto-calibrate TRV
       temperature offsets" (D-12) and <ha-switch .checked=${enabled}
       @change=${this._onCalibrationToggle}></ha-switch>
     - Always rendered (D-13) — not gated on TRV compatibility.

2. _onCalibrationToggle as an arrow-function class field (NOT a method, for
   correct `this` binding in Lit listeners — see existing comment at lines
   288-293), mirroring the _onResetTemperatures auto-save pattern (lines
   336-344):
     private _onCalibrationToggle = async (e: Event) => {
       const enabled = (e.target as HTMLInputElement).checked;
       try {
         await this.ws.setCalibrationConfig(enabled);
         await this.panel.reloadConfig();
         this.panel.showToast("Saved", false);
       } catch {
         this.panel.showToast("Save failed", true);
       }
     };
   No Save button (auto-save on change, D-12).

3. render(): add ${this._renderOptionsCard()} as the THIRD card, after
   ${this._renderStatusCard()} and ${this._renderTemperaturesCard()}.

4. CSS: add .option-row and .option-label to the existing static styles css
   block (lines 117-286), following the .status-row / .status-label naming.
   .option-row: display: flex; align-items: center; justify-content:
   space-between; (+ spacing consistent with sibling rows).

Comply with .editorconfig (2-space, 80 cols). Then run make build to bundle the
panel. Commit: feat(09-04): add Options card with calibration toggle
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | tail -5 && grep -q "_renderOptionsCard" src/components/global-settings-tab.ts && grep -q "_onCalibrationToggle" src/components/global-settings-tab.ts && echo OK</automated>
  </verify>
  <done>Options card renders as the third card with the toggle wired to setCalibrationConfig via _onCalibrationToggle; tsc clean; make build succeeds.</done>
</task>

<task type="auto">
  <name>Task 3: Full suite green + build + deploy</name>
  <files>custom_components/climate_manager/www/panel.js</files>
  <action>
Run the full Python suite to confirm Plans 01-03 integrate cleanly with the
deployed build, then build and deploy the panel:
  - make test (full pytest suite must be green)
  - make lint (editorconfig clean across Python + TS)
  - make build (bundles frontend/src into custom_components/.../www/panel.js)
  - make deploy (copies the integration to the live HA instance, per project
    Makefile convention from MEMORY.md "Use Makefile for operations")
Do not hand-edit panel.js — it is a build artifact produced by make build.
Commit: chore(09-04): build and deploy calibration UI
  </action>
  <verify>
    <automated>make test 2>&1 | tail -5 && make lint 2>&1 | tail -3</automated>
  </verify>
  <done>Full pytest suite green; make lint clean; make build + make deploy complete without error; panel.js regenerated.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Verify Options card visibility and persistence on live HA</name>
  <action>
Human verifies the deployed Options card in a live HA 2026.x instance. This is
the Pitfall 6 decision point: confirm the ha-switch actually renders (vs. the
known HA 2026.x invisible-component breakage) and the toggle persists. See
how-to-verify for exact steps. If ha-switch is invisible, apply the native
<input type="checkbox"> fallback and rebuild/redeploy before approving.
  </action>
  <what-built>
The "Options" card in the Global Settings tab with an "Auto-calibrate TRV
temperature offsets" toggle (ha-switch), auto-saving via the
set_calibration_config WS command. Backend calibration engine, capability guard,
offset helper, and WS persistence are all implemented and unit-tested.
  </what-built>
  <how-to-verify>
1. Open Home Assistant (live 2026.x instance) and navigate to the Climate
   Manager panel -> Global Settings tab.
2. Confirm a third card titled "Options" appears below "Temperatures".
3. PITFALL 6 CHECK: confirm the toggle is actually VISIBLE. If the ha-switch
   renders nothing (invisible — the same HA 2026.x breakage seen for
   ha-textfield/ha-tabs/ha-select), STOP and report it: the fallback is to
   replace <ha-switch> with a styled native <input type="checkbox"> keeping the
   identical _onCalibrationToggle auto-save behavior (D-12 specifics), then
   rebuild/redeploy.
4. Toggle the switch ON. Confirm a "Saved" toast appears.
5. Reload the page. Confirm the toggle remains ON (persisted across reload).
6. Toggle OFF, reload, confirm it stays OFF.
7. (Optional, if Tado X hardware available) With calibration ON and a room that
   has a reference temperature_sensor 1C above the TRV reading, wait one
   evaluate cycle (~1 min) and confirm the TRV's temperature_offset attribute
   changes. Confirm a sensor-less or non-Tado-X room produces no errors in the
   HA log.
  </how-to-verify>
  <resume-signal>Type "approved" if the toggle is visible and persists, or describe the issue (e.g., "ha-switch invisible — apply checkbox fallback").</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — no type errors
- `make test` — full suite green
- `make lint` — editorconfig clean
- Human checkpoint: Options card visible, toggle auto-saves and persists across reload
</verification>

<success_criteria>
- ClimateConfig carries calibration_enabled / calibration_threshold (optional)
- ws-client.setCalibrationConfig calls the set_calibration_config command
- Options card is the third Global Settings card with an auto-saving toggle
- Frontend built and deployed; panel.js regenerated via make build
- Human verifies visibility (ha-switch vs. checkbox fallback) and persistence
</success_criteria>

<output>
Create `.planning/phases/09-trv-temperature-offset-auto-calibration/09-04-SUMMARY.md` when done.
</output>
