---
phase: 05-zone-crud-evaluation-engine
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - custom_components/climate_manager/websocket.py
  - tests/test_websocket.py
autonomous: true
requirements:
  - ZONE-07
  - ZONE-09

must_haves:
  truths:
    - "WS command climate_manager/delete_zone moves all rooms with
      zone_id==target zone to the Default Zone (via pop, never explicit None)
      and removes the zone from runtime_config['zones'] in a single save
      (ZONE-07)"
    - "delete_zone returns ERR_NOT_FOUND for an unknown zone_id and never
      mutates runtime_config when the zone is absent"
    - "WS command climate_manager/set_zone_time_program validates the program
      via validate_daily_program BEFORE any mutation; an invalid program returns
      ERR_INVALID_FORMAT and leaves
      runtime_config['zones'][zone_id]['time_program'] unchanged (ZONE-09;
      Pitfall 6)"
    - "WS command climate_manager/reset_zone_time_program with target='default'
      restores zones[zone_id]['time_program'] to
      copy.deepcopy(_DEFAULT_DAILY_PROGRAM); target='global' restores it to
      copy.deepcopy(runtime_config['global_time_program']) (ZONE-09; Pitfall 2)"
    - "delete_zone failure (validate_zone_assignment raises ValueError) restores
      BOTH runtime_config['zones'] AND runtime_config['rooms'] from snapshots
      (CR-01)"
  artifacts:
    - path: "custom_components/climate_manager/websocket.py"
      provides:
        "3 new factories _make_ws_delete_zone, _make_ws_set_zone_time_program,
        _make_ws_reset_zone_time_program + 3 async_register_command calls"
      contains: "def _make_ws_delete_zone("
    - path: "custom_components/climate_manager/websocket.py"
      provides: "validate_daily_program pre-save gate in set_zone_time_program"
      contains: "validate_daily_program"
    - path: "tests/test_websocket.py"
      provides:
        "5 new integration tests for delete/set_time_program/reset_time_program"
      contains: "test_ws_delete_zone_migrates_rooms"
  key_links:
    - from: "_make_ws_delete_zone"
      to: "runtime_config['rooms'][area_id].pop('zone_id', None)"
      via:
        "iteration over rooms to migrate orphans to Default Zone (D-06 sparse
        model)"
      pattern: "\\.pop\\(\"zone_id\", None\\)"
    - from: "_make_ws_set_zone_time_program"
      to: "validate_daily_program(msg['program'])"
      via: "pre-save gate that returns BEFORE any mutation on failure (T-03-05)"
      pattern: "validate_daily_program\\(msg\\[\"program\"\\]\\)"
    - from: "_make_ws_reset_zone_time_program"
      to:
        "copy.deepcopy(_DEFAULT_DAILY_PROGRAM |
        runtime_config['global_time_program'])"
      via: "target selector branches on msg['target']"
      pattern: "copy\\.deepcopy\\("
---

<objective>
Add the three remaining zone WebSocket commands: delete_zone (room migration + CR-01
snapshot-rollback over both zones and rooms), set_zone_time_program (validate_daily_program
pre-save gate), reset_zone_time_program (target='default' | 'global' selector with deepcopy).
These three are deliberately separated from Plan 01 because they each require additional
defensive logic (CR-01, validation gate, target enum) and combining them with the simple
trio would push the plan past the context budget.

Purpose: Complete the WS contract for zone CRUD. After this plan lands, the
panel (Phase 6) can drive every zone operation. The handler logic mirrors three
existing patterns in websocket.py (set_room_config CR-01, set_time_program
validation gate, reset_time_program deepcopy) — this plan is essentially a
mechanical adaptation of those three patterns to the zone sub-dict.

Output: 3 new factory functions + 3 async_register_command calls in
websocket.py, and 5 new integration tests in tests/test_websocket.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-zone-crud-evaluation-engine/05-CONTEXT.md
@.planning/phases/05-zone-crud-evaluation-engine/05-RESEARCH.md
@.planning/phases/05-zone-crud-evaluation-engine/05-PATTERNS.md
@.planning/phases/05-zone-crud-evaluation-engine/05-01-SUMMARY.md

@custom_components/climate_manager/websocket.py
@custom_components/climate_manager/storage.py
@custom_components/climate_manager/schedule.py
@custom_components/climate_manager/const.py @tests/test_websocket.py </context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add delete_zone + set_zone_time_program + reset_zone_time_program handlers</name>
  <files>custom_components/climate_manager/websocket.py</files>
  <read_first>
    - custom_components/climate_manager/websocket.py (current state after Plan 01: see _make_ws_set_room_config CR-01 at lines 341-358 for the snapshot-rollback pattern; _make_ws_set_time_program at lines 305-313 for the validate_daily_program gate; _make_ws_reset_time_program at lines 442-443 for the deepcopy pattern; _make_ws_reset_room_to_global_program at lines 472-477 for the "copy from current runtime config" pattern)
    - custom_components/climate_manager/storage.py (validate_zone_assignment lines 25-60 — understand which configurations it rejects: dangling zone_id, duplicate zone_id, explicit zone_id=None)
    - custom_components/climate_manager/schedule.py (validate_daily_program signature: `(program: dict) -> tuple[bool, str | None]`)
    - .planning/phases/05-zone-crud-evaluation-engine/05-CONTEXT.md (D-06 write-then-evaluate; Pitfall 1 delete_zone ordering; Pitfall 2 deepcopy for reset_zone_time_program; Pitfall 6 validate-before-mutate)
    - .planning/phases/05-zone-crud-evaluation-engine/05-PATTERNS.md (Pattern 2 CR-01 snapshot-rollback for BOTH zones and rooms; Pattern 3 validate_daily_program pre-save gate; Pattern 4 deepcopy for time_program reset; shared "Sparse key removal" pattern; Anti-patterns table)
    - .planning/phases/05-zone-crud-evaluation-engine/05-RESEARCH.md (Code Examples: delete_zone handler skeleton lines 421-457; Pitfall 1 ordering requirement; Pitfall 5 deepcopy from runtime_config not DEFAULT_CONFIG)
  </read_first>
  <behavior>
    - delete_zone {zone_id: str}: if zone_id not in runtime_config["zones"] → ERR_NOT_FOUND, return (no mutation). Otherwise: snapshot zones AND rooms via copy.deepcopy; iterate runtime_config["rooms"].values(), for each room_cfg where room_cfg.get("zone_id") == zone_id call room_cfg.pop("zone_id", None); then del runtime_config["zones"][zone_id]; try store.async_save; on ValueError restore BOTH snapshots and send ERR_INVALID_FORMAT; on success send {success: True} and schedule evaluate.
    - set_zone_time_program {zone_id: str, program: dict}: call (ok, err) = validate_daily_program(msg["program"]); if not ok → send ERR_INVALID_FORMAT(err) and return BEFORE any mutation (T-03-05). If zone_id not in runtime_config["zones"] → ERR_NOT_FOUND, return. Otherwise: runtime_config["zones"][zone_id]["time_program"] = msg["program"]; save; send result; schedule evaluate.
    - reset_zone_time_program {zone_id: str, target: vol.In(['default', 'global'])}: if zone_id not in runtime_config["zones"] → ERR_NOT_FOUND, return. If target=='default': runtime_config["zones"][zone_id]["time_program"] = copy.deepcopy(_DEFAULT_DAILY_PROGRAM); if target=='global': runtime_config["zones"][zone_id]["time_program"] = copy.deepcopy(runtime_config["global_time_program"]). Save; send result; schedule evaluate.
  </behavior>
  <action>
    Append three new factory functions at the end of websocket.py, after the three factories added in Plan 01. No new module-level imports required — `copy`, `_DEFAULT_DAILY_PROGRAM`, and `validate_daily_program` are already imported.

    Factory 1: `_make_ws_delete_zone(entry)`
      Schema: `vol.Required("type"): f"{DOMAIN}/delete_zone"`, `vol.Required("zone_id"): str`.
      Handler body (in this exact order — Pitfall 1):
        1. `runtime_config = entry.runtime_data.runtime_config`
        2. `zone_id = msg["zone_id"]`
        3. Guard: `if zone_id not in runtime_config.get("zones", {}): send_error ERR_NOT_FOUND; return`
        4. CR-01: `zones_backup = copy.deepcopy(runtime_config.get("zones", {}))` and `rooms_backup = copy.deepcopy(runtime_config.get("rooms", {}))`
        5. Migrate rooms: `for room_cfg in runtime_config.get("rooms", {}).values(): if room_cfg.get("zone_id") == zone_id: room_cfg.pop("zone_id", None)` — MUST use pop (not assign None) per D-06 sparse model and Pitfall — assigning None makes validate_zone_assignment raise
        6. Remove zone: `del runtime_config["zones"][zone_id]`
        7. `try: await entry.runtime_data.store.async_save(runtime_config) except ValueError as exc:` → restore `runtime_config["zones"] = zones_backup` AND `runtime_config["rooms"] = rooms_backup`, then send_error ERR_INVALID_FORMAT(str(exc)) and return
        8. `connection.send_result(msg["id"], {"success": True})`
        9. `hass.async_create_task(entry.runtime_data.coordinator.async_evaluate())`

    Factory 2: `_make_ws_set_zone_time_program(entry)`
      Schema: `vol.Required("type"): f"{DOMAIN}/set_zone_time_program"`, `vol.Required("zone_id"): str`, `vol.Required("program"): dict`.
      Handler body order (Pitfall 6 — validate-before-mutate):
        1. `ok, err = validate_daily_program(msg["program"])`
        2. `if not ok: connection.send_error(msg["id"], websocket_api.ERR_INVALID_FORMAT, err); return` (no mutation)
        3. `runtime_config = entry.runtime_data.runtime_config`
        4. Guard: `if msg["zone_id"] not in runtime_config.get("zones", {}): send_error ERR_NOT_FOUND; return`
        5. `runtime_config["zones"][msg["zone_id"]]["time_program"] = msg["program"]`
        6. save → send result → schedule evaluate

    Factory 3: `_make_ws_reset_zone_time_program(entry)`
      Schema: `vol.Required("type"): f"{DOMAIN}/reset_zone_time_program"`, `vol.Required("zone_id"): str`, `vol.Required("target"): vol.In(["default", "global"])`.
      Handler body:
        1. `runtime_config = entry.runtime_data.runtime_config`
        2. Guard: `if msg["zone_id"] not in runtime_config.get("zones", {}): send_error ERR_NOT_FOUND; return`
        3. If `msg["target"] == "default"`: `runtime_config["zones"][msg["zone_id"]]["time_program"] = copy.deepcopy(_DEFAULT_DAILY_PROGRAM)` (Pitfall 5 — never directly assign without deepcopy)
        4. Else (target=="global"): `runtime_config["zones"][msg["zone_id"]]["time_program"] = copy.deepcopy(runtime_config["global_time_program"])` (Pitfall 2 — deepcopy avoids shared list refs)
        5. save → send result → schedule evaluate

    Register all three in `async_register_commands` after the three calls added by Plan 01: delete_zone, set_zone_time_program, reset_zone_time_program (in that order).

    Tests for these handlers live in Task 2 — implement tests first (RED), then handlers (GREEN).

  </action>
  <verify>
    <automated>uv run pytest tests/test_websocket.py -x -k "delete_zone or set_zone_time_program or reset_zone_time_program"</automated>
  </verify>
  <acceptance_criteria>
    - websocket.py contains `def _make_ws_delete_zone(entry: ClimateManagerConfigEntry):`
    - websocket.py contains `def _make_ws_set_zone_time_program(entry: ClimateManagerConfigEntry):`
    - websocket.py contains `def _make_ws_reset_zone_time_program(entry: ClimateManagerConfigEntry):`
    - websocket.py contains `room_cfg.pop("zone_id", None)` inside _make_ws_delete_zone (Pitfall: must be pop, never `= None`)
    - websocket.py contains `del runtime_config["zones"][zone_id]` inside _make_ws_delete_zone (and the del runs AFTER the rooms pop loop, not before — Pitfall 1)
    - websocket.py contains `validate_daily_program(msg["program"])` followed within 5 lines by `return` (validate-before-mutate gate)
    - websocket.py contains `vol.In(["default", "global"])` in the reset_zone_time_program schema
    - websocket.py contains `copy.deepcopy(_DEFAULT_DAILY_PROGRAM)` AND `copy.deepcopy(runtime_config["global_time_program"])` in _make_ws_reset_zone_time_program
    - `grep -c '^    websocket_api.async_register_command' custom_components/climate_manager/websocket.py` returns 17 (14 after Plan 01 + 3 new from this task)
    - `uv run pytest tests/test_websocket.py -x -k "delete_zone or set_zone_time_program or reset_zone_time_program"` exits 0
  </acceptance_criteria>
  <done>
    All three handlers implemented and registered; the 5 new tests from Task 2 pass; existing 14 websocket tests still pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 5 integration tests for delete/set_zone_time_program/reset_zone_time_program</name>
  <files>tests/test_websocket.py</files>
  <read_first>
    - tests/test_websocket.py (current state after Plan 01 — _setup_entry helper; seeding pattern from Plan 01 tests)
    - custom_components/climate_manager/const.py (MODE_TIME_PROGRAM, _DEFAULT_DAILY_PROGRAM, DOMAIN; the zone schema comment block at lines 152-170)
    - custom_components/climate_manager/schedule.py (validate_daily_program — understand what makes a partial program invalid: missing day keys among mon/tue/wed/thu/fri/sat/sun, or non-list values)
    - .planning/phases/05-zone-crud-evaluation-engine/05-RESEARCH.md (Phase Requirements → Test Map — rows for ZONE-07 and ZONE-09 list the exact test function names)
    - .planning/phases/05-zone-crud-evaluation-engine/05-PATTERNS.md (Pattern 14 validation rejection — capture original value before sending, assert unchanged after rejection; Pattern 15 seeding existing zones)
  </read_first>
  <behavior>
    - test_ws_delete_zone_migrates_rooms: seed runtime_config with one custom zone and two rooms — one room with zone_id=that_zone_id, one room with no zone_id. Send delete_zone. Assert success; assert "zones" dict no longer has the zone_id key; assert the migrated room no longer has "zone_id" key (pop, not None); assert the unaffected room is unchanged.
    - test_ws_delete_zone_not_found: send delete_zone with a fabricated zone_id; assert msg["success"] is False; assert runtime_config["zones"] unchanged (still empty after fresh setup).
    - test_ws_set_zone_time_program_rejects_partial: seed a custom zone with a known time_program; capture it as original_program (deep dict equality reference); send set_zone_time_program with {"mon": []} (missing 6 day keys); assert msg["success"] is False; assert runtime_config["zones"][zone_id]["time_program"] == original_program (Pitfall 6 — no mutation occurred).
    - test_ws_reset_zone_time_program_default: seed a custom zone whose time_program is mutated to a single-period {"mon": [{"start":"00:00","mode":"comfort"}], ...} for all 7 days; send reset_zone_time_program {zone_id, target: "default"}; assert success; assert zones[zone_id]["time_program"] == _DEFAULT_DAILY_PROGRAM (deep equality); additionally mutate the returned program and assert _DEFAULT_DAILY_PROGRAM module constant is unchanged (proves deepcopy isolation).
    - test_ws_reset_zone_time_program_global: seed a custom zone with a known different time_program; mutate runtime_config["global_time_program"]["wed"] to a sentinel value; send reset_zone_time_program {zone_id, target: "global"}; assert success; assert zones[zone_id]["time_program"]["wed"] == that sentinel; mutate the zone's program["wed"] afterwards and assert runtime_config["global_time_program"]["wed"] is unchanged (Pitfall 2 — deepcopy avoids shared list reference).
  </behavior>
  <action>
    Append 5 async test functions to tests/test_websocket.py. Imports already covered by Plan 01 (copy, _DEFAULT_DAILY_PROGRAM, MODE_TIME_PROGRAM, DOMAIN).

    Test scaffold (every test):
      ```
      async def test_<name>(hass, hass_ws_client):
          entry = await _setup_entry(hass)
          # ... seed runtime_config as needed ...
          client = await hass_ws_client()
          await client.send_json_auto_id({"type": f"{DOMAIN}/<command>", ...})
          msg = await client.receive_json()
          # ... assertions ...
      ```

    Seeding helper inline at top of each test that needs a custom zone:
      ```
      zone_id = "test-zone-1"
      entry.runtime_data.runtime_config.setdefault("zones", {})[zone_id] = {
          "name": "Test",
          "mode": MODE_TIME_PROGRAM,
          "time_program": copy.deepcopy(_DEFAULT_DAILY_PROGRAM),
      }
      ```

    test_ws_delete_zone_migrates_rooms: Add two rooms after seeding:
      `entry.runtime_data.runtime_config["rooms"] = {"area_a": {"zone_id": zone_id}, "area_b": {}}`
      After WS delete: assert `zone_id not in entry.runtime_data.runtime_config["zones"]`; assert `"zone_id" not in entry.runtime_data.runtime_config["rooms"]["area_a"]`; assert `entry.runtime_data.runtime_config["rooms"]["area_b"] == {}` (untouched).

    test_ws_delete_zone_not_found: no seeding; send delete_zone with `zone_id="ghost"`; assert `msg["success"] is False`; assert `entry.runtime_data.runtime_config.get("zones", {}) == {}`.

    test_ws_set_zone_time_program_rejects_partial: seed zone; `original = copy.deepcopy(entry.runtime_data.runtime_config["zones"][zone_id]["time_program"])`; send with `{"program": {"mon": []}}`; assert failure; assert `entry.runtime_data.runtime_config["zones"][zone_id]["time_program"] == original`.

    test_ws_reset_zone_time_program_default: seed zone with a non-default program (e.g., `entry.runtime_data.runtime_config["zones"][zone_id]["time_program"] = {d: [{"start": "00:00", "mode": "comfort"}] for d in ["mon","tue","wed","thu","fri","sat","sun"]}`); send target="default"; assert `entry.runtime_data.runtime_config["zones"][zone_id]["time_program"] == _DEFAULT_DAILY_PROGRAM`. Then mutate the assigned program (e.g., `entry.runtime_data.runtime_config["zones"][zone_id]["time_program"]["mon"].append({"start": "12:00", "mode": "reduced"})`) and assert `len(_DEFAULT_DAILY_PROGRAM["mon"]) == 5` (the original weekday default — unchanged, proves deepcopy).

    test_ws_reset_zone_time_program_global: seed zone with a single-period weekly program; mutate `entry.runtime_data.runtime_config["global_time_program"]["wed"] = [{"start": "00:00", "mode": "comfort"}]` BEFORE the WS call; send target="global"; assert `entry.runtime_data.runtime_config["zones"][zone_id]["time_program"]["wed"] == [{"start": "00:00", "mode": "comfort"}]`. Then `entry.runtime_data.runtime_config["zones"][zone_id]["time_program"]["wed"].append({"start": "06:00", "mode": "normal"})` and assert `entry.runtime_data.runtime_config["global_time_program"]["wed"] == [{"start": "00:00", "mode": "comfort"}]` (still one element — proves the two are not sharing the same list).

  </action>
  <verify>
    <automated>uv run pytest tests/test_websocket.py::test_ws_delete_zone_migrates_rooms tests/test_websocket.py::test_ws_delete_zone_not_found tests/test_websocket.py::test_ws_set_zone_time_program_rejects_partial tests/test_websocket.py::test_ws_reset_zone_time_program_default tests/test_websocket.py::test_ws_reset_zone_time_program_global -x</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_websocket.py defines `async def test_ws_delete_zone_migrates_rooms(hass, hass_ws_client):`
    - tests/test_websocket.py defines `async def test_ws_delete_zone_not_found(hass, hass_ws_client):`
    - tests/test_websocket.py defines `async def test_ws_set_zone_time_program_rejects_partial(hass, hass_ws_client):`
    - tests/test_websocket.py defines `async def test_ws_reset_zone_time_program_default(hass, hass_ws_client):`
    - tests/test_websocket.py defines `async def test_ws_reset_zone_time_program_global(hass, hass_ws_client):`
    - test_ws_delete_zone_migrates_rooms asserts `"zone_id" not in entry.runtime_data.runtime_config["rooms"]["area_a"]` (must be key absence, not None)
    - test_ws_set_zone_time_program_rejects_partial captures original program BEFORE the send and asserts deep equality AFTER the failed call (proves no mutation on validation rejection)
    - test_ws_reset_zone_time_program_default mutates the assigned program post-call and verifies _DEFAULT_DAILY_PROGRAM is unchanged (deepcopy isolation)
    - test_ws_reset_zone_time_program_global mutates the assigned program post-call and verifies runtime_config["global_time_program"] is unchanged (deepcopy isolation)
    - `grep -c '^async def test_ws_' tests/test_websocket.py | grep -v '^#'` returns at least 19 (9 original + 5 from Plan 01 + 5 from this plan)
    - `uv run pytest tests/test_websocket.py -x` exits 0
  </acceptance_criteria>
  <done>
    All 5 new tests pass against the handlers added in Task 1; existing 14 tests remain green; ZONE-07 and ZONE-09 are observably satisfied at the WS layer.
  </done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                           | Description                                                                                                                                                                 |
| ---------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| WS client → integration handlers   | Same boundary as Plan 01 — auth gated by HA core                                                                                                                            |
| WS handler → store.async_save      | validate_zone_assignment runs inside async_save; delete_zone is the only handler that mutates BOTH zones and rooms in a single save, so the rollback path must restore both |
| Handler → in-memory runtime_config | delete_zone mutations are not atomic across zones+rooms — CR-01 snapshot protects against partial writes if save fails                                                      |

## STRIDE Threat Register

| Threat ID | Category  | Component                                                                                           | Disposition | Mitigation Plan                                                                                                                                                                                                                           |
| --------- | --------- | --------------------------------------------------------------------------------------------------- | ----------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-05-06   | Tampering | delete_zone leaves orphan zone_id references in rooms if order is wrong (Pitfall 1)                 | mitigate    | Handler iteration order enforced: pop zone_id from all rooms FIRST, THEN del runtime_config["zones"][zone_id], THEN save. Acceptance criterion grep checks for both lines and the order is enforced by test_ws_delete_zone_migrates_rooms |
| T-05-07   | Tampering | delete_zone failure mid-mutation corrupts in-memory state                                           | mitigate    | CR-01 snapshot of BOTH zones AND rooms before any mutation; restore both on ValueError. Pattern derived from set_room_config CR-01 (websocket.py:341-358)                                                                                 |
| T-05-08   | Tampering | set_zone_time_program writes invalid program to runtime_config before validation (Pitfall 6)        | mitigate    | validate_daily_program called BEFORE any runtime_config access; explicit `return` on failure. Acceptance criterion: test_ws_set_zone_time_program_rejects_partial asserts no mutation occurred                                            |
| T-05-09   | Tampering | reset_zone_time_program(target='global') shares list reference between zone and global (Pitfall 2)  | mitigate    | copy.deepcopy enforced for both target branches. Acceptance criterion: test_ws_reset_zone_time_program_global mutates post-call and asserts isolation                                                                                     |
| T-05-10   | DoS       | set_zone_time_program with extremely large program payload                                          | accept      | HA WebSocket message size limits apply at transport layer; validate_daily_program enforces structural shape but doesn't reject by size — no v1.1 surface to enforce stricter limits                                                       |
| T-05-11   | Tampering | delete_zone setting room["zone_id"] = None instead of popping (would fail validate_zone_assignment) | mitigate    | Specified `room_cfg.pop("zone_id", None)` in action; acceptance criterion greps for the pop call literally; storage.py:48-52 also rejects explicit None at the storage layer (defense in depth)                                           |
| T-05-SC   | Tampering | npm/pip/cargo installs                                                                              | accept      | No external packages — same as Plan 01                                                                                                                                                                                                    |

</threat_model>

<verification>
- All 19+ tests in tests/test_websocket.py pass: `uv run pytest tests/test_websocket.py -x`
- No existing test regressions: `uv run pytest tests/ -x`
- websocket.py async_register_commands has exactly 17 registered commands (11 original + 6 zone commands)
- delete_zone handler has the rooms-pop loop BEFORE the del zones[zone_id] line (Pitfall 1 enforced by source order)
</verification>

<success_criteria>

- 3 new WS handlers exist and are registered: delete_zone,
  set_zone_time_program, reset_zone_time_program
- 5 new tests exist and all pass
- ZONE-07 (delete + room migration) and ZONE-09 (time_program edit + reset) are
  observably satisfied
- After this plan all 6 zone CRUD WS commands are live in websocket.py
- CR-01 snapshot-rollback verified by delete_zone exercising both zones and
  rooms snapshots in the handler (visible in source) </success_criteria>

<output>
Create `.planning/phases/05-zone-crud-evaluation-engine/05-02-SUMMARY.md` when done
</output>
