---
phase: 16-presence-heating-log-traces
plan: 05
type: execute
wave: 3
depends_on: ["16-01", "16-03"]
files_modified:
  - custom_components/climate_manager/room.py
files_created_tested_by: [tests/test_room_domain.py]
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "Room.apply_setpoint(period, temp, ctx) pushes the temp to every TRVGroup the room owns"
    - "Room owns its preheat state and calibration state directly (scalars, not coordinator dicts)"
    - "Room knows its assigned persons and its TRV groups"
  artifacts:
    - path: "custom_components/climate_manager/room.py"
      provides: "Room plain class — apply_setpoint, preheat, calibration (D-06)"
      contains: "class Room"
      min_lines: 120
  key_links:
    - from: "custom_components/climate_manager/room.py"
      to: "TRVGroup.push"
      via: "Room.apply_setpoint loop"
      pattern: "\\.push\\("
---

<objective>
Implement D-06: a new `room.py` module with `Room` as a plain class (no state
machine). Room owns its TRV groups, preheat state (migrated from the
coordinator's `_preheat_*` dicts, collapsed to per-room scalars), calibration
state, and its assigned persons. `Room.apply_setpoint(period, temp, ctx)` pushes
to all TRVGroups; `Room.compute_preheat(ctx)` and `Room.calibrate_trvs(ctx)` are
the per-room passes the coordinator calls after zone evaluation.

Pre-heat logic lives on `Room` (not per-state) — shared across all zone modes,
confirmed explicitly in CONTEXT.md specifics.

Purpose: Fourth domain module; `zone.py` calls `room.apply_setpoint`, and the
coordinator calls `room.compute_preheat`/`room.calibrate_trvs`.
Output: `custom_components/climate_manager/room.py` + green
`tests/test_room_domain.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-presence-heating-log-traces/16-CONTEXT.md
@.planning/phases/16-presence-heating-log-traces/16-RESEARCH.md
@.planning/phases/16-presence-heating-log-traces/16-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Room class + apply_setpoint TRVGroup delegation</name>
  <read_first>
    - tests/test_room_domain.py (RED scaffold to turn GREEN)
    - custom_components/climate_manager/trv.py (TRVGroup.push signature from plan 16-03)
    - custom_components/climate_manager/coordinator.py (lines 501-570 _push_temperatures — what apply_setpoint replaces per room)
    - .planning/phases/16-presence-heating-log-traces/16-CONTEXT.md (D-06 Room ownership, D-09 apply_setpoint call flow)
    - .planning/phases/16-presence-heating-log-traces/16-PATTERNS.md (room.py: apply_setpoint→TRVGroup delegation, preheat/calibration state ownership)
  </read_first>
  <behavior>
    - Room(area_id, ...) exposes area_id, assigned_persons (list[Person]), _trv_groups (list[TRVGroup])
    - apply_setpoint(period, temp, ctx) awaits group.push(temp, period, ctx) for every group in _trv_groups
    - apply_setpoint with zero groups is a no-op (no error)
    - Room owns _preheat_active (bool), _preheat_target (float|None), _preheat_suppressed (bool), _preheat_in_progress (dict) as instance scalars, not coordinator dicts
    - Room owns calibration tracking fields (_calibration_last_changed/delta/offset) as instance state
  </behavior>
  <action>
Create `room.py` with SPDX header, docstring,
`from __future__ import annotations`, `import logging`, `import asyncio`, and
`_LOGGER = logging.getLogger(__name__)`. Import `EvalContext`, `Person`,
`TRVGroup` under `TYPE_CHECKING`.

Define `class Room`. `__init__(self, hass, area_id)` stores `hass`, `area_id`,
`assigned_persons: list = []`, `_trv_groups: list = []`, and the preheat /
calibration state fields migrated from coordinator `__init__` (lines 127-132,
155-159) collapsed to per-room scalars: `_preheat_active: bool = False`,
`_preheat_target: float | None = None`, `_preheat_suppressed: bool = False`,
`_preheat_in_progress: dict = {}`, `_calibration_last_changed`,
`_calibration_last_delta`, `_calibration_last_offset`. Add an `_zone` weakref or
plain reference set at coordinator init so `compute_preheat` can read
`zone.preheat_enabled` (per 16-PATTERNS preheat zone-enabled lookup).

Implement `async def apply_setpoint(self, period, temp, ctx)` iterating
`self._trv_groups` and awaiting `group.push(temp, period, ctx)` — use
`asyncio.gather` over the groups (16-PATTERNS asyncio.gather pattern). No
platform branching (TRVGroup already encapsulates that).
  </action>
  <verify>
    <automated>make test 2>&1 | grep -E "test_room_domain" | tail -10; make test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - room.py contains `class Room`
    - room.py contains `async def apply_setpoint`
    - room.py contains `_trv_groups` and a `.push(` call within apply_setpoint
    - room.py defines `_preheat_active`, `_preheat_target`, `_preheat_suppressed` as instance attributes (not coordinator dicts)
    - apply_setpoint delegation test in tests/test_room_domain.py passes
    - `make test` full suite green at/above baseline
  </acceptance_criteria>
  <done>Room exists as a plain class owning TRV groups and preheat/calibration state; apply_setpoint delegates to every TRVGroup.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate compute_preheat + calibrate_trvs onto Room</name>
  <read_first>
    - custom_components/climate_manager/coordinator.py (lines 572-860 _async_preheat / _async_preheat_room; lines 882-1074 _async_calibrate / _async_calibrate_room; lines 615-628 preheat zone-enabled lookup)
    - custom_components/climate_manager/room.py (Room from Task 1)
    - custom_components/climate_manager/trv.py (TRV.calibrate, TRV.push_temperature)
    - tests/test_preheat.py (existing coordinator preheat scenarios — behaviour must be preserved)
    - .planning/phases/16-presence-heating-log-traces/16-PATTERNS.md (room.py: preheat/calibration migration; D-06)
  </read_first>
  <behavior>
    - compute_preheat(ctx) reproduces _async_preheat_room logic for one room: reads preheat_enabled from the owning zone, computes lead time / inertia, sets _preheat_active/_preheat_target/_preheat_suppressed, pushes the pre-heat setpoint when due
    - calibrate_trvs(ctx) reproduces _async_calibrate_room: per-TRV offset computation + TRV.calibrate, updating the calibration tracking fields
    - No heating-behaviour change vs. the coordinator implementation (refactor only) — existing preheat test scenarios still hold
  </behavior>
  <action>
Move `_async_preheat_room` (coordinator lines 587-860) into
`Room.compute_preheat(self, ctx)` and `_async_calibrate_room` (lines 995-1074)
into `Room.calibrate_trvs(self, ctx)`. Translate coordinator dict access to Room
instance state: `self._preheat_in_progress[area_id]` →
`self._preheat_in_progress` (now per-room), `self._preheat_active[area_id]` →
`self._preheat_active`, etc. Read `preheat_enabled` from the owning zone
reference (per 16-PATTERNS preheat zone-enabled lookup — `self._zone` already set
at init) instead of re-reading the config dict. Calibration writes go through
`TRV.calibrate(offset, ctx)`; preheat setpoint pushes go through
`self.apply_setpoint(period, temp, ctx)` or directly through a TRVGroup as the
original code dictated. Preserve the frost-lock guard
(`self._frost_locked` — migrate from coordinator `_frost_locked_rooms` snapshot,
set per-room at zone evaluation). Do NOT change convergence/inertia math.

Note: the corresponding coordinator methods are DELETED in plan 16-06 (the
integration wave), not here — this plan only adds the methods on Room. The
existing `test_preheat.py` continues to exercise the coordinator path until
16-06 rewires it.
  </action>
  <verify>
    <automated>make test 2>&1 | grep -E "test_room_domain|test_preheat" | tail -15; make test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - room.py contains `async def compute_preheat`
    - room.py contains `async def calibrate_trvs`
    - compute_preheat reads preheat-enabled from the owning zone reference (grep: `_zone` or `zone.preheat_enabled`)
    - room-domain preheat/calibration ownership tests pass
    - existing tests/test_preheat.py still passes (coordinator path untouched in this plan)
    - `make test` full suite green at/above baseline
  </acceptance_criteria>
  <done>Room owns compute_preheat and calibrate_trvs with the coordinator's exact inertia/convergence math preserved; existing preheat tests stay green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Room → TRVGroup.push / TRV.calibrate | resolved setpoint + offset cross into TRV push/calibrate |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-09 | Tampering | inertia model corruption from bad samples | mitigate | Preserve the "exclude samples that never reached target" guard from coordinator _async_preheat_room (PREHEAT-03) verbatim |
| T-16-10 | Denial of service | calibrate/preheat exception in eval loop | mitigate | TRV.calibrate/push already exception-safe (plan 16-03); Room methods do not add unguarded service calls |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH.md: zero external deps) |
</threat_model>

<verification>
- `tests/test_room_domain.py` passes
- `tests/test_preheat.py` still passes (coordinator path unchanged until 16-06)
- `make test` full suite green at/above 249 baseline
</verification>

<success_criteria>
Room owns apply_setpoint (TRVGroup delegation), compute_preheat, calibrate_trvs,
and all preheat/calibration state as per-room scalars, with no change to heating
behaviour or inertia math.
</success_criteria>

<output>
Create `.planning/phases/16-presence-heating-log-traces/16-05-SUMMARY.md` when done
</output>
