---
phase: 16-presence-heating-log-traces
plan: 03
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - custom_components/climate_manager/trv.py
  - tests/test_trv.py
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "A TRV object owns entity_id, last_pushed, and platform"
    - "TRV.push_temperature pushes only when last_pushed differs from the desired temp (anti-flap)"
    - "TRV.push_temperature emits a DEBUG heating log only when the setpoint actually changes"
    - "The startup push (last_pushed is None) fires and logs"
    - "A TRVGroup assembled from matter_mappings pushes each physical TRV exactly once (Matter dedup)"
  artifacts:
    - path: "custom_components/climate_manager/trv.py"
      provides: "TRV + TRVGroup classes (D-07)"
      contains: "class TRVGroup"
      min_lines: 80
  key_links:
    - from: "custom_components/climate_manager/trv.py"
      to: "set_trv_temperature"
      via: "TRV.push_temperature internal call"
      pattern: "set_trv_temperature\\("
---

<objective>
Implement D-07/D-11: extend the existing `trv.py` with two new classes —
`TRV` (owns `entity_id`, `last_pushed`, `platform`; async `push_temperature`,
`push_off`, `calibrate`) and `TRVGroup` (one logical push unit assembled at init
from `matter_mappings`, no platform branching at push time).

The `_push_if_changed` anti-flap guard and the D-03 manual-override hold move
verbatim from `coordinator.py` into `TRV.push_temperature`, gaining the DEBUG
heating log line (OBS-01). The Matter-entity dedup frozenset from
`_push_temperatures` moves into `TRVGroup` assembly so a Matter TRV already
covered by a tado_x mapping is never pushed twice (RESEARCH Pitfall 4).

Purpose: TRV/TRVGroup is the second leaf of the dependency graph; `room.py`
consumes it next.
Output: extended `custom_components/climate_manager/trv.py` + green
`tests/test_trv.py` additions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-presence-heating-log-traces/16-CONTEXT.md
@.planning/phases/16-presence-heating-log-traces/16-RESEARCH.md
@.planning/phases/16-presence-heating-log-traces/16-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TRV class with anti-flap push + DEBUG heating log</name>
  <read_first>
    - custom_components/climate_manager/trv.py (existing helpers — preserve verbatim; add classes after them)
    - custom_components/climate_manager/coordinator.py (lines 1627-1668 _push_if_changed anti-flap + manual-override hold; lines 1593-1625 _push_off_safely; lines 1582-1591 _push_safely)
    - .planning/phases/16-presence-heating-log-traces/16-CONTEXT.md (D-01 short-name, D-07 TRV signature, D-11 log-on-change)
    - .planning/phases/16-presence-heating-log-traces/16-RESEARCH.md (Pattern 4 DEBUG format; Code Examples anti-flap)
    - tests/test_trv.py (existing structure + the Wave 0 additions to turn GREEN)
  </read_first>
  <behavior>
    - TRV(entity_id, platform) starts with last_pushed = None
    - push_temperature(20.0, room_name="area_kitchen", zone_name="zone_main", slot="normal", ctx) on a fresh TRV: pushes via set_trv_temperature, sets last_pushed=20.0, emits one DEBUG record `heating | room=kitchen temp=20.0°C zone=main slot=normal` (short-name strip per D-01)
    - Second push_temperature(20.0, ...): no push, no DEBUG record (last_pushed == desired)
    - push_temperature when reported HA temperature != last_pushed: manual-override hold — no push (preserve coordinator D-03 hold)
    - State unavailable/None: silently skipped, no push, no log
    - push_off(frost_temp, ctx): pre-sets frost temp then OFF; anti-flap on "off" sentinel; exceptions caught and WARNING-logged, never raised
  </behavior>
  <action>
After the existing functions in `trv.py`, add `import logging` and
`_LOGGER = logging.getLogger(__name__)` if not present, plus `weakref` only if
needed (TRV does not need a weakref — it holds its own state).

Define `class TRV` with `__init__(self, hass, entity_id, platform)` storing
`entity_id`, `platform`, and `last_pushed: float | str | None = None`.

Implement `async def push_temperature(self, desired_temp, *, room_name,
zone_name, slot, ctx)`: copy the anti-flap guard from coordinator
`_push_if_changed` lines 1639-1668 — availability guard via
`hass.states.get(entity_id)`, `last = self.last_pushed` (string sentinel → None),
early-return if `last == desired_temp`, manual-override hold when reported
temperature differs from last. After the guards pass, emit the DEBUG line
`heating | room=%s temp=%s°C zone=%s slot=%s` using a module-level
`_short_name()` helper (strip `.`-prefixed domain and `area_`/`zone_` prefixes
per D-01 — copy the helper from 16-PATTERNS Shared Patterns), then
`await set_trv_temperature(...)` and set `self.last_pushed = desired_temp`.
Wrap the service call in the exception-safe pattern (catch `Exception`,
`# noqa: BLE001`, WARNING-log) so it never propagates.

Implement `async def push_off(self, frost_temp, ctx)` migrating
`_push_off_safely` (lines 1593-1625) using `self.last_pushed == "off"` as the
anti-flap sentinel. Implement `async def calibrate(self, offset, ctx)` as a thin
wrapper — leave the body delegating to existing calibration helpers; the full
calibration logic move is handled in plan 16-04 (room) and may call this.

Add a `_short_name(entity_id: str) -> str` module-level helper exactly as in
16-PATTERNS Shared Patterns (D-01).
  </action>
  <verify>
    <automated>make test 2>&1 | grep -E "test_trv" | tail -10; make test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - trv.py contains `class TRV`
    - trv.py contains `async def push_temperature`
    - trv.py contains the literal format string `heating | room=%s temp=%s°C zone=%s slot=%s`
    - trv.py contains `def _short_name`
    - trv.py contains a `last == desired_temp` (or equivalent) early-return anti-flap guard
    - DEBUG heating tests in tests/test_trv.py pass; the repeat-no-log anti-spam test passes
    - `make test` full suite green at/above baseline
  </acceptance_criteria>
  <done>TRV class owns push/anti-flap/DEBUG-log behaviour; startup push fires and logs; repeat identical setpoint produces no push and no log.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add TRVGroup with Matter-dedup assembly</name>
  <read_first>
    - custom_components/climate_manager/coordinator.py (lines 501-570 _push_temperatures: matter_entity_set frozenset, tado_x/matter/else platform branches)
    - custom_components/climate_manager/trv.py (the TRV class added in Task 1)
    - .planning/phases/16-presence-heating-log-traces/16-CONTEXT.md (D-07 TRVGroup contract)
    - .planning/phases/16-presence-heating-log-traces/16-RESEARCH.md (Pitfall 4 Matter dedup)
    - tests/test_trv.py (TRVGroup assembly test from Wave 0)
  </read_first>
  <behavior>
    - TRVGroup assembled from a room's entity list + matter_mappings contains the correct push targets (tado_x entity → its mapped Matter entities; standalone matter entity → itself; standalone other → itself)
    - A Matter entity already present in any matter_mappings value is NOT added as a standalone TRV (no double-push)
    - TRVGroup.push(temp, slot, ctx) iterates its TRVs and awaits push_temperature on each with no platform conditionals
  </behavior>
  <action>
Define `class TRVGroup` with a factory/classmethod (e.g.
`from_room_config`) or an `__init__` that takes the room's entity list,
`matter_mappings`, `room_name`, and `zone_name`. Move the
`matter_entity_set = frozenset(eid for eids in matter_mappings.values() for eid
in eids)` dedup and the tado_x/matter/else `to_set` assembly from coordinator
`_push_temperatures` (lines 519-570) into this assembly step. Store the resulting
list of `TRV` instances on `self._trvs` and remember `room_name`/`zone_name` for
the DEBUG log.

Implement `async def push(self, temp, slot, ctx)` that iterates `self._trvs`
calling `await trv.push_temperature(temp, room_name=self._room_name,
zone_name=self._zone_name, slot=slot, ctx=ctx)` — wrap with `asyncio.gather`
following 16-PATTERNS asyncio.gather pattern; no platform branching here.
  </action>
  <verify>
    <automated>make test 2>&1 | grep -E "test_trv" | tail -10; make test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - trv.py contains `class TRVGroup`
    - trv.py contains `frozenset(` for the Matter dedup set
    - trv.py contains `async def push` on TRVGroup
    - TRVGroup assembly test asserts a tado_x-mapped Matter entity is pushed once (no double-push)
    - `make test` full suite green at/above baseline
  </acceptance_criteria>
  <done>TRVGroup assembles correct push targets with Matter dedup at init and pushes each physical TRV exactly once with no push-time platform branching.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TRV → climate.set_temperature | desired_temp + entity_id cross into HA climate service calls |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-05 | Denial of service | TRV push failure propagating into eval loop | mitigate | Exception-safe wrapper (catch Exception, WARNING-log) in push_temperature/push_off — never raises into coordinator loop |
| T-16-06 | Tampering | double-push to same physical TRV | mitigate | Matter-entity frozenset dedup at TRVGroup assembly (Pitfall 4) |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH.md: zero external deps) |
</threat_model>

<verification>
- All `tests/test_trv.py` tests pass (existing + new)
- `make test` full suite green at/above 249 baseline
- No double-push for Matter entities covered by a tado_x mapping
</verification>

<success_criteria>
TRV and TRVGroup exist in trv.py. Anti-flap, manual-override hold, DEBUG heating
log, startup-push, and Matter dedup all behave per D-07/D-11 and pass their
Wave 0 tests.
</success_criteria>

<output>
Create `.planning/phases/16-presence-heating-log-traces/16-03-SUMMARY.md` when done
</output>
