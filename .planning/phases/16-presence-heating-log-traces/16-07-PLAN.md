---
phase: 16-presence-heating-log-traces
plan: 07
type: execute
wave: 5
depends_on: ["16-02", "16-03", "16-04", "16-05", "16-06"]
files_modified:
  - custom_components/climate_manager/coordinator.py
  - tests/test_coordinator.py
autonomous: false
requirements: [OBS-01]
must_haves:
  truths:
    - "coordinator.async_evaluate builds one EvalContext then delegates to Zone/Room domain objects"
    - "Zone, Person, Room, TRV, TRVGroup objects are instantiated at coordinator init from stored config across the five-module layout per D-08 (eval_context.py, zone.py, person.py, room.py, trv.py)"
    - "_last_pushed, _preheat_* and _calendar_cache dicts are removed from the coordinator"
    - "The full test suite (249+ baseline) is green with no reference to the deleted coordinator internals in passing tests"
    - "All three structured log families (presence INFO, zone INFO, heating DEBUG) appear in an end-to-end async_evaluate run"
  artifacts:
    - path: "custom_components/climate_manager/coordinator.py"
      provides: "Refactored orchestrator delegating to domain objects (D-09)"
      contains: "EvalContext("
  key_links:
    - from: "custom_components/climate_manager/coordinator.py"
      to: "Zone.evaluate"
      via: "async_evaluate zone loop"
      pattern: "zone.evaluate\\(ctx\\)"
    - from: "custom_components/climate_manager/coordinator.py"
      to: "Room.compute_preheat"
      via: "async_evaluate per-room pass"
      pattern: "compute_preheat\\(ctx\\)"
---

<objective>
Implement D-09: refactor `coordinator.py` to instantiate `Zone`, `Person`,
`Room`, `TRV`, and `TRVGroup` objects from stored config at init, and rewrite
`async_evaluate` to build one `EvalContext` per cycle, loop zones
(`zone.evaluate(ctx)`), then run the per-room preheat/calibration pass
(`room.compute_preheat(ctx)`, `room.calibrate_trvs(ctx)`). Remove the now-dead
coordinator internals: `_last_pushed`, `_preheat_*` dicts, `_calendar_cache`,
`_prefetch_calendars`, `_compute_present_persons`, `_compute_desired_temps`,
`_apply_presence_overrides`, `_push_temperatures`, `_push_if_changed`,
`_push_safely`, `_push_off_safely`, `_async_preheat*`, `_async_calibrate_room`,
`_resolve_zone_config`.

WS persistence is UNCHANGED (deferred item — WS handlers still mutate config
dicts). The coordinator reconstructs domain objects from the config dict on
`async_setup_entry`, so WS mutations remain visible on the next tick.

Final task is a human checkpoint: deploy and confirm the three log families
appear in HA logs at the documented levels (OBS-01).

Purpose: Integration wave — wires all five domain modules into the live
evaluate cycle and retires the monolith.
Output: refactored `coordinator.py`, updated `tests/test_coordinator.py`, human
log verification.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-presence-heating-log-traces/16-CONTEXT.md
@.planning/phases/16-presence-heating-log-traces/16-RESEARCH.md
@.planning/phases/16-presence-heating-log-traces/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Instantiate domain objects at coordinator init</name>
  <files>custom_components/climate_manager/coordinator.py</files>
  <read_first>
    - custom_components/climate_manager/coordinator.py (lines 110-264 __init__ + async_evaluate; lines 519-570 _push_temperatures assembly; lines 1292-1314 _resolve_zone_config)
    - custom_components/climate_manager/__init__.py (lines 122-170 async_setup_entry — coordinator construction, runtime_data shape)
    - custom_components/climate_manager/zone.py (Zone constructor + mode-factory)
    - custom_components/climate_manager/person.py (Person constructor + mode-factory)
    - custom_components/climate_manager/room.py (Room constructor, _zone reference, _trv_groups)
    - custom_components/climate_manager/trv.py (TRV, TRVGroup.from_room_config)
    - .planning/phases/16-presence-heating-log-traces/16-CONTEXT.md (D-09 evaluate flow; integration points)
    - .planning/phases/16-presence-heating-log-traces/16-PATTERNS.md (coordinator refactor section: __init__ removals, retained fields)
  </read_first>
  <action>
Add a `_build_domain_objects(self, config)` method called from `__init__` (and
re-callable after WS config changes — see note below) that constructs:
`self._zones: dict[str, Zone]` (Default Zone keyed `"default"` plus each custom
zone), `self._persons: dict[str, Person]`, `self._rooms: dict[str, Room]`.
For each room, assemble its `TRVGroup`/`TRV` instances via
`TRVGroup.from_room_config(entities, matter_mappings, room_name, zone_name)`
(reusing the Matter dedup that moved into trv.py in plan 16-03), wire
`room._zone` to its owning Zone, and populate `room.assigned_persons` from each
person's `room_ids`. Set `zone._rooms` to the Room objects in that zone. Keep
these dicts as the SINGLE strong references to domain objects (Pitfall 1 — never
store modes' owners in temporaries).

In `__init__`, REMOVE the now-owned-elsewhere state: `self._last_pushed`,
`self._preheat_in_progress/active/target/suppressed`, `self._calendar_cache`,
`self._calendar_warn_issued`. RETAIN status-payload fields
(`_last_active_period`, `_last_present_persons`, `_last_room_periods`,
`_last_zone_periods`), the HA-tracker/Matter listener dicts, and
`_frost_locked_rooms` until consumed by Room.

WS-rebuild note: because WS handlers mutate `runtime_config` in place (deferred
— unchanged), add a lightweight rebuild hook so the next `async_evaluate` sees
new config. Simplest correct approach: call `_build_domain_objects(config)` at
the top of `async_evaluate` only when a cheap config fingerprint changed
(e.g. compare a stored hash of the zones/persons/rooms structure), OR rebuild
unconditionally each cycle if construction is cheap. Choose rebuild-on-change
and document the fingerprint in a comment.
  </action>
  <verify>
    <automated>python -c "import ast; ast.parse(open('custom_components/climate_manager/coordinator.py').read())"; make test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - coordinator.py contains `_build_domain_objects` and `self._zones`, `self._persons`, `self._rooms`
    - coordinator.py constructs TRVGroup via the trv.py factory (grep: `TRVGroup`)
    - coordinator.py sets `room._zone` and populates `room.assigned_persons`
    - coordinator.py `__init__` no longer defines `self._last_pushed` (grep returns 0 outside comments: `grep -v '^[[:space:]]*#' coordinator.py | grep -c '_last_pushed ='` is 0)
    - `python -c ast.parse` succeeds
  </acceptance_criteria>
  <done>Coordinator builds Zone/Person/Room/TRV/TRVGroup graphs from config with correct cross-wiring and single strong references; dead init dicts removed.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite async_evaluate + delete migrated methods + update tests</name>
  <files>custom_components/climate_manager/coordinator.py, tests/test_coordinator.py</files>
  <read_first>
    - custom_components/climate_manager/coordinator.py (lines 166-264 async_evaluate; all method bodies being deleted — see objective list with line refs in PATTERNS)
    - custom_components/climate_manager/eval_context.py (EvalContext constructor)
    - tests/test_coordinator.py (lines 83-105 _make_runtime_config; lines 112-159 mock targets to update)
    - tests/test_preheat.py (preheat scenarios now served by Room — confirm they pass against the new path)
    - .planning/phases/16-presence-heating-log-traces/16-PATTERNS.md (coordinator refactor: refactored async_evaluate shape; test mock-target updates)
    - .planning/phases/16-presence-heating-log-traces/16-RESEARCH.md (Open Question 3: grep _last_pushed before deleting; Pitfall 7 test mocking)
  </read_first>
  <action>
Rewrite `async_evaluate` to the D-09 shape: build
`ctx = EvalContext(now=now, hass=self._hass,
period_temperatures=period_temperatures)`; `for zone in self._zones.values():
await zone.evaluate(ctx)`; then `for room in self._rooms.values(): await
room.compute_preheat(ctx); await room.calibrate_trvs(ctx)`; preserve the
Matter-listener first-tick registration and the
`self._hass.bus.async_fire(f"{DOMAIN}_status_update",
self._build_status_payload())` at the end. Recompute the status-payload fields
(`_last_present_persons` from `ctx._presence_cache`, `_last_zone_periods`/
`_last_room_periods` from each Zone's `_current_period`) so
`_build_status_payload` is unchanged in shape.

DELETE the migrated methods (now dead): `_prefetch_calendars`,
`_compute_present_persons`, `_compute_desired_temps`,
`_apply_presence_overrides`, `_push_temperatures`, `_push_if_changed`,
`_push_safely`, `_push_off_safely`, `_async_preheat`, `_async_preheat_room`,
`_async_calibrate_room`, `_resolve_zone_config`. First grep `_last_pushed` and
every deleted method name across the module to confirm no remaining caller
(RESEARCH Open Question 3); fix any straggler reference before deletion. Keep
`_async_calibrate` only if it still orchestrates the per-room calibrate loop —
otherwise fold it into the Room loop.

Update `tests/test_coordinator.py`: keep `_make_runtime_config` unchanged; change
mock targets from coordinator dict access to domain-object access per Pitfall 7
(`coordinator._last_pushed[eid] = X` → `trv_obj.last_pushed = X`; access TRVs
via `coordinator._rooms[area]._trv_groups[i]._trvs[j]`). No new scenarios — only
mock-target changes so existing assertions exercise the new code paths. Run the
full suite and fix any test still referencing deleted internals.
  </action>
  <verify>
    <automated>grep -v '^[[:space:]]*#' custom_components/climate_manager/coordinator.py | grep -c '_push_if_changed\|_compute_present_persons\|_prefetch_calendars' ; make test 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - coordinator.py contains `EvalContext(` and `zone.evaluate(ctx)` and `compute_preheat(ctx)`
    - coordinator.py (non-comment lines) contains no definition of `_push_if_changed`, `_compute_present_persons`, `_prefetch_calendars` (grep count 0)
    - tests/test_coordinator.py references domain objects (grep: `_rooms` or `_trv_groups` or `last_pushed`) instead of `coordinator._last_pushed[`
    - `make test` full suite green at/above 249 baseline
    - tests/test_preheat.py passes against the Room-served preheat path
  </acceptance_criteria>
  <done>async_evaluate delegates to the domain graph via EvalContext; all migrated coordinator methods are deleted; the full suite is green with mocks retargeted to domain objects.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of OBS-01 log families in live HA</name>
  <files>custom_components/climate_manager/coordinator.py</files>
  <action>
Deploy the refactored integration and manually confirm the three OBS-01
structured log families render correctly in a live HA instance with no duplicate
spam and short-stripped names. This is a verification-after-automation
checkpoint — no further code is written unless the operator reports a defect.
  </action>
  <what-built>
The coordinator now delegates the full evaluate cycle to Zone/Person/Room/TRV
domain objects, which emit the three OBS-01 structured log families:
- INFO `presence | person=<name> home=<bool> reason=<source>` on presence flip
- INFO `zone | zone=<name> state=<old>→<new> reason=<why>` on zone state change
- DEBUG `heating | room=<name> temp=<T>°C zone=<zone> slot=<slot>` on TRV push
Anti-spam ensures repeated identical states produce no duplicate lines.
  </what-built>
  <how-to-verify>
1. Run `make build` then `make deploy` to push the integration to the HA test
   instance.
2. Set the HA logger to capture this integration at debug. In HA config (or via
   the logger service), set
   `custom_components.climate_manager: debug`.
3. Run `make logs` and watch for at least one `heating | room=... temp=...°C
   zone=... slot=...` DEBUG line on the first evaluate (startup push fires).
4. Trigger a person presence change (toggle a person to force_present /
   force_absent in the panel) and confirm a single
   `presence | person=<name> home=<bool> reason=force_present` (or
   force_absent) INFO line appears — and that toggling back produces exactly one
   more line, not duplicates.
5. Trigger a zone mode/period change (switch the Default Zone mode in the panel)
   and confirm a single
   `zone | zone=<name> state=<old>→<new> reason=user:...` INFO line.
6. Confirm names are short-stripped (e.g. `person=alice`, `zone=main`,
   `room=kitchen` — no `person.`, `zone_`, `area_` prefixes).
7. Confirm no `None[None]→...` line on startup (first-tick suppression).
  </how-to-verify>
  <verify>
    <human-check>Operator confirms all three log families appear with short names, no duplicate spam, and no first-tick None[None] line.</human-check>
  </verify>
  <done>Operator types "approved" after confirming all three OBS-01 log families render correctly in live HA logs.</done>
  <resume-signal>Type "approved" once all three log families appear correctly with no duplicate spam and short names, or describe what is wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| coordinator → domain objects | config dict (WS-mutable) reconstructed into Zone/Person/Room graph each cycle |
| domain objects → climate services | resolved setpoints/offsets pushed to TRVs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-13 | Tampering | stale domain graph after WS config change | mitigate | Rebuild-on-change fingerprint at top of async_evaluate so next tick reflects new config (WS persistence unchanged) |
| T-16-14 | Information disclosure | weakref GC AssertionError | mitigate | Coordinator _zones/_persons/_rooms dicts are the single strong refs (Pitfall 1) |
| T-16-15 | Denial of service | double-push after refactor | mitigate | TRVGroup Matter dedup assembled once at init (Pitfall 4); _last_pushed fully removed (Pitfall 5) so no dual source of truth |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH.md: zero external deps) |
</threat_model>

<verification>
- `make test` full suite green at/above 249 baseline
- No non-comment reference to deleted coordinator internals remains
- Human checkpoint confirms all three OBS-01 log families render correctly with
  short names, no duplicate spam, and no first-tick None[None] line
</verification>

<success_criteria>
The coordinator is a thin orchestrator: it builds the domain graph from config,
runs one EvalContext per cycle, delegates to Zone/Room, and emits the three
structured OBS-01 log families. All migrated monolith code is deleted, the full
suite is green, and a human confirms the logs in a live HA instance.
</success_criteria>

<output>
Create `.planning/phases/16-presence-heating-log-traces/16-07-SUMMARY.md` when done
</output>
