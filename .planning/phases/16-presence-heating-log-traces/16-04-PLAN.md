---
phase: 16-presence-heating-log-traces
plan: 04
type: execute
wave: 3
depends_on: ["16-01", "16-02"]
files_modified:
  - custom_components/climate_manager/person.py
files_created_tested_by: [tests/test_person.py]
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "A person transitioning present↔absent logs `presence | person=<name> home=<bool> reason=<source>` at INFO once per flip"
    - "Repeated identical presence produces no duplicate log line — anti-spam driven by Person._last_home, no separate guard dict (D-10)"
    - "reason is the mode name only: scheduled|ha|calendar|force_present|force_absent (D-08)"
    - "The person name in the log is short-stripped: person.alice → person=alice (D-01)"
    - "A person assigned to rooms in two zones evaluates exactly once per cycle (presence cache)"
  artifacts:
    - path: "custom_components/climate_manager/person.py"
      provides: "Person + PersonMode state machine (D-05)"
      contains: "class PersonMode"
      min_lines: 100
  key_links:
    - from: "custom_components/climate_manager/person.py"
      to: "ctx._presence_cache"
      via: "Person.evaluate cache check + store"
      pattern: "_presence_cache"
    - from: "custom_components/climate_manager/person.py"
      to: "schedule.resolve_presence"
      via: "PersonModeScheduled.is_present"
      pattern: "resolve_presence\\("
---

<objective>
Implement D-05/D-08/D-10: a new `person.py` module with `PersonMode` (plain base
class, `assert False` overloads — NOT ABC) and five concrete subclasses
(`PersonModeScheduled`, `PersonModeHA`, `PersonModeCalendar`,
`PersonModeForcePresent`, `PersonModeForceAbsent`), plus the `Person` class.

The per-mode branches of `coordinator._compute_present_persons` (lines
1333-1377) map directly onto the subclass `is_present(ctx)` implementations.
`Person.evaluate(ctx)` adds the INFO presence log on `_last_home` flip
(OBS-01) — with the D-08 reason field carrying the mode name only and the D-01
short-stripped person name — and the `ctx._presence_cache` dedup so a person in
two zones evaluates once. Per D-10 the anti-spam guard is the Person's own
`_last_home` state; no separate log-state dict.

Purpose: Third leaf — `zone.py`'s ProgramPresences mode consumes Person next.
Output: `custom_components/climate_manager/person.py` + green
`tests/test_person.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-presence-heating-log-traces/16-CONTEXT.md
@.planning/phases/16-presence-heating-log-traces/16-RESEARCH.md
@.planning/phases/16-presence-heating-log-traces/16-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PersonMode base + five subclasses (weakref, assert-False)</name>
  <read_first>
    - tests/test_person.py (the RED scaffold to turn GREEN)
    - custom_components/climate_manager/coordinator.py (lines 1316-1377 _compute_present_persons — branch-to-subclass mapping)
    - custom_components/climate_manager/schedule.py (resolve_presence, resolve_calendar_presence signatures)
    - custom_components/climate_manager/const.py (PRESENCE_HA, PRESENCE_CALENDAR, PRESENCE_AUTOMATIC, PRESENCE_PRESENT, PRESENCE_ABSENT, DEFAULT_PREHEAT_LEAD_MINUTES)
    - custom_components/climate_manager/eval_context.py (EvalContext.calendar_events contract — calendar mode fetches via ctx)
    - .planning/phases/16-presence-heating-log-traces/16-RESEARCH.md (Pattern 3 weakref base; A3 async is_present)
    - .planning/phases/16-presence-heating-log-traces/16-PATTERNS.md (person.py section: branch mapping, weakref pattern, const imports)
  </read_first>
  <behavior>
    - PersonModeHA.is_present(ctx): True iff hass.states.get(person_id).state == "home"
    - PersonModeCalendar.is_present(ctx): awaits ctx.calendar_events(entity_id), passes events + event_means + wakeup_advance to resolve_calendar_presence
    - PersonModeScheduled.is_present(ctx): delegates to resolve_presence(person_config, now, ...)
    - PersonModeForcePresent.is_present(ctx) → True; PersonModeForceAbsent.is_present(ctx) → False
    - PersonMode.is_present base raises AssertionError (assert False) — NOT NotImplementedError
    - next_occupied_at base returns None; only Scheduled and Calendar override it
    - Each mode exposes a reason_label attribute equal to its mode-name string — the D-08 reason content (scheduled|ha|calendar|force_present|force_absent)
  </behavior>
  <action>
Create `person.py` with SPDX header, docstring,
`from __future__ import annotations`, `import logging`, `import weakref`, and
`_LOGGER = logging.getLogger(__name__)`. Import constants and
`resolve_presence`/`resolve_calendar_presence` per the 16-PATTERNS person.py
import block. Import `EvalContext` from `.eval_context` under `TYPE_CHECKING`.

Define `class PersonMode` with `__init__(self, person)` storing
`weakref.ref(person)` as `self._person_ref` and a `person` property that
dereferences with `assert p is not None`. Base `async def is_present(self, ctx)`
uses `assert False, f"{type(self).__name__}.is_present() not implemented"`. Base
`def next_occupied_at(self, ctx)` returns `None`. Add a class-level
`reason_label` string on each subclass — the D-08 reason content: `"scheduled"`,
`"ha"`, `"calendar"`, `"force_present"`, `"force_absent"` (mode name only).

Implement the five subclasses by copying the matching branch from
`_compute_present_persons`: HA reads `self.person.person_id` state from
`self.person.hass`; Calendar reads `self.person.calendar_config`, awaits
`ctx.calendar_events(eid)`, then calls `resolve_calendar_presence` with the same
args the coordinator uses (event_means, gap_handling, gap_threshold_minutes,
wakeup_advance fallback chain, start_of_local_day); Scheduled/Force* delegate to
`resolve_presence` or return the literal bool. Read all config through
`self.person.<field>` (weakref) — NO config parameters on `is_present`. Only
`PersonModeScheduled` and `PersonModeCalendar` override `next_occupied_at`
(forward-walk / calendar lookup); leave HA and Force* using the base `None`.
  </action>
  <verify>
    <automated>make test 2>&1 | grep -E "test_person" | tail -10; make test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - person.py contains `class PersonMode` and all five subclass names PersonModeScheduled, PersonModeHA, PersonModeCalendar, PersonModeForcePresent, PersonModeForceAbsent
    - person.py contains `assert False` (not `raise NotImplementedError`, not `@abstractmethod`)
    - person.py contains `weakref.ref(`
    - person.py contains `resolve_calendar_presence(` and `resolve_presence(`
    - `make test` person tests for the five mode behaviours pass
  </acceptance_criteria>
  <done>PersonMode base + five weakref-backed assert-False subclasses replicate the coordinator presence branches; calendar mode fetches via ctx.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Person class — INFO presence log + presence-cache dedup</name>
  <read_first>
    - tests/test_person.py (presence-flip log + cache-dedup assertions)
    - custom_components/climate_manager/person.py (the PersonMode subclasses from Task 1)
    - .planning/phases/16-presence-heating-log-traces/16-CONTEXT.md (D-05 Person.evaluate contract, D-08 reason, D-10 anti-spam)
    - .planning/phases/16-presence-heating-log-traces/16-PATTERNS.md (person.py: anti-spam + INFO log pattern; presence cache dedup; _short_name)
  </read_first>
  <behavior>
    - Person.evaluate(ctx): if person_id already in ctx._presence_cache, return it WITHOUT calling the mode (cache dedup, Pitfall 6 — key by full person_id)
    - Otherwise await self._mode.is_present(ctx); store result in ctx._presence_cache[person_id] and self._last_home
    - On _last_home flip (result != previous _last_home), emit one INFO `presence | person=<short> home=<bool> reason=<mode-name>`; no log when unchanged (D-10 anti-spam on _last_home, no separate guard dict)
    - person name in log is short-stripped per D-01 (person.alice → alice) but cache key remains full person_id
    - reason in the INFO line is the mode-name only per D-08 (scheduled|ha|calendar|force_present|force_absent)
    - change_mode(new_mode, reason) swaps self._mode (called by WS handlers; no behaviour change to persistence)
  </behavior>
  <action>
Add `class Person` to `person.py`. `__init__` stores `person_id`, `hass`,
`room_ids: list[str]`, schedule/calendar config fields, `_mode: PersonMode`
(constructed from the person's stored `mode` value via a small mode-factory
mapping the PRESENCE_* constant to the subclass), and
`_last_home: bool | None = None` (the D-10 anti-spam state — no separate
log-state dict).

Implement `async def evaluate(self, ctx) -> bool`: check
`if self.person_id in ctx._presence_cache: return ctx._presence_cache[...]`
FIRST (key by full person_id — Pitfall 6). Else `result = await
self._mode.is_present(ctx)`; if `result != self._last_home`, emit
`_LOGGER.info("presence | person=%s home=%s reason=%s",
_short_name(self.person_id), result, self._mode.reason_label)` — the name is
short-stripped per D-01 and `reason_label` is the D-08 mode name. Then set
`self._last_home = result` and `ctx._presence_cache[self.person_id] = result`;
return result. Reuse the `_short_name` helper (import from `.trv` or define a
local copy consistent with D-01).

Implement `def change_mode(self, new_mode, reason)` swapping `self._mode`.
Implement `def next_occupied_at(self, ctx)` delegating to
`self._mode.next_occupied_at(ctx)`.
  </action>
  <verify>
    <automated>make test 2>&1 | grep -E "test_person" | tail -10; make test 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - person.py contains `class Person`
    - person.py contains the literal format string `presence | person=%s home=%s reason=%s`
    - person.py contains `ctx._presence_cache` read before the mode call and write after
    - person.py keys the presence cache by `self.person_id` (full id, not short name)
    - presence-flip-logs-once and no-duplicate-on-repeat tests pass
    - multi-zone dedup test passes (mode.is_present called once)
    - `make test` full suite green at/above baseline
  </acceptance_criteria>
  <done>Person.evaluate logs INFO once per presence flip with mode-name reason, dedupes via ctx._presence_cache keyed by full person_id, and never double-evaluates a multi-zone person.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PersonModeCalendar → ctx.calendar_events | user-configured calendar entity_id flows through EvalContext |
| PersonModeHA → hass.states | reads person.* entity state (HA-owned, trusted) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-07 | Tampering | calendar entity_id | mitigate | Prefix validation owned by EvalContext.calendar_events (plan 16-02); PersonModeCalendar only passes entity_id through |
| T-16-08 | Spoofing | presence cache key collision | mitigate | Cache keyed by full person_id (Pitfall 6); short-name used only for log display |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH.md: zero external deps) |
</threat_model>

<verification>
- `tests/test_person.py` passes (all five modes, log-on-flip, anti-spam, dedup)
- `make test` full suite green at/above 249 baseline
- `assert False` used for base overloads (no ABC, no NotImplementedError)
</verification>

<success_criteria>
person.py replicates all coordinator presence branches as weakref-backed
PersonMode subclasses, logs INFO once per flip with mode-name reason (D-08),
and dedupes multi-zone evaluation via ctx._presence_cache.
</success_criteria>

<output>
Create `.planning/phases/16-presence-heating-log-traces/16-04-SUMMARY.md` when done
</output>
