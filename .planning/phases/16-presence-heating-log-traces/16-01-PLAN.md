---
phase: 16-presence-heating-log-traces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/test_eval_context.py
  - tests/test_zone.py
  - tests/test_person.py
  - tests/test_room_domain.py
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "Running make test discovers four new domain test files"
    - "Each scaffold test currently fails (RED) because its target module does not yet exist"
  artifacts:
    - path: "tests/test_eval_context.py"
      provides: "EvalContext calendar-cache test scaffold"
      contains: "def test_"
    - path: "tests/test_zone.py"
      provides: "Zone/ZoneMode log + anti-spam test scaffold"
      contains: "def test_"
    - path: "tests/test_person.py"
      provides: "Person/PersonMode presence + log test scaffold"
      contains: "def test_"
    - path: "tests/test_room_domain.py"
      provides: "Room.apply_setpoint delegation test scaffold"
      contains: "def test_"
  key_links:
    - from: "tests/test_zone.py"
      to: "custom_components/climate_manager/zone.py"
      via: "import"
      pattern: "from custom_components.climate_manager.zone import"
---

<objective>
Wave 0 (Nyquist gate). Create the four MISSING domain test files identified in
16-VALIDATION.md and 16-RESEARCH.md "Wave 0 Gaps" so that every later
implementation task has a pre-existing automated verification target.

These tests are written RED first: they import from `eval_context.py`,
`zone.py`, `person.py`, and `room.py` — modules that do not yet exist — so they
fail at import time until the implementation plans land. They encode the locked
log-format contracts (OBS-01) and anti-spam behaviour (D-10/D-11) as
executable assertions.

Purpose: No implementation task in this phase may verify with "MISSING" — these
scaffolds satisfy the Nyquist sampling requirement.
Output: Four new test files under `tests/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-presence-heating-log-traces/16-CONTEXT.md
@.planning/phases/16-presence-heating-log-traces/16-RESEARCH.md
@.planning/phases/16-presence-heating-log-traces/16-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold eval_context + zone + person test files (RED)</name>
  <read_first>
    - tests/test_schedule.py (pure-Python unit test header + import pattern, lines 1-34)
    - tests/test_trv.py (hass-fixture test pattern, lines 1-60 — needed for eval_context calendar fetch)
    - .planning/phases/16-presence-heating-log-traces/16-CONTEXT.md (D-01 name strip, D-02 EvalContext, D-03 zone state format, D-08/D-10 reason + anti-spam)
    - .planning/phases/16-presence-heating-log-traces/16-RESEARCH.md (Pattern 4 exact log format strings)
    - custom_components/climate_manager/const.py (PRESENCE_*, MODE_*, PERIOD_* constant string values)
  </read_first>
  <action>
Create three pytest files using the project test header/import conventions from
test_schedule.py (pure) and test_trv.py (hass fixture). Each file imports its
target module (`eval_context`, `zone`, `person`) which does not exist yet — the
import failure is the intended RED state. Add a leading module docstring naming
the scenarios. Use `pytest.importorskip` is FORBIDDEN — imports must be direct
top-level so the failure is real.

`tests/test_eval_context.py` (async, uses `hass` fixture): test that
`EvalContext.calendar_events(entity_id)` fetches via `hass.services.async_call`
exactly once per entity per cycle and returns the cached list on the second
call for the same entity_id (deduplication). Assert the service is called once
when two callers request the same entity. Reference D-02 cache fields
`_calendar_cache` and `_presence_cache`.

`tests/test_zone.py` (pure, no hass needed for log assertions — use `caplog`):
test that a Zone whose ZoneMode evaluation changes period emits exactly one INFO
record matching `zone | zone=%s state=%s→%s reason=%s` (per D-03 format
`state=<old_period>[<old_mode>]→<new_period>[<new_mode>]`), and that a second
evaluation with the SAME period emits NO additional record (anti-spam, D-10
`_current_period`). Use `caplog.set_level(logging.INFO,
logger="custom_components.climate_manager.zone")`. Assert the short-name strip
(D-01): `zone_main` renders as `zone=main`.

`tests/test_person.py` (pure, `caplog`): test that `Person.evaluate(ctx)` emits
one INFO record matching `presence | person=%s home=%s reason=%s` on a
`_last_home` flip and none on repeat (D-10), that `reason` is the mode name only
per D-08 (`scheduled`/`ha`/`calendar`/`force_present`/`force_absent`), that the
person name is short-stripped per D-01 (`person.alice` → `person=alice`), and
that a second `evaluate` call on the same cycle returns the cached value from
`ctx._presence_cache` without re-running the mode. Stub the five PersonMode
subclass names referenced in D-05 in import statements.

Set placeholder assertion targets to the locked contract strings so the test is
deterministic once the modules exist.
  </action>
  <verify>
    <automated>make test 2>&1 | grep -E "test_eval_context|test_zone|test_person" | grep -qiE "error|fail" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_eval_context.py contains `from custom_components.climate_manager.eval_context import`
    - tests/test_zone.py contains the literal substring `state=` and `reason=` in an assertion
    - tests/test_zone.py contains `from custom_components.climate_manager.zone import`
    - tests/test_person.py contains the literal substring `presence | person=` in an assertion
    - tests/test_person.py contains `from custom_components.climate_manager.person import`
    - `make test` reports collection/import errors for these three files (RED — target modules absent)
  </acceptance_criteria>
  <done>Three RED test files exist with import statements against not-yet-existing modules and assertions encoding the locked log formats.</done>
</task>

<task type="auto">
  <name>Task 2: Scaffold room_domain test file (RED)</name>
  <read_first>
    - tests/test_trv.py (hass-fixture test pattern, lines 1-60)
    - tests/test_preheat.py (preheat scenario reference — Room owns preheat state per D-06)
    - .planning/phases/16-presence-heating-log-traces/16-CONTEXT.md (D-06 Room class, D-07 TRVGroup, D-09 apply_setpoint flow)
    - custom_components/climate_manager/const.py (PERIOD_NORMAL, PERIOD_FROST_PROTECTION, DEFAULT_PERIOD_TEMPERATURES)
  </read_first>
  <action>
Create `tests/test_room_domain.py` (async, `hass` fixture) per the test_trv.py
header convention. Import `Room` from
`custom_components.climate_manager.room` (does not exist yet — RED). Encode two
scenarios as executable tests:

1. `Room.apply_setpoint(period, temp, ctx)` delegates to each TRVGroup in
   `room._trv_groups` — assert a fake/mock TRVGroup's `push` is awaited once per
   group with the given temp and period (D-09 flow `room.apply_setpoint` →
   `group.push`).
2. Room owns preheat and calibration state directly (D-06) — assert the Room
   instance exposes the preheat-state attributes migrated from the coordinator
   (`_preheat_active`, `_preheat_target`, `_preheat_suppressed`) as scalars, not
   coordinator dicts.

Use `make test`-discoverable async test functions. The import failure against
the absent `room` module is the intended RED state.
  </action>
  <verify>
    <automated>make test 2>&1 | grep -E "test_room_domain" | grep -qiE "error|fail" && echo "RED as expected"</automated>
  </verify>
  <acceptance_criteria>
    - tests/test_room_domain.py contains `from custom_components.climate_manager.room import Room`
    - tests/test_room_domain.py contains an assertion referencing `apply_setpoint`
    - tests/test_room_domain.py contains an assertion referencing `_trv_groups`
    - `make test` reports an import/collection error for this file (RED — room.py absent)
  </acceptance_criteria>
  <done>RED room-domain test file exists asserting apply_setpoint→TRVGroup delegation and Room-owned preheat state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test harness → integration modules | Tests import integration code; no external/untrusted input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Tampering | npm/pip/cargo installs | mitigate | No packages installed this phase (RESEARCH.md: zero external deps); no install tasks |
| T-16-02 | Information disclosure | log lines contain entity IDs/zone names | accept | HA logs are local files; not an exploitable surface (RESEARCH.md Security Domain) |
</threat_model>

<verification>
- `make test` collects four new test files
- All four fail RED (their target modules do not exist yet)
- No existing test regresses to error due to the new files
</verification>

<success_criteria>
Four new test files exist under `tests/`, each importing a not-yet-existing
domain module and asserting the locked OBS-01 log contracts. `make test` shows
them RED.
</success_criteria>

<output>
Create `.planning/phases/16-presence-heating-log-traces/16-01-SUMMARY.md` when done
</output>
