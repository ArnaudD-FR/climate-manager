---
phase: 16-presence-heating-log-traces
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - custom_components/climate_manager/eval_context.py
files_created_tested_by: [tests/test_eval_context.py]
autonomous: true
requirements: [OBS-01]
must_haves:
  truths:
    - "EvalContext is created once per evaluation cycle and carries now, hass, period_temperatures"
    - "calendar_events(entity_id) fetches via calendar.get_events at most once per entity per cycle"
    - "A second caller for the same entity_id reads the cached event list, not a second fetch"
  artifacts:
    - path: "custom_components/climate_manager/eval_context.py"
      provides: "EvalContext dataclass — per-cycle shared cache (D-02)"
      contains: "class EvalContext"
      min_lines: 40
  key_links:
    - from: "custom_components/climate_manager/eval_context.py"
      to: "calendar.get_events"
      via: "hass.services.async_call"
      pattern: "async_call\\(.*calendar.*get_events"
---

<objective>
Implement D-02: a standalone `eval_context.py` module exposing the `EvalContext`
dataclass — the per-cycle shared cache created once at the start of every
`async_evaluate`. It replaces the upfront `_prefetch_calendars` pass with a
lazy, on-demand `calendar_events(entity_id)` async method that fetches once per
entity per cycle.

Built as a standalone file (RESEARCH Open Question 2 recommendation) to avoid
the import cycle: `zone.py`/`person.py` import `EvalContext` from here, never
from `coordinator.py`.

Purpose: All downstream domain modules (person, zone, room) depend on this
context object. It is the first leaf of the dependency graph.
Output: `custom_components/climate_manager/eval_context.py` + green
`tests/test_eval_context.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/16-presence-heating-log-traces/16-CONTEXT.md
@.planning/phases/16-presence-heating-log-traces/16-RESEARCH.md
@.planning/phases/16-presence-heating-log-traces/16-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement EvalContext dataclass with lazy calendar cache</name>
  <read_first>
    - tests/test_eval_context.py (the RED scaffold this task turns GREEN)
    - custom_components/climate_manager/__init__.py (ClimateManagerData dataclass pattern, lines 71-108)
    - custom_components/climate_manager/coordinator.py (lines 266-343 — _prefetch_calendars + _fetch_one inner-function structure to migrate; T-11-03 calendar. prefix check)
    - .planning/phases/16-presence-heating-log-traces/16-CONTEXT.md (D-02 field list and method contract)
  </read_first>
  <behavior>
    - calendar_events("calendar.work") fetches once via hass.services.async_call("calendar","get_events",...) and caches under _calendar_cache["calendar.work"]
    - Second call to calendar_events("calendar.work") returns cache, no second service call
    - On HomeAssistantError from the fetch, returns [] (empty list) and records a single WARNING per entity (preserve T-11-03 / D-04 once-per-entity warn semantics)
    - entity_id not starting with "calendar." is rejected before any service call (ASVS V5 input validation, inherited from _prefetch_calendars)
    - _presence_cache and _calendar_cache default to empty dicts via field(default_factory=dict)
  </behavior>
  <action>
Create `eval_context.py` with the SPDX header `# SPDX-License-Identifier: MIT`,
module docstring, `from __future__ import annotations`, and
`_LOGGER = logging.getLogger(__name__)`.

Define `@dataclass class EvalContext` with required positional fields first:
`now: datetime`, `hass: HomeAssistant`, `period_temperatures: dict[str, float]`;
then cache fields `_calendar_cache: dict[str, list] = field(default_factory=dict)`
and `_presence_cache: dict[str, bool] = field(default_factory=dict)`.

Implement `async def calendar_events(self, entity_id: str) -> list:` — migrate
the fetch + WARNING-once + empty-list-fallback logic from
`coordinator._prefetch_calendars` / its `_fetch_one` inner function, but invert
it to lazy: if `entity_id` already in `self._calendar_cache`, return it; else
validate the `calendar.` prefix (return `[]` and warn on mismatch), call
`get_events`, store the result (or `[]` on `HomeAssistantError`), and return.
Use `dt_util.start_of_local_day` / the same `start`/`end` window the coordinator
uses. Keep the once-per-entity WARNING by tracking a `_warn_issued` set field on
the dataclass (default_factory=set) so repeated failures within a long-running
process do not spam — mirror coordinator `_calendar_warn_issued` semantics.

Do NOT add a `_presence_cache` read/write here — that is owned by
`Person.evaluate` (plan 16-03); this module only provides the field.
  </action>
  <verify>
    <automated>make test 2>&1 | tail -5; python -c "import ast,sys; ast.parse(open('custom_components/climate_manager/eval_context.py').read())"</automated>
  </verify>
  <acceptance_criteria>
    - eval_context.py contains `class EvalContext`
    - eval_context.py contains `field(default_factory=dict)` for both cache fields
    - eval_context.py contains `async def calendar_events`
    - eval_context.py contains a `calendar.` prefix guard before any service call (grep: `startswith("calendar."` or equivalent)
    - eval_context.py first line is `# SPDX-License-Identifier: MIT`
    - `make test` shows tests/test_eval_context.py passing (GREEN); no other test regresses
  </acceptance_criteria>
  <done>EvalContext dataclass exists with lazy, deduplicated, prefix-validated calendar fetching; test_eval_context.py is green; full suite stays at or above baseline.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| EvalContext → calendar.get_events | entity_id (user-configured) crosses into an HA service target |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-03 | Tampering | calendar entity_id used as service target | mitigate | Validate `entity_id.startswith("calendar.")` in calendar_events before async_call (inherited T-11-03 / ASVS V5) |
| T-16-04 | Denial of service | repeated failing get_events | mitigate | Per-entity WARNING-once via `_warn_issued`; empty-list fallback never raises into the eval loop |
| T-16-SC | Tampering | npm/pip/cargo installs | accept | No packages installed this phase (RESEARCH.md: zero external deps) |
</threat_model>

<verification>
- `tests/test_eval_context.py` passes
- `make test` full suite is green at or above 249 baseline
- `eval_context.py` imports nothing from `coordinator.py` (no import cycle)
</verification>

<success_criteria>
EvalContext exists as a standalone dataclass with one-fetch-per-entity-per-cycle
calendar caching, prefix validation, and warning-once fallback. The Wave 0
scaffold test for it is green.
</success_criteria>

<output>
Create `.planning/phases/16-presence-heating-log-traces/16-02-SUMMARY.md` when done
</output>
