---
phase: 11-calendar-presence-backend
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified:
  - custom_components/climate_manager/websocket.py
  - frontend/src/types.ts
  - tests/test_calendar.py
autonomous: true
requirements: [CAL-01, CAL-04]
must_haves:
  truths:
    - "D-08: set_person_config persists calendar_config {entity_id, event_means} for a person"
    - "D-09: calendar_config is sparse-merged additively (absent for non-Calendar persons, no migration of existing persons)"
    - "D-09b: exactly one calendar_config per person — a single calendar entity, no multi-source support"
    - "D-10: set_person_config persists preheat_lead_minutes for a person (default 60, clamped 0–480)"
    - "An entity_id not starting with calendar. is rejected before persistence"
    - "Persisting calendar config triggers a coordinator re-evaluate"
  artifacts:
    - path: "custom_components/climate_manager/websocket.py"
      provides: "set_person_config accepts and validates calendar_config + preheat_lead_minutes"
      contains: "calendar_config"
    - path: "frontend/src/types.ts"
      provides: "PersonConfig calendar fields + calendar period color/labels"
      contains: "calendar_config"
  key_links:
    - from: "websocket.set_person_config"
      to: "store.async_save + coordinator.async_evaluate"
      via: "sparse-merge update then write-then-evaluate"
      pattern: "async_create_task"
---

<objective>
Extend the `set_person_config` WebSocket handler to accept and persist
`calendar_config` and `preheat_lead_minutes` (with an entity-id prefix guard),
and add the matching TypeScript types plus calendar period color/label tokens in
`types.ts`.

Purpose: CAL-01/CAL-04 persistence layer — the frontend (Plan 04) writes these
fields and the backend (Plan 02) reads them on next evaluate. The handler already
sparse-merges any dict, so the work is the validation guard, the docstring
contract, and the type definitions the UI depends on.

Output: Extended `websocket.py`, extended `types.ts`, a WS-level persistence test
in `tests/test_calendar.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-calendar-presence-backend/11-CONTEXT.md
@.planning/phases/11-calendar-presence-backend/11-RESEARCH.md
@.planning/phases/11-calendar-presence-backend/11-PATTERNS.md
@.planning/phases/11-calendar-presence-backend/11-VALIDATION.md
@.planning/phases/11-calendar-presence-backend/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend set_person_config to persist + validate calendar fields</name>
  <files>custom_components/climate_manager/websocket.py, tests/test_calendar.py</files>
  <read_first>
    - custom_components/climate_manager/websocket.py (read _make_ws_set_person_config
      lines 451-500 in full — the vol schema, the schedule_even/odd seeding block,
      the setdefault+update sparse-merge, and the write-then-evaluate tail)
    - tests/test_calendar.py (read the integration-test setup added in Plan 02 for
      MockConfigEntry + hass fixture style)
    - .planning/phases/11-calendar-presence-backend/11-PATTERNS.md (websocket.py
      section — the documented accepted-keys docstring and the entity_id prefix check)
    - .planning/phases/11-calendar-presence-backend/11-RESEARCH.md (Security Domain
      V5 — calendar_config.entity_id validation)
  </read_first>
  <behavior>
    Add to tests/test_calendar.py:
    - test_ws_persists_calendar_config: send set_person_config with
      config={"mode":"calendar","calendar_config":{"entity_id":"calendar.x",
      "event_means":"present"}}; assert runtime_config["persons"][pid]
      ["calendar_config"] == {"entity_id":"calendar.x","event_means":"present"}
      and store.async_save was awaited.
    - test_ws_persists_preheat_lead_minutes: send config={"preheat_lead_minutes":90};
      assert persisted value == 90.
    - test_ws_rejects_non_calendar_entity_id: send
      calendar_config={"entity_id":"light.kitchen","event_means":"absent"}; assert
      the invalid calendar_config is NOT persisted (either dropped or the command
      returns an error result) and no light.* entity is ever passed to get_events.
  </behavior>
  <action>
    The vol schema `vol.Required("config"): dict` already accepts `calendar_config`
    and `preheat_lead_minutes` via the existing `update(msg["config"])` sparse-merge
    (D-09 additive sparse merge — no migration of existing persons) — no new vol
    field is structurally required for them to pass through. Add input validation
    before the merge (RESEARCH Security V5): if `incoming` contains a
    `calendar_config`, verify it is a dict whose `entity_id` is a string starting
    with `"calendar."` (D-08 schema {entity_id, event_means}; D-09b exactly one
    calendar_config per person); if it fails the check, drop the `calendar_config`
    key from `incoming` (do not persist an invalid entity target) so the bad value
    never reaches `_prefetch_calendars`. Optionally clamp `preheat_lead_minutes`
    (D-10) to the int range 0–480 if present (matches the UI input bounds;
    out-of-range values are dropped). Update the handler docstring to enumerate the
    full set of accepted sparse keys including `calendar_config: {"entity_id": str,
    "event_means": "absent"|"present"}` and `preheat_lead_minutes: int (0–480)`.
    Keep the existing schedule_even/odd seeding block, the setdefault+update merge,
    and the write-then-evaluate tail (`store.async_save` → `send_result` →
    `async_create_task(coordinator.async_evaluate())`) unchanged.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_calendar.py -k "ws_" -v -x</automated>
  </verify>
  <acceptance_criteria>
    - websocket.py docstring for set_person_config lists `calendar_config` and `preheat_lead_minutes` as accepted keys
    - test_ws_persists_calendar_config passes (round-trips entity_id + event_means)
    - test_ws_persists_preheat_lead_minutes passes
    - test_ws_rejects_non_calendar_entity_id passes (light.* entity_id not persisted)
    - `.venv/bin/python -m pytest tests/test_calendar.py -k "ws_" -v -x` exits 0
  </acceptance_criteria>
  <done>Calendar config and preheat lead time persist via the existing sparse-merge with an entity-id guard; re-evaluate still fires.</done>
</task>

<task type="auto">
  <name>Task 2: Add calendar types and period tokens to types.ts</name>
  <files>frontend/src/types.ts</files>
  <read_first>
    - frontend/src/types.ts (read the PersonConfig interface lines 42-50,
      PRESENCE_COLORS lines 170-173, PERIOD_LABELS lines 176-183,
      PERIOD_DISPLAY_NAMES lines 186-193)
    - .planning/phases/11-calendar-presence-backend/11-UI-SPEC.md (Color section —
      calendar color #5C6BC0, label "C", display name "Calendar")
  </read_first>
  <action>
    Extend the `PersonConfig` interface with two optional fields:
    `calendar_config?: { entity_id: string; event_means: "absent" | "present" }`
    (D-08) and `preheat_lead_minutes?: number` (D-10; absent = 60). Add
    `calendar: "#5C6BC0"` to `PRESENCE_COLORS` (UI-SPEC fixed semantic indigo-400),
    `calendar: "C"` to `PERIOD_LABELS`, and `calendar: "Calendar"` to
    `PERIOD_DISPLAY_NAMES`. Two-space indent, 80-col, LF, trailing newline per
    .editorconfig. No other changes.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'calendar_config' frontend/src/types.ts` returns >= 1
    - `grep -c 'preheat_lead_minutes' frontend/src/types.ts` returns >= 1
    - PRESENCE_COLORS contains `calendar: "#5C6BC0"`; PERIOD_LABELS contains `calendar: "C"`; PERIOD_DISPLAY_NAMES contains `calendar: "Calendar"`
    - `cd frontend && npx tsc --noEmit` exits 0 (no type errors)
  </acceptance_criteria>
  <done>PersonConfig carries calendar_config + preheat_lead_minutes; calendar period color/label/name tokens present; TypeScript compiles.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel → WS handler | Untrusted client config dict crosses into set_person_config and gets persisted |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-06 | Tampering | `calendar_config.entity_id` from the panel | mitigate | Prefix-validate `entity_id.startswith("calendar.")`; drop invalid calendar_config before persist/service-call (ASVS V5) |
| T-11-07 | Denial of Service | out-of-range preheat_lead_minutes | mitigate | Clamp/drop values outside 0–480 before persist |
| T-11-08 | Elevation of Privilege | unauthenticated WS access | accept | Existing HA WS auth gate (T-03-06) covers all commands; no new endpoint added |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs (D-02); tsc uses existing devDeps; no new [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_calendar.py -k "ws_" -v` — WS persistence tests green
- `cd frontend && npx tsc --noEmit` — types compile
- `make lint` — editorconfig clean (Python 4-space, TS 2-space, 80-col, LF)
</verification>

<success_criteria>
- set_person_config persists calendar_config + preheat_lead_minutes with entity-id guard
- Non-calendar entity_id is rejected before persistence
- PersonConfig type + calendar period tokens added; TypeScript compiles
- Re-evaluate still triggered after persist
</success_criteria>

<output>
Create `.planning/phases/11-calendar-presence-backend/11-03-SUMMARY.md` when done
</output>
</content>
