---
phase: 11-calendar-presence-backend
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - custom_components/climate_manager/const.py
  - custom_components/climate_manager/schedule.py
  - tests/test_calendar.py
autonomous: true
requirements: [CAL-01, CAL-03, CAL-04]
must_haves:
  truths:
    - "D-03: PRESENCE_CALENDAR = \"calendar\" constant exists alongside the other presence modes in const.py"
    - "D-08, D-09: calendar_config {entity_id, event_means} is a sparse additive key (absent = not using Calendar mode, no migration)"
    - "A calendar event active at now with event_means=absent makes the person absent"
    - "No active calendar event with event_means=absent makes the person present"
    - "D-10, D-11, D-12: preheat_lead_minutes (per-person, default 60, fixed offset) treats a calendar-absent person whose active event ends within the lead window as present (pre-heat)"
    - "D-06: A schedule period with state='calendar' resolves presence via its own attached calendar_config (per-period, not person-level)"
    - "D-07: Calendar period state is non-recursive — a calendar state inside calendar mode is not a supported dispatch path"
    - "All-day (DATE-only) events are parsed without TypeError on aware/naive comparison"
  artifacts:
    - path: "custom_components/climate_manager/const.py"
      provides: "PRESENCE_CALENDAR and DEFAULT_PREHEAT_LEAD_MINUTES constants"
      contains: "PRESENCE_CALENDAR = \"calendar\""
    - path: "custom_components/climate_manager/schedule.py"
      provides: "resolve_calendar_presence() pure helper + calendar period state in resolve_presence()"
      contains: "def resolve_calendar_presence"
    - path: "tests/test_calendar.py"
      provides: "Pure unit tests for calendar resolution logic"
      contains: "def test_calendar_mode_absent_during_event"
  key_links:
    - from: "schedule.py:resolve_presence"
      to: "schedule.py:resolve_calendar_presence"
      via: "period state == 'calendar' dispatch using calendar_cache"
      pattern: "resolve_calendar_presence"
---

<objective>
Add the pure-Python calendar presence resolution layer: the `PRESENCE_CALENDAR`
constant, `DEFAULT_PREHEAT_LEAD_MINUTES`, a synchronous `resolve_calendar_presence()`
helper, calendar-aware date parsing, and `"calendar"` period-state support inside
`resolve_presence()`. All logic is HA-import-free and unit-tested first (TDD).

Purpose: This is the deterministic core of the calendar feature (CAL-01, CAL-03,
CAL-04). Keeping it pure Python preserves the `schedule.py` "no HA imports"
contract so existing `test_schedule.py` stays valid and the coordinator (Plan 02)
only has to supply already-fetched event lists.

Output: Extended `const.py`, extended `schedule.py`, new `tests/test_calendar.py`
covering all pure resolution behaviors.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-calendar-presence-backend/11-CONTEXT.md
@.planning/phases/11-calendar-presence-backend/11-RESEARCH.md
@.planning/phases/11-calendar-presence-backend/11-PATTERNS.md
@.planning/phases/11-calendar-presence-backend/11-VALIDATION.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add calendar constants to const.py</name>
  <files>custom_components/climate_manager/const.py</files>
  <read_first>
    - custom_components/climate_manager/const.py (read the presence mode constant
      block around lines 40-43, the defaults block around lines 57-64, and the
      persons sub-schema comment block around lines 130-151 — see current state
      before editing)
  </read_first>
  <behavior>
    - Test: `from custom_components.climate_manager.const import PRESENCE_CALENDAR`
      succeeds and equals the string "calendar"
    - Test: `DEFAULT_PREHEAT_LEAD_MINUTES` imports and equals the int 60
  </behavior>
  <action>
    Add `PRESENCE_CALENDAR = "calendar"` immediately after the existing
    `PRESENCE_HA = "ha"` line (per D-03, D-21 define-before-use ordering — this
    constant must precede any schedule.py import). Add
    `DEFAULT_PREHEAT_LEAD_MINUTES: int = 60` (per D-10) immediately after the
    existing `DEFAULT_GLOBAL_MODE` line in the defaults block. Extend the persons
    sub-schema comment block (after the `schedule_odd` comment) to document two
    new sparse keys: `calendar_config` with shape `{"entity_id": "calendar.*",
    "event_means": "absent" | "present"}` (default "absent", absent when not
    using Calendar mode, per D-08/D-09) and `preheat_lead_minutes` int default 60
    (absent = default, per D-10). Do NOT add either new key to the `DEFAULT_CONFIG`
    dict — additive sparse schema, absent = default (D-09).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_calendar.py::test_constants -x</automated>
  </verify>
  <acceptance_criteria>
    - `const.py` contains the exact line `PRESENCE_CALENDAR = "calendar"`
    - `const.py` contains `DEFAULT_PREHEAT_LEAD_MINUTES: int = 60`
    - `grep -v '^#' custom_components/climate_manager/const.py | grep -c 'calendar_config'` returns 0 (calendar_config is documented only in comments, never added to DEFAULT_CONFIG)
    - `.venv/bin/python -m pytest tests/test_calendar.py::test_constants -x` exits 0
  </acceptance_criteria>
  <done>Both constants importable; DEFAULT_CONFIG unchanged; schema comment documents new keys.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED+GREEN — resolve_calendar_presence() helper and date parsing</name>
  <files>custom_components/climate_manager/schedule.py, tests/test_calendar.py</files>
  <read_first>
    - custom_components/climate_manager/schedule.py (read the module docstring
      lines 1-6 "pure Python — no HA imports" contract, the `_parse_time` helper
      around line 71, and `resolve_presence` around lines 123-180)
    - tests/test_schedule.py (read header/import pattern and a couple of test
      function bodies to match style)
    - .planning/phases/11-calendar-presence-backend/11-RESEARCH.md (read the
      "Pre-heat Lead Time" detection algorithm and "Checking if an event covers
      now" sections; Landmine 3 on all-day DATE strings)
  </read_first>
  <behavior>
    Write these tests in tests/test_calendar.py FIRST (RED), all under
    `@pytest.mark.freeze_time("2026-06-01 12:00:00")` where time matters:
    - test_constants: imports PRESENCE_CALENDAR == "calendar" and
      DEFAULT_PREHEAT_LEAD_MINUTES == 60
    - test_calendar_mode_absent_during_event: event 08:00-17:00 +02:00, now=12:00,
      event_means="absent", preheat=60 → resolve_calendar_presence returns False
    - test_calendar_mode_present_no_event: events=[], event_means="absent" →
      returns True
    - test_calendar_mode_present_during_event_when_means_present: active event,
      event_means="present" → returns True
    - test_calendar_mode_absent_no_event_when_means_present: events=[],
      event_means="present" → returns False
    - test_preheat_triggers_at_boundary: active event ends exactly 60 min from
      now, event_means="absent", preheat=60 → returns True (event_end <= now+lead)
    - test_preheat_no_trigger_before_boundary: active event ends 61 min from now,
      preheat=60 → returns False
    - test_allday_event_handling: all-day event with DATE-only strings
      start="2026-06-01" end="2026-06-02", now=12:00, event_means="absent" →
      returns False and raises no TypeError
  </behavior>
  <action>
    Add `from .const import ... PRESENCE_CALENDAR` to the existing const import
    block (alphabetical position). Add a module-level pure helper
    `_parse_calendar_dt(s: str, start_of_local_day) -> datetime.datetime`: if "T"
    in `s`, return `datetime.datetime.fromisoformat(s)` (timed event, carries
    offset); else parse `datetime.date.fromisoformat(s)` and return
    `start_of_local_day(d)` to get an aware local-midnight datetime (Landmine 3 —
    never compare naive vs aware). `start_of_local_day` is passed in as a callable
    so schedule.py imports nothing from homeassistant (the coordinator will pass
    `dt_util.start_of_local_day`).

    Add the public pure helper `resolve_calendar_presence(events: list[dict],
    event_means: str, now: datetime.datetime, preheat_lead_minutes: int = 60,
    start_of_local_day=None) -> bool` implementing the RESEARCH "Pre-heat Lead
    Time" algorithm (D-10 fixed per-person offset, D-12 independent of Phase 12
    inertia learning): walk all events; for each, skip if missing start/end; parse
    via `_parse_calendar_dt`; `event_active = event_start <= now < event_end`.
    When event_means=="absent" and an event is active: return True if
    `event_end <= now + timedelta(minutes=preheat_lead_minutes)` (pre-heat:
    person returns soon → heat now), else return False. When event_means=="present"
    and an event is active: return True. After the loop (no active event): return
    True if event_means=="absent" (no event = present), else False.
    `start_of_local_day` defaults to a module-local fallback only used by tests;
    document that production callers pass `dt_util.start_of_local_day`. In the
    test file, import and pass `dt_util.start_of_local_day` from
    `homeassistant.util.dt` so the test exercises the real conversion.
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_calendar.py -k "calendar_mode or preheat or allday or constants" -v -x</automated>
  </verify>
  <acceptance_criteria>
    - `schedule.py` contains `def resolve_calendar_presence(` and
      `def _parse_calendar_dt(`
    - `grep -c "import homeassistant\|from homeassistant" custom_components/climate_manager/schedule.py` returns 0 (pure Python contract preserved — Landmine 4)
    - test_preheat_triggers_at_boundary and test_preheat_no_trigger_before_boundary both pass (boundary at exactly lead-minutes is inclusive → present)
    - test_allday_event_handling passes with no TypeError
    - `.venv/bin/python -m pytest tests/test_calendar.py -k "calendar_mode or preheat or allday or constants" -v -x` exits 0
  </acceptance_criteria>
  <done>resolve_calendar_presence() passes the exhaustive event_means × active × preheat table; schedule.py has zero HA imports.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: RED+GREEN — period state "calendar" inside resolve_presence()</name>
  <files>custom_components/climate_manager/schedule.py, tests/test_calendar.py</files>
  <read_first>
    - custom_components/climate_manager/schedule.py (read resolve_presence lines
      123-180 in full, especially the period walk at lines 168-179 that returns
      `active_state == "present"`)
    - .planning/phases/11-calendar-presence-backend/11-RESEARCH.md (Open Question 2
      — Option A: optional `calendar_cache` param; Landmine 7 return-type)
  </read_first>
  <behavior>
    Add to tests/test_calendar.py (RED first):
    - test_calendar_period_state_resolves: a single-mode schedule whose active
      period has `state: "calendar"` and `calendar_config:{entity_id:"calendar.x",
      event_means:"absent"}`; call resolve_presence(person_config, now,
      calendar_cache={"calendar.x":[active_event]}) → returns False (active event,
      absent). With calendar_cache={"calendar.x":[]} → returns True (no event).
    - test_calendar_period_state_missing_cache_defaults_absent: period state
      "calendar" but entity_id not in calendar_cache → treated as empty events →
      present (event_means absent, no event). Confirm no exception.
    - test_present_absent_periods_unchanged: a schedule using only "present"/
      "absent" period states returns the same bool with and without the
      calendar_cache argument (regression guard).
  </behavior>
  <action>
    Extend `resolve_presence` signature with a new optional keyword parameter
    `calendar_cache: dict | None = None` (per RESEARCH Open Question 2 Option A —
    minimal footprint, schedule.py stays self-contained). In the period walk,
    after determining the `active_state` for the current time, branch on the
    active period's state: if `active_state == "calendar"`, read the active
    period's own `calendar_config` dict (entity_id, event_means), look up
    `events = (calendar_cache or {}).get(entity_id, [])`, and return
    `resolve_calendar_presence(events, event_means_default_absent, now,
    preheat=person preheat or 60)`. Otherwise keep the existing
    `return active_state == "present"` behavior unchanged. The period whose
    `start` is the latest one <= current_time is the active period — its
    calendar_config (NOT a person-level one) is used (D-06, per-period config).
    Calendar period state does NOT recurse (D-07): a "calendar" period inside a
    calendar-mode person is not a path that occurs because mode dispatch routes
    calendar-mode persons elsewhere. Do not change the default-before-first-period
    behavior. Pass `start_of_local_day` through to resolve_calendar_presence as
    the same callable schedule.py received (None default in pure tests; the test
    passes `dt_util.start_of_local_day`).
  </action>
  <verify>
    <automated>.venv/bin/python -m pytest tests/test_calendar.py -k "period_state" -v -x</automated>
  </verify>
  <acceptance_criteria>
    - resolve_presence accepts `calendar_cache=` keyword without breaking existing positional calls
    - test_calendar_period_state_resolves passes (active event → absent; empty → present)
    - test_present_absent_periods_unchanged passes (regression: non-calendar schedules unaffected)
    - `.venv/bin/python -m pytest tests/test_schedule.py -x` exits 0 (existing schedule tests still green — no signature regression)
    - `.venv/bin/python -m pytest tests/test_calendar.py -k "period_state" -v -x` exits 0
  </acceptance_criteria>
  <done>Calendar period state resolves via per-period calendar_config and the pre-fetched cache; present/absent periods and existing tests unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| event dict → parser | Calendar event start/end strings (from HA, but treated as untrusted format) cross into `_parse_calendar_dt` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | `_parse_calendar_dt` on malformed/missing start/end strings | mitigate | Skip events missing start or end; only `datetime.fromisoformat`/`date.fromisoformat` used — no eval, no external parser (D-02 no deps) |
| T-11-02 | Denial of Service | naive-vs-aware datetime TypeError on all-day events | mitigate | All-day DATE strings converted via start_of_local_day callable → always aware (Landmine 3); regression-tested by test_allday_event_handling |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan (D-02 — manifest.json requirements stays []); RESEARCH Package Legitimacy Audit skipped, no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `.venv/bin/python -m pytest tests/test_calendar.py -v` — all pure-logic tests green
- `.venv/bin/python -m pytest tests/test_schedule.py -v` — no regression from resolve_presence signature change
- `grep -c "from homeassistant" custom_components/climate_manager/schedule.py` returns 0
- `make lint` — editorconfig clean (4-space Python, 80-col, LF, final newline)
</verification>

<success_criteria>
- PRESENCE_CALENDAR and DEFAULT_PREHEAT_LEAD_MINUTES defined and importable
- resolve_calendar_presence() implements the full event_means × active × preheat table
- Period state "calendar" resolves via per-period calendar_config + calendar_cache
- schedule.py remains HA-import-free; existing test_schedule.py passes
- All tests in tests/test_calendar.py (pure subset) pass
</success_criteria>

<output>
Create `.planning/phases/11-calendar-presence-backend/11-01-SUMMARY.md` when done
</output>
</content>
</invoke>
