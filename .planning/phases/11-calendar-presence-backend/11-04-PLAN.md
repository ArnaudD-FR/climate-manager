---
phase: 11-calendar-presence-backend
plan: 04
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - frontend/src/components/person-card.ts
  - frontend/src/components/persons-tab.ts
autonomous: false
requirements: [CAL-01, CAL-03, CAL-04]
must_haves:
  truths:
    - "The presence mode picker offers a Calendar option"
    - "D-15, D-16: Selecting Calendar mode shows a native <select> entity picker, an event_means <select> toggle (Absent/Present during events), and a pre-heat lead input"
    - "D-11: The pre-heat lead time is editable as a per-person minutes number input, visible only when Calendar mode is selected"
    - "D-15: The calendar entity picker is a native <select> listing hass calendar.* entities by friendly name (entity_id stored)"
    - "Changing any calendar control auto-saves via setPersonConfig (no Save button)"
    - "D-14: The scheduling/calendar section renders above the room associations list"
    - "D-17: A schedule period can be set to Calendar state via the period-state select (present/absent/calendar), showing inline calendar config below the row"
  artifacts:
    - path: "frontend/src/components/person-card.ts"
      provides: "Calendar mode UI, badge, layout reorder, period calendar state"
      contains: "PRESENCE_MODE_CALENDAR"
  key_links:
    - from: "person-card.ts calendar controls"
      to: "ws.setPersonConfig"
      via: "@change handlers auto-save calendar_config + preheat_lead_minutes"
      pattern: "setPersonConfig"
    - from: "person-card.ts entity picker"
      to: "hass.states"
      via: "filter id.startsWith('calendar.')"
      pattern: "startsWith\\(\"calendar\\.\"\\)"
---

<objective>
Build the Calendar mode UI in the person card: a Calendar option in the mode
select, a calendar config block (entity picker + event_means + pre-heat lead
input) visible when Calendar mode is selected, a Calendar badge, the D-14 layout
reorder (scheduling above rooms), and the Calendar period state in the schedule
period editor with inline config. All controls auto-save on change.

Purpose: CAL-01/CAL-03/CAL-04 frontend — the user-facing surface that writes the
config Plans 01-03 consume. This is UI/Lit rendering work (standard plan, not TDD)
verified by a human checkpoint since Lit rendering is poorly unit-testable
(VALIDATION manual-only table).

Output: Extended `person-card.ts` and (if needed) `persons-tab.ts` hass binding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-calendar-presence-backend/11-CONTEXT.md
@.planning/phases/11-calendar-presence-backend/11-RESEARCH.md
@.planning/phases/11-calendar-presence-backend/11-PATTERNS.md
@.planning/phases/11-calendar-presence-backend/11-UI-SPEC.md
@.planning/phases/11-calendar-presence-backend/11-VALIDATION.md
@.planning/phases/11-calendar-presence-backend/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Calendar mode option, badge, config block, and layout reorder</name>
  <files>frontend/src/components/person-card.ts, frontend/src/components/persons-tab.ts</files>
  <read_first>
    - frontend/src/components/person-card.ts (read presence mode constants lines
      40-45; _getBadgeInfo switch lines 511-523; the .mode-badge.scheduled CSS lines
      220-224; _onModeChange handler lines 320-342; the mode select in render lines
      599-626; and the expanded-section render order lines 592-775 — mode select,
      room associations, presence schedule)
    - frontend/src/components/persons-tab.ts (read the hass.states filter at lines
      110-112 and the PersonCard binding `.panel=${this.panel}` around line 165 to
      confirm hass reaches the card)
    - frontend/src/types.ts (confirm PersonConfig.calendar_config, preheat_lead_minutes
      types from Plan 03 are present)
    - .planning/phases/11-calendar-presence-backend/11-UI-SPEC.md (Component
      Inventory, Copywriting Contract, Interaction Contract, Layout Reorder D-14)
    - .planning/phases/11-calendar-presence-backend/11-PATTERNS.md (person-card.ts
      section — the save-handler and calendar-config-block templates)
  </read_first>
  <action>
    Add `const PRESENCE_MODE_CALENDAR = "calendar";` to the mode constants. Add
    `<option value=${PRESENCE_MODE_CALENDAR} ...>Calendar</option>` to the mode
    `<select>` after the HA option (existing generic `_onModeChange` already saves
    `{ mode }` — no handler change). Add a `case PRESENCE_MODE_CALENDAR: return {
    cls: "calendar", text: "Calendar" }` to `_getBadgeInfo()` and a
    `.mode-badge.calendar` CSS rule mirroring `.mode-badge.scheduled` (muted
    secondary background/text — UI-SPEC Color). Compute
    `const calendarEntityIds = Object.keys(this.panel.hass?.states ?? {})
    .filter((id) => id.startsWith("calendar.")).sort();` (D-15 native <select>
    entity picker populated from hass.states filtered to calendar.*) and render
    option text as `friendly_name ?? id`; when the list is empty render the single
    disabled option "No calendar entities found in Home Assistant." (UI-SPEC empty
    state).

    Add three auto-save handlers copying the `_onModeChange`
    setPersonConfig→reloadConfig→showToast pattern (Phase 9 D-09 auto-save, no Save
    button): `_onCalendarEntityChange` (saves
    `{calendar_config:{entity_id:newId, event_means:currentMeans}}`),
    `_onEventMeansChange` (saves `{calendar_config:{entity_id:currentId,
    event_means:newMeans}}`), `_onPreheatChange` (parse int, only save
    `{preheat_lead_minutes:val}` when 0<=val<=480). Render the calendar config block
    only when `currentMode === PRESENCE_MODE_CALENDAR`: section label "Calendar
    source" + entity `<select class="mode-select">` (placeholder "— Select a
    calendar —", value ""); section label "Event means" + `<select>` with "Absent
    during events" (value "absent", default) / "Present during events" (value
    "present") — D-16 native <select> event_means toggle; section label "Pre-heat
    lead time" + `<input type="number" min="0" max="480" step="5">` bound to
    `.value=${String(preheat_lead_minutes ?? 60)}` with a trailing `<span>min</span>`
    (D-11 per-person lead-time input, Calendar-mode only; HA 2026.x: native input,
    NOT ha-textfield). Set the calendar-mode schedule-hint copy to "Presence
    determined by calendar events on the selected entity."

    Apply the D-14 reorder of the expanded section to: (1) presence mode label +
    select, (2) hint/stuck-mode paragraph, (3) calendar config block (mode ===
    calendar only), (4) presence schedule section (mode === scheduled only),
    (5) room associations (moved LAST). If `this.panel.hass` is not reliably
    populated, add `.hass=${this.hass}` to the PersonCard binding in persons-tab.ts
    and read from `this.hass` instead — otherwise leave persons-tab.ts unchanged.
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - person-card.ts contains `PRESENCE_MODE_CALENDAR = "calendar"` and a Calendar `<option>` in the mode select
    - person-card.ts contains `_onCalendarEntityChange`, `_onEventMeansChange`, `_onPreheatChange`, each calling `setPersonConfig`
    - `grep -c 'startsWith("calendar.")' frontend/src/components/person-card.ts` returns >= 1
    - The calendar config block is gated on `=== PRESENCE_MODE_CALENDAR` and renders the number input with min="0" max="480"
    - Room associations render after the scheduling/calendar section (D-14 reorder)
    - `cd frontend && npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>Calendar mode selectable; config block + badge present; auto-save wired; layout reordered; build green.</done>
</task>

<task type="auto">
  <name>Task 2: Calendar period state in the schedule period editor</name>
  <files>frontend/src/components/person-card.ts</files>
  <read_first>
    - frontend/src/components/person-card.ts (read the presence schedule / period
      editor render block and the period-state select; the time-bar usage and how
      period state values are currently constrained to present/absent)
    - frontend/src/types.ts (PRESENCE_COLORS/PERIOD_LABELS/PERIOD_DISPLAY_NAMES now
      include "calendar" from Plan 03 — reuse these tokens, do not redefine colors)
    - .planning/phases/11-calendar-presence-backend/11-UI-SPEC.md (Calendar period
      state section + "Period inline label" copy "Calendar: " + friendly name)
    - .planning/phases/11-calendar-presence-backend/11-RESEARCH.md (Landmine 6 —
      keep PRESENCE_CYCLE click-cycling as present/absent; calendar is select-only)
  </read_first>
  <action>
    In the Scheduled-mode period editor, extend the period-state `<select>` options
    from present/absent to present/absent/calendar (add `<option value="calendar">
    Calendar</option>`) — D-17 period state options extend to ["present", "absent",
    "calendar"]. Do NOT add "calendar" to the time-bar PRESENCE_CYCLE used for
    click/drag cycling (Landmine 6 — calendar is reachable only via the explicit
    period-state select). When a period's `state === "calendar"`, render an inline
    calendar config block below that period row (D-17 inline calendar config appears
    when calendar is selected): the same entity picker + event_means select pattern
    as Task 1 but scoped to the period (writes the period's own `calendar_config`,
    per D-06, NOT the person-level one). The period summary/inline label shows
    "Calendar: " + the selected entity's friendly_name from `hass.states` (UI-SPEC
    copy) rather than the raw entity_id. Period blocks rendered with state "calendar"
    use the existing `PRESENCE_COLORS.calendar` (#5C6BC0), `PERIOD_LABELS.calendar`
    ("C"), and `PERIOD_DISPLAY_NAMES.calendar` ("Calendar") tokens from types.ts.
    Persist period changes through the same schedule-write path the period editor
    already uses (auto-save).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - The period-state select includes a `value="calendar"` option labeled "Calendar"
    - "calendar" is NOT added to the time-bar click/drag PRESENCE_CYCLE (grep the cycle definition to confirm it stays present/absent)
    - A period with state "calendar" renders an inline entity picker + event_means select writing the period's own calendar_config
    - Calendar period blocks use PRESENCE_COLORS.calendar / PERIOD_LABELS.calendar from types.ts (no hardcoded #5C6BC0 in person-card.ts)
    - `cd frontend && npx tsc --noEmit` exits 0 and `npm run build` succeeds
  </acceptance_criteria>
  <done>Period editor offers Calendar state via select with inline per-period config; click-cycling unchanged; build green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of Calendar mode UI</name>
  <action>
    After Tasks 1-2 ship, pause for blocking human verification of the Calendar
    mode UI. Run `make build` then `make deploy`, then walk the how-to-verify
    steps below with the user. Do NOT auto-advance — Lit rendering is not
    unit-testable (VALIDATION manual-only table). Fix any reported issues and
    re-deploy before resuming. Resume only on an explicit "approved" signal.
  </action>
  <what-built>
    Calendar presence mode in the person card: mode option + badge, calendar config
    block (entity picker, event_means, pre-heat lead input), D-14 layout reorder
    (scheduling above rooms), and Calendar period state in the schedule editor with
    inline per-period config. All auto-save on change.
  </what-built>
  <how-to-verify>
    1. Run `make build` then `make deploy`; reload the Climate Manager panel in HA.
    2. Open a person card and expand it. Confirm the scheduling/presence section
       renders ABOVE the room associations list (D-14).
    3. Open the presence mode picker — confirm a "Calendar" option exists. Select it.
    4. Confirm a "Calendar source" entity picker lists your HA calendar.* entities by
       friendly name; an "Event means" select ("Absent during events" default /
       "Present during events"); and a "Pre-heat lead time" number input showing 60
       with a "min" suffix.
    5. Pick a calendar, toggle event_means, change the lead time — each should show a
       "Saved" toast with no Save button. Reload the panel and confirm selections
       persist (entity, event_means, lead time).
    6. Switch the person to Scheduled mode. In the period editor, set a period's state
       to "Calendar" via the state select — confirm an inline calendar entity picker
       appears for that period and the period shows the calendar color (indigo) and
       "C" label. Confirm clicking/dragging time-bar segments still only toggles
       present/absent (never lands on calendar).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| panel UI → WS | User-entered calendar entity_id / lead time crosses to the WS handler (validated server-side in Plan 03) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Tampering | lead-time number input | mitigate | Native input min="0" max="480"; handler skips save on NaN/out-of-range; server clamps (Plan 03 T-11-07) — defense in depth |
| T-11-10 | Information Disclosure | calendar entity list source | accept | Entity ids derived from hass.states already visible to the authenticated panel session; no new exposure |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs (D-02); build uses existing devDeps; no new [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `cd frontend && npx tsc --noEmit` — types compile
- `cd frontend && npm run build` — bundle builds (or `make build`)
- `make lint` — editorconfig clean (TS 2-space, 80-col, LF, final newline)
- Human checkpoint (Task 3) approved
</verification>

<success_criteria>
- Calendar mode selectable with full config block; auto-save works; selections persist on reload
- Scheduling section renders above rooms (D-14)
- Period editor offers Calendar state with inline per-period config; click-cycling unchanged
- Frontend builds clean; human verification approved
</success_criteria>

<output>
Create `.planning/phases/11-calendar-presence-backend/11-04-SUMMARY.md` when done
</output>
</content>
