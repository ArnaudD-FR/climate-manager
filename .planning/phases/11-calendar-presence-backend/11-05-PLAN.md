---
phase: 11-calendar-presence-backend
plan: 05
type: execute
wave: 4
depends_on: ["11-03"]
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [CAL-01, CAL-02, CAL-03, CAL-04]
must_haves:
  truths:
    - "CAL-01..04 describe the HA-native calendar-entity approach, not Pronote/iCal direct fetch"
    - "ROADMAP Phase 11 success criteria reference calendar mode + event_means + cache + pre-heat lead, not schedule_type=pronote/ical"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "Rewritten CAL-01..04 reflecting the shipped design"
      contains: "calendar"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 11 success criteria aligned to HA-native design"
      contains: "calendar"
  key_links:
    - from: ".planning/REQUIREMENTS.md CAL-01..04"
      to: "implemented Phase 11 behavior"
      via: "wording matches resolve_calendar_presence + _prefetch_calendars + preheat_lead_minutes"
      pattern: "event_means|preheat_lead_minutes|_calendar_cache"
---

<objective>
Rewrite the superseded CAL-01..04 requirements in REQUIREMENTS.md and the Phase 11
success criteria in ROADMAP.md to reflect the shipped HA-native calendar-entity
design (the original text describes direct Pronote/iCal fetching, which CONTEXT.md
D-01/D-02 explicitly replaced).

Purpose: CONTEXT.md out-of-scope and RESEARCH.md "Requirements Update" sections both
mandate this rewrite so the requirement IDs accurately trace to what was built.
Documentation-only; no code.

Output: Updated REQUIREMENTS.md (CAL-01..04 + traceability) and ROADMAP.md
(Phase 11 success criteria + plan list).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-calendar-presence-backend/11-CONTEXT.md
@.planning/phases/11-calendar-presence-backend/11-RESEARCH.md
@.planning/phases/11-calendar-presence-backend/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite CAL-01..04 in REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read the §Calendar Presence Sources block lines 5-20
      and the Traceability table lines 80-94)
    - .planning/phases/11-calendar-presence-backend/11-RESEARCH.md (the "Requirements
      Update → Proposed rewritten requirements" section with CAL-01..04 new text)
    - .planning/phases/11-calendar-presence-backend/11-CONTEXT.md (D-01..D-13 — the
      authoritative design these requirements must describe)
  </read_first>
  <action>
    Replace the four CAL-01..04 bullet bodies under §Calendar Presence Sources with
    the HA-native wording (sourced from RESEARCH "Proposed rewritten requirements"):
    CAL-01 — person settable to "Calendar" presence mode pointing at a `calendar.*`
    HA entity; active event + `event_means` (default "absent", or "present")
    determines presence; calendar error → fallback absent without log spam.
    CAL-02 — coordinator fetches `get_events` once per unique `calendar.*` entity per
    `async_evaluate` cycle (cached in `_calendar_cache`); persons sharing an entity
    share the fetch. CAL-03 — in Scheduled mode, a period can have state "calendar"
    resolved via its attached `calendar_config`; not recursive (D-07). CAL-04 —
    per-person `preheat_lead_minutes` (default 60, range 0–480) treats a
    calendar-absent person as present when the active event ends within
    `preheat_lead_minutes`, enabling pre-heat before return. Keep the `- [ ]`
    checkbox format and the **CAL-0N** bold-id convention; 2-space indent, 80-col,
    LF. Update the Traceability table rows for CAL-01..04: set Plan column to the
    plan(s) that own each (CAL-01 → 11-01/11-02/11-03/11-04; CAL-02 → 11-02; CAL-03 →
    11-01/11-04; CAL-04 → 11-01/11-03/11-04) — or a concise accurate mapping. Do not
    touch PREHEAT-*, MCALIB-*, or UI-* rows.
  </action>
  <verify>
    <automated>grep -c 'event_means\|preheat_lead_minutes\|calendar\.' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - CAL-01..04 bodies no longer contain "Pronote", "iCal", "ICS URL", "RRULE", or "recurring-ical-events"
    - CAL-01 mentions "Calendar" presence mode and `event_means`
    - CAL-02 mentions per-cycle / once-per-entity caching
    - CAL-04 mentions `preheat_lead_minutes` default 60
    - Traceability table CAL-01..04 Plan column no longer says "TBD"
    - `make lint` passes (markdown 80-col, 2-space, LF)
  </acceptance_criteria>
  <done>CAL-01..04 describe the shipped HA-native design; traceability maps to real plans; no Pronote/iCal direct-fetch language remains.</done>
</task>

<task type="auto">
  <name>Task 2: Update Phase 11 success criteria and plan list in ROADMAP.md</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (read the Phase 11 detail block lines 90-109 — Goal,
      Success Criteria referencing schedule_type="pronote"/"ical", and the
      **Plans:** TBD line)
    - .planning/phases/11-calendar-presence-backend/11-CONTEXT.md (phase boundary +
      decisions for accurate success-criteria wording)
  </read_first>
  <action>
    Rewrite the Phase 11 Goal line and the four Success Criteria to match the shipped
    design: replace `schedule_type = "pronote"` / `"ical"` language with: (1) setting
    a person to Calendar mode with a `calendar.*` entity makes them absent during
    active events when `event_means` is "absent" (present when "present"); (2) the
    coordinator fetches `get_events` once per unique calendar entity per evaluate
    cycle and falls back to absent with a single WARNING on entity error; (3) a
    Scheduled-mode period can use state "calendar" resolved via its `calendar_config`;
    (4) a per-person `preheat_lead_minutes` (default 60) treats a calendar-absent
    person as present when the active event ends within the lead window. Replace the
    **Plans:** TBD line with the actual list of 5 plans (one bullet per plan with a
    brief objective): 11-01 backend calendar logic + tests, 11-02 coordinator cache
    integration, 11-03 WebSocket persistence + types, 11-04 frontend person-card UI,
    11-05 requirements/roadmap update. Use the `- [ ]` checkbox format matching the
    Phase 10 plan-list style (lines 84-88). Markdown 2-space indent, 80-col, LF.
    Do not edit other phases.
  </action>
  <verify>
    <automated>grep -c 'pronote\|ical\|RRULE' .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - Phase 11 Success Criteria no longer reference `schedule_type = "pronote"` or `"ical"`
    - Success criteria mention Calendar mode, event_means, per-cycle fetch, and preheat_lead_minutes
    - The **Plans:** line lists 5 plans (11-01..11-05) with brief objectives, not "TBD"
    - The `grep -c 'pronote\|ical\|RRULE' .planning/ROADMAP.md` count covers only legacy milestone-summary text (the Phase 11 detail block itself contains none)
    - `make lint` passes
  </acceptance_criteria>
  <done>Phase 11 ROADMAP success criteria and plan list reflect the HA-native calendar design; 5 plans enumerated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none) | Documentation-only changes to planning markdown; no runtime trust boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-11 | Repudiation | requirement traceability drift | mitigate | CAL-01..04 rewritten to match shipped behavior; traceability table updated to real plan IDs — keeps audit trail accurate |
| T-11-SC | Tampering | npm/pip/cargo installs | accept | No package installs (D-02); doc-only plan; no [ASSUMED]/[SUS] packages |
</threat_model>

<verification>
- `grep -i 'pronote\|recurring-ical-events' .planning/REQUIREMENTS.md` returns no matches inside CAL-01..04 bodies
- ROADMAP Phase 11 plan list shows 5 plans
- `make lint` — markdown editorconfig clean
</verification>

<success_criteria>
- CAL-01..04 describe the HA-native calendar approach with accurate traceability
- ROADMAP Phase 11 success criteria + plan list match the shipped design
- No Pronote/iCal direct-fetch language remains in Phase 11 requirements/criteria
</success_criteria>

<output>
Create `.planning/phases/11-calendar-presence-backend/11-05-SUMMARY.md` when done
</output>
