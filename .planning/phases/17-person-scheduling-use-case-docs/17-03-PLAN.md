---
phase: 17-person-scheduling-use-case-docs
plan: 03
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - docs/use-cases/rotating-shift-worker/harness.html
  - docs/use-cases/rotating-shift-worker/Makefile
  - docs/use-cases/rotating-shift-worker/README.md
  - docs/use-cases/rotating-shift-worker/screenshots/
  - docs/use-cases/shared-custody-odd-even-weeks/harness.html
  - docs/use-cases/shared-custody-odd-even-weeks/Makefile
  - docs/use-cases/shared-custody-odd-even-weeks/README.md
  - docs/use-cases/shared-custody-odd-even-weeks/screenshots/
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "docs/use-cases/rotating-shift-worker/ documents an HA-mode persona whose
       presence follows the HA person/device_tracker state, with NO schedule
       editor (D-05, D-10)"
    - "Its person card shows the clean 'HA' / live-tracking mode (not the
       '⚠ HA (no trackers)' variant) because device_trackers is populated in
       hass.states (D-10)"
    - "docs/use-cases/shared-custody-odd-even-weeks/ documents a scheduled
       even/odd-week persona (even = present all week, odd = absent) showing the
       Even/Odd week-switcher tabs (D-11)"
    - "Each folder's `make screenshots` produces at least one PNG including the
       expanded Persons-tab card (D-04)"
    - "After this plan, root `make screenshots` exits 0 and emits the existing
       panel-tab set plus all five use-case screenshot sets (D-02, D-03)"
  artifacts:
    - path: "docs/use-cases/rotating-shift-worker/harness.html"
      provides: "ha-mode CONFIG for person.marc + device_trackers in
                 hass.states (D-10)"
      contains: "device_trackers"
    - path: "docs/use-cases/shared-custody-odd-even-weeks/harness.html"
      provides: "scheduled/even_odd CONFIG for person.sofia (D-11)"
      contains: "even_odd"
    - path: "docs/use-cases/rotating-shift-worker/Makefile"
      provides: "per-scenario docker capture"
      contains: "HARNESS_PATH"
    - path: "docs/use-cases/shared-custody-odd-even-weeks/Makefile"
      provides: "per-scenario docker capture"
      contains: "HARNESS_PATH"
    - path: "docs/use-cases/rotating-shift-worker/README.md"
      provides: "persona showcase noting no schedule editor (D-05, D-06)"
      contains: "Configuration Summary"
    - path: "docs/use-cases/shared-custody-odd-even-weeks/README.md"
      provides: "persona showcase + config summary table (D-06)"
      contains: "Configuration Summary"
  key_links:
    - from: "docs/use-cases/rotating-shift-worker/harness.html"
      to: "hass.states['person.marc'].attributes.device_trackers"
      via: "non-empty array → persons-tab hasDeviceTrackers true → clean HA
            badge"
      pattern: "device_trackers"
    - from: "root Makefile use-case-screenshots"
      to: "all five docs/use-cases/*/Makefile"
      via: "$(MAKE) -C iteration"
      pattern: "MAKE.*-C"
---

<objective>
Create the final two persona use-case folders — `rotating-shift-worker` (D-10,
HA tracking, no schedule editor per D-05) and `shared-custody-odd-even-weeks`
(D-11, scheduled even/odd weeks) — and verify the full root `make screenshots`
delegation emits all five sets cleanly.

Purpose: completes the five-scenario mode coverage (HA tracker + even/odd) and
proves the D-03 root delegation works end-to-end across every use-case folder.
Output: Two more `docs/use-cases/<slug>/` packages and a confirmed green root
`make screenshots`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-person-scheduling-use-case-docs/17-CONTEXT.md
@.planning/phases/17-person-scheduling-use-case-docs/17-RESEARCH.md
@.planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
@.planning/phases/17-person-scheduling-use-case-docs/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the rotating-shift-worker use-case folder (D-10, D-05)
  </name>
  <files>docs/use-cases/rotating-shift-worker/harness.html,
    docs/use-cases/rotating-shift-worker/Makefile,
    docs/use-cases/rotating-shift-worker/README.md,
    docs/use-cases/rotating-shift-worker/screenshots/</files>
  <read_first>
    - docs/test-harness.html — the harness.html analog (copy + swap mock block).
    - .planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
      §"D-10: rotating-shift-worker" — the exact CONFIG.persons entry for
      person.marc (mode 'ha', room_ids ['bedroom']) and the mockHass.states
      entry with `device_trackers: ['device_tracker.marc_phone']`.
    - .planning/phases/17-person-scheduling-use-case-docs/17-RESEARCH.md
      §"PersonMode → card rendering" and the badge note — a non-empty
      device_trackers array makes the card render the clean "HA" label instead
      of "⚠ HA (no trackers)"; ha mode shows NO schedule editor (D-05).
    - .planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
      Makefile + README templates (slug differs).
    - frontend/src/components/person-card.ts — confirm `_getBadgeInfo()` /
      haOptionLabel rendering and that ha mode shows mode selector + hint only.
  </read_first>
  <action>
    Create docs/use-cases/rotating-shift-worker/. Write harness.html from the
    docs/test-harness.html copy with CONFIG.persons holding the single
    `person.marc` entry: `mode: 'ha'`, `room_ids: ['bedroom']` per PATTERNS.md
    §D-10. mockHass.states MUST include `'person.marc': { state: 'not_home',
    attributes: { friendly_name: 'Marc', device_trackers:
    ['device_tracker.marc_phone'] } }` — the non-empty device_trackers array is
    required so the card renders the clean "HA"/live-tracking label, not the
    "⚠ HA (no trackers)" warning variant. Absolute panel.js URL;
    current-schema keys only (no room_mode). This is the D-05 "no schedule
    editor" scenario — the expanded card shows the mode selector + hint + room
    associations only. Write the Makefile from the template with `SLUG :=
    rotating-shift-worker`. Write README.md as a showcase: Marc rotating-shift
    persona intro explaining that irregular shifts are best tracked by real
    location rather than a fixed schedule, a Configuration Summary table
    (Presence mode: HA / live tracking | Schedule shape: none — follows the HA
    person entity's home/away state | Assigned rooms: Bedroom), and a
    Screenshots section embedding the expanded card with a one-sentence
    annotation explicitly noting the deliberate absence of a schedule editor
    (contrast with the schedule-driven personas). editorconfig-clean Markdown.
    Run `make -C docs/use-cases/rotating-shift-worker screenshots`; commit PNGs.
  </action>
  <verify>
    <automated>make -C docs/use-cases/rotating-shift-worker screenshots &amp;&amp; ls docs/use-cases/rotating-shift-worker/screenshots/*.png &amp;&amp; grep -q 'device_trackers' docs/use-cases/rotating-shift-worker/harness.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -d docs/use-cases/rotating-shift-worker/screenshots` succeeds.
    - `ls docs/use-cases/rotating-shift-worker/screenshots/*.png | wc -l` is at
      least 1.
    - `grep -c "mode: 'ha'"
      docs/use-cases/rotating-shift-worker/harness.html` is at least 1 (ha
      mode set).
    - `grep -c "device_trackers"
      docs/use-cases/rotating-shift-worker/harness.html` is at least 1 (clean
      HA badge — D-10).
    - `grep -c "/custom_components/climate_manager/www/panel.js"
      docs/use-cases/rotating-shift-worker/harness.html` is at least 1.
    - `grep -c "HARNESS_PATH=/docs/use-cases/rotating-shift-worker/harness.html"
      docs/use-cases/rotating-shift-worker/Makefile` is at least 1.
    - `grep -v '^#' docs/use-cases/rotating-shift-worker/README.md | grep -c
      "Configuration Summary"` is at least 1.
    - `grep -c "room_mode"
      docs/use-cases/rotating-shift-worker/harness.html` returns 0.
    - `make lint` exits 0.
  </acceptance_criteria>
  <done>
    The rotating-shift-worker folder is complete with a generated person-card
    PNG showing the clean HA mode and no schedule editor; its `make
    screenshots` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the shared-custody-odd-even-weeks use-case folder (D-11)
  </name>
  <files>docs/use-cases/shared-custody-odd-even-weeks/harness.html,
    docs/use-cases/shared-custody-odd-even-weeks/Makefile,
    docs/use-cases/shared-custody-odd-even-weeks/README.md,
    docs/use-cases/shared-custody-odd-even-weeks/screenshots/</files>
  <read_first>
    - docs/test-harness.html — the harness.html analog (copy + swap mock block).
    - .planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
      §"D-11: shared-custody-odd-even-weeks" — the exact CONFIG.persons entry
      for person.sofia (mode 'scheduled', schedule_type 'even_odd',
      schedule_even all-present, schedule_odd all-absent) and the
      mockHass.states entry.
    - .planning/phases/17-person-scheduling-use-case-docs/17-RESEARCH.md
      Pitfall 3 and Open Question 2 — the active week (Even vs Odd) is chosen by
      getWeekParity(new Date()) inside the browser at card-expand and is NOT
      controllable from the harness CONFIG. Decision for this plan: accept
      whichever parity renders (Pitfall 3 option a) and annotate the README that
      both Even and Odd week tabs exist; do NOT add a page.evaluate toggle.
    - .planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
      Makefile + README templates (slug differs).
    - frontend/src/components/person-card.ts — confirm even_odd renders the
      Even/Odd week-switcher tabs and the `updated()` parity re-evaluation.
  </read_first>
  <action>
    Create docs/use-cases/shared-custody-odd-even-weeks/. Write harness.html
    from the docs/test-harness.html copy with CONFIG.persons holding the single
    `person.sofia` entry: `mode: 'scheduled'`, `schedule_type: 'even_odd'`,
    `room_ids: ['bedroom']`, `schedule_even` = all-present every day,
    `schedule_odd` = all-absent every day, exactly per PATTERNS.md §D-11. Add
    `'person.sofia': { state: 'home', attributes: { friendly_name: 'Sofia' } }`
    to mockHass.states. Absolute panel.js URL; current-schema keys only (no
    room_mode). Write the Makefile from the template with `SLUG :=
    shared-custody-odd-even-weeks`. Write README.md as a showcase: Sofia
    shared-custody persona intro (child present even weeks, absent odd weeks), a
    Configuration Summary table (Presence mode: Scheduled (even/odd weeks) |
    Schedule shape: even week = present all week, odd week = absent all week |
    Assigned rooms: Bedroom), and a Screenshots section embedding the expanded
    card with a one-sentence annotation noting the Even/Odd week-switcher tabs
    and that the screenshot shows whichever week parity is current (both tabs
    exist). Per Pitfall 3 do NOT depend on a fixed parity. editorconfig-clean
    Markdown. Run `make -C docs/use-cases/shared-custody-odd-even-weeks
    screenshots`; commit PNGs.
  </action>
  <verify>
    <automated>make -C docs/use-cases/shared-custody-odd-even-weeks screenshots &amp;&amp; ls docs/use-cases/shared-custody-odd-even-weeks/screenshots/*.png &amp;&amp; grep -q 'even_odd' docs/use-cases/shared-custody-odd-even-weeks/harness.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -d docs/use-cases/shared-custody-odd-even-weeks/screenshots`
      succeeds.
    - `ls docs/use-cases/shared-custody-odd-even-weeks/screenshots/*.png |
      wc -l` is at least 1.
    - `grep -c "even_odd"
      docs/use-cases/shared-custody-odd-even-weeks/harness.html` is at least 1.
    - `grep -c "schedule_even"
      docs/use-cases/shared-custody-odd-even-weeks/harness.html` is at least 1.
    - `grep -c "/custom_components/climate_manager/www/panel.js"
      docs/use-cases/shared-custody-odd-even-weeks/harness.html` is at least 1.
    - `grep -c
      "HARNESS_PATH=/docs/use-cases/shared-custody-odd-even-weeks/harness.html"
      docs/use-cases/shared-custody-odd-even-weeks/Makefile` is at least 1.
    - `grep -v '^#'
      docs/use-cases/shared-custody-odd-even-weeks/README.md | grep -c
      "Configuration Summary"` is at least 1.
    - `grep -c "room_mode"
      docs/use-cases/shared-custody-odd-even-weeks/harness.html` returns 0.
    - `make lint` exits 0.
  </acceptance_criteria>
  <done>
    The shared-custody-odd-even-weeks folder is complete with a generated
    person-card PNG showing the even/odd week-switcher; its `make screenshots`
    exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify the full root delegation emits all five sets</name>
  <files>docs/use-cases/</files>
  <read_first>
    - Makefile (root) — confirm the `use-case-screenshots` target and the
      `screenshots` recipe invoking it (added in 17-01).
    - .planning/phases/17-person-scheduling-use-case-docs/17-VALIDATION.md
      §"Phase Requirements → Test Map" — the phase-gate assertions
      (5 README.md, ≥5 PNG, `make screenshots` exits 0).
    - .planning/phases/17-person-scheduling-use-case-docs/17-RESEARCH.md
      Pitfall 6 — `use-case-screenshots` must run after the existing docker
      capture so panel.js is fresh.
  </read_first>
  <action>
    Run the full root `make screenshots` end-to-end and confirm it (a) rebuilds
    panel.js, (b) emits the existing docs/screenshots/ panel-tab set unchanged,
    and (c) delegates to all five docs/use-cases/*/Makefile producing each
    scenario's screenshots/ PNGs (D-02, D-03). This is a verification task — no
    new files authored beyond the PNGs already committed by the scenario tasks
    and any re-emitted by the full run. Do not modify the harnesses or Makefiles
    in this task; if the full run fails for a scenario, fix is out of scope here
    and the failing scenario task must be revisited. Confirm the existing six
    docs/screenshots/ PNGs (overview, rooms, persons, zone, zone-upstairs,
    global-settings) are still present and unaffected.
  </action>
  <verify>
    <automated>make screenshots &amp;&amp; test "$(find docs/use-cases -name README.md | wc -l)" -ge 5 &amp;&amp; test "$(find docs/use-cases -name '*.png' | wc -l)" -ge 5</automated>
  </verify>
  <acceptance_criteria>
    - `make screenshots` exits 0 (full root delegation green — DOC-01).
    - `find docs/use-cases -name 'README.md' | wc -l` returns exactly 5.
    - `find docs/use-cases -name '*.png' | wc -l` returns at least 5 (every
      scenario emitted at least one PNG).
    - Each of the five slug folders has a non-empty screenshots/ directory:
      `for d in simple-schedule business-calendar student-mixed-schedule
      rotating-shift-worker shared-custody-odd-even-weeks; do
      ls docs/use-cases/$d/screenshots/*.png >/dev/null; done` exits 0.
    - The existing panel-tab set is intact:
      `ls docs/screenshots/overview.png docs/screenshots/persons.png
      docs/screenshots/global-settings.png` exits 0 (D-03 untouched).
  </acceptance_criteria>
  <done>
    Root `make screenshots` exits 0 and produces the existing panel-tab set
    plus all five use-case screenshot sets; five READMEs and at least five PNGs
    exist under docs/use-cases/.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → local Playwright/docker harness | Only static, committed mock config + tooling cross here; no network input, no user-supplied data, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information disclosure | mock CONFIG (person.marc/sofia) + committed PNGs | mitigate | Persona data is fictional (Marc/Sofia, device_tracker.marc_phone is a mock id); no real HA tokens or PII. Harnesses are scanned for credential/email patterns; screenshots show only mock state. |
| T-17-02 | Tampering | OUTPUT_DIR / HARNESS_PATH paths | accept | Hard-coded literals in committed per-scenario Makefiles, not untrusted input. Low severity, local tooling. |
| T-17-SC | Tampering | npm installs | accept | No new packages; docker `npm install` resolves the already-locked docs/package.json. |
</threat_model>

<verification>
- `make -C docs/use-cases/<slug> screenshots` exits 0 for both new slugs and
  emits at least one PNG each.
- rotating-shift-worker harness sets ha mode with a non-empty device_trackers
  array (clean HA badge, no schedule editor — D-05/D-10).
- shared-custody harness sets schedule_type even_odd with even/odd programs
  (D-11); README annotates the parity caveat (Pitfall 3).
- Root `make screenshots` exits 0; five READMEs and ≥5 PNGs under
  docs/use-cases/; existing docs/screenshots/ set intact.
- `make lint` exits 0.
</verification>

<success_criteria>
- Two docs/use-cases/<slug>/ folders exist (rotating-shift-worker,
  shared-custody-odd-even-weeks), each with harness.html, Makefile, README.md,
  and at least one committed PNG.
- Mode coverage complete across all five docs: single-week, calendar, HA
  tracker, even/odd.
- Root `make screenshots` is green and emits all five use-case sets plus the
  existing panel-tab set.
- editorconfig-clean.
</success_criteria>

<output>
Create `.planning/phases/17-person-scheduling-use-case-docs/17-03-SUMMARY.md`
when done.
</output>
