---
phase: 17-person-scheduling-use-case-docs
plan: 02
type: execute
wave: 2
depends_on: ["17-01"]
files_modified:
  - docs/use-cases/simple-schedule/harness.html
  - docs/use-cases/simple-schedule/Makefile
  - docs/use-cases/simple-schedule/README.md
  - docs/use-cases/simple-schedule/screenshots/
  - docs/use-cases/business-calendar/harness.html
  - docs/use-cases/business-calendar/Makefile
  - docs/use-cases/business-calendar/README.md
  - docs/use-cases/business-calendar/screenshots/
  - docs/use-cases/student-mixed-schedule/harness.html
  - docs/use-cases/student-mixed-schedule/Makefile
  - docs/use-cases/student-mixed-schedule/README.md
  - docs/use-cases/student-mixed-schedule/screenshots/
autonomous: true
requirements: [DOC-01]
must_haves:
  truths:
    - "docs/use-cases/simple-schedule/ documents a scheduled single-week
       persona (home mornings/evenings, away ~09-17 weekdays, home weekends)
       with generated screenshots (D-07)"
    - "docs/use-cases/business-calendar/ documents a calendar-mode persona
       driven by a calendar.* entity (events mean away) with generated
       screenshots (D-08)"
    - "docs/use-cases/student-mixed-schedule/ documents a scheduled single-week
       persona with varied per-weekday hours and home weekends (D-09)"
    - "Each folder's `make screenshots` produces at least one PNG in its
       screenshots/ folder including the expanded Persons-tab card (D-04)"
    - "Each README is a conceptual showcase: persona intro + config summary
       table + annotated screenshots, no click-by-click guide (D-06)"
  artifacts:
    - path: "docs/use-cases/simple-schedule/harness.html"
      provides: "scheduled/single CONFIG for person.emma (D-07)"
      contains: "schedule_type"
    - path: "docs/use-cases/business-calendar/harness.html"
      provides: "calendar-mode CONFIG for person.noah + calendar.work_meetings
                 in hass.states (D-08)"
      contains: "calendar_config"
    - path: "docs/use-cases/student-mixed-schedule/harness.html"
      provides: "scheduled/single varied-weekday CONFIG for person.lena (D-09)"
      contains: "schedule_type"
    - path: "docs/use-cases/simple-schedule/Makefile"
      provides: "per-scenario docker capture with OUTPUT_DIR + HARNESS_PATH"
      contains: "HARNESS_PATH"
    - path: "docs/use-cases/business-calendar/Makefile"
      provides: "per-scenario docker capture"
      contains: "HARNESS_PATH"
    - path: "docs/use-cases/student-mixed-schedule/Makefile"
      provides: "per-scenario docker capture"
      contains: "HARNESS_PATH"
    - path: "docs/use-cases/simple-schedule/README.md"
      provides: "persona showcase + config summary table (D-06)"
      contains: "Configuration Summary"
    - path: "docs/use-cases/business-calendar/README.md"
      provides: "persona showcase + config summary table (D-06)"
      contains: "Configuration Summary"
    - path: "docs/use-cases/student-mixed-schedule/README.md"
      provides: "persona showcase + config summary table (D-06)"
      contains: "Configuration Summary"
  key_links:
    - from: "docs/use-cases/<slug>/harness.html"
      to: "/custom_components/climate_manager/www/panel.js"
      via: "absolute-URL script src (server resolves from project root)"
      pattern: "/custom_components/climate_manager/www/panel.js"
    - from: "docs/use-cases/<slug>/Makefile"
      to: "docs/screenshot.js scenario mode"
      via: "OUTPUT_DIR + HARNESS_PATH env vars on docker run"
      pattern: "HARNESS_PATH=/docs/use-cases"
---

<objective>
Create the first three persona use-case folders — `simple-schedule` (D-07),
`business-calendar` (D-08), `student-mixed-schedule` (D-09) — each with a
per-scenario harness.html, a local Makefile, a showcase README, and generated
screenshots produced by that folder's `make screenshots`.

Purpose: D-02 (folder-per-use-case with local Makefile) and the three scheduled/
calendar scenario mappings. These three are grouped because they share the same
shape (each adds nothing the others touch — distinct slug folders), keeping the
plan within budget.
Output: Three `docs/use-cases/<slug>/` packages with committed PNGs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-person-scheduling-use-case-docs/17-CONTEXT.md
@.planning/phases/17-person-scheduling-use-case-docs/17-RESEARCH.md
@.planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
@.planning/phases/17-person-scheduling-use-case-docs/17-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the simple-schedule use-case folder (D-07)</name>
  <files>docs/use-cases/simple-schedule/harness.html,
    docs/use-cases/simple-schedule/Makefile,
    docs/use-cases/simple-schedule/README.md,
    docs/use-cases/simple-schedule/screenshots/</files>
  <read_first>
    - docs/test-harness.html (full file, 273 lines) — the harness.html analog.
      Copy verbatim; replace ONLY the inline `<script type="module">` mock
      block (~lines 150-271). Head/CSS/HA-component stubs (~lines 1-145) and
      the panel.js `<script>` tag (~line 147) stay identical.
    - .planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
      §"docs/use-cases/<slug>/harness.html" and §"D-07: simple-schedule" —
      the exact CONFIG.persons entry for person.emma and the
      mockHass.states entry.
    - .planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
      §"docs/use-cases/<slug>/Makefile" — the per-scenario Makefile template
      (SLUG, PROJECT_ROOT via `$(shell cd ../.. && pwd)`, docker run with
      OUTPUT_DIR + HARNESS_PATH, `cd /app/docs && node screenshot.js`).
    - .planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
      §"docs/use-cases/<slug>/README.md" — the README showcase template.
    - .planning/phases/17-person-scheduling-use-case-docs/17-RESEARCH.md
      Pitfall 1 (docker CWD must stay /app/docs) and Pitfall 2 (panel.js
      absolute URL path).
    - frontend/src/components/person-card.ts and frontend/src/types.ts —
      confirm PersonConfig field names (mode, schedule_type, schedule,
      room_ids) before authoring the CONFIG block.
  </read_first>
  <action>
    Create docs/use-cases/simple-schedule/. Write harness.html by copying
    docs/test-harness.html and replacing only the inline mock `<script
    type="module">` block so CONFIG.persons holds the single `person.emma`
    entry: `mode: 'scheduled'`, `schedule_type: 'single'`, `room_ids:
    ['bedroom', 'living_room']`, and the weekday schedule (away 00:00, present
    07:00, away 09:00, present 17:30, away 23:00 Mon-Fri; all-present Sat/Sun)
    exactly per PATTERNS.md §D-07. Add `'person.emma': { state: 'home',
    attributes: { friendly_name: 'Emma' } }` to mockHass.states. Keep the
    panel.js src as the absolute `/custom_components/climate_manager/www/
    panel.js`. Keep CONFIG top-level keys to the current schema only
    (`default_zone` object, `zones`, `rooms` with NO `room_mode`, `persons`,
    `climate_entities`); do not introduce deprecated `global_mode` /
    `default_zone_name` / `room_mode` keys.
    Write the local Makefile from the PATTERNS template with `SLUG :=
    simple-schedule`, `PROJECT_ROOT := $(shell cd ../.. && pwd)`, a `.PHONY:
    screenshots` line, and a docker run that mounts `$(PROJECT_ROOT):/app`,
    sets `OUTPUT_DIR=/app/docs/use-cases/$(SLUG)/screenshots` and
    `HARNESS_PATH=/docs/use-cases/$(SLUG)/harness.html`, and runs
    `cd /app/docs && npm install ... && node screenshot.js`. TAB indentation.
    Write README.md as a conceptual showcase (D-06): a one/two-sentence Emma
    persona intro, a "Configuration Summary" table (Presence mode: Scheduled
    (single week) | Schedule shape: away weekday ~09:00-17:30, home mornings/
    evenings + weekends | Assigned rooms: Bedroom, Living Room), and a
    "Screenshots" section embedding persons.png with a one-sentence annotation.
    Reference only PNG filenames that the scenario-mode driver emits (overview
    + persons). 2-space indent, max 80 cols, LF, final newline, no trailing
    whitespace. No numbered step-by-step reproduction.
    Run `make -C docs/use-cases/simple-schedule screenshots` to generate the
    PNGs into docs/use-cases/simple-schedule/screenshots/. Commit the PNGs.
  </action>
  <verify>
    <automated>make -C docs/use-cases/simple-schedule screenshots &amp;&amp; ls docs/use-cases/simple-schedule/screenshots/*.png &amp;&amp; grep -q 'climate_manager/www/panel.js' docs/use-cases/simple-schedule/harness.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -d docs/use-cases/simple-schedule/screenshots` succeeds.
    - `ls docs/use-cases/simple-schedule/screenshots/*.png | wc -l` is at
      least 1 (persons card screenshot present).
    - `grep -c "schedule_type" docs/use-cases/simple-schedule/harness.html`
      is at least 1.
    - `grep -c "/custom_components/climate_manager/www/panel.js"
      docs/use-cases/simple-schedule/harness.html` is at least 1 (absolute
      panel.js URL — Pitfall 2).
    - `grep -c "HARNESS_PATH=/docs/use-cases/simple-schedule/harness.html"
      docs/use-cases/simple-schedule/Makefile` is at least 1.
    - `grep -v '^#' docs/use-cases/simple-schedule/README.md | grep -c
      "Configuration Summary"` is at least 1.
    - `grep -c "room_mode" docs/use-cases/simple-schedule/harness.html`
      returns 0 (deprecated key absent).
    - No real HA tokens/PII: `grep -niE "token|secret|password|@.*\\.(com|fr)"
      docs/use-cases/simple-schedule/harness.html` returns no matches (T-17-01).
    - `make lint` exits 0.
  </acceptance_criteria>
  <done>
    The simple-schedule folder contains harness.html, Makefile, README.md, and
    at least one generated PNG; `make -C docs/use-cases/simple-schedule
    screenshots` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build the business-calendar use-case folder (D-08)</name>
  <files>docs/use-cases/business-calendar/harness.html,
    docs/use-cases/business-calendar/Makefile,
    docs/use-cases/business-calendar/README.md,
    docs/use-cases/business-calendar/screenshots/</files>
  <read_first>
    - docs/test-harness.html — the harness.html analog (copy + swap mock block).
    - .planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
      §"D-08: business-calendar" — the exact CONFIG.persons entry for
      person.noah (calendar_config with entity_id, event_means, gap_handling;
      wakeup_advance_minutes) AND the two required mockHass.states entries
      (person.noah + calendar.work_meetings).
    - .planning/phases/17-person-scheduling-use-case-docs/17-RESEARCH.md
      Pitfall 4 — the calendar entity MUST exist in mockHass.states or the
      entity select renders "No calendar entities found".
    - .planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
      Makefile + README templates (same as Task 1, slug differs).
    - frontend/src/components/person-card.ts — confirm calendar-mode card shows
      the calendar config (entity, event_means, gap_handling,
      wakeup_advance_minutes) and NO schedule editor.
  </read_first>
  <action>
    Create docs/use-cases/business-calendar/. Write harness.html from the
    docs/test-harness.html copy with CONFIG.persons holding the single
    `person.noah` entry: `mode: 'calendar'`, `room_ids: ['office', 'bedroom']`,
    `calendar_config: { entity_id: 'calendar.work_meetings', event_means:
    'absent', gap_handling: 'day_span' }`, `wakeup_advance_minutes: 60` per
    PATTERNS.md §D-08. mockHass.states MUST include BOTH `'person.noah':
    { state: 'home', attributes: { friendly_name: 'Noah' } }` AND
    `'calendar.work_meetings': { state: 'off', attributes: { friendly_name:
    'Work meetings' } }` (Pitfall 4 — without the calendar entity the select
    renders empty). Keep absolute panel.js URL and current-schema CONFIG keys
    (no room_mode). Write the Makefile from the template with `SLUG :=
    business-calendar`. Write README.md as a showcase: Noah persona intro
    framing calendar-driven presence (work meetings/travel mean away), a
    Configuration Summary table (Presence mode: Calendar | Source: a work
    `calendar.*` entity, events = away, wake-up advance 60 min | Assigned
    rooms: Office, Bedroom), and a Screenshots section embedding the expanded
    person card with a one-sentence annotation noting it shows the calendar
    config selectors (no schedule editor). editorconfig-clean Markdown.
    Run `make -C docs/use-cases/business-calendar screenshots`; commit the PNGs.
  </action>
  <verify>
    <automated>make -C docs/use-cases/business-calendar screenshots &amp;&amp; ls docs/use-cases/business-calendar/screenshots/*.png &amp;&amp; grep -q 'calendar.work_meetings' docs/use-cases/business-calendar/harness.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -d docs/use-cases/business-calendar/screenshots` succeeds.
    - `ls docs/use-cases/business-calendar/screenshots/*.png | wc -l` is at
      least 1.
    - `grep -c "calendar_config"
      docs/use-cases/business-calendar/harness.html` is at least 1.
    - `grep -c "calendar.work_meetings"
      docs/use-cases/business-calendar/harness.html` is at least 2 (config
      entity_id + hass.states entry — Pitfall 4).
    - `grep -c "/custom_components/climate_manager/www/panel.js"
      docs/use-cases/business-calendar/harness.html` is at least 1.
    - `grep -c "HARNESS_PATH=/docs/use-cases/business-calendar/harness.html"
      docs/use-cases/business-calendar/Makefile` is at least 1.
    - `grep -v '^#' docs/use-cases/business-calendar/README.md | grep -c
      "Configuration Summary"` is at least 1.
    - `grep -c "room_mode" docs/use-cases/business-calendar/harness.html`
      returns 0.
    - `make lint` exits 0.
  </acceptance_criteria>
  <done>
    The business-calendar folder is complete with a generated person-card PNG
    showing the calendar config; `make -C docs/use-cases/business-calendar
    screenshots` exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build the student-mixed-schedule use-case folder (D-09)</name>
  <files>docs/use-cases/student-mixed-schedule/harness.html,
    docs/use-cases/student-mixed-schedule/Makefile,
    docs/use-cases/student-mixed-schedule/README.md,
    docs/use-cases/student-mixed-schedule/screenshots/</files>
  <read_first>
    - docs/test-harness.html — the harness.html analog (copy + swap mock block).
    - .planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
      §"D-09: student-mixed-schedule" — the exact CONFIG.persons entry for
      person.lena with the varied per-weekday schedule (different class times
      Mon/Tue/Thu/Fri, all-present Wed/Sat/Sun) and the mockHass.states entry.
    - .planning/phases/17-person-scheduling-use-case-docs/17-PATTERNS.md
      Makefile + README templates (slug differs).
    - frontend/src/components/person-card.ts — confirm single-week schedule
      editor renders the per-day time bars.
  </read_first>
  <action>
    Create docs/use-cases/student-mixed-schedule/. Write harness.html from the
    docs/test-harness.html copy with CONFIG.persons holding the single
    `person.lena` entry: `mode: 'scheduled'`, `schedule_type: 'single'`,
    `room_ids: ['bedroom']`, and the varied per-weekday schedule exactly per
    PATTERNS.md §D-09 (Mon multi-block class gaps, Tue different hours, Wed
    all-present, Thu varied, Fri varied, Sat/Sun all-present) to showcase that
    a single-week schedule can differ day to day (D-09). Add `'person.lena':
    { state: 'not_home', attributes: { friendly_name: 'Lena' } }` to
    mockHass.states. Absolute panel.js URL; current-schema keys only (no
    room_mode). Write the Makefile from the template with `SLUG :=
    student-mixed-schedule`. Write README.md as a showcase: Lena student
    persona intro emphasising the day-to-day class-time variation, a
    Configuration Summary table (Presence mode: Scheduled (single week) |
    Schedule shape: varies per weekday — different class hours each day, home
    weekends | Assigned rooms: Bedroom), and a Screenshots section embedding
    the expanded person card with a one-sentence annotation pointing at the
    differing per-day bars. editorconfig-clean Markdown.
    Run `make -C docs/use-cases/student-mixed-schedule screenshots`; commit PNGs.
  </action>
  <verify>
    <automated>make -C docs/use-cases/student-mixed-schedule screenshots &amp;&amp; ls docs/use-cases/student-mixed-schedule/screenshots/*.png &amp;&amp; grep -q 'person.lena' docs/use-cases/student-mixed-schedule/harness.html</automated>
  </verify>
  <acceptance_criteria>
    - `test -d docs/use-cases/student-mixed-schedule/screenshots` succeeds.
    - `ls docs/use-cases/student-mixed-schedule/screenshots/*.png | wc -l` is
      at least 1.
    - `grep -c "schedule_type"
      docs/use-cases/student-mixed-schedule/harness.html` is at least 1.
    - `grep -c "person.lena"
      docs/use-cases/student-mixed-schedule/harness.html` is at least 1.
    - `grep -c "/custom_components/climate_manager/www/panel.js"
      docs/use-cases/student-mixed-schedule/harness.html` is at least 1.
    - `grep -c
      "HARNESS_PATH=/docs/use-cases/student-mixed-schedule/harness.html"
      docs/use-cases/student-mixed-schedule/Makefile` is at least 1.
    - `grep -v '^#' docs/use-cases/student-mixed-schedule/README.md | grep -c
      "Configuration Summary"` is at least 1.
    - `grep -c "room_mode"
      docs/use-cases/student-mixed-schedule/harness.html` returns 0.
    - `make lint` exits 0.
  </acceptance_criteria>
  <done>
    The student-mixed-schedule folder is complete with a generated person-card
    PNG showing the varied weekday schedule; its `make screenshots` exits 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo files → local Playwright/docker harness | Only static, committed mock config + tooling cross here; no network input, no user-supplied data, no secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Information disclosure | mock CONFIG (person.emma/noah/lena) + committed PNGs | mitigate | Persona data is fictional; no real HA tokens, entity IDs from a live deployment, or PII. Each task includes a grep acceptance check for token/secret/email patterns in the harness; reviewers confirm screenshots show only mock data. |
| T-17-02 | Tampering | OUTPUT_DIR / HARNESS_PATH paths | accept | Both are hard-coded literals in the committed per-scenario Makefiles, not derived from untrusted input. Local build tooling, low severity. |
| T-17-SC | Tampering | npm installs | accept | No new packages; docker `npm install` resolves the already-locked docs/package.json. No install-time checkpoint required. |
</threat_model>

<verification>
- `make -C docs/use-cases/<slug> screenshots` exits 0 for each of the three
  slugs and emits at least one PNG under that folder's screenshots/.
- Each harness.html loads panel.js via the absolute URL and contains no
  deprecated room_mode key.
- business-calendar harness.html includes calendar.work_meetings in both the
  person config and mockHass.states (Pitfall 4).
- `make lint` exits 0 across all new files.
- No real credentials/PII present in any harness CONFIG or PNG (T-17-01).
</verification>

<success_criteria>
- Three docs/use-cases/<slug>/ folders exist (simple-schedule,
  business-calendar, student-mixed-schedule), each with harness.html, Makefile,
  README.md, and at least one committed PNG.
- Each README is a D-06 conceptual showcase with a Configuration Summary table.
- Each scenario's mode matches its decision (D-07 scheduled/single, D-08
  calendar, D-09 scheduled/single varied).
- editorconfig-clean.
</success_criteria>

<output>
Create `.planning/phases/17-person-scheduling-use-case-docs/17-02-SUMMARY.md`
when done.
</output>
