---
milestone: 1.3
audited: 2026-06-06T15:20:00Z
status: tech_debt
scores:
  requirements: 13/17 fully satisfied (3 partial/adjusted, 0 unsatisfied)
  phases: 8/8 functionally complete
  integration: 6/6 seams clean
  flows: 4/4 complete (1 caveat)
gaps:
  requirements:
    - id: "CAL-04"
      status: "partial"
      phase: "Phase 11"
      verification_status: "complete (phase 11) — but runtime no-op"
      evidence: >
        wakeup_advance_minutes persists and is plumbed into
        resolve_calendar_presence as preheat_lead_minutes, but that
        parameter is explicitly ignored (schedule.py:217-218,227-232:
        "Reserved for future morning pre-heat (D-10). Currently unused").
        The CAL-04 behaviour (treat a calendar-absent person as present
        within the lead window) does not fire. The user-facing goal
        (rooms warm before the person returns) is instead delivered by
        Phase 12 predictive pre-heat via next_occupied_at on calendar
        sources. Decide: wire wakeup_advance, or formally mark it
        superseded by Phase 12 and drop it.
    - id: "UI-01"
      status: "adjusted"
      phase: "Phase 10"
      verification_status: "no VERIFICATION.md (UAT + VALIDATION present)"
      evidence: >
        Original spec: hide the HA option when the person has no device
        trackers. Shipped behaviour (deliberate, quick task 260601-d04):
        the option is ALWAYS shown with a "⚠" warning + edit link.
        shouldShowHaOption(presence-mode.ts:41-43) always returns true
        and is dead (referenced only by its own test). Requirement text
        was never updated to match the shipped design.
    - id: "UI-02"
      status: "adjusted"
      phase: "Phase 10"
      verification_status: "no VERIFICATION.md (UAT + VALIDATION present)"
      evidence: >
        Spec said rename "HA" to "Live tracking". Shipped label is
        "HA home tracking" (haOptionLabel, presence-mode.ts:50-52), a
        deliberate choice (see memory project_ha_mode_rename). Intent
        (clear rename from bare "HA") is met; the literal string differs.
  integration: []
  flows:
    - flow: "Pre-heat before person returns (wakeup_advance)"
      status: "caveat"
      evidence: >
        wakeup_advance_minutes has no runtime effect (see CAL-04).
        Pre-heat-before-return works via Phase 12 inertia pre-heat, not
        the Phase 11 wakeup_advance mechanism.
tech_debt:
  - phase: "process"
    items:
      - "Phases 10, 13, 15, 16 have no verifier VERIFICATION.md (used UAT + VALIDATION instead). Mitigated by 282 passing tests + integration check, but no formal goal-backward verification artifact exists."
      - "REQUIREMENTS.md traceability checkboxes are stale: only ARCH-01 and DOC-01 are [x], though 13/17 are fully satisfied. Traceability table omits ARCH-01/02, OBS-01, DOC-01 entirely."
      - "Nyquist: only Phase 15 has nyquist_compliant:true; all 8 VALIDATION.md files are status:draft (overall: partial). Discovery-only — run /gsd-validate-phase per phase to close."
  - phase: "13-matter-tado-x-real-time-calibration"
    items:
      - "suggest_matter_mappings WS command is wired backend + ws-client (ws-client.ts:211) but no UI component calls it — reserved auto-suggest capability, manual drag-drop pairing used instead."
      - "ROADMAP phase-13 detail lists 13-03-PLAN.md as unchecked [ ]; the frontend was actually delivered via quick task 260603-uo0. Progress table already shows 13 complete."
  - phase: "roadmap-hygiene"
    items:
      - "ROADMAP 'In Progress' bullet list (lines 58-65) only names phases 10-13; phases 14-17 are present in Phase Details + Progress table but missing from the milestone bullet summary. Phase-10 detail checkbox at the milestone summary also lags the Progress table."
nyquist:
  compliant_phases: ["15"]
  partial_phases: ["10", "11", "12", "13", "14", "16", "17"]
  missing_phases: []
  overall: "partial"
---

# Milestone v1.3 — Calendar Presence & Pre-heat — Audit

**Audited:** 2026-06-06
**Status:** `tech_debt` — all requirements functionally covered, no critical
blockers, but accumulated items need acknowledgment before close.

## Scope

Phases 10–17 (8 phases, ~71 plans across the milestone). All marked complete in
the ROADMAP Progress table; `STATE.md status: milestone_complete`.
`manifest.json` already at `1.3.0`.

## Headline

- **0 critical integration findings.** Cross-phase wiring is clean: the Phase 14
  Default Zone consolidation, Phase 15 `room_mode` removal, and Phase 16
  coordinator domain-graph rewrite all integrate correctly with the Phase
  11/12/13 feature paths.
- **282 Python tests pass.**
- **3 requirements are partial/adjusted, 0 are unsatisfied** (no missing or
  broken features — the three are design supersessions / spec drift, with the
  user-facing goal met in each case).
- The one earlier hard defect (Phase 14 `room-card.ts` reading a removed flat
  key) is **already fixed** — `default_zone_preheat_enabled` no longer appears
  in `frontend/src`; `room-card.ts:833` reads `default_zone?.preheat_enabled`.

## Requirements coverage (3-source cross-reference)

| REQ | Phase | VERIFICATION | SUMMARY | Final | Note |
|-----|-------|--------------|---------|-------|------|
| CAL-01 | 11 | complete | ✓ | **satisfied** | |
| CAL-02 | 11 | complete | ✓ | **satisfied** | |
| CAL-03 | 11 | complete | ✓ | **satisfied** | |
| CAL-04 | 11 | complete | ✓ | **partial** | wakeup_advance no-op; goal met by Phase 12 |
| PREHEAT-01 | 12 | complete | ✓ | **satisfied** | |
| PREHEAT-02 | 12 | complete | ✓ | **satisfied** | |
| PREHEAT-03 | 12 | complete | ✓ | **satisfied** | |
| PREHEAT-04 | 12 | complete | ✓ | **satisfied** | |
| PREHEAT-05 | 12 | complete | ✓ | **satisfied** | next_occupied_at accepts calendar + even/odd |
| MCALIB-01 | 13 | (UAT/VALIDATION) | ✓ | **satisfied** | manual pairing wired E2E |
| MCALIB-02 | 13 | (UAT/VALIDATION) | ✓ | **satisfied** | listener lifecycle ghost-safe |
| UI-01 | 10 | (UAT/VALIDATION) | ✓ | **adjusted** | always-show + ⚠ replaced hide-option |
| UI-02 | 10 | (UAT/VALIDATION) | ✓ | **adjusted** | label "HA home tracking" not "Live tracking" |
| ARCH-01 | 14 | human_needed | ✓ | **satisfied** | sole defect fixed post-verify |
| ARCH-02 | 15 | (VALIDATION) | ✓ | **satisfied** | no room_mode refs remain |
| OBS-01 | 16 | (UAT/VALIDATION) | ✓ | **satisfied** | domain graph + structured logs |
| DOC-01 | 17 | passed | ✓ | **satisfied** | |

**13 satisfied · 2 adjusted (UI-01/02) · 1 partial (CAL-04) · 0 unsatisfied.**

## Cross-phase integration (from integration checker — PASS)

- **Phase 14 → all:** no active read of any removed flat key
  (`global_mode`/`global_time_program`/`default_zone_name`/`default_zone_preheat_enabled`)
  anywhere in `custom_components/` or `frontend/src/`. ARCH-01 round-trips
  through the panel.
- **Phase 16 → 11/12/13:** `async_evaluate` builds one `EvalContext` then drives
  zone → person (calendar) → Matter listener refresh → `compute_preheat` →
  `_async_calibrate`. No path orphaned by the rewrite.
- **Phase 12 ↔ 14:** zone-scoped `preheat_enabled` read correctly for both
  Default and custom zones (GAP-01 closure holds).
- **Phase 15:** only residual `room_mode` references are the one-time storage
  strip shim and the WS incoming-config strip — no active reads.
- **WS surface:** 19 frontend command types ↔ 19 backend handlers, diff empty
  both directions. Removed commands absent from both sides.

## E2E flows

| Flow | Status |
|------|--------|
| Calendar presence drives heating | **complete** |
| Pre-heat before scheduled period | **complete** (via Phase 12 inertia; `wakeup_advance` itself inert) |
| Matter state_change triggers calibration | **complete** |
| Default Zone config round-trips through panel | **complete** |

## Recommendation

No critical blockers — the milestone is shippable as `tech_debt`. Before
closing, decide on the two substantive items:

1. **CAL-04 `wakeup_advance_minutes`** — either wire it into
   `_next_occupied_calendar` / presence, or formally record it as superseded by
   Phase 12 predictive pre-heat and drop the field (it currently sits in
   config/UI doing nothing). This is the only finding touching a documented
   requirement behaviour.
2. **UI-01 / UI-02 spec drift** — update the requirement text to the shipped
   "HA home tracking + ⚠ warning" design, and remove the dead
   `shouldShowHaOption` helper.

Everything else (missing VERIFICATION.md for 4 phases, stale checkboxes,
Nyquist drafts, unsurfaced suggest command, ROADMAP bullet staleness) is
low-risk housekeeping that can be acknowledged as deferred at close — exactly
how v1.0/v1.1/v1.2 closed.
